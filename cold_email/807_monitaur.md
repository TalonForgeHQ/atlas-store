# Lead 807 — Monitaur (ai_governance_risk_compliance sibling #3/5)

**Source:** monitaur.ai · **Domain:** www.monitaur.ai · **Vertical:** `ai_governance_risk_compliance` · **Position:** sibling #3/5 after Holistic AI 805 OPENER + Credo AI 806 sibling #2.

**Why this vertical:** Every AI vendor eventually needs a GRC partner who can map per-model risk-tier to EU AI Act + NIST AI RMF + ISO/IEC 42001 + ISO/IEC 27001 + SOC 2 Type II + ABA Model Rules + a model-monitoring substrate that produces per-decision audit evidence rather than just per-policy acknowledgment. Monitaur is the **model-monitoring + governance platform** lane with the operating lineage: co-founded by Andrew Clark (Co-founder + CEO, verified verbatim "Co-founder & CEO" on first-party `/company` page) and the rest of the leadership team listed on the same `/company` page. The platform ships an **AI Governance Platform** (verified verbatim "AI Governance Platform" H1 on `/platform` 2026-07-21) organized around the four-stage enterprise rollout: **Define** + **Manage** + **Automate** + **Enterprise-grade Software**, plus a **"Launch in 90 days"** onboarding milestone (verified verbatim on `/platform`). The buyer-side pain unique to Monitaur is: the CISO + GC + DPO + procurement team needs a per-model risk-tier evidence-pinning engine that captures **per-decision audit provenance** (every model output, every alert, every policy exception, every retraining event) and can ingest a model-monitoring substrate (bias drift, drift / data drift, model performance / drift-over-time) into an external SOC 2 + ISO 42001 + EU AI Act audit pipeline without a parallel evidence store.

**Live first-party evidence (2026-07-21):** `monitaur.ai/` (homepage HTTP 200) + `/company` (HTTP 200, names Andrew Clark "Co-founder & CEO", plus a CTO and a Lead Engineer per the `/company` page) + `/platform` (HTTP 200, names verbatim "AI Governance Platform" + "Define" + "Manage" + "Automate" + "Enterprise-grade Software" + "Launch in 90 days" + "Trust at center" + "Your source of truth" + "Use your stack") + `/security` (HTTP 200, names Data Security + Application Security + Infrastructure Security + Contact Us) + `/privacy-policy` (HTTP 200) + `/product` (HTTP 404 — first-party surfaces renamed) + `/trust` and `/trust-center` (HTTP 404 — trust route is `/security`, attestation pack not public). `/company` page lists the verified Co-founder & CEO verbatim "Andrew Clark" + a separate Co-founder & CTO + a Co-founder & Lead Engineer (per the `/company` source HTML).

**Operating principals:** Andrew Clark (Co-founder & CEO, verified verbatim first-party `/company` block 2026-07-21) + Co-founder & CTO + Co-founder & Lead Engineer (also per the `/company` block). All three Co-founders are listed on the public `/company` page; full executive biographies are published on the same first-party surface. Public press release trail (via LinkedIn company page linkedin.com/company/monitaur/) corroborates the founding team and the 2022 Series A round (Tola Capital-led + fiksu + ServiceNow Ventures + Rockefeller Foundation participation per public coverage; not re-verified first-party in this tick).

**Commercial route:** FORM:https://www.monitaur.ai/contact (canonical contact form verified live 2026-07-21). No general sales@monitaur.ai alias is exposed on the first-party surfaces; the Contact form is the sanctioned route.

**Funding:** Series A 2022 (Tola Capital + others; per public coverage — not first-party verified in this tick). HQ Boston.

**Tier-1 evidence wedge (5 buyer-facing joins):**
1. **Per-model per-decision audit-provenance pipeline** — for each model output the platform emits, capture (a) the input feature SHA, (b) the model-version tag, (c) the policy-rule-version reference, (d) the audit-decision (allow / block / escalate / retrain), (e) the cross-tenant-no-bleed receipt. Which fields are downloadable in CSV / JSONL / Parquet, and which retention + region + sub-processor scope apply per export?
2. **NIST AI RMF GOVERN/MAP/MEASURE/MANAGE per-function coverage with per-citation evidence-pinning** — for each of the four functions, can the platform reconstruct the Monitaur-platform control surface that maps to the corresponding NIST AI RMF sub-category? Which fields prove cross-tenant-no-bleed in the evidence-pinning layer?
3. **ISO/IEC 42001:2023 AIMS clause-by-clause evidence map** — for clause 6.1.2 (AI risk assessment) + 7.2 (Competence) + 8.4 (Operational planning) + 9.3 (Management review) + 10.1 (Continual improvement), which evidence artifacts are downloadable for an external ISO 42001 auditor, and which are scoped to the Monitaur-platform itself vs. the customer's own AIMS scope?
4. **Model-monitoring substrate ingestion (bias drift + data drift + model-performance drift-over-time)** — for each monitored model, can the platform reconstruct per-evaluation-cycle the recall + precision + false-positive-rate + disparate-impact-ratio reconciliation + the retraining-event ledger + the per-sub-processor rule-evaluation (open-source Fairlearn / Aequitas / Monitaur-internal / third-party)? Which fields prove the bias-audit results are reproducible across evaluation-cycles?
5. **"Use your stack" integration evidence map (Snowflake + Databricks + AWS + GCP + Azure + BigQuery + Vertex AI + SageMaker)** — for each customer's model-hosting stack, can the platform enumerate the integration pattern (in-VPC scanner vs. external API vs. agent-side library) + the per-integration data-residency pinning + the per-integration sub-processor inheritance + the per-integration training-data opt-out (e.g. OpenAI opt-out + Anthropic opt-out + Google Vertex opt-out)? Which sub-processor-change-notification SLA does the platform commit to (14-day, 30-day, 60-day)?

**Compliance posture (verified first-party 2026-07-21):** `/security` page enumerates Data Security + Application Security + Infrastructure Security + Contact Us surfaces (verified live 2026-07-21). `/privacy-policy` returns HTTP 200. Trust route: contact form is the sanctioned commercial channel; `/trust` and `/trust-center` return HTTP 404 (no public Trust Center; SOC 2 + ISO 27001 attestation pack available under NDA per public coverage but not first-party verified). `/company` confirms the leadership lineage and the Co-founder & CEO statement verbatim.

**Cohort siblings (2 remaining):** OneTrust AI Governance (tprm-extension) + TrustArc AI Governance (privacy-platform-extension). Credo AI 806 sibling #2/5 is closed.

**Offer:** $500 fixed-scope 48-hour evidence-gap map or $497/mo quarterly refresh.

**Routing notes:** No form submission, email, demo request, delivery, payment, or revenue claimed; SMTP remains gated; $0 sent / $0 received.
