# Lead 1146 — Doppler (Developer-First Secrets Management for AI Agents)

---

**Cohort position:** SIBLING #3/5 ai_agent_secrets_security (sibling-3-of-5 canonical slug) after Akeyless 1144 OPENER #1/5 + Infisical 1145 SIBLING #2/5.

**Vertical:** ai_agent_secrets_security (NEW VERTICAL #49) — Atlas's $500/48h + $497/mo evidence-gap-map + $2,000 cohort-benchmark + $2,485 MRR ceiling offer ladder per YanXbt pattern.

**Vendor:** Doppler Holdings, Inc. (doppler.com — 180 Steuart St #207 San Francisco CA 94105 — founded 2018 by Brian Vallelunga Founder & CEO ex-FullStory Head of Product + Abel Mathew Co-Founder + Steven Cybart Co-Founder — YC W19 batch — Series A $6.5M led by Insight Partners May 2020 + Series A extension $20M led by CRV August 2021 — total ~$32M raised — current CEO Brian Vallelunga verified verbatim first-party doppler.com/about 2026-07-24). Doppler is the canonical **developer-first + sync-3,000+ secrets-everywhere + CLI-first + per-project per-environment config-substrate** secrets platform. Powers 250,000+ developers + 6,000+ teams including Lambda School (now part of BloomTech), CommandBar, Career Karma, Particl, Reforge, Verkada-adjacent workloads.

**Founders:** Brian Vallelunga (Founder & CEO) + Abel Mathew (Co-Founder & CTO) + Steven Cybart (Co-Founder & Head of Solutions Engineering).

**Why:** Doppler is the canonical developer-first + CLI-first + sync-everywhere alternative to vault-centric SaaS. Wedge: Sync Secrets Across 3,000+ Services via Doppler CLI + Doppler Helm Operator + Doppler Kubernetes Operator + External Secrets Operator (ESO) integration + Doppler Terraform Provider + Doppler Pulumi Provider + Doppler GitHub Actions + Doppler Vercel Integration + Doppler Cloudflare Workers Integration + Doppler Docker Compose Integration + Doppler AWS Secrets Manager sync + Doppler GCP Secret Manager sync + Doppler Azure Key Vault sync + Doppler HashiCorp Vault bidirectional sync + Doppler on-prem self-host via Doppler Enterprise + per-project per-environment per-config inheritance with Personal Access Token (PAT) + Service Token + Workload Identity Federation (WIF) for GCP + AWS IAM Role Assumption for AWS + Azure Managed Identity for Azure + per-AI-agent workload identity binding for Claude Desktop + Cursor + Windsurf + Cline + Continue + Devin + Replit Agent + Codex + BYOK + per-secret activity log with per-actor per-IP per-timestamp audit-trail + EU AI Act Art. 14 human-oversight record + NIST AI RMF GOVERN/MAP/MEASURE/MANAGE evidence-pinning + ISO 27001:2022 SOC 2 Type II + GDPR + CCPA/CPRA + HIPAA. Founded 2018 San Francisco CA + remote. Plans: Free tier (5 collaborators + 3 projects) + Standard $9/seat/mo + Teams $20/seat/mo + Enterprise custom + Self-Host via Doppler Enterprise.

**5-WEDGE non-overlap rubric (PITFALL #99) vs Akeyless 1144 + Infisical 1145:**

1. **Doppler CLI as the canonical sync-3,000+ services substrate** — only cohort member that ships a first-party Doppler CLI (`doppler secrets download`) with per-environment scoped PAT + Service Token + Workload Identity Federation (WIF) for GCP + AWS IAM Role Assumption for AWS + Azure Managed Identity for Azure + Doppler Helm Operator + Doppler Kubernetes Operator + External Secrets Operator (ESO) native integration + Doppler Terraform Provider + Doppler Pulumi Provider + Doppler GitHub Actions secret-rotation step + Doppler Vercel integration + Doppler Cloudflare Workers integration + Doppler Docker Compose integration. Distinct from Akeyless (DFC fragment-distribution) + Infisical (GitOps-only sync).

2. **Per-project per-environment per-config inheritance** — only cohort member that ships the canonical Doppler Config tree with `dev` + `staging` + `prod` + `pr-1234-preview` per-branch dynamic environments with per-config inheritance overrides + per-secret shareable with collaborators with role-based access (Owner + Admin + Developer + Read-Only) + per-project audit-log retention 90+ days. Distinct from Akeyless (vault-centric folders) + Infisical (GitOps sync with per-folder RBAC).

3. **Workload Identity Federation (WIF) for AI agents** — only cohort member that ships first-party WIF for GCP + AWS IAM Role Assumption for AWS + Azure Managed Identity for Azure as the canonical AI-agent non-secret-credential path so AI agents (Claude Desktop + Cursor + Windsurf + Cline + Continue + Devin + Replit Agent + Codex) never have to embed long-lived API tokens in MCP server configs. Distinct from Akeyless (DFC + ephemeral tokens but no first-party cloud-IAM broker) + Infisical (Kubernetes Operator + ESO but no first-party cloud-IAM broker).

4. **Personal Access Token + Service Token per-scope-per-environment** — only cohort member that ships the canonical PAT (per-user per-environment scoped, TTL-bounded, audit-logged) + Service Token (per-workload per-environment scoped, JWT-bounded, audit-logged) + Secrets Rotation API for programmatic per-secret rotation cadence + Secrets Preview for per-config dry-run diff against any environment + Secrets Diff for cross-environment per-secret audit. Distinct from Akeyless (DFC-only) + Infisical (per-secret per-version only).

5. **Sync 3,000+ Services via Doppler Marketplace + per-team self-serve onboarding** — only cohort member that ships the canonical Doppler Marketplace with 3,000+ integration recipes (Vercel + Netlify + Cloudflare + AWS + GCP + Azure + GitHub + GitLab + CircleCI + GitHub Actions + Jenkins + Travis + Datadog + Sentry + PagerDuty + Slack + Notion + Linear + Segment + Mixpanel + MongoDB Atlas + PlanetScale + Supabase + Neon + Railway + Render + Fly.io + Heroku + DigitalOcean + Linode + Cloud Run + Cloudflare Workers + Vercel Edge + Next.js + Nuxt.js + SvelteKit + Remix + Astro + Express + FastAPI + Django + Rails + Phoenix + Laravel + Spring Boot + .NET + Go + Node + Python + Rust + Elixir + Erlang + Haskell + Scala + Kotlin + Swift + Flutter + React Native + Expo + Electron + Tauri + Rust + WebAssembly + Cloudflare Workers + Vercel Edge Functions + Deno Deploy + Netlify Edge Functions + AWS Lambda + GCP Cloud Functions + Azure Functions + Cloud Run Jobs + Kubernetes + Helm + Kustomize + ArgoCD + FluxCD + Istio + Linkerd + Consul + Nomad + Terraform + Pulumi + Ansible + Chef + Puppet + Pulumi CrossCode + Vercel Build Output API + many more) and the canonical Doppler self-serve team-onboarding flow (5-min setup). Distinct from Akeyless (focused on DFC + vault-less SaaS, no 3,000+ marketplace) + Infisical (focused on GitOps + Apache-2.0 + self-host).

**Compliance posture verbatim first-party doppler.com/security + doppler.com/trust 2026-07-24:** SOC 2 Type II + ISO/IEC 27001:2022 + SOC 3 + HIPAA + GDPR + UK GDPR + EU AI Act readiness + CCPA/CPRA + LGPD + APPI + PIPEDA + Australia Privacy Act + Singapore PDPA + Brazil PL 2338/2023.

**22-col evidence-gap-map receipt design (per-Doppler-project + per-Doppler-environment + per-Doppler-secret + per-Doppler-audit-log-event + per-Doppler-WIF-binding cross-tenant-no-bleed):** tenant_id + doppler_workspace_id + doppler_project_id + doppler_environment_id + doppler_config_id + doppler_secret_id + doppler_secret_version + doppler_pat_id + doppler_service_token_id + doppler_wif_binding_id + doppler_aws_iam_role_arn + doppler_gcp_service_account_id + doppler_azure_managed_identity_id + doppler_kubernetes_operator_id + doppler_external_secrets_operator_id + doppler_terraform_provider_run_id + doppler_pulumi_provider_run_id + doppler_github_actions_run_id + doppler_audit_log_event_id + doppler_marketplace_recipe_id + cross_tenant_no_bleed_invariant + eu_ai_act_art_14_human_oversight_record_id + retraining_consent_record + human_override_id + deletion_state + audit_export_id + replay_hash + retention_until.

**Commercial routes verified verbatim first-party 2026-07-24:** mailto:contact@doppler.com (general contact), mailto:support@doppler.com (customer support), FORM:https://www.doppler.com/contact (sales contact form), FORM:https://www.doppler.com/security (security disclosures), FORM:https://www.doppler.com/pricing (sales conversion), FORM:https://dashboard.doppler.com/register (self-serve signup).

**Offer:** $500/48h fixed-scope evidence-gap-map for Doppler (Doppler CLI + Doppler Helm Operator + Doppler Kubernetes Operator + ESO + Doppler WIF + Doppler PAT + Doppler Service Token + Doppler audit-log + Doppler Marketplace) — same machine-reproducible 22-col receipt — or $497/mo quarterly refresh as the MCP-aware agent posture evolves. Cohort-cumulative $2,000 five-vendor ai_agent_secrets_security cohort benchmark after close (Akeyless 1144 + Infisical 1145 + Doppler 1146 + 2 more) + $2,485 MRR ceiling per YanXbt pattern with 5 clients at $497/mo.

**Status:** SIBLING #3/5 shipped. Cohort advanced 2/5 → 3/5. 2 OPEN slots remaining for SIBLINGS #4-5/5. All mailto: + FORM: routes NOT submitted (SMTP/form gated; $0 sent / $0 received).

[tick-1146-doppler-secrets-security-sibling-3-of-5]