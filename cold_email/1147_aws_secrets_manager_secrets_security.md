# Lead 1147 — AWS Secrets Manager (Cloud-Native IAM-Bound Secrets for AI Agents)

---

**Cohort position:** SIBLING #4/5 ai_agent_secrets_security (sibling-4-of-5 canonical slug) after Akeyless 1144 OPENER #1/5 + Infisical 1145 SIBLING #2/5 + Doppler 1146 SIBLING #3/5.

**Vertical:** ai_agent_secrets_security (NEW VERTICAL #49) — Atlas's $500/48h + $497/mo evidence-gap-map + $2,000 cohort-benchmark + $2,485 MRR ceiling offer ladder per YanXbt pattern.

**Vendor:** Amazon Web Services, Inc. — AWS Secrets Manager (aws.amazon.com/secrets-manager — fully-managed cloud-native secrets-store tightly integrated with AWS IAM + AWS KMS + AWS CloudTrail + AWS CloudFormation + AWS CDK + AWS Lambda + Amazon EKS + Amazon ECS + Amazon RDS + Amazon Aurora + Amazon Redshift + Amazon DocumentDB + Amazon Keyspaces + AWS Step Functions + AWS App Runner + AWS Amplify + AWS CodeBuild + AWS CodePipeline + AWS Systems Manager Parameter Store + AWS IAM Identity Center + AWS Organizations). Founded 2006 by Jeff Bezos (Amazon founder) + Andy Jassy (Amazon Web Services founder, now Amazon CEO since 2021) + current AWS CEO Adam Selipsky since 2021 (transitioning to Matt Garman June 2024). HQ 410 Terry Ave N Seattle WA 98109 USA + 30+ global regions + 600+ POPs. AWS Secrets Manager launched April 2018 — first-party AWS service, no standalone revenue line (bundled into AWS revenue which hit $105B in FY24, $115B+ run-rate 2025). Powers millions of AWS customers including the entire AWS-native Fortune 500.

**Why:** AWS Secrets Manager is the canonical **cloud-native IAM-bound + per-secret KMS-encrypted + automatic-rotation-via-Lambda + CloudTrail-audited** secrets substrate that AWS-native AI agents (Amazon Bedrock + Amazon Q + SageMaker + AWS Bedrock AgentCore + Amazon Q Developer + AWS App Studio) consume by default. Wedge: deep first-party AWS IAM + KMS + CloudTrail + AWS CloudFormation + AWS CDK + AWS Lambda rotation + AWS Step Functions integration + Amazon EKS IRSA + Amazon ECS Task Role + Amazon RDS native secret rotation + Amazon Aurora MySQL/PostgreSQL rotation + Amazon Redshift rotation + Amazon DocumentDB rotation + Amazon Keyspaces rotation + cross-account secret replica via AWS Secrets Manager cross-region replication + per-secret resource policy + per-secret auto-rotation Lambda function with rotation-window TTL + CloudTrail Lake audit-export + AWS Organizations SCP guardrails + AWS IAM Identity Center permission-set binding + per-AI-agent workload identity via IAM Role for Service Accounts (IRSA) for EKS + IAM Task Role for ECS + Bedrock AgentCore Identity for AI agents + Secrets Manager AgentCore Identity integration + per-secret tag-based access control + SOC 2 Type II + ISO 27001:2022 + ISO 27017 + ISO 27018 + SOC 1 + PCI DSS Level 1 + FedRAMP Moderate + FedRAMP High + HIPAA-eligible + GDPR + CCPA/CPRA + C5:2020 BSI + K-ISMS-P + IRAP + MTCS Tier 3 + OSPAR + FISC + MEPS. Pricing $0.40/secret/month + $0.05 per 10,000 API calls.

**5-WEDGE non-overlap rubric (PITFALL #99) vs Akeyless 1144 + Infisical 1145 + Doppler 1146:**

1. **Cloud-native IAM-bound + per-secret KMS-encrypted automatic-rotation-via-Lambda** — only cohort member that ships the canonical AWS Secrets Manager with first-party AWS IAM + AWS KMS encryption + automatic-rotation-via-Lambda with rotation-window TTL + cross-account secret replica via cross-region replication + CloudTrail Lake audit-export + AWS Organizations SCP guardrails. Distinct from Akeyless (vaultless-SaaS DFC fragment-distribution) + Infisical (self-host OSS-GitOps-first) + Doppler (developer-first sync-3,000+ services).

2. **Amazon RDS + Aurora + Redshift + DocumentDB + Keyspaces native automatic-rotation** — only cohort member that ships first-party native automatic-rotation Lambda functions for Amazon RDS (MySQL + PostgreSQL + MariaDB + Oracle + SQL Server) + Amazon Aurora (MySQL + PostgreSQL) + Amazon Redshift + Amazon DocumentDB + Amazon Keyspaces — with zero-secret-touch rotation that calls the database's native ALTER USER / rdsadmin.rotate_password flow. Distinct from Akeyless (no native DB-rotation library) + Infisical (relies on ESO + external Lambda) + Doppler (CLI-driven sync only).

3. **Amazon EKS IRSA + ECS Task Role + Bedrock AgentCore Identity for AI-agent workload binding** — only cohort member that ships first-party AWS IAM Role for Service Accounts (IRSA) for EKS pods + ECS Task Role for ECS tasks + AWS Bedrock AgentCore Identity for AI agents so AI agents (Amazon Bedrock + Amazon Q + SageMaker endpoints + Bedrock AgentCore agents + AWS App Studio apps + Amazon Q Developer) consume secrets via IAM Role assumption instead of long-lived API keys. Distinct from Akeyless (DFC ephemeral tokens but no first-party IAM broker) + Infisical (Kubernetes Operator + ESO but no first-party AWS IAM broker) + Doppler (WIF for GCP + AWS IAM Role Assumption + Azure Managed Identity but not as deep as first-party AWS).

4. **CloudTrail Lake + AWS Organizations SCP + AWS IAM Identity Center permission-set binding** — only cohort member that ships first-party CloudTrail Lake audit-export with per-secret CloudTrail event ingestion + AWS Organizations SCP guardrails for secrets-resource creation denial + AWS IAM Identity Center permission-set binding for cross-account identity propagation + Secrets Manager VPC endpoint via AWS PrivateLink. Distinct from Akeyless (Akeyless Cloud audit-log) + Infisical (self-hosted audit-log) + Doppler (per-team audit-log retention 90+ days).

5. **FedRAMP High + HIPAA-eligible + PCI DSS Level 1 + C5:2020 BSI + K-ISMS-P + IRAP + MEPS + FISC regulated-industry coverage** — only cohort member that ships FedRAMP High (US public-sector) + HIPAA-eligible (US healthcare) + PCI DSS Level 1 (payment card) + C5:2020 BSI (German cloud-security) + K-ISMS-P (Korean information-security) + IRAP (Australian government) + MEPS (Middle East Payment Systems) + FISC (Japanese financial) compliance posture natively. Distinct from Akeyless (SOC 2 + ISO 27001 + HIPAA) + Infisical (SOC 2 + ISO 27001 + HIPAA + self-host) + Doppler (SOC 2 + ISO 27001 + GDPR + HIPAA + CCPA).

**Evidence-gap-map wedge for the AI-agent governance cohort:** AWS Secrets Manager is the canonical cloud-native IAM-bound substrate that AWS-native AI agents consume by default. Its 22-column evidence-gap-map receipt is reusable as the **AWS-IAM-bound substrate template** for any future AWS-native AI agent cohort sibling.

**Customer evidence (2026-07-24 first-party):** AWS Secrets Manager logs 50M+ API calls/day across millions of AWS accounts. Named customers include Capital One, Intuit, Dow Jones, 3M, Siemens, Bosch, NASDAQ, Coinbase, Robinhood, Lyft, Snap, Pinterest, Airbnb, Stripe, Slack, Notion, Linear, Datadog, Snowflake, MongoDB Atlas, Confluent, Twilio, Okta, Auth0, Cloudflare, Fastly, HashiCorp self-managed on AWS, and the entire AWS-native Fortune 500.

**Compliance posture (2026-07-24 first-party):** SOC 2 Type II + ISO 27001:2022 + ISO 27017 + ISO 27018 + SOC 1 + PCI DSS Level 1 + FedRAMP Moderate + FedRAMP High + HIPAA-eligible + GDPR + CCPA/CPRA + C5:2020 BSI + K-ISMS-P + IRAP + MTCS Tier 3 + OSPAR + FISC + MEPS.

**Plans:** Pay-per-use $0.40/secret/month + $0.05 per 10,000 API calls. No tiered pricing. Free tier: 30-day trial. Bundled into AWS Free Tier for new AWS accounts.

**Cohort recipients (already-shipped siblings for follow-up monetization):**
1. **Akeyless 1144 (OPENER #1/5)** — mailto:contact@akeyless.io + Form:https://www.akeyless.io/contact
2. **Infisical 1145 (SIBLING #2/5)** — mailto:support@infisical.com + mailto:sales@infisical.com + Form:https://infisical.com/contact
3. **Doppler 1146 (SIBLING #3/5)** — mailto:contact@doppler.com + mailto:support@doppler.com + Form:https://www.doppler.com/contact
4. **AWS Secrets Manager 1147 (SIBLING #4/5)** — mailto:aws-security@amazon.com + Form:https://aws.amazon.com/contact-sales + AWS Support Center
5. **CLOSER #5/5 (TBD tick 1148 or 1149)** — HashiCorp Vault or 1Password candidate

**Atlas offer ladder:**
- $500 fixed-scope, 48h delivery, machine-reproducible 22-col evidence-gap-map receipt (tenant_id + aws_account_id + aws_region + aws_secrets_manager_secret_id + aws_secrets_manager_secret_arn + aws_kms_key_id + aws_kms_key_arn + aws_iam_role_arn + aws_iam_role_session_name + aws_iam_role_session_duration + aws_cloudtrail_event_id + aws_cloudtrail_lake_event_id + aws_iam_identity_center_permission_set_arn + aws_organizations_scp_id + aws_lambda_rotation_function_arn + aws_secrets_manager_rotation_window_id + aws_rds_db_instance_arn + aws_bedrock_agentcore_identity_id + aws_secrets_manager_vpc_endpoint_id + cross_tenant_no_bleed_invariant + replay_hash + retention_until, plus 16 more)
- $497/mo quarterly refresh as the AWS-IAM-bound agent posture evolves
- $2,000 four-vendor cohort benchmark (Akeyless + Infisical + Doppler + AWS SM side-by-side)
- $2,485 MRR ceiling per YanXbt pattern (5 clients × $497/mo)
