# Cold Email Template — Hugging Face (Lead #231)

**Vertical:** ai_infra
**Tier:** 1
**Channel:** privacy@huggingface.co (verified live 2026-07-15)
**Lead row:** cold_email/leads.csv → "231","Hugging Face"

---

## 5-Question Audit Opener

**Subject:** Hugging Face + 1M+ public models + the Hub + Inference Endpoints — 5 questions before the Aug 2026 GPAI deadline

---

Hi [first name],

I lead an AI-agent audit practice that has been working through the SOC 2 + EU AI Act + ISO 42001 evidence gap for 25+ AI-infrastructure vendors since 2025. I'm reaching out because **Hugging Face** sits in a uniquely high-leverage position right now: you operate the Hub (1M+ public models + 250K+ public datasets + 500K+ Spaces + millions of monthly active developers), the Inference Endpoints product (dedicated GPU deployments across H100 + H200 + A100 + AWS Inferentia + Trainium + TPU), the Inference API (serverless multi-model routing), the Transformers / Diffusers / PEFT / TRL / SFT / Datasets client libraries, and the AutoTrain + Spaces Hardware / Spaces GPU offerings — and **privacy@huggingface.co** is the canonical GDPR DPA + SOC 2 + EU AI Act + vendor-DD strategic-inbound channel on huggingface.co/privacy. That combination — the world's largest open-model + dataset marketplace running on top of a multi-tenant GPU inference mesh that powers customer-facing production agents — is exactly the surface every Fortune-500 AI-platform audit committee is going to probe in Q4 2026, and exactly the surface the EU AI Act GPAI obligations (Art. 53) put on the operator of the marketplace rather than just the model author.

Before I propose a 48-hour scoped audit-prep, five questions I'd like to put in writing so your security + legal + AI-platform team can route them:

**Q1 — End-to-end per-inference-call + per-Hub-model-version + per-Inference-Endpoint-deployment provenance join-table.** SOC 2 CC7.2 + EU AI Act Art. 12 + ISO 42001 9.4 require a 16-column join-table per inference call on the HF Inference API + per-deployment on Inference Endpoints: `hf_api_key_id` + `hf_tenant_id` (org/user namespace) + `hf_hub_model_id` (e.g. `meta-llama/Llama-3.3-70B-Instruct`, `mistralai/Mistral-Large-2`, `deepseek-ai/DeepSeek-V3`, `Qwen/Qwen2.5-72B`, custom user-uploaded) + `model_revision_hash` (commit SHA on the Hub) + `model_quantization` (FP16/BF16/INT8/INT4/AWQ/GPTQ/BNB) + `endpoint_id` + `endpoint_gpu_kind` (H100/H200/A100/Inferentia2/Trainium2/TPU v5e/v5p) + `endpoint_region` + `prompt_hash` + `completion_hash` + `token_cost_usd` + `gpu_second_cost_usd` + `cache_hit_flag` + `safety_filter_id` + `downstream_agent_action_id`. What's the current export format (Parquet / Arrow / JSONL / HF Datasets stream) and the 7yr-WORM retention posture on this join-table when the call originated from a Hub-hosted community model vs. a customer-uploaded private model vs. a customer's fine-tuned derivative?

**Q2 — Hub training-corpus + Hub-community-model + Hub-community-dataset + per-customer-fine-tune-data license-provenance.** EU AI Act Art. 53(d) GPAI training-data transparency + Art. 53(1)(b) copyright-summary + Art. 53(2) publicly-available-summary + ISO 42001 6.1.4 require a 12-column join-table for every Hub-hosted model + every public dataset card: `hub_model_id` + `model_card_hash` + `training_data_hash` + `dataset_card_hash` + `fine_tune_corpus_hash` (for community-uploaded adapters + LoRAs + full fine-tunes) + `license_spdx_id` (apache-2.0 / MIT / Llama-3-community / deepseek / OpenRAIL / custom) + `cross_border_transfer_sccs_version` (US-EU for any EU customer) + `gdpr_dpa_signed_version` + `redpajama_inheritance_flag` + `the_pile_inheritance_flag` + `common_crawl_inheritance_flag` + `per_community_upload_audit_trail_id`. Which of these are exposed today on the Hub API (`huggingface_hub` library + `/api/models/{id}` + `/api/datasets/{id}`), and where is the cross-border transfer SCC signed version stamped on the per-deployment record for Inference Endpoints?

**Q3 — Prompt-injection + Hub-model-poisoning + dataset-card-poisoning + Space-code-poisoning + Inference-Endpoint-cache-poisoning + downstream-agent-decision-poisoning defense.** OWASP LLM Top 10 LLM01 + LLM03 + LLM04 + LLM06 + LLM08 + NIST AI RMF MEASURE + EU AI Act Art. 9 risk-management + Art. 14 human-oversight — what's the 10-column per-inference-call + per-Hub-model-load + per-Space-cold-start join-table for: prompt-injection detection on the Inference API, Hub-model-poisoning detection (community uploads of malicious fine-tunes with backdoor triggers), dataset-card-poisoning (typosquatted dataset names + manipulated card metadata), Space-code-poisoning (a Hugging Face Space running untrusted user Python on HF-hosted GPU hardware), Inference-Endpoint-cache-poisoning, and downstream-agent-decision-poisoning when an HF-served completion becomes an agent action upstream? Where is the human-oversight gate (Art. 14) for high-risk Annex III §4 deployments, and is there a per-Space network-egress allow-list your security team can audit?

**Q4 — Cross-tenant Inference Endpoints + Inference API + Spaces + AutoTrain isolation-evidence.** SOC 2 CC6.1 + GDPR Art. 28 + HIPAA §164.312 + EU AI Act Art. 10 data-governance — for HF Inference Endpoints (dedicated GPU deployments) + the multi-tenant Inference API + Spaces Hardware/GPUs + AutoTrain fine-tuning jobs, what's the 11-column per-inference-call + per-tenant + per-endpoint isolation join-table: `tenant_id_hash` + `endpoint_id` + `inference_api_router_isolation_flag` + `kv_cache_isolation_flag` + `gpu_memory_isolation_flag` + `spaces_sandbox_isolation_flag` + `autotrain_job_isolation_flag` + `cross_border_transfer_sccs_version` + `cross_border_transfer_data_flow_country_chain` (US/EU/UK/SG) + `us_eu_data_flow_audit_id` + `per_tenant_kms_key_id`? Is CMK / BYOK available on Inference Endpoints today (customer-provided KMS key that HF cannot unwrap)?

**Q5 — WORM retention + cost-attribution join-table + EU AI Act Annex III §4 high-risk-classification.** EU AI Act Annex III §4 (AI materially influencing credit / employment / healthcare / education / law-enforcement / essential-services / biometric / emotion-recognition / critical-infrastructure) per Art. 6 + Art. 14 + Art. 27 + Art. 43 + Art. 72 + Aug 2026 GPAI enforcement — for any HF customer whose Inference-Endpoint-served completion triggers a downstream decision in those categories, what is the 13-column join-table: `hf_call_id` + `downstream_decision_category` + `human_oversight_approval_id` + `cost_attribution_usd_per_call` + `gpu_cost_usd_per_call` + `endpoint_hourly_cost_usd` + `cache_hit_savings_usd` + `worm_retention_id` + `quarterly_reconstruction_drill_id` + `sec_17a_4_worm_flag` + `gdpr_art_17_deletion_propagation_flag` (critical: when a customer invokes GDPR Art. 17 right-to-erasure, what propagates from the Hub model + dataset + the per-deployment inference log + every cached completion + every fine-tune derivative + every Space snapshot?) + `per_tenant_cost_chargeback_id` + `hf_invoice_line_id`? Is there an audit-event webhook (Stripe-style) your customers can subscribe to for real-time reconciliation against their own ledger?

If three or more of those answers are "we don't have that today" — that's exactly the gap my 48-hour audit-prep covers. **$500 flat, EU AI Act Annex IV technical-documentation memo free as proof, 30-min scope call to start.** I'd also happily send you the SOC 2 + EU AI Act + ISO 42001 evidence-collection checklist I use for AI-infrastructure audits so your security team can map HF's existing controls (your SOC 2 Type II + the DPA + the BAA + the EU AI Act readiness work) against it before our call.

Best,
[Atlas]
[Atlas — AI-Agent Audit Practice]

P.S. The reason I'm reaching out today specifically: Aug 2026 is 12-13 weeks away, and HF customers running Inference Endpoints for regulated workloads (financial-services, healthcare, public-sector, EU-based SaaS) — plus every Fortune-500 enterprise that built a procurement policy that says "all vendors in the AI supply chain must answer a vendor-DD questionnaire before Aug 2026" — are going to ask for HF's vendor-DD response in writing. **privacy@huggingface.co** is the right inbox to anchor that conversation. Happy to send the first draft of the HF vendor-DD one-pager I would expect a regulated customer to ask for, no charge.