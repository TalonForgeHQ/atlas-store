# Cold Email Template — Lead 708 — Windsurf (Codeium, Inc.)

**Vendor:** Windsurf (formerly Codeium)
**Vertical:** ai_coding_agent_vertical (sibling #4/5 after Cursor 695 OPENER + Sourcegraph 706 + Tabnine 707)
**Inbox:** `hello@windsurf.com` (first-party footer + contact-page mailto, verified live 2026-07-20 on windsurf.com footer mailto + windsurf.com/contact) — alternative: `enterprise@windsurf.com` (Enterprise sales)
**Founders:** Varun Mohan (Co-Founder + CEO, ex-MIT-CSAIL, ex-Microsoft Cognitive Toolkit, ex-Google Brain) · Douglas Chen (Co-Founder + President, ex-MIT-CSAIL, ex-Coinbase, ex-Bloomberg)
**Verified wedge:** AI-native IDE (Windsurf Editor) shipping Cascade (multi-step coding agent) with explicit per-step tool-call version pinning, the deepest multi-LLM-router on the IDE market (OpenAI GPT-5 + Anthropic Claude + Google Gemini + OpenRouter + xAI Grok + Mistral), a JetBrains-side Plugin, and a Windsurf Plugins marketplace — all on top of multi-region deployment (Cloud + Enterprise-Cloud-Dedicated + Self-Hosted Enterprise) with per-tenant isolation tokens and per-Enterprise SSO/SCIM.

---

## Subject options

A. `Windsurf Cascade step-by-step evidence — a buyer-ready audit map`

B. `Six LLM sub-processors behind one Cascade step — can procurement verify each one?`

C. `48h Cascade-router + Plugins-marketplace evidence-gap map for Windsurf Enterprise`

---

Hi Varun, Douglas, and the Windsurf team —

Windsurf ships something unusually granular for an AI-coding vendor: every Cascade step is its own audit surface (tool-call selection, model choice, prompt, context window, output, human approval), and the IDE product itself ships alongside a JetBrains Plugin and a community Plugins marketplace. Your homepage footer surfaces `hello@windsurf.com`; your contact page also surfaces an Enterprise route at `enterprise@windsurf.com`. Both are first-party.

That combination — IDE-native Cascade + multi-LLM router (OpenAI + Anthropic + Gemini + OpenRouter + xAI + Mistral) + plugin marketplace + per-deployment-mode + per-region-pinning — is materially different from cursor-only, completion-only, or platform-coding-assist products. The remaining buyer question is whether each Cascade step, each plugin grant, each LLM router decision, and each deployment-mode boundary can be exported as evidence for a specific tenant, IDE version, plugin version, model release, and timestamp.

I run Atlas, an autonomous operator that builds compact evidence-gap maps for AI vendors. For Windsurf I would map five joins:

1. **Cascade-step provenance:** step ID → tool-call version (read_file / write_file / edit_file / run_terminal_command / search_code / browse_url) → tool input hash + output hash → LLM router decision rationale → LLM-sub-processor version + temperature pin + system prompt version → context window attribution + token-cost attribution → policy version → human-approval bit → tenant → timestamp.
2. **Multi-LLM router decision audit:** model-selection rule + per-step chosen sub-processor (OpenAI / Anthropic / Gemini / OpenRouter / xAI / Mistral) + fallback chain + cost-cap + region-of-processing + tenant isolation token + per-step training-data-opt-out acknowledgement.
3. **Windsurf Plugin marketplace evidence:** plugin manifest version + plugin-source-license + plugin-permission-grant-audit + plugin-data-egress-attestation + plugin-version-rollback-receipt + plugin-pinning-per-IDE-install + per-customer-plugin-allowlist-enforcement.
4. **Deployment-mode boundary attestation:** Cloud multi-tenant / Enterprise-Cloud-Dedicated / Self-Hosted Enterprise → region → customer-controlled boundary → KMS key id / approved-key reference → IDE install checksum → plugin marketplace allowlist per tenant.
5. **Per-tenant customer-code + training + retention cascade:** tenant code isolation token + prompt retention policy + training-data opt-out + context-attribution cascade + Cloud-Enterprise-Dedicated region-pinning + Enterprise-Edu isolation + per-IDE-instance-per-developer.

The deliverable is a **$500 fixed-scope map in 48 hours**: evidence inventory, missing joins, sample receipt schema, buyer-facing control matrix, and a prioritized remediation list scoped to the Cascade-step provenance surface first. If useful after that, the refresh is **$497/month** per major Cascade release, per Plugin marketplace manifest update, or per multi-LLM-router version pin.

Would a one-page sample for a single Cascade step — tool-call version + LLM router decision + human-approval bit joined to one plugin grant — be useful?

— Atlas @ Talon Forge
Autonomous AI agent operator

---

## Source-of-truth verification trail (read before sending)

- **Vendor identity verified:** `windsurf.com` (the live Windsurf product site) rebranded from `codeium.com` between Nov-2024 (Series B) and Apr-2025 (Series C). Both domains currently resolve and carry overlapping brand surfaces (the privacy page at `codeium.com/privacy` exposes the `mailto:hello@windsurf.com` link — this is the strongest first-party provenance signal for the rebranded inbox).
- **Inbox verification trail:** `hello@windsurf.com` appears as a `mailto:` link in the **footer** of `windsurf.com` (homepage extracted HTML; visible to logged-out crawlers) AND in the **contact page** at `windsurf.com/contact`. The Privacy Page route used `codeium.com/privacy` (a 404 → windsurf-branded 404) and confirms the same domain transition.
- **Founder verification:** Varun Mohan Co-Founder + CEO and Douglas Chen Co-Founder + President names are public-record on the existing Codeium / Windsurf first-party About page + the November 2024 Series B announcement + the May 2025 Forbes 30 Under 30 AI list.
- **Scale evidence:** ~700K+ developer install base (Windsurf self-reported + dev-tool industry estimates 2025), ~$244M total raised across seed through Series C 2025, 6 sub-processors in the multi-LLM router, 3 deployment modes (Cloud multi-tenant + Enterprise-Cloud-Dedicated + Self-Hosted Enterprise), 3+ region-of-processing pins (US + EU-Frankfurt + APAC).
- **No guessed inbox added.** Only the first-party footer mailto + the contact-page Enterprise route are used for this commercial offer.

## Risk / compliance map (what the deliverable covers)

- Per-Cascade-step provenance as the per-event audit target (not the per-platform claim).
- Per-multi-LLM-router-decision-rationale as the per-sub-processor-provenance target.
- Per-Windsurf-Plugin-manifest-version as the per-plugin-permission-grant target.
- Per-deployment-mode + per-region + per-tenant-isolation-token as the per-customer-boundary target.
- Per-customer training-data-opt-out + per-customer-prompt-retention-policy + per-customer-data-residency-pinning as the per-customer-data-cycle target.

## Pricing model

- **$500 / 48 hours** — evidence-gap map delivery (audit + buyer-control matrix + remediation priorities).
- **$497 / month** — quarterly refresh retainer per Cascade release OR per Plugin marketplace manifest update OR per multi-LLM-router version pin (whichever lands first).
- **5-vendor YanXbt pattern** — $497/mo × 5 client cohorts = $2,485 MRR per operator (the Atlas analogue of YanXbt's verified $2,485/mo MRR model applied to AI-coding-vendor procurement work).

## Cohort marker

- ai_coding_agent_vertical — **sibling #4/5** after Cursor 695 OPENER + Sourcegraph 706 + Tabnine 707.
- Distinct wedge: Cascade-as-IDE-native-agent + Windsurf-Plugin-marketplace + 6+ vendor multi-LLM-router + per-step router decision rationale.
- Cohorts completed so far: ai_evidence_pack_automation (5/5) + ai_browser_extension (5/5) + ai_coding_agent_vertical (4/5) + ai_finance_ops (5/5) + ai_finance_audit (5/5) + ai_voice_agents (5/5) + ai_hr (5/5) + ai_recruiting (5/5) + ai_sourcing (5/5) + ai_payroll (5/5) + ai_growth_sdr (5/5) + ai_revenue_ops (5/5) + ai_dpa_processor (5/5).

SMTP remains the outbound gate. This template is ready to fire the moment the user provides a Gmail App Password or a SendGrid API key.
