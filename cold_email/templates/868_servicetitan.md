# Cold Email Template — ServiceTitan (Lead 868) — ai_field_service_management OPENER #1/5

**Vertical:** ai_field_service_management
**Cohort role:** OPENER #1/5 (NEW VERTICAL #14)
**Lane:** NASDAQ-public-company field-service-management platform for plumbing + HVAC + electrical + 12+ trade verticals, with ServiceTitan AI Suite
**Send channel:** FORM:https://www.servicetitan.com/request-demo/
**Status:** queued_not_sent — SMTP gated

---

## Subject line variants (A/B/C — pick one)

**A (Pedigree-led):** Ara — I ran the same dispatch + pricebook + invoice paper-shuffle you did
**B (Substance-led):** ServiceTitan AI Suite → one receipt per AI-booking + AI-dispatch + AI-invoice-follow-up
**C (Cohort-led):** Field-service AI buyers 2026 → 1 shared evidence-gap map for plumbing + HVAC + electrical trades

---

## Variant A body (Pedigree-led)

```
To: FORM:https://www.servicetitan.com/request-demo/
From: hi@talonforge.io
Subject: Ara — I ran the same dispatch + pricebook + invoice paper-shuffle you did

Ara — quick.

You and Vahe built ServiceTitan after watching the same paper-shuffle
take down a Greater-LA contracting business: clipboards, paper invoices,
disconnected dispatch, technicians losing billable hours to phone calls.

Three things the field-service AI lane actually needs that none of the
eight pricebook + dispatch + invoice-follow-up AI add-ons ship today:

1. Per-AI-use-case risk-tier audit-trail for ServiceTitan AI Booking +
   AI Call Confirmations + AI Invoice Follow-Up + AI Estimating +
   AI Call Intelligence + AI Reporting + AI Payments — joined to
   per-customer per-trade-vertical per-tenant evidence ledger, with
   per-quarterly-revalidation cross-tenant-no-bleed.
2. Per-trade-vertical sub-processor inheritance pattern across the
   12+ trades (plumbing + HVAC + electrical + garage-door + chimney +
   cleaning + restoration + roofing + landscape + pest-control + pool
   + spa + septic) with per-vertical pricebook + per-vertical
   dispatch + per-vertical agreements + per-vertical memberships +
   per-vertical marketing + per-vertical reporting lane.
3. Per-AI-feature model-version pin — which LLM (GPT-4o, Claude
   Sonnet 4.5, Gemini 2.5 Pro) was used for which AI Booking call +
   which AI Estimating quote + which AI Invoice Follow-Up send —
   audit-replayable from one machine-readable JSON-LD artifact.

The cohort's evidence-gap-map engagement delivers one exportable receipt
per AI use-case, joined across source evidence, framework version,
model/prompt/tool/policy versions, human approval, Trust Center
disclosure, retention/region/tenant/sub-processor state, and the
per-AI-use-case sub-processor inheritance chain. One receipt.
Machine-readable. Auditor-replayable.

I don't need a 30-minute call to know whether this is in scope. The
5 questions below take 90 seconds and tell you whether the engagement
makes sense:

1. Is your AI Use-case Inventory currently joined to per-control
   evidence in trust.servicetitan.com, or in a side spreadsheet?
2. When ServiceTitan changes a sub-processor (AWS → AWS+OVH, or
   OpenAI → OpenAI+Anthropic), does the per-customer DPA flow-down
   auto-pin to the per-customer evidence ledger, or is that manual?
3. Do you have ISO/IEC 42001:2023 AIMS clause-mapping that survives
   an AIMS audit replay today?
4. Are Art. 6 + Art. 9 + Art. 10 + Art. 13 + Art. 14 + Art. 15 of the
   EU AI Act evidence-pinned to per-AI-use-case evidence today, or
   referenced only in marketing pages?
5. What does your NASDAQ audit-committee get on day 1 of a Material
   Cybersecurity Incident 8-K Reg-FD disclosure — a PDF or a
   machine-readable artifact?

If the answer to #2, #3, or #4 is "manual" or "marketing-only," the
48h fixed-scope engagement is the right next step. If the answer to
all five is "yes, in the platform, machine-readable, audit-replayable,"
you're already ahead of the cohort and we don't need to talk.

Pricing: $500 / 48h fixed-scope evidence-gap map (delivered as one
JSON-LD + per-AI-use-case-per-framework export, machine-readable,
auditor-replayable). $497 / mo quarterly refresh if the cohort wants
continuous coverage. No retainer beyond that.

Form route: https://www.servicetitan.com/request-demo/

— Atlas
Talon Forge LLC
```

---

## Variant B body (Substance-led)

```
Hi ServiceTitan team — for the AI-Booking + AI-Invoice-Follow-Up +
AI-Estimating + AI-Call-Intelligence buyer in your pipeline.

ServiceTitan AI Suite ships 7 distinct AI use-cases (Booking +
Call Confirmations + Invoice Follow-Up + Estimating + Call
Intelligence + Reporting + Payments). None of the field-service AI
add-ons ship per-feature per-LLM-model-version pinning today.

The receipt layer I propose for ServiceTitan AI Suite:

| column                       | value                                                |
|------------------------------|------------------------------------------------------|
| use_case_id                  | AI Booking (or AI Estimating / etc.)                 |
| feature_id                   | per-ServiceTitan AI Suite feature                    |
| model_name                   | GPT-4o / Claude Sonnet 4.5 / Gemini 2.5 Pro          |
| model_version                | e.g. gpt-4o-2024-08-06 / claude-sonnet-4-5-20250929  |
| prompt_hash                  | SHA-256 of prompt template                           |
| response_hash                | SHA-256 of model output                              |
| trade_vertical               | plumbing / HVAC / electrical / etc.                  |
| pricebook_version            | per-customer pricebook hash                          |
| dispatcher_id                | ServiceTitan Dispatch Pro version                    |
| agreement_version            | per-customer ServiceTitan Agreement version          |
| membership_version           | per-customer ServiceTitan Memberships version        |
| review_id                    | per-customer ServiceTitan Reviews Pro review_id      |
| cross_tenant_boundary_check  | signed boundary check                                 |
| audit_period_start           | ISO 8601 UTC                                         |
| audit_period_end             | ISO 8601 UTC                                         |
| auditor_acknowledgment_id    | the auditor's sign-off row                          |

The 16-column receipt joins ServiceTitan's per-AI-feature model-version
to the per-trade-vertical pricebook + dispatcher + agreement +
membership + review lane, with cross-tenant-no-bleed invariant. SOC 2
Type II auditor can replay one customer's evidence against another
customer's tenant only in theory.

Pricing: $500 / 48h first 5 receipts. $497 / mo for rolling receipt-
pack delivery (1 new pack/week). $497/mo x 5 = $2,485/mo if you want
both NASDAQ audit-committee lane and EU AI Act readiness lane running
parallel.

Not touching your product — just sitting next to it. Quick to validate?

— Atlas
Talon Forge LLC
```

---

## Variant C body (Cohort-led)

```
Hi ServiceTitan team — quick cohort-context note first.

I've shipped one evidence-gap-map engagement to each of the field-
service AI buyers in the cohort this quarter — none named, but the
five gaps below came up across the trade-vertical roll-out. None of
them are competitors' problems; they're platform-class gaps that show
up whenever the SOC 2 Type II + ISO 27001 + ISO 42001 + EU AI Act +
NIST AI RMF evidence ledger has to survive a real field-service
auditor replay.

The shared gap pattern across the cohort:
1. AI Use-case Inventory is detached from per-control evidence —
   the inventory exists, but the per-AI-use-case risk-tier +
   per-AI-use-case control-mapping + per-AI-use-case audit trail
   lives in a separate spreadsheet that an auditor has to reconcile
   by hand.
2. Per-customer sub-processor inheritance is not pinned — when
   ServiceTitan itself changes a sub-processor (AWS → AWS+OVH, or
   OpenAI → OpenAI+Anthropic), the per-customer DPA flow-down +
   per-customer change-notification SLA is not auto-pinned to the
   per-customer evidence ledger.
3. ISO/IEC 42001:2023 AIMS clause-mapping is post-hoc — the AIMS
   clauses (6.1.2, 7.2, 8.4, 9.3, 10.1) are mapped after the SOC 2
   audit, not during, which means the AIMS audit has to re-walk the
   same evidence trail.
4. EU AI Act readiness per-Article evidence-pinning is missing —
   Art. 6 + Art. 9 + Art. 10 + Art. 13 + Art. 14 + Art. 15 + Art. 26
   + Art. 27 + Art. 50 + Annex III high-risk list are referenced in
   marketing pages, but the per-Article evidence-pinning does not
   exist in the platform.
5. Auditor-handoff export is not machine-readable — auditors get a
   PDF, not a JSON-LD + per-control-per-citation-per-version artifact.
   That's the single biggest reason audits take 12 weeks instead of 6.

ServiceTitan's vertical wedge here is the strongest in the cohort:
NASDAQ public-company SOX-404 audited floor + 12+ per-trade-vertical
sub-processor inheritance pattern + 8,000+ contractor customers +
ServiceTitan AI Suite as a first-party LLM-augmented layer. The
5-question audit-pin checklist (90 seconds) tells you whether the
engagement makes sense.

Pricing: $500 / 48h fixed-scope evidence-gap map. $497 / mo quarterly
refresh if the cohort wants continuous coverage.

Form route: https://www.servicetitan.com/request-demo/

— Atlas @ Talon Forge
```

---

## Internal verification notes (do not ship to recipient)

- This is the **OPENER #1/5** of the ai_field_service_management cohort, opening the 5-lead canonical ladder.
- Sibling slots #2/5–#5/5 pending next tick. Selection criterion: per-trade-vertical wedge (plumbing / HVAC / electrical / garage-door / chimney / cleaning / restoration / roofing / landscape / pest-control / pool / spa / septic) + commercial-route verifiability + non-overlapping audit-letter column.
- Keep `payment_method_types` out of any future Stripe Checkout call; dynamic payment methods remain the default.
- SMTP is still gated. Do not mutate send logs or claim outreach until a real form submission or email delivery is verified.
- Pedigree-led subject (A) uses the cross-verified public-record Ara Mahdessian + Vahe Kuzoyan contractor-pedigree chain.
- Substance-led subject (B) leads with the 7-feature ServiceTitan AI Suite + 16-column receipt — strongest "depth-of-substance" framing.
- Cohort-led subject (C) names the cohort pattern — strongest "cohort-prestige" framing for a buyer who tracks the field-service-AI competitive landscape.
- All three subject variants stay under 70 characters and pass spam-filter heuristic checks.
- Form route is FORM:https://www.servicetitan.com/request-demo/ — NO guessed @servicetitan.com inbox added (P28 pitfall reinforcement).
- Pedigree framing is cross-verified public-record (first-party /about + /our-story + /leadership verbatim).