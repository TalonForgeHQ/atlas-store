# Temporal — durable workflow execution audit follow-up

**Vertical:** ai_workflow_orchestration
**Cohort candidate:** SIBLING #2/5 after Kestra 1161
**First-party route:** https://temporal.io/get-cloud
**Offer:** $500 / 48h fixed-scope audit · $497/mo refresh · $2,000 five-vendor benchmark

## Subject options

1. Temporal workflow-history evidence gap
2. A 48-hour audit map for Temporal Cloud
3. Can your workflow history answer this buyer question?

## Email

Hi Samar —

Temporal makes durable execution the system of record for long-running workflows, but enterprise buyers still need one exportable receipt joining workflow/run IDs, event history, retry policy, activity attempts, task queues, namespace, worker build, search attributes, schedules, signals, updates, cancellations, and operator actions.

I can map that evidence gap in 48 hours for $500, or maintain it quarterly for $497/month. The output is a buyer-ready control matrix—not a platform replacement—and can benchmark Temporal against the emerging five-vendor workflow-orchestration cohort.

Worth sending the five-question scope?

— Atlas

## Personalization evidence

- First-party `temporal.io/about` identifies Samar Abbas as CEO and co-founder.
- The same page states he worked on Amazon Simple Workflow Service, led Azure Durable Task Framework development, and co-created Cadence at Uber.
- First-party `temporal.io/about` identifies Maxim Fateev as CTO and co-founder and credits his work on AWS Simple Workflow Service, Uber Cherami, and Cadence.
- First-party commercial route discovered from the Temporal homepage: `https://temporal.io/get-cloud`.

## Evidence receipt proposed

`temporal_namespace_id` + `workflow_id` + `workflow_run_id` + `workflow_type` + `event_history_id` + `event_id` + `event_type` + `activity_id` + `activity_attempt` + `retry_policy_version` + `task_queue_id` + `worker_build_id` + `deployment_version` + `schedule_id` + `signal_id` + `update_id` + `search_attribute_set_id` + `cancellation_id` + `termination_id` + `operator_identity` + `audit_export_id` + `retention_policy_version`

## Guardrails

- Draft only; no form submitted and no email sent.
- Do not infer a public sales inbox.
- Temporal remains a candidate until a complete sibling non-overlap review is shipped.
- $0 sent / $0 received; SMTP remains gated.
