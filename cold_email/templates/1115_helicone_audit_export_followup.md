# 1115 — Helicone AI Gateway audit-export replay follow-up

**Vertical:** `ai_agent_audit_export` (follow-up frame; existing Helicone lead 1066 is not duplicated)
**Tick:** `2026-07-24-fast-exec-helicone-audit-export-followup-1115`
**Buyer frame:** SOC 2 Type II / EU AI Act / ISO 42001 auditor replay of an OpenAI-compatible gateway request
**First-party routes:** `mailto:contact@helicone.ai` on the Helicone homepage + `FORM:https://www.helicone.ai/contact`; neither is submitted

**Subject options**

A. Helicone audit-export replay — can one gateway request reconcile to cost and provider evidence?
B. Phoenix-style audit evidence for Helicone routing, fallback, cache, and downstream action
C. A 48-hour Helicone gateway replay map for SOC 2 and AI-agent audit exports

Hi {{first_name}},

Helicone already has a separate gateway/observability lead in our catalog (lead 1066). This is a narrower follow-up for the new `ai_agent_audit_export` buyer question: can an auditor replay one OpenAI-compatible request byte-for-byte from tenant boundary through provider routing, cache/fallback decisions, cost reconciliation, and the downstream agent action without cross-tenant bleed? No duplicate lead row is created.

**Paragraph 1 — Why Helicone is the right replay recipient.** First-party Helicone pages describe an AI Gateway and LLM Observability platform, an OpenAI-compatible unified API with access to 100+ models, automatic logging, observability, fallbacks, unified billing, sessions, alerts, and evaluations. The first-party homepage also states SOC 2 Type II certification, HIPAA compliance, encryption, least-privilege controls, regular penetration testing, and a self-hosted AI Gateway. The commercial route is the live Contact Us form at https://www.helicone.ai/contact; the homepage also exposes `mailto:contact@helicone.ai`.

**Paragraph 2 — Five scoped evidence questions.** I would map one deterministic export row joining `tenant_id + helicone_workspace_id + helicone_request_id + session_id + user_id + provider + model + prompt_hash + response_hash + cache_status + retry_count + fallback_chain_id + rate_limit_policy + input_tokens + output_tokens + cost_usd + latency_ms + status_code + downstream_agent_action_id + audit_export_id + cross_tenant_no_bleed_proof` and answer:

1. **Which** tenant, workspace, session, user, provider, model, and model version produced the response?
2. **Can** an auditor replay the exact prompt and response hashes while preserving the tenant-scoping boundary?
3. **How** did cache hits, retries, rate limits, and fallback-provider decisions change the final route and latency?
4. **What** token, cost, status, and latency fields reconcile the Helicone request to the provider invoice and downstream agent action?
5. **Why** can the exported request-to-action record be retained, deleted, and independently checked for cross-tenant no-bleed?

**Paragraph 3 — Offer and honest status.** The fixed-scope deliverable is a **$500/48h** Helicone audit-export replay map for one production request class, a **$497/month** quarterly evidence refresh, or a **$2,000** five-vendor `ai_agent_audit_export` benchmark after the cohort fills (Arize AI + Helicone + Maxim AI + Langfuse + LangSmith). No form was submitted and no email was sent: authenticated SMTP/forms remain gated, so **$0 sent / $0 received** and no payment or revenue is claimed.

No duplicate lead row is created for Helicone: the existing gateway lead is 1066, while this 1115 artifact is a differentiated audit-export follow-up frame.

— Atlas @ Talon Forge LLC
