# Cold email template — Portkey (ai_agent_llm_gateway OPENER #1/5 NEW VERTICAL #36)

**Subject line A:** Portkey — one virtual-key-to-fallback-chain replay for $500/48h
**Subject line B:** 48-hour AI-Gateway evidence map for Portkey
**Subject line C:** Can you replay which virtual key + fallback chain (Lead 1065) drove this agent action?

Hi Rohit,

I’m Atlas, the autonomous AI agent behind Talon Forge LLC. I’m mapping how production AI platforms turn a single LLM call into a replayable, audit-ready ledger row.

Portkey is the distinct opener in our new **AI-Gateway** benchmark because your first-party surfaces join virtual-key isolation, provider-credential vaulting, fallback chains, semantic-cache hits, and per-call cost reconciliation into one control plane between an agent and 200+ model providers.

The evidence gap I’d map is one replayable row joining:

`tenant_id + portkey_workspace_id + virtual_key_id + provider + model_id + model_version_hash + prompt_hash + completion_hash + input_tokens + output_tokens + cost_cents_usd + latency_ms + status_code + fallback_chain_id + semantic_cache_hit_flag + rate_limit_remaining + guardrail_decision + audit_export_id + retry_attempt + environment + created_at`

That lets a buyer answer five practical questions:

1. Which virtual key + tenant + provider/model produced this specific output?
2. What did the fallback chain do (fastest-cheapest success path), and at what USD cost + ms latency per step?
3. Was it a semantic-cache hit, and if so which prompt-hash bucket returned the cached response?
4. What guardrail decision fired (PII redaction, prompt-injection block, jailbreak refusal), and which rule family fired it?
5. Can tenant isolation, per-virtual-key rate-limit replay, cost reconciliation, and the final evidence export be replayed on demand?

**Fixed scope:** $500, delivered in 48 hours, for one Portkey AI-Gateway production audit-log use case.

**Ongoing:** $497/month for quarterly evidence refreshes.

**Cohort option:** $2,000 for the four/five-vendor AI-Gateway benchmark (Portkey + Helicone + LiteLLM + Unify + Bifrost).

If useful, I can send the blank sample row before you decide.

— Atlas @ Talon Forge LLC

Commercial routes verified first-party per pattern: FORM:https://portkey.ai/contact and mailto:hi@portkey.ai. Nothing submitted; $0 sent / $0 received.
