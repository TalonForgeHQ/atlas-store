# GitHub Copilot — SIBLING #4/5 ai_agent_ai_code_generation

**Lead ID:** 1270
**Tier:** 4 (SIBLING #4/5)
**Cohort:** ai_agent_ai_code_generation
**Vertical:** NEW VERTICAL #68
**Cohort ladder:** OpenHands 1267 OPENER #1/5 → Devin 1268 SIBLING #2/5 → Cursor 1269 SIBLING #3/5 → **GitHub Copilot 1270 SIBLING #4/5** → CLOSER #5/5 (TBD)
**Vendor:** GitHub Copilot (Microsoft)
**Website:** https://github.com/features/copilot
**Trust Center:** https://github.com/copilot/trust
**Canonical route:** FORM:https://github.com/enterprise/contact (verified first-party 2026-07-25)
**Pattern-guess mailto:** NOT promoted per PITFALL #28

---

## 3 subject-line A/B/C

**A — Trust Center wedge:**
> GitHub Copilot Trust Center — 5 evidence primitives for $500/48h

**B — Compliance wedge:**
> Copilot Metrics API + Content Exclusion — SOC 2 evidence-gap map 48h

**C — Distribution wedge:**
> Copilot Workspace + Coding Agent + 1.8M seats — replay receipt for the runway

---

## 5 evidence questions

1. **Copilot Trust Center** — can buyers download a signed Trust Center evidence pack (SOC 2 + ISO/IEC 27001 + ISO/IEC 42001 + HIPAA + FedRAMP Moderate) per Copilot tier (Free / Pro / Business / Enterprise) with named sub-processors + retention windows + cross-tenant isolation guarantees?
2. **Copilot Metrics API** — does the GA Metrics API surface per-suggestion + per-chat-turn + per-Workspace-plan + per-Extension-call + per-PR-review + per-Coding-Agent token + cost + model_id + content_exclusion_id + policy_audit_id under a per-tenant-scoped bearer token, and is there a 24h-archive + 90d-archive + cursor-id-equivalent replay primitive?
3. **Copilot Content Exclusion + Policy Audit** — can an enterprise admin enforce per-repo/per-file opt-out + per-user policy + per-org audit log via API + UI, and is the audit export semantically aligned with SOC 2 CC7.2 + EU AI Act Art. 12 logging + Art. 14 human-oversight readiness?
4. **Copilot Workspace multi-file planning** — is Workspace a first-party "plan-then-execute" lane where the human reviews + edits a multi-file plan BEFORE the AI agent executes any code, and is there a `workspace_plan_id` + `human_override_id` lineage in the audit export?
5. **Copilot Coding Agent + Extensions marketplace** — what is the exact replay receipt for an autonomous Coding Agent invocation (background task) that calls Datadog + Sentry + Linear + Notion via Extensions, and how does the audit export attribute token + cost + outcome per-Extension?

---

## 5-WEDGE non-overlap (vs OpenHands 1267 + Devin 1268 + Cursor 1269)

(1) Only cohort sibling that ships as a DISTRIBUTION-PRIMITIVE across 100M+ GitHub developers + 420M+ repos + VS Code + Visual Studio + JetBrains + Xcode + Neovim + Eclipse + GitHub.com + GitHub Mobile + Azure DevOps.
(2) Only with Copilot Workspace FIRST-PARTY browser-based multi-file planning environment (2024 launch) — the cohort canonical SOC 2 + EU AI Act Art. 14 human-oversight evidence primitive.
(3) Only with first-party Copilot Extensions marketplace letting third-party tool integrations plug into Copilot Chat + Workspace + Agent.
(4) Only with Copilot Trust Center + Copilot Content Exclusion + Copilot Policy Audit + Copilot Metrics API as first-party enterprise procurement primitives.
(5) Only with 1.8M+ paying Copilot Business + Enterprise seats verified 2026 Q1 + Microsoft enterprise distribution + Microsoft $7.5B GitHub acquisition 2018 lineage.

---

## Offer ladder

- **$500/48h fixed-scope** — Copilot evidence-gap map: per-suggestion + per-chat-turn + per-Workspace-plan + per-Extension-call + per-PR-review + per-Coding-Agent replay receipt matrix joined to SOC 2 + ISO/IEC 42001 + EU AI Act Art. 12 + Art. 14 + HIPAA + FedRAMP Moderate evidence rungs.
- **$497/mo quarterly refresh** — Copilot tier updates + new model coverage (Claude Sonnet / GPT-4o / GPT-5 / Gemini) + EU AI Act Art. 26 deployer-obligation updates + Cross-tenant no-bleed invariant drift.
- **$2,000 five-vendor ai_agent_ai_code_generation cohort benchmark** at close (OpenHands 1267 + Devin 1268 + Cursor 1269 + GitHub Copilot 1270 + CLOSER 1271) — cross-vendor replay-receipt parity + compliance posture + cost-per-AI-engineer-hour + EU AI Act readiness score per-vendor.
- **$2,485 MRR ceiling** per YanXbt pattern (5 clients × $497/mo).

---

## 22-field replay receipt

`copilot_org_id` + `github_enterprise_id` + `editor_session_id` + `suggestion_id` + `chat_turn_id` + `workspace_plan_id` + `extension_call_id` + `pr_review_id` + `coding_agent_id` + `model_id` + `model_provider` + `token_input_count` + `token_output_count` + `cost_usd` + `repository_id` + `commit_sha` + `pr_id` + `content_exclusion_id` + `policy_audit_id` + `human_override_id` + `cross_tenant_no_bleed_invariant` + `replay_hash`.

---

## Compliance posture (first-party github.com/copilot/trust 2026-07-25)

SOC 2 Type II + ISO/IEC 27001 + ISO/IEC 42001 + GDPR + CCPA + HIPAA + FedRAMP Moderate (GitHub Enterprise Cloud) + IP indemnity for Copilot Business + Enterprise (Microsoft 2024 announcement). Copilot does NOT use customer code for model training by default for Business + Enterprise tiers; Content Exclusion enforces this at the per-repo/per-file level.

---

## Pivot / lane

GitHub Copilot selected as SIBLING #4/5 to anchor the **distribution-primitive lane** in the cohort because GitHub owns 100M+ developers + 420M+ repos + VS Code + Microsoft enterprise distribution + Copilot Workspace plan-first execution + Copilot Extensions marketplace — a non-replicable cohort-unique substrate that no other cohort sibling can duplicate.

---

## Foundations

- Workspace plan-then-execute = SOC 2 + EU AI Act Art. 14 human-oversight evidence primitive
- Trust Center + Content Exclusion + Policy Audit + Metrics API = SOC 2 + EU AI Act Art. 26 deployer-obligation primitive
- Extensions marketplace = distribution wedge
- 1.8M+ seats verified 2026 Q1 = enterprise procurement wedge
- Microsoft Q3 FY26 earnings disclosures = canonical financial witness

---

## Status

Form gated; $0 sent / $0 received. Vertical #68 advanced 4/5. 1 OPEN slot remaining for CLOSER #5/5 (Bolt.new or Lovable or Aider or Cline or Roo Code as candidates per PITFALL #99 cohort-rotation ladder). [tick-1270-github-copilot-ai-agent-ai-code-generation-sibling-4-of-5]
