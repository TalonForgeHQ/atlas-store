# incident.io — ai_agent_operations sibling #2/5 (Lawrence Jones, CEO, OnCall Ltd, London)

**Vertical:** ai_agent_operations
**Cohort role:** sibling #2/5 (after Resolve AI 1094 OPENER #1/5)
**Lead:** 1095
**Commercial route:** FORM:https://incident.io/contact
**Offer:** $500 / 48h evidence-gap map OR $497/mo quarterly refresh OR $2,000 five-vendor cohort benchmark

---

## Subject lines (3)

1. incident.io ↔ Resolve AI: a replayable evidence row from page to postmortem
2. ai_agent_operations sibling #2/5 — incident.io evidence-gap map, $500/48h
3. London incident.io — on-call → AI summary → postmortem evidence chain for SOC 2

---

## Body (canonical, 1-of-3 send)

Hi Lawrence,

Your first-party page describes incident.io as an all-in-one incident management platform unifying on-call scheduling, real-time incident response, AI summaries, status pages, and post-mortems — and the PagerDuty Rescue Program + Opsgenie Rescue Program position you as the explicit migration target for teams leaving legacy on-call stacks.

The ai_agent_operations cohort Atlas is building this quarter places incident.io as **sibling #2/5**, after Resolve AI 1094. The cohort's distinguishing axis is: an AI agent that runs in production, reads real infra state, and is bound to a verifiable incident-replay trail. The unresolved gap for incident.io specifically is the join between the **on-call schedule + escalation policy + page event**, the **AI-generated incident summary**, the **postmortem document**, the **status-page incident**, and the **customer comms send** — a single replayable row that lets a SOC 2 auditor reconcile one customer-impacting incident from page to postmortem to public comms.

The 20-column receipt below is the artifact we will deliver in 48 hours for **$500**:

```
tenant_id + incident_id + on_call_schedule_id + escalation_policy_id +
page_id + service_id + severity + channel_id + ai_summary_id +
rca_doc_id + rca_evidence_chain_id + remediation_action_id +
human_override_id + status_page_incident_id + postmortem_id +
customer_impact_id + customer_comm_send_id + token_spend_usd +
latency_ms + audit_export_id
```

It is deliberately narrow enough to implement for one production incident while preserving the causal chain from page to postmortem to customer comms, with no cross-tenant bleed.

Three concrete questions the replay should answer for incident.io:

1. Which on-call schedule + escalation policy fired the page, and which channel carried it?
2. Which AI summary and rca evidence chain were produced, and is the postmortem linked?
3. Which status page update + customer comm went out, and can a SOC 2 auditor reconcile the row to the audit export?

If useful, Atlas will run this as a fixed-scope 48-hour engagement (**$500**) for one production incident end-to-end, or as part of the **$497/month** quarterly cohort refresh, or as the **$2,000** five-vendor ai_agent_operations benchmark covering incident.io and four sibling candidates.

Atlas @ Talon Forge
FORM:https://incident.io/contact — first-party commercial route; no form submitted this tick; SMTP/form gated; **$0 sent / $0 received**.

---

## Variant 2 — security/GRC buyer

Same offer, addressed to the SOC 2 / ISO 27001 buyer. Hook: incident.io's first-party security page advertises SOC 2 Type II + ISO/IEC 27001:2022; the evidence-gap row above is the SOC 2 CC7.2 + ISO 27001 A.5.24 (incident management planning) + A.5.25 (assessment and decision on information security events) + A.5.26 (response to information security incidents) + A.5.27 (learning from information security incidents) + A.5.28 (collection of evidence) cross-walk that auditors ask for and rarely receive in one row.

## Variant 3 — customer-success / on-call lead

Same offer, addressed to the on-call lead. Hook: the PagerDuty Rescue Program and Opsgenie Rescue Program convert teams that just decided to leave — the 48h evidence row is the same artifact that closes the migration ticket, and the cohort benchmark is the comparison the buyer's CTO will be asked for.

---

## Compliance + evidence footer

Evidence status: first-party product claims retrieved from incident.io + incident.io/blog + incident.io/security on 2026-07-23; Lawrence Jones CEO verified at LinkedIn lawrence2jones + incident.io/about; no email or form submitted this tick; queued_not_sent; **$0 sent / $0 received**.

Pricing reference: $500 / 48h fixed-scope; $497/month quarterly refresh; $2,000 five-vendor cohort benchmark. Commercial route: FORM:https://incident.io/contact.

Atlas @ Talon Forge — template 1095 — incident.io SIBLING #2/5 ai_agent_operations — page-to-postmortem evidence-gap map — 2026-07-23.
