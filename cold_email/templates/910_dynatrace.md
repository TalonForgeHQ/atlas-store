# Lead 910 — Dynatrace (Cold Email Template)

**Recipient:** Rick McConnell, Chief Executive Officer
**CTO + Co-Founder:** Bernd Greifeneder (originally from Linz Austria founding team 2005)
**Commercial route:** `https://www.dynatrace.com/request-demo/` (verbatim first-party, verified 2026-07-22)
**Vertical:** `ai_observability_platform_broad` — SIBLING #2/5 (after Datadog 891 OPENER #1/5)

## Subject variants

**A (51 chars):** `Rick — Dynatrace's causal-AI evidence-replay audit map`
**B (56 chars):** `Dynatrace — proof from AI span to Smartscape topology`
**C (52 chars):** `A 48-hour evidence map for Dynatrace's AI Observability`

## Recommended body

Hi Rick,

Dynatrace's first-party platform story for the AI era is unusually concrete: OneAgent automatic full-stack topology discovery + Grail schema-on-read data lakehouse + Davis causal-AI engine for deterministic root cause + a NAMED first-party `/platform/ai-observability/` product line for LLMs + agents + retrieval pipelines + Threat Observability + Application Security inside the same platform. NYSE-listed DT, 50 offices worldwide, named customers Air Canada + TD Bank + Dell Technologies + Virgin Money + TELUS.

The procurement gap is usually the join between those surfaces. Can a reviewer replay one AI Observability span — for an LLM call, an agent action, a retrieval pipeline step — through the Davis causal finding + the OneAgent host + process + the Smartscape topology node + the Grail data-lakehouse table that produced it, joined to a Threat Observability alert at the same tenant boundary, without manual reconciliation?

I build that evidence map. For Dynatrace, I would scope a 16-column receipt around:

`tenant_id + ai_observability_span_id + llm_request_id + agent_action_id + retrieval_pipeline_step_id + prompt_template_version_id + llm_model_version_id + token_cost_record_id + davis_causal_finding_id + oneagent_host_id + oneagent_process_id + smartscape_topology_node_id + grail_data_lakehouse_table_id + threat_observability_alert_id + tenant_boundary_id + audit_replay_id`

Five buyer-facing questions:

1. Can every AI Observability span — for LLM + agent + retrieval — be pinned to the Dynatrace tenant + OneAgent host + process + Smartscape topology + Grail table across all customers that share the account?
2. Can Davis causal-AI findings be replayed deterministically — root cause + topology dependency chain + contributing span tree — rather than statistically?
3. Can Threat Observability alerts + Application Security findings join to the same AI Observability span export via the tenant boundary + host + process + topology without manual reconciliation?
4. Can Grail retain per-tenant per-LLM-call + per-token-cost + per-Davis-finding records at compliance-grade retention with cross-tenant no-bleed isolation?
5. Can procurement-grade audit replay for EU AI Act Art. 9 + 14 + 15 + 50 + NIST AI RMF GOVERN/MAP/MEASURE/MANAGE be exported as one artifact — Davis finding + OneAgent host + Smartscape + Grail + AI Observability span + Threat Observability alert — without stitching?

Engagement format: $500 for a 48-hour fixed-scope Davis-AI-Observability evidence-gap map. Optional $497/mo quarterly refresh to keep the evidence fresh across Davis version + OneAgent version + Grail schema + AI Observability span-schema changes. Five clients at that rate = $2,485 MRR ceiling under the YanXbt pattern.

Three cold lines if you want a tighter draft:

> Datadog shows the cohort is converging on broad AI-observability surface. The clean differentiator for Dynatrace is the Davis causal-AI engine + OneAgent + Grail + Smartscape automatic topology + the NAMED first-party AI Observability product line — and that is also the surface where the audit-replay question is hardest.

> If a Fortune-500 buyer asks for a single artifact that pins an AI Observability span to the Davis finding + the OneAgent host + the Smartscape topology + the Grail table + the Threat Observability alert at one tenant boundary — Dynatrace's first-party story implies yes, but the cold file says it is not stitched together yet.

> The strongest wedge is Dynatrace's Davis causal-AI engine + OneAgent + Grail + Smartscape + AI Observability + Threat Observability substrate, mapped against EU AI Act Art. 9 + 14 + 15 + 50 and NIST AI RMF GOVERN/MAP/MEASURE/MANAGE, joined as one exportable audit-replay artifact.

Best,

Atlas @ Talon Forge  
talonforge.substack.com · @TalonForgeHQ (X) · talonforgehq.github.io/atlas-store

—
