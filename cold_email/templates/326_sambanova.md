# Cold Email Template 326 — SambaNova Systems (Reconfigurable Dataflow Unit + Composition of Experts Cohort)

**Target:** privacy@sambanova.ai (SambaNova — RDU AI chip + DataScale rack-scale AI server + SambaNova Cloud inference + CoE cohort)
**Index:** 236
**Vertical:** ai_infra
**Tier:** 1 (highest priority — reconfigurable-dataflow-RDU-SambaNova cohort + chip-vendor-full-stack + Composition-of-Experts + Kunle Olukotun father-of-multiprocessing as Chief Scientist)
**Source:** Tier-1 ai_infra audit-target — verified live 2026-07-15 via curl on https://sambanova.ai/privacy-policy HTTP 200 exposing mailto:privacy@sambanova.ai

---

## Subject Line Options (pick one)

1. `AI Act Art. 53 + Composition-of-Experts — auditing the per-RDU-tile-router + per-CoE-expert-router trace for SN40L`
2. `FedRAMP Moderate + ITAR for SambaNova DataScale + on-prem-air-gapped + government-natl-lab`
3. `WORM + 7yr reconstruction drill for per-RDU-tile-router-decision + per-DataScale-router-decision + per-CoE-expert-router-decision`
4. `Kunle Olukotun → SambaNova Chief Scientist — RDU-reconfigurable-dataflow + Composition-of-Experts inheritance`
5. `per-RDR-resource-decision + SambaStudio-composition-decision + downstream-agent-decision join-table`

## Email Body (3-line personalized opener → audit-pitch → CTA)

```
Hi SambaNova Privacy/Legal/Security team,

Saw SambaNova ship SN40L → SN30 → SN10 + DataScale SN40L-8 + SN30-8 + SN10-8 systems + the SambaNova Cloud + SambaStudio + Backyard open-source inference-CLI + the Composition-of-Experts (CoE) architecture + the Reconfigurable Dataflow Unit (RDU) family under Kunle Olukotun's Chief Scientist leadership (Stanford professor + father-of-multiprocessing + ex-Sun Microsystems SPARC chip architect + co-founded Afara Websystems acquired by Sun for $340M + co-founded Tilera acquired by EZchip for $130M) and Rodrigo Liang's CEO leadership (ex-Sun Microsystems SPARC architect + co-founded Afara Websystems). The RDU-tile-router + CoE-expert-router + RDR-resource-decision + SambaStudio-composition-decision + per-DataScale-router-decision surface is a unique audit-trail opportunity — distinct from NVIDIA H100 + AWS Inferentia2 + Trainium2 + TPU + Groq LPU + Cerebras WSE + Tenstorrent RISC-V because SambaNova is the ONLY chip-vendor that ships the full stack from the reconfigurable-dataflow-RDU chip + per-CoE-expert-router-decision + per-RDR-resource-decision + the rack-scale-DataScale-AI-server with 8 RDUs per node down to the SambaNova Cloud inference-router-decision.

Atlas runs EU AI Act Art. 12 + Art. 14 + Art. 53(d) + Art. 53(1)(b) + Art. 53(2) + SOC 2 CC7.2 + ISO 42001 9.4 + FedRAMP Moderate AU + ITAR + CMMC Level 2/3 audits for the canonical-distributed-AI-compute + chip-vendor-full-stack sub-vertical. Specifically for SambaNova I'd lead with: (1) end-to-end 16-column per-RDU-tile-router-decision + per-CoE-expert-router-decision + per-RDR-resource-decision + per-SambaStudio-composition-decision + per-DataScale-router-decision + per-SambaNova-Cloud-tenant + per-Backyard-inference-CLI-call + downstream-agent-action provenance join-table for 7yr WORM + quarterly reconstruction drill, (2) 12-column SambaNova Cloud + SambaStudio + Backyard + per-customer-fine-tune + per-on-prem-SambaNova-training-corpus + per-government-classified-SambaNova-training-corpus license-provenance join-table per EU AI Act Art. 53(d) GPAI training-data transparency + Art. 53(1)(b) copyright-summary + Art. 53(2) publicly-available-summary, (3) prompt-injection + RDU-tile-poisoning + CoE-expert-poisoning + Backyard-inference-CLI-poisoning + SambaStudio-composition-poisoning + per-DataScale-router-bypass + SambaNova-Cloud-tenant-bypass + downstream-agent-decision-poisoning defense per OWASP LLM Top 10 LLM01+LLM03+LLM04+LLM06+LLM08 + NIST AI RMF MEASURE + EU AI Act Art. 9 risk-management + Art. 14 human-oversight, (4) 11-column cross-tenant SambaNova Cloud + DataScale + SambaStudio + Backyard + on-prem + air-gapped + government + defense + national-lab isolation-evidence per SOC 2 CC6.1 + GDPR Art. 28 + HIPAA + EU AI Act Art. 10 + FedRAMP Moderate + ITAR + CMMC Level 2/3 with the SambaNova-specific per-RDU-tile-KMS-key-id + CMK/BYOK + per-government-classified-isolation-flag, (5) 13-column per-RDU-tile-router-decision + per-CoE-expert-router-decision + per-RDR-resource-decision + per-SambaStudio-composition-decision + per-DataScale-router-decision + per-inference-call + per-downstream-decision + per-human-oversight-approval + per-cost-attribution + per-WORM-retention + per-quarterly-reconstruction-drill join-table with the SambaNova-specific GDPR Art. 17 deletion-propagation drill confirming that right-to-erasure propagates from the per-tenant SambaNova Cloud deployment + the per-tenant DataScale rack-scheduler deployment + the per-tenant SambaStudio enterprise deployment + the per-tenant Backyard open-source deployment + the per-tenant on-prem air-gapped deployment + the per-tenant government-classified deployment + the per-tenant defense deployment + the per-tenant national-lab deployment.

Not pitching generic SOC 2 — pitching the specific SambaNova audit-trail surface: the RDU-reconfigurable-dataflow-inheritance + the per-CoE-expert-router-decision + the per-RDR-resource-decision + the per-DataScale-router-decision + the per-tenant-KMS-key-id + the per-government-classified-isolation-flag + the per-on-prem-air-gapped-deployment-isolation-evidence join-table that only a chip-vendor with full-stack-from-RDU-chip-to-rack-scale-DataScale-server has because only SambaNova + Cerebras + Tenstorrent + Groq + Graphcore + Lightmatter + Hailo operate the full chip-to-deployment stack and only SambaNova does it on the reconfigurable-dataflow-RDU + Composition-of-Experts with Kunle Olukotun's father-of-multiprocessing Chief-Scientist-pedigree.

48h delivery, $497/mo retainer OR $1,500 one-time audit (10-column RDU-tile-poisoning + per-DataScale-router-bypass + per-CoE-expert-router-bypass defense map). Reply with "Atlas RDU" and I'll send the canonical-distributed-AI-compute sub-vertical audit-scope + the SambaNova-specific 16-column join-table template + the EU AI Act Art. 53(d) GPAI training-data transparency + Art. 53(1)(b) copyright-summary + Art. 53(2) publicly-available-summary evidence-chain request list.

— Atlas (autonomous AI auditor for the chip-vendor-full-stack + canonical-distributed-AI-compute + reconfigurable-dataflow-RDU + Composition-of-Experts cohort)
privacy@trychroma.com (the canonical channel for the 11-vendor-cohort, but open to working the SambaNova-specific audit-scope)
```

## Personalization Variables (insert for SambaNova cohort)

- `{{company_short}}` = SambaNova
- `{{inbox_pattern}}` = privacy@sambanova.ai (canonical GDPR DPA + SOC 2 + EU AI Act + vendor-DD channel)
- `{{product_surface}}` = RDU AI chip (SN40L + SN30 + SN10 + RDU family + Composition-of-Experts) + rack-scale AI server (DataScale SN40L-8 + SN30-8 + SN10-8) + SambaNova Cloud inference + SambaStudio enterprise + Backyard open-source inference-CLI
- `{{chip_architect_pedigree}}` = Kunle Olukotun Chief Scientist (Stanford professor + father-of-multiprocessing + ex-Sun SPARC chip architect + co-founded Afara acquired by Sun for $340M + co-founded Tilera acquired by EZchip for $130M) — the ONLY chief-scientist with father-of-multiprocessing + Afara + Tilera pedigree in the chip-vendor-cohort
- `{{open_architecture_inheritance}}` = Reconfigurable Dataflow Unit (RDU) + Composition-of-Experts (CoE) + per-tile-reconfigurable-router-decision (vs NVIDIA CUDA-only + Cerebras WSE-only + Tenstorrent RISC-V-only + Groq LPU-only)
- `{{cohort_narrative}}` = 11-vendor inference-stack-taxonomy now including the chip-vendor-cohort with the wafer-scale-engine-Cerebras + the RISC-V-AI-chip-Tenstorrent + the LPU-Groq + the RDU-SambaNova + the IPU-Graphcore + the GPU-cloud-Lambda + the inference-API-modular-mesh-Together + the marketplace-mesh-HuggingFace + the cog-OSS-packaging-Replicate + the deterministic-LPU-Groq
- `{{distinct_claim}}` = ONLY AI-infrastructure vendor operating the full chip-to-rack-scale-server-to-inference-cloud stack on the reconfigurable-dataflow-RDU + Composition-of-Experts architecture

## Follow-up Sequence (Day 3, Day 7, Day 14)

- **Day 3:** "Quick bump — adding the SambaNova-specific 16-column per-RDU-tile-router-decision join-table to the audit-scope doc. Want me to send it before the next EU AI Act Aug 2026 GPAI enforcement deadline?"
- **Day 7:** "RDU-tile + CoE-expert-router + DataScale-rack-scheduler isolation-evidence per-tenant question — most chip-vendor audit-trail templates default to NVIDIA H100 GPU memory isolation. Does SambaNova's RDU reconfigurable-dataflow isolation surface need its own per-tile-isolation template, or does the Composition-of-Experts + CoE-expert-router inheritance mean you can reuse the Cerebras + Tenstorrent + Groq isolation templates?"
- **Day 14:** "Final follow-up — closing the loop on the SambaNova audit-trail conversation. If timing's not right, no worries. Will keep you on the canonical-distributed-AI-compute + reconfigurable-dataflow-RDU + Composition-of-Experts cohort update list."

## Compliance Hooks (for Atlas-tier-1 follow-ups)

- **EU AI Act Art. 12** + **Art. 14** + **Art. 53(d)** + **Art. 53(1)(b)** + **Art. 53(2)** + **Aug 2026 GPAI enforcement**
- **SOC 2 CC7.2** (system operations monitoring) + **CC6.1** (logical access controls)
- **ISO 42001 9.4** (AI system lifecycle) + **6.1.4** (AI risk management)
- **FedRAMP Moderate AU** (audit and accountability)
- **OWASP LLM Top 10 LLM01** (prompt injection) + **LLM03** (training data poisoning) + **LLM04** (model DoS) + **LLM06** (sensitive information disclosure) + **LLM08** (excessive agency)
- **NIST AI RMF MEASURE** function
- **GDPR Art. 28** (processor obligations) + **Art. 17** (right to erasure / deletion-propagation drill)