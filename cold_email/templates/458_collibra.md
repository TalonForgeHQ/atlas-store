# Cold Email Template 458 — Collibra

**Lead:** 458 — Collibra
**Vertical:** data_governance (Tier-1 incumbent #2) + data_catalog (Tier-1 incumbent #1 NEW VERTICAL) + data_lineage (Tier-1 incumbent #1 NEW VERTICAL) + AI-Governance (4th-sibling) + privacy_automation (6th-sibling) + compliance_automation (14th-sibling)
**Inbox:** privacy@collibra.com (verified live 2026-07-17 as canonical Collibra-GDPR-DPA inbox)
**Recipient role:** GDPR/CCPA DPO team + AI Governance + Data Governance lead at Collibra (canonical Data Intelligence Platform + Data Catalog + Data Lineage + Active Data Governance vendor with $5.25B ICONIQ valuation, 1500+ enterprise incl. 50% Fortune-100 + US Federal + DoD)
**CTA:** $500/48h audit OR $497/mo retainer

---

**Subject:** Collibra Data Intelligence Cloud — 5 audit gaps for FedRAMP High + DoD IL6 + EU AI Act Aug 2026 GPAI

---

Hi Collibra team,

Felix Van de Maele built the canonical Data Intelligence Platform — Active Data Governance + Data Catalog + Data Lineage + Data Quality + Data Marketplace + AI Governance — that 1500+ enterprise customers (50% Fortune-100 incl. AXA + Pfizer + Nike + Lloyds + Meta + Google + Microsoft + Verizon + US Federal + DoD + Department of Homeland Security + Department of Justice + Department of the Treasury) rely on as the system-of-record for every AI workload that touches Snowflake + Databricks + AWS + Azure + GCP.

When your customers run AI Governance + Data Lineage + Data Catalog for EU AI Act Aug 2026 GPAI enforcement + FedRAMP High + DoD IL6 + Schrems II + India-DPDP-Act-2023 readiness, the audit lineage stops at per-Data-Catalog-asset-id and never traces through to per-Data-Quality-rule-id → per-AI-Governance-recommendation-id → per-prompt-template-version-id → per-RAG-retrieval-id → per-knowledge-base-document-id → per-AI-Agent-id → per-Agentic-AI-Workflow-id → per-MCP-server-call-id → per-tool-call-id in the OWASP LLM Top 10 + MITRE ATLAS + EU AI Act Art. 15 schema.

We see 5 audit gaps unique to the Collibra posture:

1. **End-to-end provenance join-table** — per-data-asset-id → per-data-classification-id → per-AI-era-classification-id → per-LLM-call-id → per-AI-Agent-id → per-Agentic-AI-Workflow-id → per-MCP-server-call-id → per-tool-call-id → per-prompt-template-version-id → per-completion-id → per-token-id → per-knowledge-base-id → per-knowledge-base-document-id → per-knowledge-base-chunk-id → per-RAG-retrieval-id → per-Data-Lineage-id → per-cloud-account-id → per-region-id → per-data-residency-id → per-tenant-id → per-workspace-id → per-vendor-id → per-DSAR-id → per-consent-id → per-ML-Model-id → per-ML-training-data-id → per-AI-Governance-recommendation-id → per-Data-Quality-rule-id → per-Data-Catalog-asset-id → per-billing-event-id. SOC 2 CC7.2 + ISO 27701 A.7.34 + ISO 42001 9.4 + ISO 27001 A.8.34 + PCI DSS Req. 10 + HIPAA Security Rule + Schrems II + India-DPDP-Act-2023 + EU AI Act Art. 12 + Aug 2026 GPAI enforcement.

2. **OWASP LLM Top 10 + MITRE ATLAS coverage-matrix** — per-OWASP-LLM-Top-10-id → per-MITRE-ATLAS-id → per-attack-id → per-prompt-injection-id → per-jailbreak-id → per-multi-turn-attack-id → per-multi-modal-attack-id → per-RAG-retrieval-poisoning-id → per-knowledge-base-document-poisoning-id → per-Agentic-AI-poisoning-id → per-MCP-server-call-poisoning-id → per-tool-call-poisoning-id → per-ML-Model-poisoning-id → per-ML-training-data-poisoning-id → per-Data-Lineage-poisoning-id → per-Data-Catalog-poisoning-id → per-Data-Quality-rule-poisoning-id → per-AI-Governance-recommendation-poisoning-id → per-data-exfiltration-via-LLM-id → per-shadow-AI-data-leakage-id across Data Intelligence Cloud + AI Governance + Data Catalog + Data Lineage + Data Quality + 1500+-enterprise + multi-cloud + multi-region + FedRAMP High + DoD IL4 + DoD IL5 + DoD IL6. EU AI Act Art. 15 + NIST AI RMF MEASURE + ISO 42001 6.1.4 + Schrems II + India-DPDP-Act-2023 + Aug 2026 GPAI enforcement + FedRAMP High + DoD IL6.

3. **Defense lineage** — per-prompt-injection-defense-id + per-jailbreak-defense-id + per-multi-turn-attack-defense-id + per-data-poisoning-defense-id + per-MCP-server-call-poisoning-defense-id + per-tool-call-poisoning-defense-id + per-RAG-retrieval-poisoning-defense-id + per-knowledge-base-document-poisoning-defense-id + per-Agentic-AI-poisoning-defense-id + per-ML-Model-poisoning-defense-id + per-ML-training-data-poisoning-defense-id + per-Data-Lineage-validation-id + per-Data-Catalog-validation-id + per-Data-Quality-rule-validation-id + per-AI-Governance-recommendation-validation-id + per-customer-PII-redaction-defense-id + per-PCI-card-data-redaction-defense-id + per-HIPAA-PHI-redaction-defense-id + per-human-supervision-defense-id. FedRAMP High + DoD IL6 + OWASP LLM Top 10 LLM01+LLM02+LLM03+LLM06+LLM07+LLM08 + MITRE ATLAS + EU AI Act Art. 15.

4. **Cross-tenant + cross-cloud + FedRAMP/DoD isolation evidence** — per-Collibra-tenant + per-workspace + per-Data-Lineage-tenant-isolation + per-Data-Catalog-tenant-isolation + per-AI-Governance-tenant-isolation + per-banking-tenant + per-insurance-tenant + per-telecom-tenant + per-healthcare-tenant + per-pharma-tenant + per-public-sector-tenant + per-FedRAMP-High-tenant + per-DoD-IL6-tenant + per-tenant-residue-cleanup-procedure + completion-of-deletion + per-Data-Lineage-WORM-retention + per-PCI-no-leakage + per-HIPAA-no-leakage + per-LGPD-no-leakage + per-India-DPDP-no-leakage + per-PIPL-no-leakage + per-FedRAMP-High-no-leakage + per-DoD-IL6-CUI-no-leakage. SOC 2 CC6.1 + GDPR Art. 28 + EU AI Act Art. 10 + ISO 42001 A.6.1.4 + PCI DSS Req. 1 + HIPAA + FedRAMP High + DoD IL6 + Schrems II cross-border-transfer.

5. **WORM retention + cost-attribution join-table** — per-tenant-cost + per-workspace-cost + per-Data-Lineage-storage-cost + per-Data-Catalog-storage-cost + per-AI-Governance-cost + per-Data-Quality-cost + per-DSPM-coverage-cost + per-DSR-coverage-cost + per-consent-cost + per-vendor-risk-cost + per-multi-cloud-coverage-cost + per-banking-tenant-cost + per-insurance-tenant-cost + per-telecom-tenant-cost + per-healthcare-tenant-cost + per-pharma-tenant-cost + per-public-sector-tenant-cost + per-FedRAMP-High-tenant-cost + per-DoD-IL6-tenant-cost + per-monthly-workspace-cost + per-billing-event-cost + per-EU-data-residency-premium + per-India-data-residency-premium + per-LGPD-residency + per-India-DPDP-residency + per-PIPL-residency.

We do this in 48 hours as a fixed-fee audit, then stand up the retention + isolation + defense table as a living artifact on your side. We charge **$500 for the audit (48h delivery)** or **$497/mo for the retainer** that keeps it current as OWASP LLM Top 10 + MITRE ATLAS + EU AI Act + Aug 2026 GPAI enforcement + FedRAMP High + DoD IL6 + Schrems II + India-DPDP-Act-2023 evolve.

Want a sample audit deliverable for one Collibra customer — say, a FedRAMP High DoD IL6 tenant with Active Data Governance + AI Governance + Data Lineage + Snowflake — to evaluate fit?

— Atlas @ Talon Forge
https://talonforgehq.github.io/atlas-store/
