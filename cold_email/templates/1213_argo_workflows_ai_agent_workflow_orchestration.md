# 1213_argo_workflows_ai_agent_workflow_orchestration.md

**Vendor:** Argo Workflows (argoproj.github.io/argo-workflows + github.com/argoproj/argo-workflows)
**Contact:** mailto:hello@argoproj.io
**Vertical:** ai_agent_workflow_orchestration
**Cohort slot:** SIBLING #4/5 (NEW VERTICAL #60) — after Temporal 1210 OPENER #1/5 + Prefect 1211 SIBLING #2/5 + Inngest 1212 SIBLING #3/5
**Founders:** Argoproj OSS project (originally created at Intuit verbatim first-party "Created by Intuit" badge 2026-07-25); CNCF Graduated project (verbatim first-party README "Argo is a Cloud Native Computing Foundation (CNCF) graduated project.")
**Status:** queued_not_sent (SMTP/form gated; 0 sent / 0 received)

---

## 3 Subject Options (under 60 chars each)

1. `Argo Workflows 1213: CNCF Graduated K8s-CRD workflow engine`
2. `Argo Workflows 1213: container-native DAGs + Argo Events`
3. `Argo Workflows 1213: Hera SDK + Intuit-originated — sibling 4/5`

## Body (use ONE subject, ship as one email)

Hi Argo Workflows team,

Sending a 5-WEDGE evidence-gap map for the `ai_agent_workflow_orchestration` cohort — sibling #4/5 of 5 next to Temporal 1210 OPENER + Prefect 1211 SIBLING #2 + Inngest 1212 SIBLING #3. Cohort-floor offer is $500/48h fixed-scope or $497/mo quarterly refresh.

**The 5-WEDGE rubric we ship per-vendor per-cohort-slot:**

1. **Per-namespace + per-WorkflowTemplate + per-Workflow + per-CronWorkflow + per-ClusterWorkflowTemplate + per-step + per-task + per-DAG-node + per-artifact + per-retry + per-TTL + per-pod + per-container audit trail** — Argo's K8s-CRD-native primitives generate a per-resource audit surface that's distinct from Temporal's per-Namespace + per-Workflow-Id + Prefect's per-flow + per-run + per-task-run + Inngest's per-app + per-function + per-event + per-step. The CRD-nativity means every Workflow + CronWorkflow + WorkflowTemplate is a Kubernetes object with full Kubernetes RBAC + admission-webhook + audit-log lane.
2. **CNCF Graduated project (verbatim first-party README 2026-07-25) — the only cohort sibling at CNCF Graduated maturity tier** — Argo Workflows is verbatim first-party "a Cloud Native Computing Foundation (CNCF) graduated project." CNCF Graduated requires passing the CNCF TOC's security audit + governance + contributor-trademark due diligence + ongoing curriculum-lanes evaluation. Temporal 1210 + Prefect 1211 + Inngest 1212 are not CNCF projects.
3. **Container-native + Kubernetes-CRD-native + Argo Events + EventSources + Sensors + EventBus + KEDA scalers as cohort-unique first-class event-driven substrate** — Argo is the only cohort sibling that ships (a) WorkflowTemplate + Workflow + CronWorkflow + ClusterWorkflowTemplate as Kubernetes CRDs (verbatim README 'Argo Workflows is implemented as a Kubernetes CRD'); (b) Argo Events OSS repo (2,678 stars Apache-2.0 verified 2026-07-25) as a first-class sibling-substrate; (c) EventSources + Sensors + EventBus primitives; (d) KEDA scalers for event-driven scaling. vs Temporal's class-based Workflow + Activity + Worker model on Temporal Server + Prefect's @flow + @task decorator model + Inngest's TS/JS/Python/Go SDK with step-functions-ergonomics.
4. **Created by Intuit verbatim first-party 'Created by Intuit' badge on argoproj.github.io landing 2026-07-25** — the only cohort sibling with a Fortune-100 enterprise founding-anchor. Intuit (the TurboTax + Credit Karma + QuickBooks parent) originally created Argo and continues as a major contributor. Temporal 1210 + Prefect 1211 + Inngest 1212 all started as OSS-native startups without a Fortune-100 founding anchor.
5. **Hera Python SDK for Argo Workflows verbatim README 'Including for Python users through the Hera Python SDK for Argo Workflows'** — Hera is the only cohort sibling with a third-party-but-canonical Python SDK explicitly endorsed in the project README. Prefect 1211 ships its own Python SDK but is the project itself; Hera is the Python-ergonomic-abstraction over the YAML CRD-native API.

**What's missing from your current public surface (per the rubric above):**

- Cross-tenant-no-bleed invariant document (per-namespace RBAC scoping + per-ServiceAccount + per-WorkflowTemplate-scope + per-EventSource-tenant-bound + per-Sensor-tenant-bound + per-ClusterWorkflowTemplate-tenant-bound).
- Per-AI-agent-per-invocation audit-provenance (per-AI-step-run + per-Argo-Events-event-source-tenant + per-pod-llm-prompt-hash + per-pod-llm-response-hash + per-decision-citation-anchor + per-decision-confidence + per-human-override-acknowledgment + 14-day change-notification SLA on AI sub-processor swaps).
- EU AI Act Aug 2 2026 readiness per-Article (Art. 6 + Art. 9 + Art. 10 + Art. 13 + Art. 14 + Art. 15 + Art. 26 + Art. 27 + Art. 50 + Annex III high-risk list) + cross-border data-residency posture for EU + UK + CH + BR + AU + SG + JP + CA + IN + ZA.
- ISO/IEC 42001:2023 AIMS clause-by-clause evidence map (Argo Workflows AI agent surface — clause 6.1.2 + 7.2 + 8.4 + 9.3 + 10.1) + per-clause citation pinning.
- Per-day-of-2026 incident-manifest export lane (per-namespace + per-WorkflowTemplate + per-Workflow + per-CronWorkflow + per-DAG-node + per-step + per-pod + per-container + per-EventSource + per-Sensor + per-tenant + per-day reproducible machine-readable artifact — same artifact reproducible across clusters + K8s versions + Argo versions).

**Cohort cohort-floor offers:**

- **$500/48h fixed-scope evidence-gap map** (1 deliverable: 1 PDF + 1 machine-readable artifact per-vendor above)
- **$497/mo quarterly refresh** (quarterly artifact + per-Article coverage)
- **$2,000 cohort benchmark** (5-vendor package: Temporal 1210 + Prefect 1211 + Inngest 1212 + Argo Workflows 1213 + CLOSER 1214)

Reply with which surface you'd like scoped first.

— Atlas
mailto:hello@argoproj.io
Cohort: ai_agent_workflow_orchestration (NEW VERTICAL #60, 3/5 → 4/5)