# AI Agent Authorization Cohort Bundle — Five-Vendor Decision Replay

**Template family:** `ai_agent_authorization` cohort bundle
**Use for:** Product Security, IAM, Platform Engineering, AI Governance, Trust, and Compliance leaders
**Offer:** $2,000 fixed five-vendor benchmark; $500/48h single-platform map; $497/month refresh
**Status:** TEMPLATE ONLY — no email or form submission is claimed

## Closed cohort (5/5)

- Permit.io (989): full-stack permissions across agents, tools, APIs, and data
- Cerbos (990): contextual and continuous authorization across applications, workloads, and AI agents
- Oso (991): Polar policy-as-code, OAuth 2.1, MCP, delegated access, and least privilege
- SGNL (998): continuous identity, CAEP signals, Zero Standing Privilege, and MCP governance
- AuthZed (999): SpiceDB relationship-based authorization, permission-aware RAG, and Cloud/Dedicated/self-hosted deployment

## Subject options

1. `{{company}} AI-agent authorization replay — five gaps buyers will test`
2. `Five-vendor agent authorization benchmark: one decision, one export`
3. `Can {{company}} replay an MCP tool decision end to end?`

## Email

Hi {{first_name}} — I completed a five-vendor evidence benchmark across Permit.io, Cerbos, Oso, SGNL, and AuthZed.

It tests whether an enterprise buyer can replay one AI-agent authorization decision from delegated identity and policy version through MCP tool selection, downstream action, result, revocation, and audit export — without rebuilding the story from screenshots and separate logs.

Five questions drive the map:

1. Can every decision join the human sponsor, delegated agent identity, tenant, requested resource, permission, and approved tool scope?
2. Does the record preserve the exact policy, schema, relationship, caveat, context, and continuous-access signal versions evaluated at decision time?
3. Can each MCP server and tool call be joined to the authorization result, downstream API or data mutation, and resulting state?
4. Are denial, timeout, retry, override, expiry, emergency stop, rollback, and revocation retained as clearly as successful actions?
5. Can one export demonstrate cross-tenant isolation and answer SOC 2, ISO 27001/42001, NIST AI RMF, EU AI Act human-oversight, and incident-review questions?

Within 48 hours I return a red/amber/green gap map, five priority missing joins, a sample decision-receipt schema, and a procurement publication checklist.

The fixed five-vendor benchmark is **$2,000**. A single-platform map is **$500 / 48 hours**, with an optional **$497/month** refresh.

Worth sending the one-page sample for {{company}}?

Best,
Atlas
Talon Forge LLC / Talon Forge HQ

## Follow-up bump

If a buyer challenged one agent action tomorrow, could {{company}} replay the sponsor, delegated identity, token, policy version, MCP server, tool call, authorization result, downstream mutation, rollback, and revocation in one export? I can send the five-row sample and leave it there.

## Route safety

Use only a verified commercial inbox or explicitly authorized commercial form. Never claim delivery, response, payment, or revenue without a receipt. Current state: queued, not sent; SMTP/form gated; $0 sent / $0 received.
