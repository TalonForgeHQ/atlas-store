# Lead 772 — Rootly (sibling #2/5 ai_incident_response_observability) follow-up

**Vertical:** `ai_incident_response_observability` sibling #2/5
**Company:** Rootly Inc.
**Founders / Executives:** JJ Tang (CEO and Co-founder) — second Co-Founder referenced in careers copy as "We founded Rootly in 2021" but not named in first-party JSON-LD
**Headquarters:** San Francisco, CA — hybrid across US + Canada + Europe + Singapore (per first-party JSON-LD jobLocation)
**Funding:** Y Combinator W21 + multiple subsequent rounds
**Commercial route:** `FORM:https://rootly.com/demo`
**Offer:** $500 fixed, 48 hours; optional $497/month quarterly refresh

## Subject A/B/C

- **Subject A:** Rootly AI SRE evidence map — automated root-cause-analysis with confidence scores, EU AI Act Art. 14(4), 48 hours
- **Subject B:** Five Rootly AI-SRE joins for the next regulated-incident-response procurement review
- **Subject C:** Quick question on Rootly per-tenant data isolation + AI-SRE sub-processor pinning — $500 fixed

## First-touch note (route-safe draft; not submitted)

> Subject: Rootly AI SRE evidence map — automated root-cause-analysis with confidence scores, EU AI Act Art. 14(4), 48 hours
>
> Hi Rootly team,
>
> Rootly's current AI-augmented surface (AI SRE automated root-cause-analysis with confidence scores + suggested fixes + IDE-plugged remediation + @Rootly AI assistant in Slack + per-incident retrospectives + status pages), layered on a hybrid US + Canada + Europe + Singapore deployment topology (per the first-party careers JSON-LD jobLocation), with an explicit PagerDuty displacement strategy ("1/2 the price" + contract buyout + migrated-for-you + 99.99%), opens a useful buyer-facing evidence question: can one record join a regulated buyer's per-incident + per-AI-SRE-analysis provenance + AI-suggested-fix lineage + multi-tenant isolation + EU AI Act Art. 14(4) human-oversight + NIS2 Art. 21(2)(e) cross-border-incident-notification chain?
>
> I can turn five gaps into a compact evidence map in 48 hours: per-incident + per-AI-SRE-analysis provenance across the AI SRE + @Rootly + retrospectives surface; per-tenant data isolation across the customer base; EU AI Act Art. 14(4) + Art. 50 + Art. 53(1)(b) cascade for AI SRE; NIS2 + DORA + HIPAA + SOC 2 Type 2 + CCPA per-incident security incident handling; sub-processor DPA + 14-day change-notification SLA + per-tenant residency pinning.
>
> Fixed scope is $500. If useful, Rootly's Demo form is the route-safe next step.
>
> — Atlas @ Talon Forge

## Bump (day 3 if no reply)

> Subject: Re: Rootly AI SRE evidence map — five joins
>
> Hi — one follow-up. The review is bounded to per-incident + per-AI-SRE-analysis provenance, per-tenant data isolation, EU AI Act Art. 14(4)/50/53(1)(b) cascade, NIS2 + DORA + HIPAA per-incident security incident handling, and sub-processor pinning. $500 fixed, 48 hours. Happy to close the loop if this is not relevant.

## Five-question audit scope

1. **Per-incident + per-AI-SRE-analysis provenance.** Can one receipt join incident severity + on-call responder identity + response-channel participation + AI root-cause-analysis run + confidence score + LLM sub-processor invoked + suggested-fix generation + IDE plugin invocation + customer-facing status-page update + post-incident-review artifact + retention window + region routing for every incident?
2. **Multi-tenant isolation.** Can a regulated buyer's CISO prove per-tenant embedding-store isolation, per-tenant retrieval-result scoping, per-tenant LLM-context window scoping, and per-tenant audit-log retention when they request an export of all AI-SRE analysis consumed by their engineers across US + Canada + Europe + Singapore deployment topology?
3. **EU AI Act Art. 14(4) + Art. 50 + Art. 53(1)(b) cascade.** Can Rootly surface a per-incident human-oversight operational record before any AI-suggested fix is applied (attorney-equivalent review-step), Art. 50 transparency-labeling on AI-suggested fixes, and Art. 53(1)(b) GPAI training-data transparency cascade across OpenAI + Anthropic + secondary LLMs?
4. **NIS2 + DORA + GDPR + HIPAA + SOC 2 Type 2 + CCPA per-incident security incident handling.** Can Rootly produce per-incident detection-to-notification SLA evidence, cross-border-incident-notification chain documentation (especially relevant given the hybrid US + Canada + Europe + Singapore topology), forensic-chain-of-custody preservation, deletion-after-retention cascade receipts, and per-tenant incident-data-residency pinning with HIPAA breach-notification alignment for clinical AI deployment failures?
5. **Sub-processor cascade DPA + 14-day change-notification SLA.** Can Rootly surface the current sub-processor list (Slack + Microsoft Teams + Atlassian Jira + Linear + GitHub + Datadog + PagerDuty legacy migrator + AWS + GCP + OpenAI + Anthropic + LLM sub-processor = 12+ sub-processors in the per-incident cascade), flow-down DPA across each, 14-day change-notification SLA per GDPR Art. 28(2), and per-tenant pinning to specific sub-processor list + region routing + retention window?

## Why this matters for Rootly specifically

The explicit PagerDuty displacement strategy ("1/2 the price" + contract buyout + migrated-for-you + 99.99%) means enterprise buyers comparing Rootly to PagerDuty will run a head-to-head compliance review — and PagerDuty's compliance posture is mature enough that the comparison forces Rootly to produce comparable evidence per-incident. The hybrid US + Canada + Europe + Singapore deployment topology adds concurrent GDPR + UK GDPR + EU AI Act + APPI + PIPEDA + Australia Privacy Act + Singapore PDPA + LGPD procurement pressure that PagerDuty's US-centric deployment does not have to absorb. The HIPAA trust badge on rootly.com/security opens healthcare-incident buyers (clinical AI deployment failures trigger HIPAA breach-notification) where the $497/mo quarterly refresh is the natural retainer.
