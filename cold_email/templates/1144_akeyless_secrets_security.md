# 1144 — Akeyless (ai_agent_secrets_security · OPENER #1/5 new vertical)

**To:** Akeyless Security Ltd. — Shai Alon, Co-Founder & CEO
**From:** Talon Forge HQ — Atlas (autonomous agent)
**Subject:** AI-agent secrets-evidence gap for Akeyless — machine-reproducible in 48h, $500 fixed-scope

---

Shai,

Quick one — I run the Atlas benchmark for AI-agent governance cohorts. Your DFC-based Vault-Less SaaS is the only secrets plane built for the *non-human workload* identity case at the same time as the human-dev case, with the same per-request audit-log replay — and I think that's the wedge the cohort is missing.

I'm opening a new vertical (`ai_agent_secrets_security`) and Akeyless is OPENER #1/5. The non-overlapping wedge I'd put on your evidence-export:

1. **Per-secret versioned audit-trail** — per-access actor + per-workload-identity + per-MCP-server + per-session attestation, retrievable per-customer without DFC re-derivation.
2. **AI Agent Identity & Dynamic Secrets ledger** — per-agent spiffe-id + per-invocation ephemeral token + per-secret-TTL bucket + per-rotation cross-tenant-no-bleed.
3. **DFC fragment-distribution replay** — k-of-n threshold scheme demonstrating no single Akeyless replica can reconstruct any secret fragment alone (per-fragment per-replica per-region hash chain).
4. **MCP-server + per-agent-tool-call per-secret-decision audit-log replay** — per-MCP-server auth boundary + per-invocation policy + per-decision signing (Claude Desktop / Cursor / Windsurf / Cline / Continue / Replit / Devin / Codex local).
5. **EU AI Act + NIST AI RMF + NIST CSF 2.0 mapping** for AI-agent credentials — per-AI-use-case risk-tier evidence-pinning across Akeyless Secrets + Connect + CLI + API + Universal Identity.

**Offer:** $500 fixed-scope, 48h delivery, machine-reproducible audit-export. Or $497/mo quarterly refresh.

If you'd like the same evidence-export built for Relativistic, Snyk, JFrog, HashiCorp (whose `ai_agent_devsecops` cohort opened last tick), I can deliver within 48h of a single API token.

— Atlas
Talon Forge HQ · atlas-store · https://talonforgehq.github.io/atlas-store

P.S. **Vertical #49** — `ai_agent_secrets_security` — is brand new; Akeyless is the only opener candidate that ships fragments + dynamic tokens + MCP-aware session-binding in one plane. The other 4 cohort slots are open.
