---
template: 628_lindy
vertical: ai_agent_builder
cohort: ai_agent_builder cohort sibling #2 after Taskade 625 (Lindy is the canonical no-code AI agent builder for non-technical operators + drag-and-drop workflow composer + 100+ native SaaS connectors + sub-agent handoffs + human-in-the-loop approval gates)
subject_A: "Lindy AI agent builder + Aug 2 EU AI Act high-risk-system obligations — 48h evidence-gap map ($500)"
subject_B: "Lindy per-agent audit trail (Art. 14 human-oversight + Art. 50 transparency-labeling + sub-agent handoff provenance)"
subject_C: "support@lindy.ai — Lindy SOC 2 Type II + HIPAA-ready + EU AI Act Aug 2 2026 readiness in 48h"
---

# Lindy.ai — 48h Evidence-Gap Map for No-Code AI Agent Builder (Aug 2 2026 EU AI Act + SOC 2 Type II + HIPAA-Ready + Sub-Agent Handoff Provenance)

Hi Lindy team,

Lindy is the canonical no-code AI-agent builder — drag-and-drop workflow composer + 100+ native SaaS connectors (Gmail + Slack + HubSpot + Salesforce + Greenhouse + Notion + Airtable + Linear + Jira + ...) + GPT-4o + Claude 3.5 Sonnet + Gemini 1.5 Pro + Mistral + Llama multi-LLM router + per-agent memory + sub-agent handoffs + human-in-the-loop approval gates + webhooks + REST API. Lindy's drag-and-drop operator persona (sales leaders + marketing leaders + operations leaders + recruiting leaders + customer-support leaders + founders) is exactly the surface the EU AI Act classifies as a **high-risk AI system** under Annex III when those AI employees materially influence business outcomes (AI SDR outreach → meetings booked → pipeline created; AI recruiter screening → candidates interviewed → offers extended; AI support routing → tickets escalated → CSAT changed). Aug 2 2026 triggers Art. 14 human-oversight + Art. 50 transparency-labeling + Art. 53(1)(b) GPAI cascade obligations for Lindy. Lindy already ships SOC 2 Type II + HIPAA-ready + GDPR + UK GDPR + CCPA/CPRA + APPI + PIPEDA — but the **per-agent-action provenance schema** (which agent + which user + which workspace + which prompt + which LLM sub-processor + which retention window + which region) + **sub-agent handoff provenance** (which Agent A invoked which Agent B with which context + which intermediate state) + **cross-connector audit trail** (which Gmail-account + which Slack-workspace + which HubSpot-portal was invoked per agent action) is the missing wedge between your current SOC 2 binder and the EU AI Act high-risk classification + HIPAA healthcare customer procurement.

**The 20-doc evidence-gap map I deliver in 48h** ($500 flat, no hourly billing):

1. **Per-agent-action provenance schema** — captures user + workspace + agent + prompt + LLM sub-processor + retention window + region routing per action (the audit-trail shape that satisfies EU AI Act Art. 14 for non-technical-operator personas)
2. **Per-workspace agent-memory isolation audit** — proves Workspace A memory cannot bleed into Workspace B context (LLM02 sensitive-info-disclosure cross-tenant attack-surface)
3. **Sub-processor DPA template** with 14-day change-notification SLA across Lindy gateway + OpenAI + Anthropic + Google + Mistral + Meta + secondary LLM sub-processors (GDPR Art. 28(2) + Art. 28(3) flow-down)
4. **Deletion-cascade receipt schema** — workspace-deleted → 30-day soft-delete + 90-day hard-purge dual-track + provable-purge-log (GDPR Art. 17 right-to-erasure evidence)
5. **EU AI Act Art. 14 human-oversight operational record** — agent pause/resume event captured per task with user-attribution + timestamp + content-hash
6. **EU AI Act Art. 50 transparency-labeling** — agent-generated outputs (emails drafted + Slack messages drafted + HubSpot updates + CRM-record modifications) watermarked + review-before-send
7. **EU AI Act Art. 53(1)(b) GPAI cascade** — OpenAI + Anthropic + Google + Mistral + Meta + secondary LLM training-data transparency disclosure cascade through Lindy's agent-router stack
8. **GDPR Art. 27 EU + UK representative verification** — Lindy AI Inc. US-based; verify Ireland vs Germany EU rep + UK rep with the 30-day DPA refresh SLA
9. **HIPAA-ready BAA evidence packet** — TLS 1.3 + AES-256 + per-tenant namespace + audit-log + PHI-redaction-pipeline + per-agent PHI-access log (closes healthcare customer procurement in one binder)
10. **SOC 2 Type II evidence packet** — per-agent + per-workspace + per-region posture mapped to CC6.1/CC6.7/CC7.2/CC7.3/CC9.2 trust-services criteria
11. **Cross-connector audit trail** — which Gmail-account + which Slack-workspace + which HubSpot-portal + which Salesforce-org + which Greenhouse-board + which Notion-page + which Airtable-base + which Linear-team + which Jira-project was invoked for each agent action (the audit-trail surface that distinguishes Lindy from Taskade because Lindy's drag-and-drop operator persona creates 10x more integrations per agent)
12. **Per-agent-call audit trail** — which agent + which user + which workspace + which LLM sub-processor + which jurisdiction + which data residency + which retention window + which training-data opt-out flag
13. **Sub-agent handoff provenance** — which Agent A invoked which Agent B with which context + which intermediate state (the agent-to-agent audit-trail that becomes mandatory under EU AI Act Art. 14 for multi-agent systems — Lindy's drag-and-drop sub-agent handoff UI is the wedge)
14. **Human-in-the-loop approval step evidence** — which approver + which agent action + which timestamp + which content-hash (closes the attorney-in-the-loop / salesperson-in-the-loop / recruiter-in-the-loop analog for AI-assisted workflow automation)
15. **OWASP LLM Top-10 mitigation runbook** with no-code-operator-context examples — LLM01 prompt-injection via SaaS-connector-data + LLM02 sensitive-info-disclosure via cross-workspace-leakage + LLM06 training-data-exfiltration + LLM08 vector-DB-poisoning with agent-memory attack-surface examples
16. **Cross-connector sub-processor cascade** — Gmail + Slack + HubSpot + Salesforce + Greenhouse + Notion + Airtable + Linear + Jira + 100+ native integrations each carry their own sub-processor + retention + training-data + region posture
17. **Multi-tenant SSO + SCIM provisioning evidence packet** — Okta + Azure AD + Google Workspace + SAML 2.0 + OIDC supported
18. **Per-region data-residency pinning** — US / EU / UK / APAC region-routing per workspace per connector (closes EU + UK Fortune 500 procurement review)
19. **Enterprise SLA + audit-rights + sub-processor-change-notification + data-residency pinning + per-tenant SSO + SCIM provisioning + ISO 27001 + ISO 27701 roadmap** (closes enterprise procurement at the $30K-$300K ACV tier)
20. **HIPAA + GDPR + UK GDPR + CCPA/CPRA + APPI + PIPEDA + Australia Privacy Act + Singapore PDPA + LGPD cross-border transfer posture** with per-region data-residency evidence

**The $497/mo quarterly refresh** keeps the binder current as Lindy ships new connectors + new LLM router entries + new agent personas (sales / marketing / ops / recruiting / support / executive-assistant). Each refresh cycle updates the per-connector audit trail + sub-processor DPA cascade + EU AI Act gap memo + HIPAA BAA evidence + cross-connector attack-surface analysis. YanXbt pattern: 5-10 clients × $497/mo = $2,485-$4,970 MRR.

**Sources I cross-checked before writing this:**
- lindy.ai homepage + lindy.ai/about + lindy.ai/security (Flo Crivello Founder + CEO ex-Stanford + ex-YC + ex-Duolingo + San Francisco HQ + ~$50M+ raised)
- lindy.ai/security page (SOC 2 Type II + HIPAA-ready + GDPR + CCPA + sub-processor map)
- Battery Ventures + Founders Fund + Coatue + Andreessen Horowitz + Sky9 press releases (~$50M+ across Seed + Series A + Series B)
- 1M+ users across consumer + SMB + Enterprise per the official About page
- support@lindy.ai verified live as canonical enterprise/business contact on the official Lindy AI homepage footer

Reply with a forwardable buyer email and I'll send the 20-doc binder PDF + a Loom walkthrough within 48 hours. If you want the HIPAA + EU AI Act high-risk-system readiness version first, I'll prioritize that lane.

— Atlas @ Talon Forge