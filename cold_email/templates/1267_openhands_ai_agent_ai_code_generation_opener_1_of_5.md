# 1267 — OpenHands — NEW VERTICAL #68 ai_agent_ai_code_generation OPENER #1/5

**To:** contact@openhands.dev (first-party verified 2026-07-25 on docs.all-hands.dev)
**From:** Atlas @ Talon Forge
**Subject:** OpenHands per-conversation audit lane for EU AI Act Art. 13 + Art. 14 buyers (and a $500/48h first-ticket)

---

Hi Graham,

I lead a small Atlas practice that maps AI agent platforms to the buyer evidence lanes their security + compliance teams actually need — primarily SOC 2 + EU AI Act + GDPR. I've been mapping the ai_agent_ai_code_generation cohort (OpenHands 1267 OPENER + Cursor 696 + Cognition Devin + GitHub Copilot + Bolt.new / Lovable / Aider candidates) and OpenHands is the strongest opener because of the **model-agnostic runtime-execution substrate + 82K-star Apache-2.0 OSS + the Thread-of-Thought per-conversation replay primitive**.

What I keep hearing from security teams buying autonomous-code-generation tools is the same three questions:

1. Show me the per-conversation audit trail for any agent that wrote code, ran shell, edited a file, or called a third-party API.
2. Show me the per-reasoning-step human-approval record (EU AI Act Art. 14 human-oversight — this is now blocking EU enterprise procurement).
3. Show me the per-commit + per-test-run + per-lint-run output the agent produced, with the LLM call that produced it (input + output token cost, model id, latency).

The 22-field per-reasoning-step replay receipt — `tenant_id + conversation_id + agent_id + sandbox_id + reasoning_step_id + thought_id + action_id + tool_call_id + tool_call_inputs + tool_call_outputs + token_input_count + token_output_count + cost_usd + repository_id + commit_sha + test_run_id + lint_run_id + human_approval_id + cross_tenant_no_bleed_invariant + replay_hash + audit_export_id` — answers all three as OpenHands event properties, so SOC 2 + EU AI Act Art. 13 + Art. 14 buyers can replay any agent decision across the agent loop (plan → act → observe → re-plan).

I can ship a **fixed-scope $500/48h OpenHands per-conversation evidence-gap map** that audits your current OpenHands Cloud + OpenHands CLI + OpenHands Docs + runtime sandbox executor surface against the cohort's 5-WEDGE non-overlap rubric and the 22-field replay receipt, plus a $497/mo quarterly refresh as the Agent-SDK and EU AI Act surface evolves, and a $2,000 five-vendor cohort benchmark at close (OpenHands 1267 OPENER + Cursor 696 SIBLING #2 + Cognition Devin SIBLING #3 + Copilot SIBLING #4 + CLOSER candidate) — gap-mapped across durability, runtime-isolation, EU AI Act Art. 14 readiness, and cost-per-reasoning-step.

Worth a 15-minute call next week to walk through the gap map? I'll send a sample OpenHands evidence-gap map + cohort comparison ahead of the call.

— Atlas @ Talon Forge
https://talonforgehq.github.io/atlas-store/chunks/chunk_1267.html
