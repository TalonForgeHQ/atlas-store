# Subject: Tailscale AI WireGuard-key-rotation audit — 5 gaps for the Aug 2 GPAI deadline

Hi Avery,

We're running a focused 5-gap audit on AI-powered zero-trust networking vendors and Tailscale fits the canonical cohort pattern we're documenting for the EU AI Act Art. 50 + Art. 53 GPAI Aug 2 2026 deadline.

What we'd surface for your team in a 48h, fixed-fee $500 audit:

1. **End-to-end AI-decision provenance join-table.** Per-tailnet-device-id → per-AI-ACL-evaluation-id → per-AI-WireGuard-key-rotation-id → per-AI-MagicDNS-resolution-id → per-AI-audit-log-search-id → per-AI-node-graph-traversal-id → per-AI-IdP-federation-id → per-AI-Funnel-deployment-id → per-AI-Taildrop-policy-id → per-Tailscale-tenant-id → per-procurement-DD-event-id → per-audit-log-entry-id → per-residency-region-id per SOC 2 Type II CC7.2 + EU AI Act Art. 12 + ISO 27001 A.12.4.

2. **AI training-corpus + fine-tune-license-provenance** for the AI ACL + AI WireGuard-key-rotation + AI audit-log-search pipeline per EU AI Act Art. 53(d) GPAI + ISO 42001 6.1.4.

3. **Prompt-injection + AI-ACL-poisoning + AI-WireGuard-key-rotation-poisoning + AI-audit-log-search-poisoning + tailnet-prompt-injection + MagicDNS-poisoning defense review** per OWASP LLM Top 10 (LLM01+LLM03+LLM06+LLM08) + MITRE ATLAS + EU AI Act Art. 14 human-oversight.

4. **Cross-tailnet-tenant isolation + per-tailnet-KMS-key-id + CMK/BYOK + per-tailnet-AI-inference + per-tailnet-AI-training-pipeline separation** per SOC 2 CC6.1 + GDPR Art. 28 + Schrems II + ISO 27701.

5. **WORM retention + cost-attribution join-table linking per-AI-ACL-cost + per-AI-WireGuard-key-rotation-cost + per-AI-audit-log-search-cost + per-tailnet-tenant-cost + per-device-cost** per SOC 2 CC7.2 + EU AI Act Art. 12 + SEC 17a-4 WORM.

Deliverable: one Markdown report, one CSV remediation backlog, one 30-min Loom walkthrough with the engineering team. $500 flat. We've previously documented cohort siblings in ai_payments (Stripe 524), ai_dev_tools (Vercel 523), finance_ops (Brex 521, BILL 522), and ai_data_security (Wiz 494, Sentra 496, Cyera 497, Securiti 498, BigID 499, OneTrust 500) — same gap-catalog shape.

Reply with "yes" and a target delivery date — I'll send scope and timing inside 4 hours. No-prep needed on your side.

Best,
Atlas (Talon Forge LLC)
https://talonforgehq.github.io/atlas-store/lead-525.html

P.S. The EU AI Act Aug 2 GPAI documentation deadline is forcing enterprise procurement teams to add an "AI-pipeline provenance" line item to vendor DD questionnaires this quarter. The audit report we generate maps directly to that line item.
