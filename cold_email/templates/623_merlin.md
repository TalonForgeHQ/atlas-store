# Cold Email Template — Lead 623 — Merlin AI

**Vendor:** Merlin AI (Foyer Tech LLC)
**Vertical:** ai_browser_extension (NEW VERTICAL cohort OPENER sibling #1)
**Inbox:** `merlin@merlinai.com` (verified live 2026-07-19 on merlinai.com homepage footer + contact section)
**Founder:** Sinha (ex-Adobe + ex-Flipkart + AI-product-strategy); Y Combinator W22 batch
**Funding:** Bootstrapped-to-profit + YC W22 batch
**Adoption:** 1M+ Chrome Web Store installs + 50+ languages + free tier (10 queries/day) + Pro ($19/mo) + Teams ($29/user/mo) + Enterprise (custom)
**Why this is a Tier-1 lead:** Merlin AI is the canonical AI-browser-extension assistant that puts GPT-4 + Claude + Gemini + Llama + Mistral + DeepSeek inside every Chrome tab. 1M+ installs across consumer + prosumer + enterprise teams. Users invoke Merlin on YouTube (summarize video), Gmail (draft reply), Twitter/X (write thread), PDF (extract clauses), Docs (summarize), any webpage (ask-the-page Q&A), and the Merlin web app (long-form SEO + image generation + code generation + multi-step agentic workflows). The Chrome extension sits on every page the user visits, making it one of the broadest AI-data-collection surfaces in the consumer market.

**Differentiation wedge (NEW VERTICAL — no prior cohort siblings):**
- **ai_browser_extension is a brand-new vertical** — no prior Atlas lead targets the AI-browser-extension cohort. Merlin is the OPENER.
- **Six-LLM sub-processor router under one extension** — GPT-4 + Claude + Gemini + Llama + Mistral + DeepSeek each carry their own DPA, retention policy, training-data opt-out, and region routing. The browser extension is the **first hop** for consumer-input content crossing into the LLM sub-processor chain.
- **Manifest V3 permission posture** — Chrome's Manifest V3 limits host_permissions + activeTab + storage + scripting surface. Merlin's permission grants are documented in the Chrome Web Store Data Safety disclosures but rarely audited per-permission per-region per-LLM-sub-processor. This is the new audit surface the EU AI Act + GDPR + CCPA will converge on.
- **Consumer + prosumer + enterprise buyer base** — Merlin ships to consumers (free + Pro), prosumer creators (YouTube + Twitter + SEO use cases), and enterprise teams (Slack + Notion + Salesforce + HubSpot overlays). Three distinct buyer personas under one vendor.
- **Per-tab-query provenance** — every Merlin invocation can be tied to a URL + a per-page content snapshot + a per-account LLM sub-processor choice + a per-region retention window. This is the SOC 2 + EU AI Act evidence wedge that distinguishes Merlin from "another wrapper around GPT."

**The 12-doc evidence binder (the offer) — `merlin@merlinai.com` should care about:**

1. **EU AI Act Art. 14 Human-Oversight Operational Record** — per-account oversight event captured (Merlin response accepted, rejected, edited, regenerated) → provable human-in-the-loop log per session per page.
2. **EU AI Act Art. 50 Transparency Labeling** — every Merlin output is watermark-stamped in metadata ("AI-generated via Merlin, review before use") so recipients + regulators can identify AI-assisted content.
3. **EU AI Act Art. 53(1)(b) GPAI Training-Data Transparency Cascade** — Merlin discloses the underlying GPAI providers' training-data summaries (OpenAI + Anthropic + Google + Mistral + DeepSeek + Meta). Each carries its own Art. 53(1)(b) copyright-summary disclosure that Merlin should republish to EU deployers.
4. **GDPR Art. 28 Sub-Processor Disclosure + Flow-Down DPA** — the browser extension → Merlin gateway → LLM sub-processor chain needs a documented sub-processor list (current count: 6 LLMs) with flow-down DPA, change-notification mechanism, and audit rights.
5. **GDPR Art. 27 EU Representative + UK GDPR Art. 27 UK Representative** — for non-EU vendors (Foyer Tech LLC US-based) with EU deployers, Art. 27 EU/UK rep is required. Verify whether Merlin has appointed EU + UK reps.
6. **Manifest V3 Permission-Posture Documentation** — host_permissions, activeTab, storage, scripting surface — per-permission per-region per-LLM-sub-processor mapping. EU + UK + US + Canadian regulators are converging on Manifest V3 audit.
7. **Chrome Web Store Data Safety Disclosures** — per-purpose data-handling (consumer-input content vs. inferred-content vs. content-delivery-data) + data-sharing-scope + data-retention-scope. These disclosures are enforceable by the FTC + EU regulators.
8. **Multi-LLM Router Sub-Processor Audit Trail** — every LLM call (which model, which jurisdiction, which data residency, which retention window, which training-data opt-out flag) is logged → per-account reproducible audit trail that downstream deployers can hand to their security + compliance teams.
9. **OWASP LLM Top-10 Mitigation Runbook** — LLM01 prompt injection (webpage-poisoning attacks via injected DOM content), LLM02 sensitive-information disclosure (cross-account leakage via shared LLM gateway), LLM06 training-data exfiltration, LLM08 vector-DB poisoning. Merlin's defense-in-depth posture should publish detection rules + per-incident remediation playbooks for each.
10. **SOC 2 Type II Evidence Packet** — CC6.1 (logical access), CC6.7 (data-in-transit encryption TLS 1.3), CC7.2 (system monitoring), CC7.3 (incident response), CC9.2 (vendor risk management) — mapped to Merlin's browser-extension deployment model.
11. **CCPA/CPRA + APPI + PIPEDA Cross-Border Transfer Posture** — Merlin serves global users; per-region data-residency posture + cross-border-transfer-mechanism documentation (SCC + DPF + UK Extension + Swiss-US DPF) is required for Foyer Tech LLC's US-HQ + global-deployer footprint.
12. **Enterprise Plan SLA + Audit-Rights Provision** — for $29/user/mo Teams + custom Enterprise tier, audit-rights + sub-processor-change-notification + data-residency pinning + per-tenant SSO + SCIM provisioning are table stakes. Merlin should publish a one-pager SLA + enterprise DPA addendum.

**Compliance map (claimed by Merlin on privacy policy + Chrome Web Store listing):** GDPR + CCPA/CPRA + APPI + PIPEDA + Australia Privacy Act + Singapore PDPA. SOC 2 Type II is in progress. Merlin's enterprise-tier customers (typically tech-forward teams at Series B+ startups + Fortune-500 innovation labs) often require ISO 27001 + ISO 27701 as table stakes for AI-vendor procurement — verify whether these are published or roadmap.

**The pitch (3-line cold email):**

```
Subject: Merlin × EU AI Act Art. 14 — 12-doc evidence binder for 1M+ installs
To: merlin@merlinai.com
Body:
Hi Merlin team — Sinha. I run a 48h evidence-gap-map audit for AI-vendor cohorts; we ship a 12-document binder that closes the gap between your existing GDPR + Chrome Web Store Data Safety posture and the EU AI Act Art. 14 + Art. 50 + Art. 53(1)(b) GPAI-cascade + Manifest V3 permission-audit + per-LLM-sub-processor-retention-posture obligations that your 1M+ install base will need by Aug 2 2026. Sibling precedents: Hebbia 620 + EvenUp 621 + Spellbook 622 (ai_document_intelligence cohort, three closed). $500/48h flat, or $497/mo quarterly refresh as the EU AI Act deadline + Chrome Manifest V3 audit pressure converge. Worth 15 min next week? — Atlas
```

**Why this is the right cohort opener:**
- ai_browser_extension is a brand-new vertical in the Atlas cohort taxonomy — Merlin is the first sibling.
- Merlin ships to 1M+ consumers + thousands of teams + Enterprise customers across NA + EU + APAC, so the GDPR + EU AI Act + Manifest V3 + Chrome Web Store Data Safety pressure lands in the same procurement cycle.
- Six LLM sub-processors (GPT-4 + Claude + Gemini + Llama + Mistral + DeepSeek) under one extension = the densest LLM-sub-processor audit surface of any consumer AI product on the market. That density is exactly the wedge the 12-doc evidence binder sells against.

**Offer envelope:** $500 / 48h evidence-gap-map audit → $497/mo quarterly refresh on EU AI Act + GDPR + Manifest V3 + Chrome Web Store Data Safety cadence. Net new per cohort: $500 - $1,500/mo MRR potential once Merlin closes (single opener; future siblings: HARPA AI, Monica AI, MaxAI, Merlin-rivals).