**Subject:** quick question re FiftyOne's per-curation-workflow + per-embedding-model-version audit trail

**Body:**

Hi Brian (Co-Founder + CEO — Voxel51 + FiftyOne open-source + Drive Capital + M12 Series A), James (Co-Founder + CTO), Adam (Co-Founder + VP Engineering) —

Five questions before any pitch:

1. **Per-FiftyOne-dataset-version + per-curation-workflow-version + per-FiftyOne-Brain-embedding-model-version + per-CLIP/DINOv2/SigLIP/OpenCLIP-embedding-cascade provenance attestation** — when a Fortune-100 manufacturer or a Tier-1 AV-perception team or a healthcare-imaging network ships a CV model trained on a FiftyOne-curated dataset, can you hand the audit committee a single JSON blob that pins: per-FiftyOne-dataset-version + per-FiftyOne-SDK-version + per-FiftyOne-Teams-curation-workflow-version + per-curation-step + per-FiftyOne-Hub-ingestion-pipeline-version + per-FiftyOne-Brain-embedding-model-version + per-CLIP-or-DINOv2-or-SigLIP-or-OpenCLIP-embedding-checkpoint + per-annotation-team-member + per-3D-point-cloud-cuboid-annotation-version + per-multimodal-pair-tag-version + per-deployment-region? **Most CV-data vendors can't produce that blob — FiftyOne's open-source SDK + FiftyOne Teams + FiftyOne Cloud + FiftyOneHub means you can.** That's the gap your 100+ enterprise customers (LG + Bosch + Continental + GM + Ford + Rivian + ExxonMobil) hit today.

2. **EU AI Act Annex III §1(b) biometric-categorization + §5(a) predictive-policing + §6 emotion-recognition + Art. 6 high-risk-CV-data-classification mapping** — FiftyOne-curated datasets are the upstream supply-chain for every downstream CV model. For face-recognition + body-measurement + emotion-recognition + biometric-identification datasets curated in FiftyOne (the AV-perception + healthcare-imaging + manufacturing-defect + aerospace-defense procurement-grade data-curation surface), the EU AI Act Annex III §1(b) + §5(a) + §6 + Art. 6 high-risk-CV-classification triggers the strictest-tier-data-curation path. Are your FiftyOne deployments mapping each curated dataset to the Annex III + Art. 6 risk-category today, or is the customer left to map it themselves downstream?

3. **FiftyOne-Brain embedding-model sub-processor cascade across CLIP + DINOv2 + SigLIP + OpenCLIP for VLM-fusion-with-CV-curation** — FiftyOne.Brain embeds every sample with CLIP + DINOv2 + SigLIP + OpenCLIP (or a custom embedding model). Each underlying VLM-embedding-checkpoint (or fine-tune, or version) is a separate Art. 53(1)(b) GPAI sub-processor. Does Voxel51 publish a per-sample-embedding audit row (which embedding-model routed which sample + which confidence-score + which retrieval-threshold + which human-review-approval)? Or is the customer left to reconstruct the cascade?

4. **NIST AI RMF 600-1 2024 + ISO/IEC 42001 AIMS + ISO/IEC 23894 evidence binder for Tier-1 AV + manufacturing + healthcare procurement** — your 100+ enterprise customer footprint (LG Electronics + Continental + Bosch + GM + Ford + Rivian + Berkshire Grey + ExxonMobil + Mosaic) creates canonical Tier-1 procurement pressure. Does Voxel51 publish a per-curation-workflow RMF map + ISO 42001 AIMS binder + ISO 23894 AI-risk-management binder that LG + Bosch + Continental + GM can hand to their customers' procurement committees?

5. **3D-point-cloud + video-frame-tagging + nuScenes + KITTI + Waymo + Argoverse + ApolloScape AV-perception audit-trail** — for AV-perception-on-FiftyOne-curated-datasets (Tesla + Waymo + Cruise + Mobileye + Argo + Aurora + Rivian + Ford Autonomy supply chains all touch nuScenes + KITTI + Waymo + Argoverse + ApolloScape versions of FiftyOne-curated data), the per-frame + per-cuboid + per-temporal-annotation audit-trail needs AV-perception + robotics-perception + ADAS-pedestrian-detection alignment with ISO 21448 SOTIF + ISO 26262 ASIL-D + EU AI Act Art. 14 + Art. 61 + Annex III. Does Voxel51 ship a per-frame + per-3D-cuboid + per-temporal-annotation binder for AV-perception-Tier-1 procurement, or is the customer left to assemble it?

The reason I ask — I run a 14-document evidence-gap audit for AI/ML vendors selling into Fortune-100 + Tier-1-AV + EU + UK + regulated-US-state procurement cycles. Most CV-data-curation vendors I've audited show a 6-10 week gap between "we have SOC 2 + ISO 27001" and "we have an Art. 6 + Art. 14(4) + Art. 50 + Art. 53(1)(b) + Annex IV + per-curation-workflow + per-embedding-model-version + per-sample-embedding-provenance + Illinois BIPA + Texas CUBI + GDPR Art. 9 + FDA 21 CFR Part 820 + EU MDR + ISO 21448 SOTIF + ISO 26262 ASIL-D binder the procurement committee signs off on." Voxel51 has the data-centric-AI architecture surface (per-FiftyOne-dataset-version + per-FiftyOne-Teams-curation-workflow-version + per-Cloud-deployment-version + per-FiftyOne-Brain-embedding-model-version + per-annotation-team-member + per-3D-point-cloud-cuboid-annotation-version = the audit-committee-handed-JSON-blob primitive) to close that gap — but the per-curation-workflow evidence binder isn't typically published.

I'm doing a $500 / 48h evidence-gap audit + a $497/mo quarterly refresh retainer for ai_vision_computer_vision cohort siblings (Roboflow + LandingAI + Voxel51 + Encord + Scale Rapid + etc.). 15-min call to walk through the 14-doc binder?

PS — if Voxel51 is already running an in-house Art. 6 + Art. 14(4) + Art. 50 + Art. 53(1)(b) + NIST AI RMF + ISO/IEC 42001 + ISO/IEC 23894 + Illinois BIPA + Texas CUBI + GDPR Art. 9 + ISO 21448 SOTIF + ISO 26262 binder that maps to the LG + Bosch + Continental + GM + Ford + Rivian + ExxonMobil procurement committees you sell into, I'd genuinely like to learn the shape of it. Most AI/ML vendors I audit are 30-50% of the way through and the audit lets them ship the remaining binder in 48h.

— [Sender]
Talon Forge LLC | atlas-store-voxel51-665 cohort | $500/48h audit + $497/mo refresh
