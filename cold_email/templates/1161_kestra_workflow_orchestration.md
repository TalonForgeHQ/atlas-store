# Kestra — ai_workflow_orchestration OPENER #1/5

**Lead:** 1161 — Kestra
**Vertical:** ai_workflow_orchestration
**Cohort position:** OPENER #1/5 (NEW VERTICAL #52)
**First-party route:** FORM:https://kestra.io/get-started (verified 2026-07-24)
**Offer:** $500 / 48h fixed-scope audit · $497/mo refresh · $2,000 five-vendor cohort benchmark

---

## Why Kestra is the opener

Kestra ships a declarative event-driven workflow orchestration platform with 1,200+ plugins and a triple-tier substrate (Apache-2.0 OSS + Cloud + Enterprise Edition on-prem/air-gapped). It is the only ai_workflow_orchestration candidate with an Algolia + Datadog + dbt + Airbyte + Plotly founder syndicate on the cap table — that signal alone makes it the cohort's natural OPENER for procurement-led diligence.

**Cohort siblings TBD:** Temporal · Argo Workflows · Mage · Modal · Windmill (Prefect excluded — acquired Dagster Labs; will appear in a future cohort as a standalone conglomerate, not as an orchestration substrate).

**Kestra adds:**
1. Declarative YAML workflow-as-code + 1,200+ plugins as first-party positioning
2. Apache-2.0 OSS + Cloud + Enterprise Edition triple substrate under one platform
3. AI Copilot with custom models (Enterprise Edition exclusive)
4. Algolia + Datadog + dbt + Airbyte + Plotly founder syndicate backing
5. Per-instance + unlimited flows/tasks/executions pricing model (not per-metered)

---

## Evidence-gap map (22 columns)

| Column | Value | Source | Confidence |
|---|---|---|---|
| `tenant_id` | customer tenant | kestra.io/cloud | high |
| `kestra_tenant_id` | per-Cloud tenant | kestra.io/cloud | high |
| `kestra_namespace` | per-flow namespace | kestra.io/docs | high |
| `kestra_flow_id` | YAML flow id | kestra.io/docs | high |
| `kestra_execution_id` | per-run id | kestra.io/docs | high |
| `kestra_taskrun_id` | per-task id | kestra.io/docs | high |
| `kestra_worker_group_id` | per-worker-group id | kestra.io/enterprise | high |
| `kestra_secret_id` | per-secret id (internal + external) | kestra.io/enterprise | high |
| `kestra_plugin_id` | per-plugin id (1,200+ total) | kestra.io/plugins | high |
| `kestra_event_listener_id` | per-event-listener id | kestra.io/docs | high |
| `kestra_schedule_id` | per-schedule id | kestra.io/docs | high |
| `kestra_audit_log_id` | per-action audit log id | kestra.io/enterprise | high |
| `kestra_rbac_role_id` | per-RBAC role id | kestra.io/enterprise | high |
| `kestra_sso_config_id` | per-SSO config id (Public + Cloud SSO) | kestra.io/enterprise | high |
| `kestra_scim_provisioning_id` | per-SCIM provisioning event | kestra.io/enterprise | high |
| `kestra_ai_copilot_run_id` | per-AI-Copilot invocation (Enterprise) | kestra.io/enterprise | medium |
| `kestra_llm_model_id` | per-custom-model deployment | kestra.io/enterprise | medium |
| `kestra_sub_processor_id` | per-sub-processor (e.g. LLM provider) | kestra.io/legal | high |
| `kestra_storage_isolation_id` | per-tenant storage isolation | kestra.io/enterprise | high |
| `kestra_plugin_version_id` | per-plugin version pin | kestra.io/enterprise | high |
| `kestra_external_log_aggregator_id` | per-external-log-aggregator wiring | kestra.io/enterprise | high |
| `audit_export_id` | per-audit-export payload | kestra.io/enterprise | high |

---

## 5-WEDGE non-overlap scaffold (vs cohort siblings TBD)

| Wedge | Kestra wedge | Distinct because |
|---|---|---|
| (1) workflow-as-code substrate | YAML declarative + 1,200+ plugins | only cohort candidate that ships plugins as a first-class substrate (vs Python SDK / DAG-Python / Helm-only) |
| (2) deployment substrate | OSS Apache-2.0 + Cloud + Enterprise on-prem/air-gapped | only cohort candidate with triple-tier on one platform |
| (3) AI substrate | AI Copilot with custom models (Enterprise) | only cohort candidate with custom-model bring-your-own in the orchestration tier |
| (4) investor signal | Algolia + Datadog + dbt + Airbyte + Plotly founder syndicate | only cohort candidate with this exact multi-tool-data founder stack on the cap table |
| (5) pricing model | per-instance + unlimited executions | only cohort candidate priced on per-instance rather than per-execution metering |

Sibling wedges (Temporal, Argo Workflows, Mage, Modal, Windmill) will be mapped against this scaffold as each OPENER/sibling ships.

---

## Buyer-aligned copy blocks (3 lines)

1. **For VP Engineering:** "Your 1,200+ plugin library plus YAML-as-code means the workflows your team already writes in Python or Cron today stay declarative tomorrow — without rewriting them."

2. **For Head of Platform:** "Kestra's Enterprise Edition runs on-prem or air-gapped under your own tenant boundary, with internal + external secret manager, public/Cloud SSO, and SCIM — so the audit-trail your security team wants lives in your perimeter, not in a SaaS log bucket."

3. **For CFO:** "$25M Series A + $36M total funding from the founders of Algolia, Datadog, dbt, Airbyte, and Plotly — your vendor is backed by people who've built the tools you already pay for."

---

## Disclaimers

- **HQ city not surfaced** on `/about-us` page extraction 2026-07-24; retained as buyer diligence gap (do not infer).
- **Series B+ funding rounds** beyond the $25M Series A + $36M total were NOT surfaced; do not infer.
- **Compliance other than SOC 2 + GDPR** (ISO 27001, HIPAA, etc.) was NOT surfaced in the `/legal` page extraction; do not infer.
- **Prefect acquired Dagster Labs** per first-party announcement surfaced 2026-07-24; excluded from cohort sibling pool.

---

**Commercial route:** FORM:https://kestra.io/get-started (verified verbatim 2026-07-24); not submitted.
**Offer:** $500 / 48h · $497 / mo · $2,000 cohort benchmark · $2,485 MRR ceiling per YanXbt pattern.
**Status:** SMTP/form gated; $0 sent / $0 received.
