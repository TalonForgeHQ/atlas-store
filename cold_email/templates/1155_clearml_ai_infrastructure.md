# 1155 — ClearML AI Infrastructure — ai_mlops_feature_store SIBLING #2/5

**First-party recipient route:** `FORM:https://clear.ml/demo` (not submitted)
**Company:** ClearML, Inc. — clear.ml
**Leadership:** Moses Guttmann (CEO & Co-Founder); Gil Westrich (CTO & Co-Founder); Noam Harel (Co-Founder, CMO & GM North America)
**Cohort:** NEW VERTICAL #51 `ai_mlops_feature_store`; Weights & Biases 1154 OPENER #1/5 → ClearML 1155 SIBLING #2/5

## Subject options

1. **ClearML Infrastructure Control Plane — 22-column AI production evidence map**
2. **ClearML + W&B — can GPU-to-agent lineage survive an audit replay?**
3. **ClearML GenAI App Engine — model, deployment, drift, and event-log join**

## Message

Hi Moses,

ClearML's first-party platform separates the **Infrastructure Control Plane**, **AI Development Center**, and **GenAI App Engine**. That is a materially different audit surface from an experiment-tracking-first stack: the evidence has to follow GPU/resource allocation, dataset versioning, experiment and model lineage, agent orchestration, deployment versions, drift/bias alerts, and the event log for every production deployment.

Atlas can deliver a fixed-scope **$500 / 48-hour** evidence-gap map that joins those ClearML surfaces into a proposed 22-column receipt, or maintain the map for **$497/month** with a quarterly refresh. The work is grounded in `clear.ml/about-us`, `clear.ml/pricing`, `clear.ml/docs/latest/docs/`, `clear.ml/demo`, and `clear.ml/contact-us`.

## Five non-overlap wedges vs Weights & Biases 1154

1. **Infrastructure Control Plane:** GPU/resource provisioning, scheduling, quotas, and autoscaling are first-class evidence surfaces.
2. **Three-layer architecture:** Infrastructure Control Plane + AI Development Center + GenAI App Engine spans infra-to-agent deployment.
3. **Deployment boundary:** first-party pricing explicitly supports hosted, self-hosted, VPC, on-premises, hybrid, and air-gapped options.
4. **GenAI App Engine and agent orchestration:** LLM deployment into GPU clusters and workflow orchestration extend beyond a run/artifact/trace ledger.
5. **Transparent commercial surface:** Community `$0` and Pro `$15/user/month + usage` expose a buyer-facing entry point alongside enterprise controls.

## Proposed audit receipt

`tenant_id + clearml_workspace_id + clearml_project_id + clearml_task_id + clearml_dataset_version_id + clearml_experiment_id + clearml_model_repository_id + clearml_model_version_id + clearml_artifact_id + clearml_pipeline_id + clearml_agent_orchestration_run_id + clearml_genai_app_run_id + clearml_gpu_cluster_id + clearml_worker_id + clearml_deployment_id + clearml_deployment_version_id + clearml_drift_event_id + clearml_bias_monitor_event_id + clearml_event_log_id + clearml_environment_id + clearml_human_override_id + cross_tenant_no_bleed_invariant`

The certification posture is deliberately **not inferred**: the public pages checked did not enumerate SOC 2, ISO, HIPAA, or GDPR certifications. That is a diligence question for the buyer, not a fabricated claim.

Best,
Atlas @ Talon Forge

**Status:** template-only; no email or form submitted. `$0 sent / $0 received`.
