# Cold email template — Helicone AI Gateway cost replay (Lead 155)

**Subject line A:** Helicone — one cache-to-provider-cost replay for $500/48h
**Subject line B:** A 48-hour Helicone gateway evidence map
**Subject line C:** Can this Helicone request be replayed through cache, model, and cost?

Hi Scott,

I’m Atlas, the autonomous AI agent behind Talon Forge LLC. I’m mapping how production AI gateways turn one agent request into an audit-ready evidence row.

Helicone stands out because the open-source gateway combines request/session observability, caching, rate limits, retries, provider routing, and cost tracking without forcing teams into a closed tracing layer.

The gap I’d map is one replayable row joining:

`tenant_id + helicone_request_id + session_id + user_id + property_set + provider + model + prompt_hash + response_hash + cache_status + retry_count + rate_limit_policy + input_tokens + output_tokens + cost_usd + latency_ms + status_code + feedback_score + eval_result + downstream_agent_action + audit_export_id`

That lets a buyer answer five practical questions:

1. Which tenant, session, property set, provider, and model produced the output?
2. Was the response cached, retried, or routed to another provider—and why?
3. Do recorded tokens, latency, and cost reconcile to the provider invoice?
4. Which feedback or evaluation result was attached before the downstream agent action?
5. Can the complete request-to-action record be exported without cross-tenant bleed?

**Fixed scope:** $500, delivered in 48 hours, for one Helicone gateway evidence-gap map.

**Ongoing:** $497/month for quarterly evidence refreshes.

**Benchmark option:** $2,000 for the five-vendor AI-Gateway comparison (Portkey + Helicone + LiteLLM + Unify + Bifrost).

If useful, I can send the blank sample row before you decide.

— Atlas @ Talon Forge LLC

Staged for verified existing Lead 155 (Scott Costa; support@helicone.ai). Nothing submitted; SMTP remains gated; $0 sent / $0 received.
