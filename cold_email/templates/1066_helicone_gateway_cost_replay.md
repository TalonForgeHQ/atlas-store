# Lead 1066 — Helicone AI Gateway SIBLING #2/5

**Subject A:** Helicone — one OpenAI-compatible request-to-cost replay for $500/48h
**Subject B:** A 48-hour Helicone gateway evidence map
**Subject C:** Can Helicone replay the route, cache, fallback, and cost behind one agent request?

Hi Helicone team,

I’m Atlas, the autonomous AI agent behind Talon Forge LLC. I’m mapping how production AI gateways turn one agent request into an audit-ready evidence row.

Helicone’s first-party homepage positions it as “Helicone / AI Gateway & LLM Observability” and describes routing and monitoring for reliable AI apps. The live homepage exposes `mailto:contact@helicone.ai`, states SOC 2 Type II certification and HIPAA compliance, and describes encryption, least-privilege controls, penetration testing, and a self-hosted AI Gateway. The first-party quickstart docs describe an OpenAI-compatible unified API with access to 100+ models, automatic logging, observability, fallbacks, and unified billing.

The gap I’d map is one replayable row joining:

`tenant_id + helicone_workspace_id + helicone_request_id + session_id + user_id + property_set_id + provider + model + prompt_hash + response_hash + cache_status + retry_count + fallback_chain_id + rate_limit_policy + input_tokens + output_tokens + cost_usd + latency_ms + status_code + eval_result_id + downstream_agent_action_id + audit_export_id`

That lets a buyer answer:

1. Which tenant, session, provider, and model produced the output?
2. Was the response cached, retried, rate-limited, or routed to a fallback provider?
3. Do tokens, latency, status, and cost reconcile to the provider invoice?
4. Which evaluation or alert was attached before the downstream agent action?
5. Can the request-to-action record be exported and deleted without cross-tenant bleed?

**Fixed scope:** $500, delivered in 48 hours, for one Helicone gateway evidence-gap map.

**Ongoing:** $497/month for quarterly evidence refreshes.

**Benchmark option:** $2,000 for the five-vendor AI-Gateway comparison (Portkey + Helicone + LiteLLM + Unify + Bifrost).

If useful, I can send the blank sample row before you decide.

— Atlas @ Talon Forge LLC

First-party routes: `mailto:contact@helicone.ai` on https://www.helicone.ai/ and `FORM:https://www.helicone.ai/contact`. Nothing submitted; SMTP/form gated; $0 sent / $0 received.
