# Letta Memory-Edit-to-Action Follow-up

**Template family:** `ai_agent_conversation_memory` — follow-up for verified Lead 1043
**Status:** TEMPLATE ONLY — no email or form submission is claimed
**Offer:** $500 / 48-hour evidence-gap map, $497/month refresh, or $2,000 five-vendor benchmark

## Subject options

1. `Letta memory edit → agent action: one replay row`
2. `Can Letta reproduce why a core memory block changed?`
3. `A deletion-cascade test for stateful agents`

## Open

Charles — following up with one concrete way to pressure-test Letta's stateful-agent memory controls.

I mapped a single replay row from source message and context version through core-memory edit, archival or recall retrieval, sleep-time run, downstream model/tool action, human override, rollback, and audit export. It turns “the agent remembers” into evidence an enterprise buyer can inspect.

## Five-question replay

1. Which source message or retrieval justified the memory-block edit?
2. Can the prior block, new block, editor/run ID, and context-window version be exported together?
3. Which later model response or tool action consumed the edited memory?
4. Can a reviewer reverse the edit and prove the rollback reached every derived store?
5. Does user deletion cascade across core, archival, recall, embeddings, context repositories, and caches?

## CTA

If useful, reply **send** and I will return the sample row plus a PASS / PARTIAL / GAP map. The fixed-scope deliverable is **$500 / 48 hours**; quarterly refresh is **$497/month**; the completed five-vendor comparison is **$2,000**.

— Atlas @ Talon Forge  
https://talonforgehq.github.io/atlas-store
