# 237 — Patronus AI (AI agent evaluation + Digital World Model simulation infrastructure) — ai_agents_infra vertical

**To:** security@patronus.ai
**From:** Atlas (Talon Forge)
**Subject:** The AI-agent-eval-pipeline + simulation-run-provenance gap your Fortune-500 enterprise customers will probe in their next SOC 2 + EU AI Act + ISO 42001 audit

---

Hi Anand + Patronus team,

Patronus sits in a different lane than every other vendor in the ai_agents_infra cluster — not the *per-LLM-call-chain* lane (AgentOps 153 + Helicone 155), not the *upstream-foundation framework + LangSmith* lane (LangChain 154), and not the *cross-agent-handoff-and-state-change* lane (CrewAI 156). Patronus is the *AI-agent-evaluation + simulation-run-provenance* lane — the eval infrastructure that regulated enterprises actually trust to *certify* an agent is safe to deploy into production. When a Fortune-500 customer runs Lynx + Digital World Model + the 30-40% model-lift measurement to greenlight a customer-service agent into production, the audit question isn't "what did each LLM call do?" — it's "what was the eval-decision provenance: which prompts went into the simulation, which world-data artifacts were sampled, which evaluator-judge LLM made the pass/fail decision, what was the per-scenario scoring decision, what was the longitudinal drift between v1 and v2 of the simulation run, and what downstream production-deployment-gate decision did the eval result unlock?" Five gaps that map to that surface:

1. **End-to-end Patronus eval-run + simulation-run + judge-decision provenance join-table reconstructible per eval-run.** Per **SOC 2 CC7.2 + EU AI Act Art. 12 + ISO 42001 §9.4** — 11-column reconstruction from a single `eval_run_id` + `simulation_run_id` linking `eval_definition_hash` + `world_data_artifact_hash` + `scenario_definition_hash` + `agent_under_test_config_hash` + `judge_llm_config_hash` + `prompt_injection_payload_hash` + `per_scenario_score_hash` + `aggregate_pass_fail_decision_hash` + `longitudinal_drift_delta_hash` + `downstream_production_gate_decision_hash` + `cost_usd_total_at_decision_time` for 7yr WORM + a quarterly reconstruction drill on the Datadog + Samsung + Fortune-500 enterprise customer stack. The auditor will want this bundled *per-eval-run*, not per-LLM-call — and Patronus is the only AI-agent-eval-as-audit-target vendor that ships the eval-decision-provenance surface as a first-class concept vs. vendors where eval output is a JSON blob the customer has to interpret and reprove independently.

2. **Digital-World-Model + 1M+ world-data-artifact license-provenance + copyright-summary evidence per EU AI Act Art. 53(d) GPAI training-data transparency + EU AI Act Art. 53(1)(b) copyright-summary + the upstream OSS infrastructure that every Patronus-using regulated-enterprise agent evaluation inherits.** Per the 30-40% model-lift claim + the 1M+ world-data artifacts + the 5K+ expert contributors that built the simulation corpus — the audit surface is a per-artifact license-detection + per-artifact attribution-chain + per-artifact copyleft-flag + license-collision-flag + training-corpus-evidence for the underlying Digital World Model training data. The auditor will ask: "For each world-data artifact sampled into the eval run, what is its license + attribution + copyleft status + EU AI Act Art. 53(1)(b) copyright-summary — and can you prove the eval result wasn't contaminated by a copyleft-licensed artifact flowing into a proprietary deployment?" This is the highest-leverage license-provenance audit target in the entire ai_agents_infra pipeline because every Patronus-using customer inherits this surface, and an EU AI Act Art. 53(d) finding here propagates to every regulated-enterprise team that used the eval result to greenlight production deployment.

3. **Cross-tenant Patronus eval-platform + simulation-backend isolation-evidence for the multi-tenant SaaS running 30-40% model-lift measurements on the Datadog + Samsung + Fortune-500 enterprise stack.** Per **SOC 2 CC6.1 + GDPR Art. 28 + HIPAA + FedRAMP Moderate + EU AI Act Art. 10** — per-tenant isolation-test-result + per-tenant CMK/BYOK optionality + per-eval-run-no-leakage-evidence + per-tenant world-data-artifact-residue-cleanup-procedure + completion-of-deletion timestamp + per-customer-no-leakage-evidence for the regulated-enterprise customer stack. The auditor will specifically probe *judge-LLM isolation* across tenants because the judge LLM is the natural cross-tenant-data-leakage surface if isolation isn't per-tenant-per-judge-call — and a judge's response could leak sensitive eval-prompt material (which may contain customer-confidential agent-design-IP) into another tenant's downstream eval context.

4. **Lynx + per-prompt-injection-detection + judge-decision-override-provenance per OWASP LLM Top 10 LLM01 + ISO 42001 §6.1.4 + NIST AI RMF MEASURE.** The unique Patronus lane — the platform's eval infrastructure is the natural instrumentation point for prompt-injection detection across the entire downstream AI-agent deployment stack. The audit surface is an 8-column per-payload detection-log: `inbound_payload_hash` + `lynx_detection_classification` + `judge_decision_override_applied` + `blocked_event_flag` + `downstream_state_change_flag` + `incident_response_escalation_id` + `per_tenant_quarantine_flag` + `regulatory_retain_until`. The SOC 2 CC7.2 timing-evidence requirement is critical here: the auditor wants proof that the Lynx detection was applied *before* the judge LLM saw the payload — not logged after — because a Lynx-flagged prompt that flowed into a judge-decision creates a chain-of-custody-evidence problem when the regulator reconstructs the eval run.

5. **EU AI Act Annex III §4 high-risk classification for any Patronus customer using eval-output to greenlight an agent that materially influences a high-risk decision lane** (credit + employment + healthcare + education + law-enforcement + access-to-essential-services + biometric-identification + critical-infrastructure) per **EU AI Act Art. 6 + 14 + 27 + 43 + 72** (Aug 2026 GPAI enforcement + Annex III §4 conformity-assessment deadline). The Patronus-supplied eval-decision-provenance package flows down to every customer that used Lynx or Digital World Model output to greenlight a production agent — and a finding here propagates to every regulated-enterprise customer in the Patronus ecosystem, including the Datadog + Samsung + Fortune-500 customer book that the $50M Series B led by Greenfield Partners (with Lightspeed + Notable Capital + Datadog + Samsung + Gokul Rajaram + Factorial Capital) positioned Patronus to serve.

---

**What I do:** 48-hour AI-agent-eval-platform audit. $500 flat. You get a written gap-analysis against SOC 2 CC7.2 + EU AI Act Art. 12 + 14 + Art. 53(d) + Art. 53(1)(b) + ISO 42001 §9.4 + OWASP LLM Top 10 LLM01 + NIST AI RMF MEASURE, with the actual 11-column eval-run provenance join-table schema + the per-artifact license-detection + per-attribution-chain + per-tenant isolation-evidence package + the Lynx prompt-injection-detection-log schema + the EU AI Act Annex III §4 conformity-assessment-deck-supplier package you can hand to the auditor — and the upstream-canonical-source language that flows down to every Patronus-using customer in your ecosystem (which is *every* Fortune-500 AI-agent-production-deployment-certification workflow, given Patronus's $50M Series B + Greenfield Partners + Lightspeed + Notable Capital + Datadog + Samsung positioning + the 30-40% model-lift + 1M+ world-data-artifact + 5K+ expert-contributor surface that no other vendor in the pipeline replicates).

**The ask:** 30-min call this week with you or your head of compliance/eng to walk through which of these 5 gaps your Datadog + Samsung + Fortune-500 enterprise customers are already asking about — and whether Patronus wants to be the canonical upstream answer for the next 1000+ AI-agent-eval-decision-provenance audit-cycles.

— Atlas
Talon Forge | atlas@talonforge.io