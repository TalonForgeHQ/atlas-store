# Cold email template — Lead 1051 (Weaviate, SIBLING #2/5 ai_agent_vector_memory)

**Cohort:** ai_agent_vector_memory
**Verified executive:** Bob van Luijt, CEO (first-party company page)
**Verified route:** FORM:https://weaviate.io/contact (sales option; not submitted)

**Subject A:** Weaviate retrieval → agent-decision receipt

**Subject B:** $500/48h Weaviate evidence map

Hi Weaviate team —

Weaviate’s open-source, AI-first vector infrastructure gives teams control over how retrieval is built and deployed. For an enterprise buyer, the hard question is whether one agent decision can be replayed from source and embedding through collection, tenant, hybrid/vector query, filters, result set, reranking, prompt placement, tool call, and downstream action.

I mapped that into a compact receipt joining tenant_id + deployment_plane + collection_id + source_document_id + chunk_id + embedding_model/version + vector_write_id + keyword/vector weights + filter_version + query_id + retrieved_object_ids + scores + reranker/version + prompt_version + llm_call_id + tool_call_id + agent_decision_id + downstream_action_id + deletion_event_id + cross_tenant_no_bleed_check + audit_export_id.

A one-page Weaviate evidence-gap map is $500/48h fixed-scope, or $497/mo for quarterly refreshes.

Reply “send it” and I’ll deliver the sample receipt tomorrow.

— Atlas
Talon Forge LLC · talonforgehq.github.io/atlas-store

P.S. The open-source plus cloud/self-managed deployment boundary makes the receipt concrete: the same retrieval decision can be checked across deployment planes rather than hidden behind generic “RAG-ready” language.
