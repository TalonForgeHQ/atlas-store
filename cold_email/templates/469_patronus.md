# Cold Email Template 469 — Patronus AI

**Vertical:** llm_evaluation
**Tier:** 1
**Persona:** Anand Kannappan (Co-Founder + CEO), Rebecca Qian (Co-Founder + CTO)
**Send date:** 2026-07-17
**Sender:** Atlas @ Talon Forge

---

## Subject

Patronus AI Evaluation Platform — the EU AI Act Aug 2026 GPAI hallucination + safety + reliability triple-gap

## Pre-header (preview text)

LLM evaluation vendor audit — 48h turnaround, $500 flat fee.

## Body

Hi Anand + Rebecca,

Patronus sits at a structurally different position in the EU AI Act stack than every other
LLM evaluation vendor. You ship **Percival** as the canonical LLM-eval-and-judge framework,
**Patronus AI Evaluation Platform** as the commercial surface, and **Patronus RL Environments**
as the agentic-feedback loop — three independent evaluation surfaces an EU AI Act Art. 12 +
Art. 53(d) + Aug 2026 GPAI enforcement auditor can reconstruct from your evidence pack, instead
of one proprietary eval stack. Three questions if you're free for 15 min this week:

1. **Triple-surface provenance join-table.** Your per-Patronus-eval-id → per-Percival-judge-id
   → per-Percival-eval-suite-id → per-Patronus-AI-Eval-run-id → per-Patronus-prompt-template-version-id →
   per-Patronus-completion-id → per-Patronus-hallucination-flag-id → per-Patronus-safety-violation-id →
   per-Patronus-RL-Environment-step-id → per-Patronus-agent-action-id → per-Patronus-tool-call-id →
   per-Patronus-MCP-server-call-id → per-Patronus-RAG-retrieval-id → per-Patronus-knowledge-base-chunk-id →
   per-Patronus-prompt-injection-flag-id → per-Patronus-OWASP-LLM-Top-10-id → per-Patronus-MITRE-ATLAS-id →
   per-Patronus-RLHF-feedback-id → per-Patronus-reward-model-score-id → per-Patronus-five-nines-uptime-id →
   per-Patronus-tenant-id → per-Patronus-billing-event-id chain — when an EU AI Act Art. 12 + Art. 53(d)
   + Aug 2026 GPAI enforcement auditor asks "show me the per-prompt-injection hallucination lineage for
   tenant T-7219 in workspace W-441 in region eu-central-1," how many joins does your evidence pack have,
   and what's the median query time? For Arize 468 + Langfuse 241 + Helicone + Braintrust, the answer
   is typically 16-22 joins across 4-7 systems; we want to map Patronus's path and quantify the
   three-surface advantage.

2. **OWASP LLM Top 10 + MITRE ATLAS + per-vertical AI Act Annex III coverage.** Your 100+ customer
   footprint + $50M Series B 2025 led by Lightspeed + $17M Series A from Lightspeed + Index Ventures seed +
   Forbes AI 50 2024 + Databricks + financial-services + customer-services + data-science customers means
   a single evaluation gap can pin-hole every customer at once. We walk through per-OWASP-LLM01 prompt
   injection → per-MITRE-ATLAS-id → per-prompt-injection-poisoning-id → per-RAG-retrieval-poisoning-id →
   per-knowledge-base-document-poisoning-id → per-Agentic-AI-poisoning-id → per-MCP-server-call-poisoning-id →
   per-tool-call-poisoning-id → per-Patronus-AI-Eval-tool-call-id → per-Patronus-RL-Environment-action-id →
   per-Patronus-AI-Eval-hallucination-flag-id → per-Patronus-AI-Eval-prompt-injection-flag-id →
   per-Patronus-OWASP-LLM01-LLM06-LLM07-LLM08-id → per-Patronus-MITRE-ATLAS-id →
   per-Patronus-five-nines-bypass-id → per-Patronus-RLHF-poisoning-id → per-Patronus-reward-model-poisoning-id →
   per-Patronus-judge-bias-id → per-Patronus-eval-suite-bypass-id → per-Patronus-data-exfiltration-via-eval-id →
   per-Patronus-shadow-AI-data-leakage-id coverage matrix for Patronus AI Evaluation Platform + Percival +
   Patronus RL Environments posture (28+ cols). The Aug 2026 GPAI enforcement deadline does not care if
   your eval was 99.9% accurate on training data — it cares if your hallucination_flag_id links to the prompt
   that triggered it.

3. **WORM retention + per-vertical cost attribution.** SOC 2 CC7.2 + EU AI Act Art. 12 + SEC 17a-4 WORM +
   PCI DSS Req. 10 + HIPAA + EU AI Act Annex III 4 + Aug 2026 GPAI enforcement +
   per-Patronus-AI-Eval-cost + per-Percival-judge-cost + per-Patronus-RL-Environment-step-cost +
   per-Patronus-hallucination-detection-cost + per-Patronus-safety-violation-cost +
   per-Patronus-prompt-injection-detection-cost + per-Patronus-RAG-evaluation-cost +
   per-Patronus-prompt-optimization-cost + per-Patronus-agent-evaluation-cost +
   per-Patronus-five-nines-uptime-cost + per-Patronus-MCP-eval-cost all converge on the same evidentiary
   surface: per-tenant WORM-retention cost, per-workspace cost, per-Patronus-eval-run cost, per-Percival-judge
   cost, per-Patronus-RL-Environment-step cost, per-Patronus-hallucination cost, per-Patronus-safety cost,
   per-Patronus-prompt-injection cost, per-DSPM-coverage cost, per-DSR-coverage cost, per-consent cost,
   per-vendor-risk cost, per-multi-cloud-coverage cost, per-vertical cost (banking + insurance + telecom +
   healthcare + pharma + public-sector), per-monthly-workspace cost, per-billing-event cost, plus the
   data-residency-premium line items (EU + India + LGPD + India-DPDP + PIPL-China).

## CTA — $500 / 48h audit OR $497/mo retainer

**$500 / 48h audit** — fixed-price audit binder scoped to Patronus AI Evaluation Platform + Percival +
Patronus RL Environments. Delivered in 48h as a 28-col + 24-col + 22-col + 20-col + 25-col table set with
executive summary + remediation roadmap. Single payment via Stripe payment link.

**$497/mo retainer** — keeps the binder live as Patronus ships v24.x releases; monthly update of all
5 audit-gap sections against Patronus AI Evaluation Platform + Percival + Patronus RL Environments release
notes; per-quarter board-ready briefing for Lightspeed + Index Ventures portfolio reviewers.

**Lightspeed portfolio bundle credit** — 15% off if you pair Patronus 469 with Arize 468 + Monte Carlo 463 +
Soda 467 + Bigeye 464 + Databricks 462 = 6 vendors across llm_evaluation + llm_observability + data_observability
+ data_quality + ml_ops lanes at $3,000 audit / $2,982/mo MRR ceiling.

P.S. — Anand, your "five nines reliability for AI agents" framing is the canonical regulatory-facing
positioning for the AI Act Art. 14 high-risk classification. The audit binder is the artifact that closes
the gap between that framing and the per-prompt-injection lineage Art. 12 demands. 15 min this week?

P.P.S. — Rebecca, the Patronus RL Environments product is uniquely positioned for the EU AI Act Art. 53(d)
post-market-monitoring requirement — every RL-Environment-step-id → reward-model-score-id →
human-feedback-id chain is the exact audit spine Aug 2026 GPAI enforcement will probe. The binder maps it.

— Atlas @ Talon Forge
   $500/48h audit · https://talonforgehq.github.io/atlas-store/contact.html
   contact@patronus.ai · security@patronus.ai · patronus.ai