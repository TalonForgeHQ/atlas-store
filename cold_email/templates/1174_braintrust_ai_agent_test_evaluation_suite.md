---
tick: 1174
vendor: Braintrust
vertical: ai_agent_test_evaluation_suite
slot: OPENER #1/5
compiled: 2026-07-24
---

# Cold email template — Braintrust (ai_agent_test_evaluation_suite OPENER #1/5)

## Subject options (pick one)

1. `Braintrust eval + proxy + Functions — ai_agent_test_evaluation_suite opener gap map`
2. `Eval-driven-development audit for Braintrust pre-prod + prod + CI`
3. `SOC 2 + ISO 27001 + HIPAA + EU AI Act Art. 10 eval-trail evidence-gap for Braintrust`

## Body

Hi {{first_name}},

**Paragraph 1 — the buyer question.** You ship LLM applications and AI agents to production customers. The audit team asks: "which product runs our pre-prod eval experiments as a CI gate against main, observes every prod LLM call with per-call scorer + per-call token cost, scores outputs with code-based + LLM-as-judge + heuristic scorers, replays any prod span against a new prompt version, and ships SOC 2 Type II + ISO 27001 + HIPAA-ready + GDPR + EU AI Act Art. 10 evidence to our enterprise customer?" Braintrust answers all five in one product — but the eval-replay audit-trail evidence (per-row-per-dataset provenance hash, per-eval-experiment scorer versioning, per-tenant encryption keys, per-Brainstore-region US/EU pinning, eval-driven-Function-deploy gate) is not surfaced on your public security page in a way an enterprise procurement team can paste into a vendor questionnaire. That's the gap we map.

**Paragraph 2 — first-party verbatim evidence we already collected (2026-07-24).** braintrustdata.com/security confirms SOC 2 Type II + ISO 27001 + HIPAA-ready + GDPR + EU AI Act Art. 10 readiness verbatim. braintrustdata.com/pricing confirms Free $0/mo + Team $249/mo + Enterprise custom pricing verbatim. github.com/braintrustdata/braintrust-sdk is the Apache-2.0 SDK source-of-truth. The Braintrust Evals API + Logs API + Proxy + Functions runtime are first-party per docs.braintrustdata.com 2026-07-24. We have not submitted any form; this is a research-only outreach (SMTP gated; $0 sent / $0 received).

**Paragraph 3 — five scoped evidence questions + three offers.**

*Evidence questions (five, scoped to the buyer question above):*
1. Where is the per-row-per-dataset provenance hash produced, stored, and exposed in the Braintrust API surface (`dataset.s3()` + per-row `id` + per-row `source` URL + per-row `metadata` JSON), and what is the cross-tenant-no-bleed invariant?
2. What is the per-eval-experiment scorer versioning model in Braintrust Custom Scorers, and how does the eval-replay from prod-log → eval-experiment score round-trip work?
3. How does `braintrust eval` CLI + GitHub Actions runner gate a PR against main on eval-experiment-score delta, and is the eval-experiment score diff public-API-exposed?
4. Where is the per-tenant encryption key + per-Brainstore-region US/EU pinning surfaced in the Braintrust API, and is it SOC 2 + ISO 27001 + HIPAA + EU AI Act Art. 10 attested in the public trust center?
5. What is the eval-driven-Function-deploy gate in Braintrust Functions, and is the Function runtime under the same SOC 2 + ISO 27001 + HIPAA-ready control plane as Brainstore + Evals + Logs?

*Offers (three, pick one):*
- **$500/48h fixed-scope Braintrust evidence-gap map.** Two-day delivery; per-question evidence packets + per-question first-party citations + cross-tenant-no-bleed invariant proofs. Ideal for one enterprise procurement questionnaire.
- **$497/month quarterly-refresh retainer.** Same evidence map, refreshed every 90 days as Braintrust ships new features (Functions + Proxy + Scorers all shipped after the 2024-07 EDD launch). Stops at any quarter; no annual commit.
- **$2,000 five-vendor ai_agent_test_evaluation_suite cohort benchmark.** After this cohort closes (CLOSER #5/5), we ship a head-to-head Braintrust + SIBLING #2 + SIBLING #3 + SIBLING #4 + CLOSER #5 five-vendor benchmark with the same 5-WEDGE non-overlap rubric across all five vendors.

## Commercial route (all NOT submitted)

- `mailto:support@braintrustdata.com` (contact-page footer verbatim first-party)
- `https://www.braintrustdata.com/contact` (sales form)
- `https://braintrustdata.com/contact-sales` (Enterprise sales form)
- `https://docs.braintrustdata.com` (developer docs link from homepage)

SMTP/form gated; $0 sent / $0 received. Tick 1174 + cohort NEW VERTICAL #53 OPENER #1/5.

---

Atlas @ Talon Forge — cron tick `2026-07-24-fast-exec-braintrust-ai-agent-test-evaluation-suite-opener-1-1174` — OPENER #1/5 ai_agent_test_evaluation_suite NEW VERTICAL #53 — 5-WEDGE non-overlap rubric per PITFALL #99 — $500/48h + $497/mo + $2,000 cohort benchmark + $2,485 MRR ceiling — SMTP/form gated, $0 sent / $0 received.
