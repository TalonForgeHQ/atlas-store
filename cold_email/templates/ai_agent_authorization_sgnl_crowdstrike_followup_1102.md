# 1102 — SGNL → CrowdStrike Day-1 Identity-Plane Follow-Up — Closed ai_agent_authorization Cohort

**Purpose:** Stage a transaction/integration follow-up for the closed `ai_agent_authorization` cohort (Permit.io 989 → Cerbos 990 → Oso 991 → SGNL 998 → AuthZed 999 → WorkOS 1000). This is a buyer-persona variant, not a duplicate lead.
**Frame:** SGNL → CrowdStrike acquisition (announced 2026-01-08) and continuous-identity-plane evidence portability across the Falcon → SGNL CAEP boundary.
**Status:** TEMPLATE ONLY — no email or form submission is claimed.

## Why this frame now

SGNL's first-party press release dated 2026-01-08 states SGNL is now part of CrowdStrike. The Day-1 question for an AI-agent authorization buyer is different from a normal decision replay: can Continuous Access Evaluation Profile (CAEP) signals, Zero Standing Privilege grants, identity-fabric node state, MCP-tool-call decisions, and audit-export evidence survive the CrowdStrike Falcon / Charlotte AI → SGNL identity-and-policy boundary on Day 1 — and can the EU AI Act Art. 14(4) human-oversight record stay readable across the boundary?

## Subject options

1. SGNL → CrowdStrike Day-1: can one MCP-tool-call authorization be replayed?
2. One identity-plane row across Falcon + SGNL + CAEP
3. A 48-hour post-acquisition authorization evidence map

## Body

Hi {{first_name}},

SGNL announced on 2026-01-08 that it joined CrowdStrike. That creates a practical Day-1 question for any team using SGNL for AI-agent authorization: can one production MCP-tool-call decision be replayed across the Falcon / Charlotte AI endpoint-security boundary and the SGNL identity-data-fabric boundary without losing CAEP signals, Zero Standing Privilege grants, MCP-tool-call decisions, human-oversight records, or audit-export portability?

I map one MCP-tool-call authorization into a portable receipt joining:

`legal_entity_id + product_owner_id + crowdstrike_tenant_id + sgnl_workspace_id + agent_identity_id + delegated_user_identity_id + device_posture_id + operational_context_id + identity_data_fabric_node_id + caep_signal_id + caep_signal_version + sgnl_policy_id + sgnl_policy_version + mcp_server_id + mcp_tool_name + tool_call_id + tool_call_decision + downstream_resource_action + standing_access_removal_event_id + jit_grant_id + jit_grant_expiry + revocation_event_id + eu_ai_act_art_14_4_human_oversight_id + cross_tenant_no_bleed_audit_trail + audit_export_id`

The fixed scope is **$500 / 48 hours**. I return a red/amber/green gap map across the six cohort vendors (Permit.io, Cerbos, Oso, SGNL, AuthZed, WorkOS), the five highest-priority missing joins for authorization-vendor sign-off, and a blank replay schema for one real MCP-tool-call. Ongoing authorization-vendor-change coverage can be covered by a **$497/month quarterly refresh**. The complete six-vendor `ai_agent_authorization` benchmark is **$2,000**.

## Five replay questions

1. Which SGNL policy version, identity-data-fabric node, and `caep_signal_version` were live at the moment of the MCP-tool-call decision, and which CrowdStrike Falcon / Charlotte AI endpoint produced the device-posture input?
2. Which `agent_identity_id`, `delegated_user_identity_id`, and `device_posture_id` capture the subject, the human delegator, and the endpoint posture feeding the call?
3. Which `mcp_server_id`, `mcp_tool_name`, `tool_call_id`, and `tool_call_decision` produced the customer-impact record, and which downstream resource action did CrowdStrike record on the endpoint side?
4. Which `standing_access_removal_event_id`, `jit_grant_id`, and `jit_grant_expiry` did Zero Standing Privilege actually fire, and which `eu_ai_act_art_14_4_human_oversight_id` recorded the human-oversight boundary?
5. Can the `cross_tenant_no_bleed_audit_trail`, `revocation_event_id`, and final `audit_export_id` be reproduced across the Falcon + SGNL boundary without dropping a column?

Worth sending the one-page blank sample for {{company}}?

Best,
Atlas
