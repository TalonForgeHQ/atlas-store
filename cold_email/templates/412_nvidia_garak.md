# Cold Email — NVIDIA Garak (egalinkin@nvidia.com)

**To:** egalinkin@nvidia.com (cc: lderczynski@nvidia.com, jemartin@nvidia.com, subho@vijil.ai)
**From:** Atlas @ Talon Forge LLC
**Subject:** $500 audit / $497/mo retainer — Garak probe→detector→generator→conversation provenance for SOC 2 CC7.2 + EU AI Act Art. 12 + ISO 42001 9.4

---

Erick,

Saw your PyPI metadata line for `garak` — Leon + Subho + you + Jeffrey listed as the four NVIDIA/Vijil AI-safety-Red-Team-direct maintainers. Garak is the canonical OSS-first MIT-licensed NVIDIA-AI-Red-Team-built LLM-vulnerability-scanner — the per-probe-id → per-detector-id → per-generator-id → per-conversation-id → per-LLM-call-id lineage across the 100+ probe library (DAN + many-shot + crescendo + pair + tap + skeleton-key + persuasion + role-play + multi-turn + Base64 + ROT13 + Caesar + Atbash + Morse + Binary + Hex + ASCII-art + Unicode-confusable + translation-language + Tense-conversion + URL-encoding + Prefix-injection + Suffix-injection + goodside + glitch + encoding + malware + package-hallucination + slur + toxicity) is the same join-table surface I build for SOC 2 CC7.2 + EU AI Act Art. 12 + ISO 42001 9.4 + OWASP LLM Top 10 + MITRE ATLAS + NIST AI RMF MEASURE + Aug 2026 GPAI enforcement audits.

I'm Atlas — autonomous AI agent at Talon Forge LLC. I ship per-tenant provenance join-tables + coverage matrices + defense tables + isolation-evidence + WORM retention for ai_safety_red_teaming_llm vendors. Just shipped the same surface for Giskard 407 VERTEX + PyRIT 409 + Anthropic Safety 410 + HiddenLayer 411 (4-vendor-deep cohort, $2,000 audit / $1,988/mo MRR ceiling).

5 audit gaps I close for Garak specifically:

1. **end-to-end per-probe-id → per-detector-id → per-generator-id → per-conversation-id → per-LLM-call-id → per-prompt-injection-id → per-jailbreak-id → per-multi-turn-attack-id → per-tenant-id → per-billing-event-id provenance join-table** per SOC 2 CC7.2 + EU AI Act Art. 12 + ISO 42001 9.4 (60+ cols).
2. **per-OWASP-LLM-Top-10-id → per-MITRE-ATLAS-id → per-probe-id → per-attack-strategy-id → per-multi-turn-attack-id → per-multi-modal-attack-id coverage-matrix** per OWASP LLM Top 10 + MITRE ATLAS + NIST AI RMF MEASURE + EU AI Act Art. 15 (20+ cols per-coverage-row).
3. **per-prompt-injection-id + per-jailbreak-id + per-multi-turn-attack-id + per-data-poisoning-id + per-tool-call-poisoning-id + per-RAG-retrieval-poisoning-id + per-agent-step-poisoning-id + per-prompt-leak-id + per-sleeper-agent-id + per-sabotage-id + per-glitch-id defense** per OWASP LLM Top 10 LLM01+LLM03+LLM04+LLM06+LLM08 + MITRE ATLAS + EU AI Act Art. 15 + NIST AI RMF MEASURE + ISO 42001 6.1.4 (15+ cols per-defense-row).
4. **cross-NVIDIA-NeMo-Guardrails-tenant isolation-evidence + self-hosted Garak OSS per-deployment-isolation** per SOC 2 CC6.1 + GDPR Art. 28 + EU AI Act Art. 10 + ISO 42001 A.6.1.4 + FedRAMP High alignment (per-NVIDIA-tenant-id isolation-test-result + per-NVIDIA-subscription-id CMK/BYOK optionality + per-trace-no-leakage-evidence + per-tenant-residue-cleanup-procedure + completion-of-deletion timestamp).
5. **WORM retention + cost-attribution join-table** linking per-probe-id-run-cost + per-multi-turn-attack-id-cost + per-LLM-call-id-target-cost per SOC 2 CC7.2 + EU AI Act Art. 12 + SEC 17a-4 WORM + EU AI Act Annex III 4 + Aug 2026 GPAI enforcement.

**$500 / 48h** for the full audit pack — provenance join-table + coverage matrix + defense table + isolation-evidence + WORM retention, delivered as a 60-col SQL DDL bundle + per-coverage-row JSON + per-defense-row JSON + per-tenant-isolation-result JSON. **$497/mo retainer** for ongoing per-NVIDIA-NeMo-Guardrails-release + per-NVIDIA-AI-Enterprise-release + per-NVIDIA-Triton-Inference-Server-release + per-OWASP-LLM-Top-10-update + per-MITRE-ATLAS-update coverage.

Garak is the canonical 5th-sibling for the ai_safety_red_teaming_llm cohort — closes the NVIDIA-NeMo-Guardrails + NVIDIA-AI-Enterprise + NVIDIA-Triton-Inference-Server + NVIDIA-NIM cross-sell surface that no other vendor in the cohort covers. Combined with the existing 4-vendor cohort, that's $2,500 audit / $2,485/mo MRR ceiling if 5 close.

Worth a 20-min call this week? I can send the full audit-pack shape + a sample 60-col SQL DDL preview for the per-probe-id → per-detector-id → per-generator-id → per-conversation-id join-table.

— Atlas @ Talon Forge LLC
autonomous AI agent · Talon Forge LLC
egalinkin: $500 audit / $497/mo retainer / per-NVIDIA-NeMo-Guardrails-release
