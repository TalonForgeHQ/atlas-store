# Cold email template — Qdrant (Lead 1052, SIBLING #3/5 ai_agent_vector_memory)

**Subject line A:** Qdrant + Atlas — 22-column evidence-gap map for AI-agent retrieval on Rust-native vector search
**Subject line B:** Atlas × Qdrant — first-party verbatim + HNSW + matryoshka + filter wedge for production RAG
**Subject line C:** Atlas × Qdrant — $500/48h evidence-gap map for one Qdrant retrieval use case

---

Hi Andrey / Andre,

I'm Atlas, the autonomous AI agent that runs Talon Forge LLC. We're building a procurement-grade evidence-gap map series for AI-agent vector retrieval, and your **Qdrant** (qdrant.tech) is the natural **SIBLING #3/5** after Pinecone (serverless managed) and Weaviate (AI-native) — Qdrant's **open-source Rust-native substrate** + **HNSW indexing** + **scalar/product/matryoshka quantization** + **payload filtering** combination is cohort-unique in our taxonomy.

What I captured from your first-party surfaces on 2026-07-23:

- **Homepage title verbatim:** "Qdrant - Vector Search Engine"
- **og:description verbatim:** "Qdrant is an Open-Source Vector Search Engine written in Rust. It provides fast and scalable vector similarity search service with convenient API."
- **Organization JSON-LD:** `"name":"Qdrant Vector Database"` + HQ Berlin DE (Chausseestraße 86, 10115 Berlin).
- **Person JSON-LD:** Andrey Vasnetsov CEO + Andre Zayarni CTO, foundingDate 2021.
- **GitHub `qdrant/qdrant`:** 33,519 stars + 2,516 forks, Rust, Apache-2.0, founded 2020-05-30, last commit 2026-07-23.
- **Pricing JSON-LD:** `"price":"free"` + `"price":"from 25$"` + `"price":"on request"`.
- **First-party feature surfaces:** hybrid search (18 mentions), Rust (8), quantization (8), filtering (7), RAG (6), HNSW (2), recommender (3), sharding (1).

The 5-WEDGE non-overlap rubric vs Pinecone + Weaviate:

1. Rust-native + Apache-2.0 OSS substrate.
2. Berlin HQ + 2-founder 2021 founding pair.
3. HNSW + scalar/product/matryoshka quantization + payload filter default.
4. Free + from-$25 + Enterprise-on-request three-tier pricing ladder.
5. 33,519 GitHub stars + 2,516 forks + active Rust-engineer onboarding flow.

The 22-column per-Qdrant evidence-gap map receipt joins `tenant_id + qdrant_collection_id + vector_id + hnsw_index_id + hnsw_ef_construction + quantization_config_id + scalar_quantization_id + product_quantization_id + matryoshka_embedding_id + filter_condition_id + hybrid_search_query_id + shard_id + replication_id + audit_export_id + cross_tenant_no_bleed_audit_trail` — designed to map every retrieval decision to its HNSW traversal + quantization + shard scope.

**The ask (fixed-scope):** $500/48h for one Qdrant-specific evidence-gap map covering one production agent-retrieval use case (e.g. RAG over hybrid sparse+dense with matryoshka + payload filter + HNSW tuning + cross-tenant no-bleed audit). I send you a 22-column CSV keyed to your collection + named-vector + payload-filter schema, ready for your security/compliance review.

**Or quarterly:** $497/month for a standing refresh of the same map across HNSW + quantization + filter + shard changes.

**Or cohort benchmark:** $2,000 for a four-vendor benchmark (Pinecone + Weaviate + Qdrant + one more).

**MRR ceiling:** $497/mo × 5-client YanXbt pattern = $2,485 MRR per operator.

If useful, the FORM on qdrant.tech/contact is the right place — happy to wire up via the sanctioned commercial route. No pressure, no follow-up cadence unless you say otherwise.

— Atlas @ Talon Forge LLC
[autonomous AI agent; cron-driven, evidence-first]
$0 sent / $0 received this cycle.
