# 1156 — Comet ML + Opik — ai_mlops_feature_store SIBLING #3/5

**First-party recipient route:** `mailto:support@comet.com` (not sent)  
**Company:** Comet ML, Inc. — comet.com  
**Leadership:** Gideon Mendels (CEO & Co-Founder), verified first-party at `comet.com/site/about-us/`  
**Cohort:** `ai_mlops_feature_store`; W&B 1154 OPENER → ClearML 1155 SIBLING #2 → Comet 1156 SIBLING #3

## Subject options

1. **Comet + Opik — one audit replay across models and agents**
2. **A 22-column evidence map for Comet ML and Opik**
3. **Can an Opik trace resolve back to the exact Comet model artifact?**

## Message

Hi Gideon,

Comet now spans two evidence planes: **ML Experiment Management / Model Registry** for model development and **Opik** for LLM and agent evaluation, tracing, prompt management, and production monitoring. The diligence gap is usually the join between them: can a production agent trace resolve back to the exact dataset, experiment, artifact, model version, prompt, evaluator, and deployment that produced the behavior?

Atlas can deliver a fixed-scope **$500 / 48-hour** evidence-gap map for that cross-plane lineage, or maintain it for **$497/month** with quarterly refreshes. The proposed receipt is grounded in Comet's first-party About, product, pricing, security, and contact surfaces.

## Five non-overlap wedges

1. **Dual-plane lifecycle:** classic ML experiment/model lineage plus Opik LLM-and-agent tracing in one vendor surface.
2. **Open-source Opik:** self-hostable evaluation and observability creates a distinct deployment and evidence boundary.
3. **Evaluator lineage:** dataset item, experiment, scorer/evaluator version, score, and human annotation can be replayed together.
4. **Prompt-to-model join:** prompt and trace versions can be tied back to the registered model/artifact lineage rather than living in a separate observability silo.
5. **AI spend tracking:** first-party navigation exposes Claude Code spend tracking alongside model and agent quality evidence.

## Proposed audit receipt

`tenant_id + comet_workspace_id + comet_project_id + experiment_id + run_id + dataset_id + dataset_version_id + artifact_id + model_registry_id + model_version_id + deployment_id + opik_project_id + trace_id + span_id + thread_id + prompt_id + prompt_version_id + evaluator_id + evaluator_version_id + evaluation_score + human_annotation_id + cross_tenant_no_bleed_invariant`

First-party verification used `https://www.comet.com/site/about-us/`, `/products/ml-experiment-tracking/`, `/products/artifacts-dataset-management/`, `/products/opik/`, `/pricing/`, `/security/`, and `/about-us/contact-us/`. The contact page publishes `support@comet.com`; this message was not sent.

Best,  
Atlas @ Talon Forge

**Status:** template-only; no email or form submitted. `$0 sent / $0 received`.
