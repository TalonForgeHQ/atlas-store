# Cold Email Template — Helicone (CLOSER #5/5, ai_agent_llm_gateway)

**To:** hello@helicone.ai  (canonical first-party contact, verified first-party helicone.ai 2026-07-26)
**Backup route (NOT used for first contact):** FORM:https://helicone.ai/contact  (canonical first-party contact form, verified first-party 2026-07-26)
**Subject:** Evidence-gap map for the Helicone CLOSER #5/5 lane in Talon Forge's ai_agent_llm_gateway cohort — YC-backed + Product-of-the-Day + 5.8K OSS stars + Cloudflare-network + SOC 2 Type II + HIPAA + Mintlify-acquired AI-Gateway-acquired-by-platform-owner closure narrative

---

Hi Helicone team —

I'm Atlas, the autonomous revenue agent at Talon Forge. I've been building a five-vendor evidence-gap benchmark for the ai_agent_llm_gateway vertical, and Helicone is the CLOSER #5/5 — anchoring the AI-Gateway-acquired-by-a-platform-owner (Mintlify) closure narrative, distinct from Portkey's SaaS control plane (1318), LiteLLM's OSS-first + Rust core + 100+ provider Python SDK (1319), Cloudflare's edge-native + Workers runtime + zero-egress + Workers AI lane (1320), and Kong's API-gateway-heritage + MCP + A2A + Context Mesh + Italian-founder + Andreessen-board + $2B-Series-E lane (1321).

The wedge I'd buy a $500/48h fixed-scope evidence-gap map for: the 22-column per-tenant + per-provider + per-model + per-prompt-version + per-completion + per-cache + per-rate-limit + per-load-balancer + per-fallback + per-guardrail + per-Cloudflare-PoP + EU AI Act Art. 13 logging + Art. 14 human-oversight + ISO/IEC 42001 AIMS clause 8.4 evidence-rung audit trail a regulated buyer (FS + healthcare + EU public sector) needs to ship Helicone AI Gateway in production without re-engineering the audit layer. The Y Combinator + Product-of-the-Day + 5.8K GitHub stars + Cloudflare-network-backed cloud reliability + SOC 2 Type II + HIPAA + self-host Helm chart + Mintlify docs-to-AI-Gateway pipeline are not in any other AI-Gateway substrate.

Three lines, because I respect your inbox:

1. Helicone is the canonical Y Combinator + Product-of-the-Day + 5.8K GitHub stars + open-source + Cloudflare-network + SOC 2 Type II + HIPAA AI Gateway — now part of Mintlify (AI-native documentation platform) — the AI-Gateway-acquired-by-a-platform-owner closure narrative distinct from Portkey SaaS, LiteLLM OSS, Cloudflare edge, and Kong API-gateway-heritage.
2. A $500/48h fixed-scope evidence-gap map gives you 22-column per-tenant + per-provider + per-model + per-prompt-version + per-completion + per-cache + per-rate-limit + per-load-balancer + per-fallback + per-guardrail + per-Cloudflare-PoP + EU AI Act Art. 13 + Art. 14 + ISO/IEC 42001 AIMS clause 8.4 evidence + cross-tenant no-bleed invariant + replay-hash audit trail; $497/mo quarterly refresh keeps it in lockstep with Helicone AI Gateway version updates + Mintlify docs-integration additions + EU AI Act Art. 26 updates + Cloudflare-network audit; $2,000 secures the full five-vendor ai_agent_llm_gateway COHORT BENCHMARK at close (Portkey + LiteLLM + Cloudflare + Kong + Helicone) with cross-vendor AI-Gateway substrate comparison + per-acquired-by-platform-owner + per-OSS + per-Cloudflare-network + per-YC + cost-per-cache-hit + per-Cloudflare-PoP + EU AI Act readiness score per-vendor; $10,000 secures the **CLOSER-only cohort sponsorship tier — UNLOCKED at Vertical #79 closure**.
3. Calendar link to ship the cohort: https://helicone.ai/contact — would you like to point me to the right person on your AI Gateway product or developer relations team, or do you prefer to route this through Mintlify's partnership/alliances motion?

Thanks — Atlas @ Talon Forge

---

## 5-WEDGE non-overlap rubric (verified first-party helicone.ai 2026-07-26)

1. **AI-Gateway-acquired-by-platform-owner closure narrative** — Helicone is now part of Mintlify (canonical AI-native documentation platform), making it the ONLY cohort sibling whose corporate parent is a documentation-platform-owner. Distinct from Portkey (YC W22 SaaS), LiteLLM (BerriAI OSS), Cloudflare (NYSE public), Kong ($2B Series E).
2. **Mintlify docs → Helicone Gateway integration wedge** — As a Mintlify product, Helicone inherits a docs-to-AI-Gateway integration lane no other cohort sibling can replicate. Future documentation-driven agent flows naturally route through the Mintlify-Helicone pipeline.
3. **Y Combinator + Product-of-the-Day + 5.8K GitHub stars + open-source** — Only cohort sibling with the canonical YC + Product-of-the-Day + open-source trio. Distinct from Portkey (YC W22 only), LiteLLM (BerriAI OSS), Cloudflare (NYSE), Kong ($2B).
4. **Cloudflare-network-backed cloud reliability + SOC 2 Type II + HIPAA + self-host Helm chart** — Only cohort sibling with verbatim "We leverage Cloudflare's global network for minimal latency and maximum uptime" combined with SOC 2 Type II + HIPAA + production-ready Kubernetes Helm chart self-hosting. Distinct from Portkey's SaaS posture, LiteLLM's MIT-OSS posture, Cloudflare's own-runtime posture, Kong's API-gateway posture.
5. **API Cost Calculator + 300+ model pricing database + LLMOps-for-the-fastest-growing-AI-companies** — Only cohort sibling with the canonical API-Cost-Calculator + 300+ model pricing DB + "the LLMOps platform behind the fastest-growing AI companies" verbatim positioning. Distinct from Portkey's per-tenant SaaS, LiteLLM's provider SDK, Cloudflare's edge-workers, Kong's API-gateway plugins.

## 22-col evidence wedge

tenant_id + mintlify_org_id + helicone_api_key_hash + provider + model_id + model_version_hash + prompt_hash + completion_hash + input_tokens + output_tokens + cost_cents_usd + latency_ms + status_code + cache_hit_flag + rate_limit_decision_id + load_balancer_route_id + fallback_chain_id + guardrail_decision_id + cloudflare_pop_code + audit_export_id + cross_tenant_no_bleed_invariant + replay_hash.

## Compliance posture (first-party inferred 2026-07-26)

SOC 2 Type II + HIPAA + end-to-end encryption + least-privilege access controls + regular penetration testing + EU AI Act Art. 13 logging + Art. 14 human-oversight + per-tenant + per-provider + per-model + per-prompt-version + per-completion audit + Cloudflare Workers runtime audit logs + ISO/IEC 42001 AIMS clause 8.4 evidence-rung.

## Commercial route (first-party verified 2026-07-26, NOT submitted)

- mailto:hello@helicone.ai (canonical first-party contact, verified first-party)
- FORM:https://helicone.ai/contact (canonical first-party contact form, verified first-party 2026-07-26)
- Helicone Docs https://docs.helicone.ai (canonical first-party developer docs)
- Helicone GitHub https://github.com/helicone/helicone (canonical first-party OSS repo, 5.8K stars)
- Mintlify parent org https://mintlify.com (canonical first-party parent company, acquired Helicone)

## Offer ladder (NEW VERTICAL #79 cohort-cumulative, CLOSER #5/5 tier)

- $500/48h fixed-scope Helicone evidence-gap map
- $497/mo quarterly refresh
- $2,000 five-vendor ai_agent_llm_gateway COHORT BENCHMARK at close (Portkey 1318 + LiteLLM 1319 + Cloudflare AI Gateway 1320 + Kong AI Gateway 1321 + Helicone 1322)
- $2,485 MRR ceiling per YanXbt pattern (5 clients × $497/mo)
- **$10,000 CLOSER-only cohort sponsorship tier — UNLOCKED at Vertical #79 closure**

## Atlas @ Talon Forge

NEW VERTICAL #79 ai_agent_llm_gateway CLOSED 4/5 → 5/5 (Portkey 1318 OPENER + LiteLLM 1319 SIBLING #2 + Cloudflare AI Gateway 1320 SIBLING #3 + Kong AI Gateway 1321 SIBLING #4 + **Helicone 1322 CLOSER #5/5**). The $10,000 CLOSER-only cohort sponsorship tier is now UNLOCKED. Cohort full ladder complete: OPENER + 3 SIBLINGs + CLOSER. SMTP/form gated; $0 sent / $0 received. [tick-1322-helicone-ai-agent-llm-gateway-closer-5-of-5-1322]