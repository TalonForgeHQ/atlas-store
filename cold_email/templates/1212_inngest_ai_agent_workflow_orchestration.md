# 1212_inngest_ai_agent_workflow_orchestration.md

**Vendor:** Inngest (inngest.com)
**Contact:** mailto:hello@inngest.com + mailto:support@inngest.com
**Vertical:** ai_agent_workflow_orchestration
**Cohort slot:** SIBLING #3/5 (NEW VERTICAL #60) — after Temporal 1210 OPENER #1/5 + Prefect 1211 SIBLING #2/5
**Founder:** Joel Aaron Smith, CEO
**Status:** queued_not_sent (SMTP/form gated; 0 sent / 0 received)

---

## 3 Subject Options (under 60 chars each)

1. `Inngest 1212: event-driven durable steps + AI Agent Kits`
2. `Inngest 1212: steps.invoke + steps.waitForEvent audit trail`
3. `Inngest 1212: Connect for cross-network Workflow — sibling 3/5`

## Body (use ONE subject, ship as one email)

Hi Inngest team,

Sending a 5-WEDGE evidence-gap map for the `ai_agent_workflow_orchestration` cohort — sibling #3/5 of 5 next to Temporal 1210 OPENER + Prefect 1211 SIBLING #2. Cohort-floor offer is $500/48h fixed-scope or $497/mo quarterly refresh.

**The 5-WEDGE rubric we ship per-vendor per-cohort-slot:**

1. **Per-app + per-function + per-event + per-step + per-replay + per-function-run audit trail** — Inngest's app_id + function_id + event_id + event_name + event_version + trigger_id (cron + webhook + event) + step_id + step_type + step_attempt + step_state_waiting_for_event + wait_for_event_id + debounce_key + throttle_key + concurrency_key + function_run_id + replay_id + sdk_language + retry_policy + cross-tenant-no-bleed + replay_hash.
2. **Event-driven serverless function-as-step durable execution with declarative TS/JS/Python/Go SDKs + steps.invoke + steps.sleep + steps.run + steps.waitForEvent + steps.waitForSignal** — Inngest's step-functions-ergonomics pattern (each step is a function-invocation that the engine durably executes); distinct from Temporal's class-based Workflow + Activity + Worker model and Prefect's @flow + @task decorator model.
3. **Inngest Cloud + Connect for cross-network function execution (no code deploy-on-Inngest — runs in customer VPC)** — Inngest Connect (2026 GA) is the cohort-unique cross-network function invocation primitive; Inngest Cloud can orchestrate functions running in customer VPCs / on-prem / cross-cloud without deploy-on-Inngest. vs Temporal's Worker-Process-on-Cloud (Temporal Worker must run on Temporal Cloud) and Prefect's Push-Work-Pool-in-customer-GCP (Prefect Cloud's push execution ships a worker to customer GCP).
4. **Batching + debounce + throttle + concurrency keys as first-class primitives** — Inngest ships batching (group multiple events into one function invocation) + debounce (delay until event arrival stops) + throttle (rate-limit per-key) + concurrency keys (per-key concurrency control) as first-class API surface. vs Temporal's Schedule + StartDelay + ContinueAsNew orchestration primitives and Prefect's Automations + Schedule + Concurrency Limit.
5. **Workflow Kits (inngest-ai + inngest-llm + inngest-agent-kit + inngest-rag + inngest-vector + inngest-chunking + inngest-embeddings) as cohort-unique AI-agent-frameworks-on-Inngest substrate** — Inngest ships first-party AI-agent Kits (inngest-ai + inngest-llm + inngest-agent-kit + inngest-rag + inngest-vector + inngest-chunking + inngest-embeddings) that frame the workflow engine as an AI-agent runtime out of the box. vs Temporal's AI-Activities (released May 2026) + Prefect's Pydantic-AI + Marvin-AI substrate.

**What's missing from your current public surface (per the rubric above):**

- Cross-tenant-no-bleed invariant document (per-app + per-Cloud-tenant + per-Connect-tenant credential scoping + per-function-run tenant boundary).
- Per-AI-agent-per-invocation audit-provenance (per-inngest-agent-kit-run + per-inngest-ai-run + per-inngest-llm-run + per-inngest-rag-run + per-model-name + per-model-version + per-prompt-hash + per-response-hash + per-decision-citation-anchor + per-decision-confidence + per-human-override-acknowledgment + 14-day change-notification SLA on AI sub-processor swaps).
- EU AI Act Aug 2 2026 readiness per-Article (Art. 6 + Art. 9 + Art. 10 + Art. 13 + Art. 14 + Art. 15 + Art. 26 + Art. 27 + Art. 50 + Annex III high-risk list) + cross-border data-residency posture for EU + UK + CH + BR + AU + SG + JP + CA + IN + ZA.
- ISO/IEC 42001:2023 AIMS clause-by-clause evidence map (Inngest Cloud AI agent surface — clause 6.1.2 + 7.2 + 8.4 + 9.3 + 10.1) + per-clause citation pinning.
- Per-day-of-2026 incident-manifest export lane (per-app + per-function + per-event + per-step + per-tenant + per-Connect-tenant + per-day reproducible machine-readable artifact — same artifact reproducible across Cloud + Dev Server + per-function-runtime + per-step-runtime + per-ai-agent-runtime).

**Cohort cohort-floor offers:**

- **$500/48h fixed-scope evidence-gap map** (1 deliverable: 1 PDF + 1 machine-readable artifact per-vendor above)
- **$497/mo quarterly refresh** (quarterly artifact + per-Article coverage)
- **$2,000 cohort benchmark** (5-vendor package: Temporal 1210 + Prefect 1211 + Inngest 1212 + Sibling #4 + Closer #5)

Reply with which surface you'd like scoped first.

— Atlas
mailto:hello@inngest.com
Cohort: ai_agent_workflow_orchestration (NEW VERTICAL #60, 2/5 → 3/5)
