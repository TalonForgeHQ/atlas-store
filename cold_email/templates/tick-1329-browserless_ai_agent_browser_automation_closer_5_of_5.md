---
template_id: tick-1329-browserless-ai-agent-browser-automation-closer-5-of-5
vendor: Browserless
cohort: ai_agent_browser_automation
cohort_role: closer-5-of-5
target: mailto:hello@browserless.io (canonical first-party from browserless.io/contact 2026-07-26)
subject_line: Browserless + 5-vendor ai_agent_browser_automation cohort CLOSER #5/5 (BrowserQL GraphQL + Docker self-host + MCP + Authenticated Profiles + 2017 Chrome-as-a-Service veteran)
preheader: First-party verified browserless.io + github.com/browserless/browserless 2026-07-26 — canonical legacy Chrome-as-a-Service veteran CLOSER for the cohort ladder after Browserbase OPENER + Steel + Anchor Browser + Hyperbrowser siblings. UNLOCKS the $10,000 CLOSER-only sponsorship tier.
---

Hi Joel + Browserless team,

First-party verified 2026-07-26 (browserless.io + github.com/browserless/browserless): Browserless is the canonical LEGACY CHROME-AS-A-SERVICE VETERAN + BROWSERQL GRAPHQL QUERY LANGUAGE + DOCKER-DEPLOYABLE SELF-HOSTED + MCP-SERVER + AUTHENTICATED PROFILES + BROWSER AGENT substrate. Verbatim title "The Browser Your AI Agents Run On | Browserless" + og:description "Give your AI agents a real cloud browser that won't crash or get blocked. Connect over MCP, Puppeteer, or Playwright with stealth and Authenticated Profiles built in." + first-party product surfaces BrowserQL (browser automation GraphQL query language) + Browsers as a Service (managed headless browsers at scale) + REST APIs (browser tasks over simple HTTP) + MCP Server (browser automation over MCP) + Self-Hosted (run on your own infrastructure) + Browser Agent + Authenticated Profiles + stealth + Puppeteer + Playwright support + free for non-commercial uses + Trust Center at /trust + Status at status.browserless.io. github.com/browserless/browserless 2026-07-26: "Deploy headless browsers in Docker. Run on our cloud or bring your own. Free for non-commercial uses." Founder lineage verified browserless.io/blog author profile 2026-07-26: Joel Griffith CEO. Browserless is the canonical CLOSER #5/5 of the ai_agent_browser_automation cohort I'm building — the cohort-closure narrative.

Why Browserless is the cohort CLOSER #5/5 (not a later sibling): the ai_agent_browser_automation vertical needs the LEGACY CHROME-AS-A-SERVICE VETERAN lane as the cohort-closure wedge after Browserbase's COMMERCIAL-AI-BROWSER-INFRASTRUCTURE OPENER + Steel's OPEN-SOURCE-FIRST-DEVELOPER-FIRST SIBLING #2 + Anchor Browser's YC-W24-CLOUD-BROWSER-API SIBLING #3 + Hyperbrowser's YC-S21-WEB-INFRA-FOR-AI-AGENTS SIBLING #4. Browserless's substrate is the canonical 2017 veteran + BrowserQL GraphQL query language + Docker-deployable self-hostable Docker image + MCP server + Authenticated Profiles + Browser Agent + Trust Center + Status Page + free-for-non-commercial-uses surface — a non-replicable veteran-cohort-closure substrate that no newer AI-native entrant can replicate from a 2017 starting point.

I'm running a five-vendor benchmark report (Browserbase 1323 OPENER + Steel 1324 SIBLING #2 + Anchor Browser 1327 SIBLING #3 + Hyperbrowser 1328 SIBLING #4 + Browserless 1329 CLOSER #5) that would publish a per-vendor evidence-gap map covering per-BrowserQL-query + per-browser-session + per-Docker-container + per-self-hosted-deployment + per-MCP-server + per-MCP-tool-call + per-Browser-Agent-run + per-Authenticated-Profile + cross-tenant no-bleed + EU AI Act Art. 13 logging + Art. 14 human-oversight.

The 22-col evidence wedge for Browserless joins tenant_id + browserless_workspace_id + browserql_query_id + browserql_query_hash + browser_session_id + docker_container_id + self_hosted_deployment_id + mcp_server_id + mcp_tool_call_id + browser_agent_run_id + authenticated_profile_id + profile_session_id + stealth_fingerprint_id + captcha_solve_id + proxy_session_id + pdf_generation_id + screenshot_id + playwright_run_id + puppeteer_run_id + api_request_id + audit_export_id + cross_tenant_no_bleed_invariant + replay_hash.

Offer ladder:
- $500 / 48 hours — fixed-scope Browserless per-BrowserQL-query + per-browser-session + per-Docker-container + per-self-hosted-deployment + per-MCP-server + per-MCP-tool-call + per-Browser-Agent-run + per-Authenticated-Profile + per-captcha-solver + per-stealth-fingerprint + per-Puppeteer + per-Playwright + per-audit-trail evidence-gap map
- $497 / month — quarterly refresh: Browserless version updates + new BrowserQL coverage + new MCP server additions + new Authenticated Profiles coverage + EU AI Act Art. 26 updates
- $2,000 — five-vendor ai_agent_browser_automation COHORT BENCHMARK at close (Browserbase 1323 OPENER + Steel 1324 SIBLING #2 + Anchor Browser 1327 SIBLING #3 + Hyperbrowser 1328 SIBLING #4 + Browserless 1329 CLOSER #5)
- $2,485 MRR ceiling per YanXbt pattern (5 clients x $497/mo)
- $10,000 CLOSER-only cohort sponsorship tier — UNLOCKED at Vertical #80 closure

I am running the CLOSER #5/5 position with $0 sent / $0 received, mailto/form gated. If the cohort-benchmark shape is interesting for Browserless as the canonical LEGACY CHROME-AS-A-SERVICE VETERAN CLOSER (vs Browserbase's commercial OPENER + Steel's open-source SIBLING + Anchor Browser's YC W24 SIBLING + Hyperbrowser's YC S21 SIBLING), the right next step would be a 20-minute walk-through of the proposed benchmark rubric before any commercial ask — I'll wait for you to set that up if useful.

Best,
Atlas
TalonForge — Atlas Store
https://talonforgehq.github.io/atlas-store