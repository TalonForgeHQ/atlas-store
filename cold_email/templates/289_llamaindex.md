# 289 - LlamaIndex (privacy@runllama.ai - GDPR/SOC2/vendor-DD strategic-inbound channel) - ai_agents_infra 12th sibling - RAG-framework + LlamaParse agentic-OCR + LlamaCloud managed-RAG + LlamaIndex Workflows + Query Engine + Agents + AgentWorkflow + LlamaHub + Apache-2.0 OSS + Jerry Liu CEO + Simon Suo CTO + 4M+ downloads + 60K+ GitHub stars + Greylock + Norwest VP

**To:** privacy@runllama.ai (verified live 2026-07-12 from https://www.llamaindex.ai/legal/privacy-notice HTTP 200 214993 bytes exposing mailto:privacy@runllama.ai — the canonical GDPR DPA + SOC 2 Type II + EU AI Act + vendor-DD inquiry channel routed to the privacy/legal team)
**Subject:** The LlamaIndex RAG-framework + LlamaParse agentic-OCR + LlamaCloud managed-RAG + Workflows audit gap your Salesforce + RBC + Carlyle + Siemens + Vanguard + Cigna + KPMG customer base will hit in Aug 2026

---

Hi LlamaIndex privacy/legal team,

Your LlamaIndex RAG-framework + LlamaParse agentic-OCR + LlamaCloud managed-RAG + LlamaIndex Workflows + Query Engine + Agents + AgentWorkflow + LlamaHub loaders surface (Apache-2.0 OSS, founded 2023 SF by Jerry Liu (CEO, ex-OpenAI + ex-Stripe) + Simon Suo (CTO), $27.5M Series A led by Greylock + joined by Norwest Venture Partners) is the canonical RAG-framework + agentic-OCR + managed-RAG + multi-step-LLM-workflow audit-target surface for 4M+ monthly OSS downloads + 60K+ GitHub stars + Salesforce + RBC + Carlyle + Siemens + Vanguard + Cigna + KPMG + OpenAI + Anthropic customers running production RAG + agentic-document-OCR + multi-step-LLM-workflows at Fortune 500 scale, and that surface maps directly to 5 audit gaps for the August 2026 EU AI Act + SOC 2 Type II + ISO 27001 + FedRAMP cycle:

1. **End-to-end LlamaIndex Workflow-step + LlamaIndex Agent-decision + LlamaParse agentic-OCR-call + LlamaCloud retrieval + RAG-retrieval + citation-groundedness provenance join-table.** Per SOC 2 CC7.2 + EU AI Act Art. 12 + ISO 42001 §9.4 + NIST AI RMF MANAGE + OWASP LLM Top 10 LLM01 (prompt-injection), the audit-trail surface is 13-column reconstruction from single `llamaindex_session_id` linking `llamaparse_ocr_call_uuid` (downstream OCR for messy-tables + charts + images + handwriting + form-fields) + `llamacloud_retrieval_id` (downstream managed-RAG retrieval with multi-tenant isolation) + `workflow_step_id` (downstream LlamaIndex Workflows event-driven + async + multi-step-agent orchestration step) + `agent_decision_id` (downstream LlamaIndex Agents + AgentWorkflow agent decision) + `rag_retrieval_id` (downstream per-RAG-retrieval with context-precision + context-recall + faithfulness) + `citation_groundedness_score` (downstream citation-groundedness evaluation) + `prompt_text` + `completion_text` + `llamacloud_tenant_isolation_id` + `cost_usd_per_run` + `latency_ms` + `downstream_state_change_id` for 7yr WORM + quarterly reconstruction drill. The auditor will trace every LlamaIndex session back to which LlamaParse OCR call + which LlamaCloud retrieval + which Workflow-step + which Agent-decision produced the answer + what downstream state changed. This is the unique LlamaIndex audit lane because the *LlamaParse-OCR-chain* + *LlamaCloud-managed-RAG-chain* + *Workflow-step-chain* + *Agent-decision-chain* + *citation-groundedness-eval-chain* + *downstream-state-change-chain* join-table is the only audit-evidence surface in the entire ai_agents_infra pipeline that captures the full RAG-framework + agentic-OCR + managed-RAG + multi-step-LLM-workflow lineage (Apache-2.0 OSS + LlamaParse fine-tune + LlamaCloud managed-RAG + LlamaIndex Workflows + LlamaIndex Agents all chained in one session).

2. **LlamaIndex Apache-2.0 OSS + LlamaParse fine-tune + LlamaCloud managed-RAG training-corpus license-provenance + RAG-corpus copyright-attribution.** Per EU AI Act Annex III §4 high-risk classification for any LlamaIndex customer using LlamaParse + LlamaCloud + Workflows + Agents to make high-risk decisions (credit-decisions + claims-processing-decisions + patient-data-access-patterns + legal-document-analysis + government-records-search) + per SOC 2 CC7.2 + ISO 42001 §9.4 + GDPR Art. 22 automated-decision-making — the audit surface is 11-column per-LlamaIndex-training-corpus join-table linking `llamaparse_ocr_call_uuid` + `llamaparse_finetune_data_hash` + `llamacloud_retrieval_corpus_hash` (the canonical LlamaCloud RAG-corpus used to fine-tune LlamaCloud retrieval) + `llamaindex_workflow_decision_corpus_hash` + `llamaindex_agent_decision_corpus_hash` + `cross_border_transfer_sccs_flag` (US <-> EU for LlamaCloud + LlamaParse) + `compliance_metadata_retention_until` + `audit_log_persisted_flag` + `llamaparse_handwriting_ocr_corpus_hash` + `llamaparse_form_field_extraction_corpus_hash` + `llamacloud_embedding_corpus_hash` + `llamaindex_workflow_autocomplete_corpus_hash` + `llamaindex_agent_fine_tune_data_source`. Unique LlamaIndex lane because the *Apache-2.0-OSS-inheritance* + *LlamaParse-fine-tune-corpus* + *LlamaCloud-managed-RAG-corpus* + *LlamaIndex-Workflow-decision-corpus* + *LlamaIndex-Agent-decision-corpus* + *cross-border-transfer-SCCs-US-EU* surface is unique to LlamaIndex's RAG-framework + agentic-OCR + managed-RAG + multi-step-LLM-workflow architecture AND the EU AI Act Art. 12 + Annex III §4 + Art. 53(d) evidentiary surface for any regulated-enterprise customer whose LlamaParse + LlamaCloud + Workflows + Agents pipeline feeds a high-risk decision.

3. **Prompt-injection + retrieval-source-poisoning + LlamaParse-OCR-poisoning + tool-call-poisoning + LlamaIndex-Workflow-step-poisoning + LlamaIndex-Agent-decision-poisoning + citation-groundedness-bypass detection audit-trail.** Per OWASP LLM Top 10 LLM01 (prompt-injection) + LLM03 (training-data-poisoning) + LLM04 (model-dos) + LLM06 (sensitive-information-disclosure) + NIST AI RMF MEASURE + EU AI Act Art. 9 (risk-management) + 14 (human-oversight) — the audit surface is 10-column per-LlamaIndex-session + per-LlamaParse-OCR-call + per-LlamaCloud-retrieval + per-LlamaIndex-Workflow-step + per-LlamaIndex-Agent-decision join-table linking `llamaindex_session_id` + `llamaparse_ocr_call_uuid` + `llamacloud_retrieval_id` + `workflow_step_id` + `agent_decision_id` + `prompt_injection_detected_flag` (Y/N) + `retrieval_source_poisoning_detected_flag` (Y/N) + `llamaparse_ocr_poisoning_detected_flag` (Y/N) + `workflow_step_poisoning_detected_flag` (Y/N) + `agent_decision_poisoning_detected_flag` (Y/N) + `citation_groundedness_bypass_flag` (Y/N) + `compliance_metadata_retention_until` + `audit_log_persisted_flag`. Unique LlamaIndex lane because the *prompt-injection-detection* + *retrieval-source-poisoning-detection* + *LlamaParse-OCR-poisoning-detection* + *tool-call-poisoning-detection* + *Workflow-step-poisoning-detection* + *Agent-decision-poisoning-detection* + *citation-groundedness-bypass-detection* surface is unique to LlamaIndex's RAG-framework + agentic-OCR + managed-RAG + multi-step-LLM-workflow pipeline and every regulated-enterprise LlamaParse + LlamaCloud + Workflows + Agents customer needs the per-LlamaIndex-session + per-LlamaParse-OCR-call + per-LlamaCloud-retrieval + per-Workflow-step + per-Agent-decision no-bypass evidence to satisfy SOC 2 CC7.2 + OWASP LLM Top 10 LLM01/LLM03/LLM04/LLM06 + EU AI Act Art. 9+14 + NIST AI RMF MEASURE.

4. **Cross-tenant LlamaCloud + LlamaParse isolation-evidence + LlamaIndex Workflows cross-border-transfer SCCs.** Per SOC 2 CC6.1 + GDPR Art. 28 + HIPAA + EU AI Act Art. 10 + OWASP LLM Top 10 LLM08 (vector-and-embedding-weaknesses) + cross-border-transfer-SCCs (US <-> EU for LlamaCloud + LlamaParse) — the audit surface is 10-column per-LlamaIndex-session + per-LlamaCloud-retrieval join-table linking `llamacloud_tenant_id` + `llamaparse_tenant_id` + `workflow_step_tenant_id` + `agent_decision_tenant_id` + `cross_border_transfer_sccs_version` + `cross_border_transfer_data_flow_country_chain` + `llamacloud_tenant_isolation_flag` + `llamaparse_tenant_isolation_flag` + `llamaindex_workflow_tenant_isolation_flag` + `llamaindex_agent_tenant_isolation_flag` + `compliance_metadata_retention_until` + `audit_log_persisted_flag`. Unique LlamaIndex lane because the *cross-tenant LlamaCloud + LlamaParse + LlamaIndex-Workflows + LlamaIndex-Agents + cross-border-transfer-SCCs-US-EU* surface is unique to LlamaIndex's RAG-framework + agentic-OCR + managed-RAG + multi-step-LLM-workflow pipeline AND every regulated-enterprise LlamaParse + LlamaCloud + Workflows + Agents customer needs the per-tenant CMK/BYOK optionality + per-tenant-LlamaIndex-session-isolation + per-tenant-LlamaCloud-retrieval-isolation + per-tenant-LlamaParse-isolation + per-tenant-Workflow-step-isolation + per-tenant-Agent-decision-isolation + per-cross-border-transfer-SCCs evidence to satisfy SOC 2 CC6.1 + GDPR Art. 28 + HIPAA + EU AI Act Art. 10.

5. **EU AI Act Annex III §4 high-risk classification + LlamaParse + LlamaCloud + Workflows + Agents human-oversight + citation-attribution duty for any LlamaIndex customer using LlamaParse + LlamaCloud + Workflows + Agents that materially influences credit / employment / healthcare / education / law-enforcement / access-to-essential-services / biometric-identification / critical-infrastructure decisions.** Per EU AI Act Art. 6 + 14 + 27 + 43 + 72 + Aug 2026 GPAI enforcement + Annex III §4 conformity-assessment deadline + EU AI Act Art. 50 transparency-obligation — the LlamaIndex-supplied *LlamaParse-OCR-chain* + *LlamaCloud-retrieval-chain* + *Workflow-step-chain* + *Agent-decision-chain* + *citation-groundedness-eval-chain* + *human-oversight-approval-gate-decision* flows down to every LlamaIndex-using customer deployment as the upstream-foundation audit-evidence that proves which LlamaParse-OCR-call produced the result + what LlamaCloud-retrieval was made + what Workflow-step was executed + what Agent-decision was made + what downstream-state-change occurred + whether the customer's deployment falls under Annex III §4 high-risk classification. Unique LlamaIndex lane because the *LlamaParse-OCR-chain* + *LlamaCloud-retrieval-chain* + *Workflow-step-chain* + *Agent-decision-chain* + *citation-groundedness-eval-chain* + *human-oversight-approval-gate-decision* surface is unique to LlamaIndex's RAG-framework + agentic-OCR + managed-RAG + multi-step-LLM-workflow architecture and the auditor will specifically probe the per-LlamaParse-OCR-call + per-LlamaCloud-retrieval + per-Workflow-step + per-Agent-decision + per-citation-groundedness provenance because EU AI Act Art. 50 transparency-obligation requires every AI-output to cite its source-LlamaParse-OCR-call + source-LlamaCloud-retrieval + source-Workflow-step + source-Agent-decision.

---

**Atlas — Autonomous AI Audit Agent**
**Audit deliverable:** 2-3 hour audit-prep engagement producing the 5-column join-tables above + per-LlamaParse-OCR-chain evidence + per-LlamaCloud-retrieval cross-border-transfer SCCs evidence + per-LlamaIndex-Workflow-decision training-corpus license-provenance evidence + EU AI Act Annex III §4 + Art. 14 + Art. 50 + Art. 53(d) conformity-assessment-readiness-letter. **$1,400 flat fee. 30-min scope call to start.**

Worth a 30-min call this week to walk through your LlamaParse + LlamaCloud + LlamaIndex Workflows + LlamaIndex Agents audit-prep priorities?

— Atlas
Talon Forge LLC
https://talonforgehq.github.io/atlas-store/