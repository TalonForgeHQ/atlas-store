# Template 1335 — Monte Carlo Data (OPENER #1/5 ai_agent_data_quality)

**Subject:** Monte Carlo field-level lineage + ai_act_art_13 logging for {customer}

**Preheader:** Cohort OPENER — 5-vendor ai_agent_data_quality benchmark closing at $2,000 with $10,000 sponsorship tier unlocked

---

Hi {first_name},

I'm shipping a 5-vendor cohort comparison for **ai_agent_data_quality** — Monte Carlo + Soda + Bigeye + Metaplane + Anomalo — and wanted to flag three things on the **Monte Carlo** row before I publish it on {date}.

**(1) The 22-col evidence wedge your SOC 2 + GDPR + EU AI Act Art. 13 audit-export must surface:**

`tenant_id + montecarlo_workspace_id + monitor_id + monitor_alert_id + field_lineage_edge_id + lineage_graph_id + dbt_test_run_id + freshness_check_id + volume_check_id + schema_change_event_id + ai_root_cause_run_id + incident_postmortem_id + audit_export_id + cross_tenant_no_bleed_invariant + replay_hash` …

**(2) The 5-WEDGE non-overlap that makes Monte Carlo the OPENER #1/5:** (a) field-level lineage since 2019; (b) ML anomaly detection on freshness/volume/schema; (c) $135M Series D 2022 ICONIQ-led; (d) cross-vendor Snowflake/Databricks/BigQuery/Redshift + dbt + Looker/Tableau coverage; (e) Barr Moses "godmother of data observability" Forbes + Datanami press lineage.

**(3) Three things I can't fully verify without your help:** (i) the canonical sales/security/partnerships mailto routes per PITFALL #28 (I have `hello@montecarlo.ai` first-party verified 2026-07-26); (ii) the canonical SOC 2 Type II badge on your trust page (I see SOC 2 referenced on Series-D enterprise posture but not as a verbatim first-party badge); (iii) whether your field-lineage is exposed via API for agent-side retrieval (the agent-RAG story breaks if it's UI-only).

I have a **$500/48h fixed-scope evidence-gap map** for Monte Carlo + a **$497/mo quarterly refresh** + a **$2,000 five-vendor ai_agent_data_quality COHORT BENCHMARK** at close, plus a **$10,000 CLOSER-only cohort sponsorship tier** that unlocks at vertical #81 closure.

Three asks: (a) 30-min call to confirm the canonical first-party commercial routes, (b) a pointer to the canonical SOC 2 Type II badge page, (c) whether the field-lineage substrate is exposed via API.

— Atlas @ Talon Forge