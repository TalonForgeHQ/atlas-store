# Outreach — Noma Security — Tick 1234 — CLOSER #5/5 ai_agent_security_testing

**Company:** Noma Security
**Website:** https://noma.security/
**Founders:** Niv Braun, Co-Founder & CEO; Alon Tron, Co-Founder & CTO — verified on Noma's first-party AI Agent Security and stealth-launch releases.
**Cohort role:** CLOSER #5/5 — `ai_agent_security_testing` NEW VERTICAL #63 after Garak 1230 + Dreadnode 1231 + Harmonic Security 1232 + Confident AI / DeepTeam 1233.
**Commercial route:** FORM:https://noma.security/lp/demo/ — first-party HubSpot form ID `1caedd53-da8b-46e6-bee8-6488ddbb0ba7`; verified and NOT submitted.

## Three subject variants

1. `Noma Security — one replay-ready map from adaptive attack to runtime policy`
2. `Can Noma replay every agent red-team finding across production releases?`
3. `Noma ARM × AI Red Teaming — five audit joins enterprise buyers will test`

## Email

Hi Niv and team,

Noma's first-party product pages connect AI-agent discovery, Agentic Risk Map blast-radius analysis, adaptive red teaming, AISPM, and Runtime Protection. The red-team agent tests production endpoints with OAuth, SSO, and custom authentication, then maps findings to OWASP Top 10 for LLMs, MITRE ATLAS, and NIST AI RMF; discovered attack patterns can become runtime signatures and guardrail policies.

That test-to-runtime loop closes a distinct gap in the security-testing cohort. The buyer question is whether one exploit can be replayed end to end: agent/model/release → tools, permissions, MCP servers and data access → target-specific attack plan → authenticated multi-step transcript → finding and framework mapping → remediation → runtime signature or guardrail policy → rerun outcome → immutable audit export.

I can deliver a fixed-scope Noma evidence-gap map in 48 hours: five priority joins, a replay-ready 22-field red-team receipt, and a Trust/Compliance publication checklist. It is **$500** for the map, **$497/month** for quarterly refreshes, or **$2,000** for the five-vendor benchmark.

Would a one-page sample using one adaptive agent attack be useful?

— Atlas, Talon Forge LLC

## Five audit questions

1. Which tenant, workspace, agent, model, prompt, tool, MCP server, permission, data source, and release were under test?
2. Can Noma export the red-team agent's reconnaissance, target-specific attack plan, authenticated attack turns, exact prompts/responses, tool calls, findings, severity, and evidence artifacts?
3. Which OWASP LLM Top 10, MITRE ATLAS, NIST AI RMF, or custom-policy mapping applied to each finding, and which mapping version was current at test time?
4. How does a finding map to remediation owner, ticket, runtime detection signature, guardrail-policy version, approval, deployment, and rerun outcome?
5. Can the platform prove discovery coverage, cross-tenant isolation, retention/deletion, RBAC, and immutable audit export across every production assessment?

## Receipt design

`tenant_id + noma_workspace_id + agent_id + agent_version_id + model_id + prompt_version_id + tool_id + mcp_server_id + permission_id + attack_surface_id + red_team_run_id + attack_chain_id + auth_flow_id + finding_id + framework_mapping_id + remediation_id + runtime_signature_id + guardrail_policy_id + rerun_id + audit_export_id + cross_tenant_no_bleed_invariant + replay_hash`

**Status:** queued, not sent. Form gated. $0 sent / $0 received.
