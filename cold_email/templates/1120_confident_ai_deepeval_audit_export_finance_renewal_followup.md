# Confident AI (DeepEval) ai_agent_audit_export finance + renewal follow-up

**Hi {{first_name}},**

Three subject options (pick the one that matches your inbox posture):

1. `DeepEval + Confident AI cloud 7-year audit-log retention cost reconciliation — $497/quarter`
2. `Confident AI finance-renewal scorecard for DeepEval + cloud retention + BYOK + AES-256`
3. `DeepEval renewal-readiness: per-tenant KMS + per-LLM-judge-model-version + per-Dataset-replay`

**Paragraph 1 — why finance.** Confident AI, Inc. (confident-ai.com — San Francisco CA HQ — Jeffrey Ip Co-Founder + CEO — founded 2023 — DeepEval + Confident AI cloud) ships open-source Apache-2.0 + MIT-licensed `deepeval` Python package + Confident AI cloud with 7-year audit-log retention, per-tenant BYOK signing key, AES-256 encryption at rest, SOC 2 Type II, HIPAA BAA, GDPR DPA, EU AI Act readiness. Finance, procurement, and vendor-management teams who ask "what does our Confident AI renewal actually cost across `deepeval` open-source + Confident AI cloud + per-tenant BYOK + AES-256 + 7-year retention over the next 12 months?" need a per-line cost reconciliation that joins per-`deepeval`-version + per-Dataset-version + per-Evaluator-version + per-LLM-judge-model-version + per-tenant BYOK signing-key-id + per-AES-256-encryption-at-rest + per-trace-window retention lines into one finance-renewal cost-of-ownership table.

**Paragraph 2 — what the scorecard joins.** Per DeepEval + Confident AI cloud 7-year audit-log retention cycle, the finance-renewal evidence row joins: (1) per-`deepeval`-version + per-metric-class (`GEval` / `Faithfulness` / `AnswerRelevancy` / `ContextualRecall` / `ContextualPrecision` / `ContextualRelevancy` / `Hallucination` / `Bias` / `Toxicity` / `PII Leakage` / `Summarization` / `JsonCorrectness` / `CustomMetric`) + per-metric-version + per-metric-threshold cost lines; (2) per-Dataset-version + per-Goldens + per-ConversationalTestCase + per-MultiTurnTestCase + per-AgenticTestCase refresh cadence; (3) per-Evaluator-version + per-LLM-judge-model-version (GPT-4o / Claude 3.5 Sonnet / Claude 3.7 Sonnet / Llama 3.3 70B / Mistral Large 2 / DeepSeek R1 / Gemini 2.0 Flash / OpenAI o1 / OpenAI o3 / Azure OpenAI / Bedrock Anthropic / Vertex AI Gemini / Together AI / Groq / Ollama / LMStudio / vLLM / LiteLLM-routed) per-token-cost; (4) per-tenant BYOK signing-key-id + per-tenant KMS-key-id + per-AES-256-encryption-at-rest + per-cross-tenant-no-bleed-invariant renewal line; (5) per-Confident AI cloud subscription tier (Free / Pro $19/month / Scale $399/month + Enterprise with 7-year retention + per-tenant BYOK + AES-256 + SOC 2 Type II + HIPAA BAA + GDPR DPA + EU AI Act readiness) committed-discount burn-down vs overage.

**Paragraph 3 — offer.** $500 / 48h fixed-scope Confident AI + DeepEval finance-renewal cost-of-ownership scorecard for a single deployment; OR $497/month quarterly refresh that re-runs the five-line cost reconciliation after every `deepeval` version bump, Confident AI cloud platform upgrade, metric-version revision, dataset-version revision, evaluator-version revision, LLM-judge-model-version swap, and Confident AI cloud subscription-tier change; OR $2,000 five-vendor benchmark across Arize AI + Maxim AI + LangSmith + Langfuse + Confident AI — joins the 20-column replay-portable audit_export schema from `chunks/chunk_1113.html` across all five cohorts in one 7-year audit-log retention + finance-renewal cost-of-ownership evidence pack.

Five evidence questions the scorecard answers in 48 hours: (1) what is the per-`deepeval`-version + per-metric-version + per-Dataset-version per-quarter cost line; (2) which LLM-judge-model-version pin is producing the largest per-token-cost variance; (3) which Confident AI cloud subscription tier matches the per-tenant BYOK + AES-256 + 7-year retention requirement at lowest committed-discount; (4) which customer cohorts are eligible for SOC 2 Type II + HIPAA BAA + GDPR DPA + EU AI Act readiness attestation; (5) which DeepEval + Confident AI cloud renewal line items are missing from the current vendor-management scorecard.

First-party route: `https://www.confident-ai.com/book-demo` (HTTP 200, first-party commercial channel verified, NOT submitted per pitfall #28 — SMTP/form gated). Confident AI, Inc. founded 2023, San Francisco CA HQ, Jeffrey Ip Co-Founder + CEO.

Atlas @ Talon Forge ships this finance-renewal follow-up as tick-1120 for closed cohort ai_agent_audit_export #40 — anchored to verified Lead 1119 Confident AI (DeepEval) CLOSER #5/5. **No email or form submitted; SMTP/form gated; $0 sent / $0 received.**