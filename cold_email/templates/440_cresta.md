# Template 440 — Cresta (voice_agents 14th-sibling)

## Subject

Ping — 14-vendor voice-agent audit binder for Cresta (Stanford AI Lab spin-off)

## Opener (3 questions, name-only greeting, email lives in CSV)

Hey {first_name},

I've been mapping the voice-agent vendor-DD landscape for compliance teams (SOC 2 + EU AI Act + HIPAA + PCI-DSS at the same time) and the canonical voice_agents chain has become 13-vendor-deep — Vapi, Retell, Bland, PolyAI, SoundHound, ElevenLabs, AssemblyAI, etc. Cresta is the 14th-sibling on the list, but the audit surface is meaningfully different because you own both the AI agent runtime and the real-time coaching layer (Agent Assist + Coaching + QA), so the OWASP LLM Top 10 + MITRE ATLAS coverage matrix needs to fold in real-time-guidance-poisoning + coaching-recommendation-poisoning + QA-evaluation-poisoning + roleplay-scenario-poisoning — three attack surfaces the other 13 siblings don't expose.

Three things make Cresta's audit-binder shape different from the other 13 siblings:

1. **Behavioral-cloning + coaching layers change the data provenance graph.** Real-time coaching stores per-call behavioral recommendations + per-coaching-session patterns + per-roleplay-scenario transcripts → the per-call-id → per-conversation-turn-id → per-LLM-call-id → per-prompt-template-version-id → per-completion-id → per-knowledge-card-id → per-next-best-action-id → per-real-time-guidance-id provenance join-table has to fold in the per-coaching-session-id and the per-roleplay-scenario-id or the SOC 2 CC7.2 + EU AI Act Art. 12 + ISO 42001 9.4 + PCI DSS Req. 10 audit can't reconstruct the agent-customer interaction chain end-to-end. The other 13 voice-agent siblings don't have coaching persistence at all, so this join-table for Cresta is roughly 50+ cols vs the standard 30-40. Does the Cresta audit binder currently produce this composite lineage?

2. **Vertical-mix expands the PCI/PHI redaction surface.** Cresta's customer base spans airlines + banks + insurance carriers + hospital systems + retailers + home-services + telecom simultaneously, which means per-vertical PCI-DSS-Req-3 + HIPAA-Security-Rule redaction is multi-tenant per billing-segment, not single-tenant. The OWASP LLM Top 10 + MITRE ATLAS coverage matrix needs to fold in per-vertical PCI-card-data-redaction-bypass + per-HIPAA-PHI-redaction-bypass + per-bank-tenant-redaction-bypass + per-insurance-PHI-redaction-bypass + per-hospital-PHI-redaction-bypass rows. The other 13 voice-agent siblings are mostly single-vertical (mostly contact-center or mostly IVR), so Cresta's coverage matrix is roughly 25-30 cols vs the standard 18-22. Is there a published coverage matrix for the per-vertical redaction lineage?

3. **Multi-vertical CISO-front-door vs single-vendor CISO-front-door.** With a customer base spanning Delta + Intuit + Brinks Home + hospital-systems + retail + insurance simultaneously, the security@/CISO-front-door has to answer across 6-8 distinct compliance regimes at once. The cross-Cresta-tenant-isolation + per-Cresta-workspace-id + per-Cresta-org-id + per-banking-tenant + per-insurance-tenant + per-airline-tenant + per-hospital-tenant + per-retail-tenant isolation-evidence lineage is roughly 15+ col where a single-vertical contact-center vendor would be 6-8 cols. Does Cresta's SOC 2 Type II Type 2 report 2025 already disclose this multi-vertical tenant isolation matrix, or is that separate evidence per vertical?

## Audit-binder deliverables (5 questions)

1. **Per-call-id → per-conversation-turn-id → per-LLM-call-id → per-prompt-template-version-id → per-completion-id → per-knowledge-card-id → per-next-best-action-id → per-real-time-guidance-id → per-Cresta-tenant-id → per-billing-event-id → per-coaching-session-id → per-roleplay-scenario-id → per-QA-evaluation-id → per-PCI-card-data-redaction-id → per-HIPAA-PHI-redaction-id → per-EU-AI-Act-content-moderation-id 50+ col SOC 2 CC7.2 + EU AI Act Art. 12 + ISO 42001 9.4 + PCI DSS Req. 10 + HIPAA Security Rule audit-binder join-table — does the Cresta audit binder produce this composite lineage?** (This is the per-vertical redaction + coaching-lineage composite that the audit needs to reconstruct the agent-customer interaction chain end-to-end per SOC 2 + EU AI Act + PCI DSS + HIPAA.)

2. **per-OWASP-LLM-Top-10-id → per-MITRE-ATLAS-id → per-attack-id → per-real-time-guidance-poisoning-id + per-behavioral-pattern-poisoning-id + per-coaching-recommendation-poisoning-id + per-QA-evaluation-poisoning-id + per-roleplay-scenario-poisoning-id + per-AI-Receptionist-routing-poisoning-id + per-Opera-orchestration-poisoning-id + per-conversation-intelligence-poisoning-id + per-insight-generation-poisoning-id + per-knowledge-card-poisoning-id + per-next-best-action-poisoning-id + per-training-data-exfiltration-id + per-behavioral-cloning-bypass-id + per-agent-persona-bypass-id + per-supervisor-coaching-bypass-id + per-AI-analyst-bypass-id + per-LLM-call-token-bypass-id + per-insurance-PHI-redaction-bypass-id + per-bank-PCI-card-redaction-bypass-id + per-hospital-PHI-redaction-bypass-id + per-airline-PII-redaction-bypass-id 25+ cols per coverage-row mapping per OWASP LLM Top 10 + MITRE ATLAS + EU AI Act Art. 15 + NIST AI RMF MEASURE — what does the published coverage matrix look like for the per-vertical + real-time-coaching + behavioral-cloning layers?** (This is the per-vertical + behavioral-cloning coverage matrix that the audit needs to assess the per-vertical PCI/PHI redaction + coaching-lineage at scale.)

3. **per-prompt-injection + per-jailbreak + per-multi-turn-attack + per-data-poisoning + per-tool-call-poisoning + per-RAG-retrieval-poisoning + per-knowledge-base-document-poisoning → per-agent-step-poisoning + per-prompt-leak + per-system-prompt-extraction + per-evaluation-result + per-judge-model-bias + per-evaluator-bypass + per-PCI-card-data-redaction + per-HIPAA-PHI-redaction + per-EU-AI-Act-content-moderation + per-GDPR-consent-verification + per-real-time-guidance-content-moderation + per-knowledge-card-content-moderation + per-next-best-action-validation + per-Opera-orchestration-validation + per-conversation-intelligence-validation + per-AI-Analyst-output-validation + per-coaching-recommendation-validation + per-roleplay-scenario-validation + per-QA-evaluation-validation 22+ cols per-defense-row lineage per OWASP LLM Top 10 LLM01+LLM02+LLM03+LLM06+LLM07+LLM08 + MITRE ATLAS + EU AI Act Art. 15 + NIST AI RMF MEASURE + ISO 42001 6.1.4 + PCI DSS Req. 3 + HIPAA Security Rule — does the published defense lineage fold in the per-vertical PCI/PHI redaction validation + per-coaching-validation layers?** (This is the per-vertical + behavioral-cloning defense lineage that the audit needs to assess what defenses are wired across the LLM + behavioral-cloning + coaching stack.)

4. **cross-Cresta-tenant + per-Cresta-workspace-id + per-Cresta-org-id + per-banking-tenant + per-insurance-tenant + per-airline-tenant + per-hospital-tenant + per-retail-tenant + per-call-recording-id + per-knowledge-base-id + per-Opera-orchestration-id + per-conversation-intelligence-id + per-coaching-session-id + per-roleplay-scenario-id + per-QA-evaluation-id + per-billing-event-id isolation-evidence lineage per SOC 2 CC6.1 + GDPR Art. 28 + EU AI Act Art. 10 + ISO 42001 A.6.1.4 + PCI DSS Req. 1 + HIPAA + FedRAMP alignment — is the multi-vertical CISO-front-door evidence pack included in the standard SOC 2 Type II Type 2 report 2025, or is it a separate vertical-evidence deliverable per banking + insurance + airline + hospital + retail customer?** (This is the cross-vertical isolation-evidence question that the audit needs to answer before any banking + insurance + hospital + retail customer deployment per PCI DSS Req. 1 + HIPAA + FedRAMP.)

5. **WORM retention + per-conversation-cost + per-LLM-call-target-cost + per-knowledge-base-storage-cost + per-call-recording-storage-cost + per-coaching-session-cost + per-roleplay-scenario-cost + per-QA-evaluation-cost + per-Opera-orchestration-cost + per-conversation-intelligence-cost + per-insight-generation-cost + per-call-min-cost + per-banking-call-cost + per-insurance-call-cost + per-airline-call-cost + per-hospital-call-cost + per-retail-call-cost + per-Cresta-org-monthly-cost + per-Cresta-workspace-monthly-cost + per-billing-event-cost cost-attribution join-table per SOC 2 CC7.2 + EU AI Act Art. 12 + SEC 17a-4 WORM + PCI DSS Req. 10 + HIPAA + EU AI Act Annex III 4 + Aug 2026 GPAI enforcement + banking + insurance + airline + hospital + retail vertical-specific cost-attribution — what's the canonical cost-attribution matrix that folds in the per-vertical + per-coaching-cost + per-roleplay-cost WORM retention deltas?** (This is the per-vertical + per-coaching-cost cost-attribution question that the audit needs to answer for any banking + insurance + airline + hospital + retail vertical rollout with per-vertical billing-segment requirements.)

## Closes

The voice_agents audit-binder cohort is 14-vendor-deep at this point (Cresta is the 14th sibling), and I've shipped reference audit binders for the 13 prior siblings that the vendor-DD teams are using as the canonical compliance shape. The 48-hour vendor-DD audit deliverable is $500 flat, or the monthly retainer is $497/mo for the full audit binder + ongoing compliance-shape updates per Aug 2026 GPAI enforcement + EU AI Act readiness. If the audit binder team wants a single canonical voice-agent chain shape across all 14 siblings, that's a $6,000 / $5,964/mo cohort-level retainer. Either way, happy to send the Cresta audit-binder shape spec + the per-vertical redaction + coaching-lineage composite.

— The voice_agents audit-binder team

P.S. The Stanford AI Lab founding spin-off + Sebastian Thrun + Tim Shi ex-OpenAI + Ping Wu ex-Google CCAI lineage is in the canonical trail — that's a notable technical-credibility floor for the voice-agent inference layer at the level of Karpathy-led Bland + Tiger Global + Greylock + a16z-backed vendor cohort.
