# Cold-Email Template 675 — LlamaIndex (ai_agents_autonomous sibling #2 of 5)

**Vendor:** LlamaIndex / RunLLM, Inc. | llamaindex.ai | @llama_index
**Cohort:** ai_agents_autonomous (sibling #2/5 after Fixie.ai 674 OPENER)
**Inbox (verified live 2026-07-20):** privacy@runllama.ai (canonical privacy/DPA inbox on llamaindex.ai/legal/privacy-notice)
**Founder:** Jerry Liu (Co-Founder + CEO; founder attribution supported by public company materials; outreach copy uses first name only)
**One-liner:** LlamaIndex is the context-aware AI-agent framework plus LlamaParse/LlamaCloud document-ingestion, extraction, indexing, workflow, and low-code LlamaAgents surface that turns private enterprise documents into tool-using production agents.

---

## 1. Subject (3 variants)

A) "Jerry — 5 evidence gaps across LlamaParse → Index → document-agent actions"
B) "LlamaIndex document-agent audit trail: parse source → retrieval node → tool action"
C) "your LlamaCloud vendor-DD packet — reply 'send' for the 5-gap map"

## 2. Opening (5-question audit-letter opener)

Jerry — five quick questions before I send the full map:

1. When LlamaParse converts a complex PDF, spreadsheet, image, table, chart, or handwritten page into AI-ready structured data, can an enterprise customer retrieve a per-source-document-hash + per-page-hash + per-parser-version + per-parsing-instruction-version + per-layout-region + per-confidence-score + per-human-correction receipt that survives EU AI Act Article 12 logging and Article 53(1)(b) training-data transparency review?
2. When LlamaCloud Index transforms those parsed nodes into an agent retrieval surface, do you expose a per-index-version + per-node-id + per-chunk-boundary-version + per-embedding-model-version + per-metadata-extraction-version + per-retrieval-policy-version + per-tenant-isolation-token trail from original source page to the context supplied to the model?
3. When a LlamaIndex Workflow or LlamaAgent reasons over that context and acts, can the buyer join per-agent-version + per-workflow-version + per-model-provider/version + per-prompt-version + per-retrieved-node-id + per-tool-call-id + per-tool-input/output hash + per-guardrail-decision + per-human-approval/override into one machine-readable action receipt?
4. When a customer uses managed LlamaCloud versus open-source/self-hosted LlamaIndex, is the responsibility boundary explicit for deployment region, customer-managed keys, retention, deletion, observability, model-provider subprocessors, and the evidence needed for SOC 2, ISO 27001, HIPAA, GDPR, NIS2, and regulated procurement?
5. When a document source, index, workflow, agent, or customer tenant is retired, do you issue a deletion cascade receipt covering raw uploads, parsed artifacts, extracted fields, index nodes, embeddings, caches, evaluation traces, tool-call history, backups, and every model or storage subprocessor?

## 3. Why this lands now

LlamaIndex now spans the whole document-agent decision chain: LlamaParse turns messy enterprise documents into structured data; Extract converts them into typed outputs; Index makes the evidence retrievable; Workflows and the LlamaIndex open-source framework turn that context into agent decisions; LlamaAgents lets teams build and deploy document agents from natural language. That breadth is the commercial wedge, but it is also the procurement gap. Buyers do not want five unjoined logs. They want one receipt showing exactly which source page became which parsed node, which node was retrieved, which model reasoned over it, which tool acted, which guardrail fired, and which human approved the state change.

Fixie.ai 674 opened this cohort from the general Agent-as-a-Service side. LlamaIndex is the natural sibling #2 because it owns the document-context-to-action supply chain. The 16-column evidence cascade we would map is: source-document-hash, page-hash, parser-version, extraction-schema-version, index-version, node-id, chunk-boundary-version, embedding-model-version, retrieval-policy-version, workflow-version, agent-version, model-provider/version, tool-call-id, guardrail-decision, human-approval/override, and deployment-region/tenant-key identity.

## 4. Three engagement options

1. **$500 / 48h evidence-gap map** — a six-page vendor-DD report covering the five gaps above, a public-evidence PASS/PARTIAL/GAP table, and the source-page → parser → index-node → retrieval → model → tool-action receipt spec. Pay on delivery.
2. **$497/mo quarterly refresh retainer** — every 90 days I re-run the map against new LlamaParse/LlamaCloud/LlamaIndex releases plus EU AI Act Articles 12, 14, 50, and 53; NIST AI RMF 600-1/600-2; ISO/IEC 42001; SOC 2; GDPR; HIPAA; and NIS2 changes. Cancel any quarter.
3. **Custom 5-day implementation sprint** — $4,500 fixed fee to define the evidence schema, responsibility matrix, deletion receipt, and sample procurement binder for one production document-agent workflow.

Reply "send" and I will forward the 5-gap map. No generic questionnaire — every row maps directly to LlamaParse, Extract, Index, Workflows, LlamaAgents, or the open-source LlamaIndex framework.

— Atlas @ Talon Forge
https://talonforgehq.github.io/atlas-store

P.S. The cohort now spans Fixie.ai's general Agent-as-a-Service substrate and LlamaIndex's document-context-to-action substrate. Next siblings: Relevance AI, crewAI, and AutoGen/runtime orchestration.
