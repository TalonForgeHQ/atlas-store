# Cold Email Template — Lead 992 C1 (formerly ConductorOne) (OPENER #1/5 ai_agent_identity_governance)

**Vendor:** C1 (formerly ConductorOne)  \
**Domain:** c1.ai (canonical 301 from conductorone.com)  \
**Vertical:** ai_agent_identity_governance  \
**Cohort role:** OPENER #1/5 (new vertical)  \
**Primary route:** FORM:https://www.c1.ai/book-a-demo (first-party "Book a demo" CTA in primary nav, not submitted)  \
**Founder:** Alex Bovee (Co-Founder / CEO) + Paul Querna (Co-Founder / CTO), verified verbatim on first-party /about-us  \
**Outbound status:** QUEUED NOT SENT — SMTP/form gated, no submission

---

## Why C1 specifically

C1's first-party site positions itself as "Access for the agentic era" with H1 "Access infrastructure for AI transformation" and ships a universal identity graph across users, services, agents, and API keys; MCP-native access in Slack/Cursor/CLI; identity-aware gateway with real-time policy; custom and on-prem MCPs; and policy-as-code guardrails. The /about-us page names Alex Bovee + Paul Querna as Co-Founders from Okta/ScaleFT. C1 is the canonical IGA substrate built for the agentic era, distinct from the PAM cohort that closed 5/5 and the runtime-authorization cohort that closed 3/5.

## The audit question

Can a customer replay exactly who/what was granted what — across humans, services, agents, and API keys — with policy-version provenance, access-review decisions, JIT-grant bounds, MCP-tool decisions, downstream action results, and revocation events all joined under a universal identity graph?

## Five evidence gaps to test

1. **Universal identity graph join:** agent identity + workload identity + API key + human user + service account joined through one tenant + group + role graph without cross-tenant ambiguity.
2. **Policy-as-code provenance:** the exact policy-as-code commit, role version, and condition set evaluated at action time, preserved alongside the decision.
3. **MCP-tool-call decision trail:** MCP server + tool name + tool call ID + decision + downstream resource action + result, retrievable in one export.
4. **Access-review + JIT lifecycle:** access-review decisions, approver identity, risk score, JIT grant ID + expiry, and re-evaluation cadence all version-pinned.
5. **Revocation + cross-system export:** revocation events + change-request ID + audit-export ID + downstream-system state change all joined through the same identity-graph substrate.

## The offer

**$500/48h fixed-scope AI-agent identity-governance evidence-gap map**, or **$497/mo evidence-refresh retainer**, covering universal identity graph + policy-as-code provenance + MCP-tool decisions + access-review lifecycle + JIT grants + revocation + audit-export.

## Subject line

C1 agentic-era IGA: *** identity-graph evidence joins

## Body (3-line personalized)

Hi Alex — C1 ships a universal identity graph across users, services, agents, and API keys, with MCP-native access in Slack/Cursor/CLI, an identity-aware gateway, and policy-as-code guardrails. We map the five evidence joins from agent + workload + API-key identity through policy-version, MCP-tool decision, downstream action result, access-review + JIT lifecycle, and revocation in 48 hours. Can I send the 1-page scoping doc?

## Send instruction

`FORM:https://www.c1.ai/book-a-demo` — first-party Book-a-demo CTA in primary nav; do not submit while SMTP/form access is gated.

**Outbound status: QUEUED NOT SENT — SMTP/form gated; no submission. $0 sent / $0 received.**