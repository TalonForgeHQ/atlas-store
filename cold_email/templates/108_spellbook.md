**Subject:** Spellbook + contract-of-record hallucination — 5 audit gaps your QSA + EU AI Act auditor will ask about in 2026

Hi Scott,

Your AmLaw 200 + Fortune 500 in-house legal-ops customer stack + the AIUC-100 conformance posture Spellbook took in 2025 is the strongest signal yet that contract-review AI has crossed from "experimental tooling" to "contract-of-record path." The audit question every Fortune 500 GRC lead + outside counsel + EU AI Act conformity auditor is asking right now is: **does the LLM layer preserve contract integrity, or can an LLM-authored clause ship into the executed contract unnoticed?**

Here are 5 audit gaps that the public material doesn't address but that every SOC 2 Type II + ABA Standing Committee + FRCP discovery + UCC §2-313 + EU AI Act Art. 12/14 auditor will ask Spellbook to evidence in 2026:

1. **Hallucinated indemnity clause past two human reviews** — ABA Model Rule 1.1 (competence — the reviewer must know what the agent produced) + UCC §2-313 (express warranty — the executed contract IS the contract-of-record) require that the mark-up provenance chain include (a) the source-paper-clause ID, (b) the retrieved clause-library-entry ID + version, (c) the prompt hash + LLM response hash, (d) the produced mark-up, (e) the second-human-review sign-off. Without (a)-(e) cryptographically bound, the firm cannot defend the *hallucinated indemnity clause* pattern — where the agent fabricates an indemnity carve-out that reads as if it was in the source paper, survives two human reviews, and ships into the executed contract. Audit artifact: per-mark-up provenance chain + hallucination-classifier score per LLM-proposed clause + second-human-review sign-off log.

2. **Mark-up provenance chain reconstructible per clause** — SOC 2 CC7.2 + FRCP 34 (production in discovery) + EU AI Act Art. 12 (logging + traceability) require that the per-clause provenance join-table be exportable on demand, 7-year WORM retention, with a quarterly reconstruction drill. The 7-column join-table: (clause_id, source_paper_clause_id, retrieved_clause_library_entry_id, prompt_hash, llm_response_hash, mark_up_hash, reviewer_id, reviewer_approval_timestamp). Audit artifact: per-clause provenance join-table + quarterly cold-storage rebuild drill.

3. **Third-party paper ingestion preserves privilege + integrity** — ABA Model Rule 1.6 (confidentiality) + FRCP 502(d) (clawback) + EU AI Act Art. 10 (data governance) + GDPR Art. 28 (processor controls) require that when the contract-AI agent ingests opposing counsel's paper, the ingestion chain preserves both *privilege* (paper received from outside counsel is privileged) AND *document integrity* (SHA-256 of the ingested paper must match SHA-256 of the source — so a tampered ingestion cannot introduce a clause the counterparty never sent). Audit artifact: per-ingestion-event privilege-stamp + SHA-256 + source-system provenance (DocuSign envelope ID / iManage doc ID / NetDocuments profile ID) + LLM-provider ephemeral-decryption log.

4. **Jurisdiction-aware clause-library drift detected + versioned** — ABA Model Rule 1.1 (competence — the lawyer must know which clause-library version the agent used) + EU AI Act Art. 14 (human oversight) + ISO 42001 §6.1.4 (AI system lifecycle — versioned training data) require that the clause library be versioned per-jurisdiction with drift detection that flags when a clause retrieved for a CA matter was actually the NY version, or a stale 2024 version for a 2026 matter. The 2025 EU AI Act reclassification of contract-review AI for HR/employment-related agreements as high-risk (Annex III §4) is the canonical regulatory-change stamp your drift detector must surface. Audit artifact: per-clause-library-entry version + per-jurisdiction applicability table + drift-detection log + regulatory-change flag.

5. **Executed-contract-of-record bound to the mark-up provenance** — UCC §2-313 (express warranty) + FRCP 34 (production) + EU AI Act Art. 12 (6-year retention minimum) + SOC 2 CC8.1 (change-management) require that the executed PDF be cryptographically bound back to the mark-up provenance chain that produced it. Audit artifact: per-executed-contract provenance-root-hash + per-clause chain binding + 7-year WORM retention + quarterly end-to-end reconstruction drill.

I'm running a 48-hour, fixed-price **$500 audit** that delivers all five artifacts as a working reference implementation + the per-jurisdiction clause-library drift detector + the executed-contract cryptographic binding + the hallucination-classifier per-clause score + a one-page memo for your QSA + EU AI Act conformity auditor. References: chunks 34 (SOC 2 AI agent), 35 (CX-AI), 39 (permission inheritance), 40 (IT-helpdesk), 42 (voice-AI), 43 (legal-AI privilege) — all live at talonforgehq.github.io/atlas-store.

Worth a 30-min call this week?

— Atlas