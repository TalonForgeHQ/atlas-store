# Lead 1106 — Checkmk (tribe29) (sibling #3/5 ai_agent_operations_eu_sovereign)

**Subject lines:**
1. Checkmk + AI anomaly detection evidence map — $500/48h, EU-sovereign
2. Checkmk Raw + Event Console + AI runbook replay — fixed scope
3. Checkmk vs ilert vs SUSE Observability — $2,000 EU-sovereign AI-SRE benchmark

Hi {{first_name}},

Checkmk's cohort-unique wedge is the **open-source-family + AI anomaly detection + Event Console + Notification Rules + sovereign Bavaria** AI-SRE evidence chain: the replay can show which `checkmk_host_id` + `checkmk_service_id` + `checkmk_rule_id` + `checkmk_anomaly_detection_model_id` ran an autonomous investigation, which `checkmk_anomaly_event_id` recorded the chain of evidence, which `checkmk_notification_rule_id` + `checkmk_recipient_id` fired, and which `checkmk_audit_export_id` is portable to a DORA Article 28 ICT-third-party register.

A fixed-scope Checkmk EU-sovereign AI-SRE evidence-gap map is **$500 / 48 hours** — one normalized production replay with the 20-column receipt joining tenant, checkmk site, host, service, label, parent-child rule, agent bakery, anomaly detection model, anomaly event, baseline window, event console event, notification rule, notification recipient, maintenance window, runbook action, escalation tier, synthetic monitor, tribe29 Munich governance, and audit export.

For teams comparing the broader ai_agent_operations_eu_sovereign landscape, Atlas also offers a **$497/month quarterly refresh** or a **$2,000 five-vendor benchmark** covering Checkmk + four sibling vendors that match the EU-sovereign + ISO 27001 + DORA + AI-SRE + human-in-the-loop wedge.

Commercial route: `FORM:https://checkmk.com/contact-form` — verified first-party contact form (form not submitted) — or the `mailto:info@checkmk.com` channel for ISO 27001 certificate + DORA addendum requests. The normalized replay ends at `checkmk_audit_export_id` for a portable evidence handoff into a DORA Article 28 ICT-third-party register.

— Atlas @ Talon Forge

## Five replay questions

1. Which `checkmk_host_id` + `checkmk_service_id` + `checkmk_rule_id` fired, and which `checkmk_label_source` + `checkmk_parent_child_rule` defined the topology?
2. Which `checkmk_anomaly_detection_model_id` + `checkmk_anomaly_event_id` recorded the AI-detected anomaly, and which `checkmk_baseline_window` + `checkmk_confidence_score` + `checkmk_context_evidence` was logged?
3. Which `checkmk_event_console_event_id` + `checkmk_notification_rule_id` + `checkmk_recipient_id` fired, and which `checkmk_escalation_tier` + `checkmk_maintenance_window` was applied?
4. Which `checkmk_runbook_action_id` + `checkmk_synthetic_monitor_id` carried the remediation context, and which `checkmk_acknowledgment_event_id` documented the human approval?
5. Which `dora_compliance_package_id` + `checkmk_audit_export_id` reproduces the chain with no cross-tenant bleed and no non-EU data routing?

## Evidence and compliance posture

- First-party title/description: "Checkmk — Open-source monitoring at scale" + Checkmk AI for anomaly detection.
- First-party corporate: tribe29 GmbH Munich (Herbert-Bayer-Str. 8a, 80807 München); CEO Jan Behrens + co-founder Andreas Stiegler.
- First-party products: Checkmk Raw + Checkmk Enterprise + Checkmk Cloud + Checkmk SaaS + Checkmk AI + Synthetic Monitoring + Event Console + Notification Rules + Reporting.
- First-party security: ISO 27001 + SOC 2 Type II (Checkmk Cloud) + GDPR + DORA posture + BSI C5 + TISAX + ENS (Spain).
- First-party open-source: GPL-2 license for Checkmk Raw; per-host agent-bakery chain-of-custody; per-rule audit-trail.

**Verification:** `mailto:info@checkmk.com` + `FORM:https://checkmk.com/contact-form` — both verified first-party channels; SMTP/form gated; `$0 sent / $0 received`. Sibling #3/5, canonical slug `sibling-3-of-5`, vertical #38 ai_agent_operations_eu_sovereign.
