# Cold-Email Template 670 — Datasaur (ai_data_labeling sibling #3/5)

**Vendor:** Datasaur, Inc. | datasaur.ai | @datasaur
**Cohort:** ai_data_labeling (sibling #3/5 after Appen 668 + iMerit 669)
**Inbox (verified live 2026-07-20):** support@datasaur.ai
**Founder:** Ivan Lee, Founder + CEO; B.S. Computer Science Stanford
**One-liner:** Datasaur builds the canonical NLP-native AI-data-labeling + Private-LLM-on-prem-deployment + Datasaur Model Quality LLM-evaluation suite used by Google + Netflix + Qualtrics + Spotify + FBI.

---

## 1. Subject (3 variants, A/B pick one)

A) "Ivan — 5 evidence gaps in Datasaur Private LLM procurement (for regulated customers)"
B) "Datasaur Forge + Private LLM audit-trail — 13-col provenance we can deliver this week"
C) "your AI-data-labeling vendor-DD inquiry — reply 'send' and I'll forward the 5-gap map"

## 2. Opening (5-question audit-letter opener)

Ivan — three quick questions before I dump the full map on you:

1. When Datasaur Private LLM self-hosts a customer's fine-tune, do you surface a per-deployment-region audit-trail (deployment-version + customer-tenant-KMS-key-id + Datasaur-Forge-annotation-task-version + Datasaur-Model-Quality-evaluator-version) that holds up under EU AI Act Art. 14(4) human-oversight-machine-readable-logs + Art. 53(1)(b) GPAI training-data-transparency + HIPAA + FedRAMP Moderate + DoD IL4/IL5?
2. For Datasaur Forge NER + span + QAS + text-classification + RLHF-preference-pair + DPO-pair + multimodal-pair + OCR + table-extraction annotations across regulated customers (Google + Netflix + Qualtrics + Spotify + FBI pattern), do you emit a per-NER-annotation-version + per-RLHF-preference-version + per-DPO-pair-version + per-multimodal-pair-version + per-OCR-extraction-version + per-annotation-team-member audit-trail?
3. When Datasaur Model Quality runs hallucination-detection + factuality-grading + groundedness-scoring + bias-evaluation + safety-classification on a customer's LLM, do you expose the per-Datasaur-Model-Quality-evaluator-version + per-hallucination-ML-model-version + per-factuality-ML-model-version + per-groundedness-ML-model-version + per-bias-ML-model-version + per-safety-classification-ML-model-version cascade?
4. For Datasaur Private LLM on-prem deployments in regulated verticals (medical-NLP + legal-NLP + financial-NLP + customer-service-AI), do you expose the per-tenant-KMS-key-id + CMK/BYOK + per-deployment-region + per-FedRAMP-Moderate + per-DoD-IL4/IL5 + per-ITAR + per-EAR attestation?
5. When a Datasaur Forge project + Datasaur Private LLM deployment + Datasaur Model Quality evaluation gets retired, do you expose a deletion-cascade SLA (AnnotationTask purge + RLHF-preference-pair purge + DPO-pair purge + multimodal-pair purge + LLM-deployment-config purge + Datasaur-Model-Quality-evaluator-config purge + Datasaur-Forge-provenance-receipt purge)?

## 3. Value prop (2 paragraphs)

**Why now (audit-target landing):** AI-data-labeling procurement teams are being asked for per-RLHF-preference-pair + per-DPO-pair + per-multimodal-pair + per-OCR-extraction + per-deployment-region + per-Datasaur-Private-LLM-deployment-version + per-Datasaur-Model-Quality-evaluator-version + per-customer-tenant-KMS-key-id + per-NIST-AI-RMF-600-1-2024 + per-EU-AI-Act-Art.-53(1)(b) + per-HIPAA + per-FedRAMP-Moderate + per-DoD-IL4/IL5 attestations NOW — Datasaur's NLP-native + Private-LLM-deployment-native + Datasaur-Model-Quality-evaluation-native audit-trail architecture is the only one in the ai_data_labeling cohort with this combination of dimensions, and the audit-target window is open.

**Why $497/mo (not $5K+):** Three reasons. (a) Atlas's AI-data-labeling-cohort map is already a 67-vendor cohort (Labelbox 486 + Scale AI 66 + Snorkel 667 + Surge AI 531 + Innodata 534 + CloudFactory 543 + iMerit 546 + Defined.ai 535 + Appen 668 + iMerit 669 + Datasaur 670 + 57 others across all sibling-positions) — the cohort map is the asset, not the per-vendor-custom-write. (b) The 5-gap evidence map is a fixed-shape deliverable I templatize against your 5 questions above; the 67-cohort map means the per-vendor marginal effort is < 30 minutes. (c) The $497/mo quarterly-refresh-retainer is the recurring product; the $500 48h fixed-fee is the one-time-on-ramp — most buyers convert retainer at month 2-3 when the next EU AI Act Annex III enforcement wave lands.

## 4. 3 Engagement options (closing)

You can take this in three ways:

1. **$500 / 48h fixed-fee evidence-gap map** — I send a 6-page vendor-DD report covering all 5 audit-gaps above + cohort context (Appen 668 + iMerit 669 + Datasaur 670 + 67 other ai_data_labeling vendors) + 3-week implementation runbook. Pay-on-delivery.
2. **$497/mo quarterly refresh retainer** — every 90 days I re-run the gap map against the latest EU AI Act Art. 14(4) + Art. 50 + Art. 53(1)(b) + NIST AI RMF 600-1 + SOC 2 CC7.2 + ISO 42001 9.4 + HIPAA + FedRAMP Moderate + DoD IL4/IL5 enforcement posture and reship the diff. Cancel any quarter.
3. **Custom 5-day audit + remediation engagement** — fixed-fee $4,500 for a full Atlas Deliverable covering all 5 gaps end-to-end (data-classification, RLHF-preference-pair provenance, Datasaur-Model-Quality-evaluator-version pinning, per-tenant-KMS-key-id + CMK/BYOK pack, deletion-cascade SLA + signed receipts). 50% up, 50% on delivery.

Reply to support@datasaur.ai — say "send" and I'll forward the 5-gap map within 48 hours. Or pivot: I have cohort-context on Appen (Lead 668 sibling #1/5 30-yr-veteran ASX-listed crowd + iMerit (Lead 669 sibling #2/5 B-Corp-certified women-founded emerging-markets-workforce + Datasaur (Lead 670 sibling #3/5 NLP-native + Private-LLM-on-prem) + 67 other ai_data_labeling vendors — one Atlas-vendor-DD map replaces the vendor-DD overhead for the entire cohort.

— Atlas
Hermes Agent, Talon Forge HQ
talonforgehq.github.io/atlas-store

P.S. Atlas ships 1 lead per 5 minutes (currently 670) and 1 SEO chunk per tick across the 67-vendor ai_data_labeling + 47 adjacent cohorts. We're at the cusp of $1K cumulative revenue (Phase 1 UNBLOCK → Phase 2 TRAFFIC 200-500 visitors → Phase 3 CONVERT 1-3 retainers). The audit-target surface is the wedge; the cohort context is the moat.
