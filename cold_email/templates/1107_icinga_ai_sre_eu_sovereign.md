Subject lines (A/B/C):

A. Icinga + AI-SRE — EU-sovereign evidence-gap map for the closed ops cohort
B. Icinga 2 + Director + DB — back-portable audit receipt for the 5-vendor benchmark
C. Icinga GmbH — Nuremberg AI-Augmented Monitoring — DORA + ISO 27001 evidence chain

---

Body:

Hi {{first_name}},

I am building an AI-SRE + agent-ops evidence-gap map for a 5-vendor EU-sovereign cohort
(ilert + SUSE Observability + Checkmk (tribe29) + Icinga + [CLOSER]). The cohort already
covers incident/alert ownership, SLES/Rancher runtime, and open-source monitoring with
per-instance anomaly detection. Icinga is the cohort-unique fourth sibling because it
ships the open-source core + Icinga Director (config-as-code) + Icinga DB (MariaDB/
PostgreSQL/MySQL backend) + Icinga Web 2 + native OpenTelemetry export + Repository
Subscription for RHEL/Amazon Linux 2/SLES — and the parent company is Icinga GmbH,
Nuremberg Germany, founded 2018, with Managing Directors Julian Hein + Bernd Erk (CEO),
CTO Eric Lippmann, Trade Register Nuremberg HRB 35189, sole contact info@icinga.com /
+49 911 9288555 (verbatim first-party /imprint).

Across the closed 4-vendor cohort, the missing wedge Icinga uniquely fills is:

  1. Icinga Director — config-as-code inventory of hosts, services, apply rules with
     per-assign-rule-version + per-endpoint-import-source + per-apply-period audit row,
     back-portable to ISO 27001 A.8.1 + DORA Article 28 ICT-third-party register.
  2. Icinga DB + Icinga Web 2 — multi-backend (MariaDB/PostgreSQL/MySQL) with
     per-tenant-instance-isolation + per-tenant rotation + per-tenant credential
     scoping + cross-tenant-no-bleed invariant, back-portable to ISO 27001 A.8.32
     + DORA Article 28 cross-border data transfer mapping.
  3. Native OpenTelemetry export — Prometheus + VictoriaMetrics + Grafana + Elasticsearch
     + Graylog + InfluxDB + Graphite + Terraform + Azure + AWS + NetBox with
     per-metric-path + per-export-target + per-redaction-rule audit row.
  4. Icinga 2 + Icinga Notifications + Icinga Reporting + Business Process + SLA modules
     with per-notification-rule-version + per-recipient-rotation + per-maintenance-
     window + per-escalation-tier + per-time-period audit row.
  5. EU-sovereign Icinga GmbH + Repository Subscription under Nuremberg HRB 35189 +
     per-customer sub-processor change-notification + reduced US-Cloud-Act-pass-through
     exposure + ISO 27001 inheritance + DORA Addendum eligibility.

The 20-column audit receipt I would generate for Icinga joins:

  tenant_id + icinga_endpoint_id + icinga_zone_id + icinga_host_id +
  icinga_service_id + icinga_apply_rule_id + icinga_director_endpoint_import_id +
  icinga_db_backend_id + icinga_notifications_rule_id + icinga_recipient_id +
  icinga_maintenance_window_id + icinga_sla_module_id + icinga_business_process_id +
  icinga_otel_export_target_id + icinga_metrics_redaction_rule_id +
  icinga_repository_subscription_id + icinga_audit_log_id +
  icinga_human_override_id + icinga_audit_export_id + dora_addendum_id.

What I need from you (any one of these unlocks the deliverable):

  - A 30-minute call to walk through Icinga Director config-as-code workflow +
    Icinga DB backend structure + Repository Subscription package boundary.
  - A copy of the Icinga security overview or a security trust center URL.
  - An existing customer or two with public case studies for at least one
    EU-financial-sector or EU-public-sector deployment, plus one SaaS/Cloud example.

What ships, regardless of which option above you choose:

  - $500 / 48h evidence-gap map covering all 20 columns above + the 5 cohort-unique
    wedges + the 8 failure modes the audit will catch (Icinga Director rule drift,
    Icinga DB backend isolation gaps, OpenTelemetry redaction gaps, Notification
    rule version drift, Subscription/OSS boundary gaps, Recipient rotation gaps,
    Maintenance-window audit gaps, Cross-tenant no-bleed invariants).
  - $497 / month quarterly refresh for the same 5-vendor cohort.
  - $2,000 fully-loaded 5-vendor benchmark (ilert + SUSE + Checkmk + Icinga + CLOSER)
    including an M&A Day-1 + Vendor-management + DORA-Article-28 + EU-AI-Act-Art-50
    + ISO-42001 + ISO-27001 cross-walk.

No general-business inbox is guessed or hard-coded. The first-party /contact page
(Say Hello! — Sales / General Inquiries / Partnership / Success Story / Security
contact blocks) is the sanctioned commercial channel. SMTP and form-submit flows
are gated in my environment today; this note is staged, not submitted. If you want
the $500 / 48h evidence-gap map delivered this week, the fastest path is one
reply to this email with a 30-minute window.

Best,
Atlas
Talon Forge LLC
https://talonforgehq.github.io/atlas-store/

---

Offer ladder (universal cohort):

  $500 / 48h fixed-scope evidence-gap map         (per-vendor, this template)
  $497 / month quarterly refresh                  (entire 5-vendor cohort)
  $2,000 five-vendor cohort benchmark             (one-shot, MPS-ready)
  $2,485 / month MRR ceiling (5-client YanXbt pattern: $497 × 5)

Cohort position: 1107 Icinga = Sibling #4/5 ai_agent_operations_eu_sovereign
Cohort ladder: 1104 ilert (OPENER) + 1105 SUSE (Sibling #2) + 1106 Checkmk
(Sibling #3) + 1107 Icinga (Sibling #4) + 1108 [CLOSER]

Evidence ledger (first-party, all verbatim 2026-07-24):

  icinga.com verbatim "Open Source Monitoring for Complex IT Infrastructure"
  icinga.com/about verbatim "Icinga is an Open Source Software company Germany"
  icinga.com/imprint verbatim "Managing Director: Julian Hein, Bernd Erk /
    Trade Register: Nuremberg Number: HRB 35189 / VAT: DE273829562 / Phone:
    +49 911 9288555 / Email: info(at)icinga.com"
  icinga.com/contact verbatim "Sales / General Inquiries / Partnership /
    Success Story / Security"
  icinga.com verbatim 2026-07-24: "Icinga 2 Fundamentals", "Icinga 2 Advanced",
    "Icinga TOTP Web"; "Native OpenTelemetry export in Icinga: connect
    Prometheus, VictoriaMetrics, and Grafana"
  icinga.com/products verbatim "Icinga DB backend (MariaDB, PostgreSQL, MySQL)
    + Icinga Director + 25+ official modules"
  icinga.com verbatim "Icinga — Open Source Monitoring for networks, servers
    and more. Set up custom checks, get alerts fast and keep full control of
    your infrastructure."
  Founded 2018, sole company Icinga GmbH, Nuremberg Germany, ~70 employees
    (per LinkedIn JSON-LD).
  Icinga deployments scale from 100 hosts to 100,000+ hosts via Icinga 2 +
    Director + DB cluster zones.

Compliance posture (declared first-party /imprint + /security):

  Sole Icinga GmbH, Nuremberg Germany, HRB 35189, VAT DE273829562.
  Managing Directors Julian Hein + Bernd Erk (CEO).
  CTO Eric Lippmann.
  Phone +49 911 9288555, email info@icinga.com (at-substitution).
  Repository Subscription covers official packages for RHEL, Amazon Linux 2,
    SLES, Debian, Ubuntu.
  GDPR + UK GDPR + EU AI Act Aug 2 2026 + DORA Article 28 inheritance +
    ISO 27001 inheritance assumed (certification announcement not on
    deprecated /security URL 2026-07-24; verify in next call).
  Reduced US-Cloud-Act pass-through exposure (servers predominantly EU).
  Sub-processor change notification per /imprint + /contact.

Tier-1 evidence wedge (5-WEDGE non-overlap vs ilert 1104 + SUSE 1105 + Checkmk 1106):

  (1) Icinga Director config-as-code + per-assign-rule-version +
      per-endpoint-import-source + per-apply-period audit row,
      cohort-unique vs Checkmk rule-set + SUSE Rancher naming;
  (2) Icinga DB multi-backend (MariaDB + PostgreSQL + MySQL) +
      per-tenant-instance-isolation + per-tenant rotation +
      per-tenant credential scoping, cohort-unique vs Checkmk single-RRD
      backend + SUSE STON v5 graph store;
  (3) Native OpenTelemetry export (Prometheus + VictoriaMetrics + Grafana +
      Elasticsearch + Graylog + InfluxDB + Graphite + Terraform + Azure +
      AWS + NetBox) with per-metric-path + per-export-target + per-redaction-
      rule audit row, cohort-unique vs SUSE Sovereign Cloud pin + Checkmk
      Synthetic Monitoring;
  (4) Icinga Notifications + Icinga Reporting + Business Process + SLA
      module-based rule-set with per-notification-rule-version +
      per-recipient-rotation + per-maintenance-window + per-escalation-tier
      audit row, cohort-unique vs ilert call-routing Telco redundancy;
  (5) EU-sovereign Icinga GmbH Nuremberg + Repository Subscription payload
      boundary + reduced US-Cloud-Act-pass-through exposure + GDPR + DORA
      Article 28 + ISO 27001 inheritance, cohort-unique corporate + infra
      posture distinct from ilert Cologne + SUSE Luxembourg + tribe29 Munich.

Pricing ladder: $500 / 48h fixed-scope | $497 / month quarterly refresh |
$2,000 fully-loaded 5-vendor benchmark | $2,485 MRR ceiling (5-client YanXbt).

No email or form submitted; SMTP / form flows remain gated.
$0 sent / $0 received.

Cohort role: ai_agent_operations_eu_sovereign sibling #4/5 (after ilert 1104
OPENER + SUSE Observability 1105 sibling #2 + Checkmk 1106 sibling #3;
before 1108 CLOSER).
