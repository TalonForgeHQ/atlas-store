# 1211_prefect_ai_agent_workflow_orchestration.md

**Vendor:** Prefect (prefect.io)
**Contact:** mailto:hello@prefect.io + mailto:careers@prefect.io
**Vertical:** ai_agent_workflow_orchestration
**Cohort slot:** SIBLING #2/5 (NEW VERTICAL #60) — after Temporal 1210 OPENER #1/5
**Founder:** Jeremiah Lowin, CEO
**Status:** queued_not_sent (SMTP/form gated; 0 sent / 0 received)

---

## 3 Subject Options (under 60 chars each)

1. `Prefect 1211: 23,474-star workflow orchestration + AI Agent ledger`
2. `Prefect 1211: per-flow + per-task + per-pydantic-ai-agent audit trail`
3. `Prefect 1211: @flow + @task vs Temporal — sibling 2 of 5 evidence gap`

## Body (use ONE subject, ship as one email)

Hi Prefect team,

Sending a 5-WEDGE evidence-gap map for the `ai_agent_workflow_orchestration` cohort — sibliang #2/5 of 5 next to Temporal 1210 OPENER. Cohort-floor offer is $500/48h fixed-scope or $497/mo quarterly refresh.

**The 5-WEDGE rubric we ship per-vendor per-cohort-slot:**

1. **Per-flow + per-task + per-run + per-work-pool + per-worker + per-concurrency-limit audit trail** — Prefect's @flow + @task + Flow.run() + per-task-run-state (Pending/Running/Completed/Failed/Crashed/Cancelled/Paused) + per-work-pool + per-worker + per-concurrency-limit + per-retry + per-cache-version + per-notification + per-artifact + per-block + per-secret + per-variable + per-incident + per-automation + per-webhook + per-pipeline + per-pydantic-ai-agent + per-marvin-agent + per-dask-task-runner + per-ray-task-runner + cross-tenant-no-bleed invariant + replay-hash.
2. **OSS-first distribution (Apache-2.0 + 23,474 GitHub stars + self-hosted Prefect Server OSS) + Cloud is optional** — distinct from Temporal's OSS+scaled-frontier-controls binary posture where OSS feels community-only and Cloud is the durable-execution-grade production path.
3. **Python-first @flow + @task decorator API + dynamic DAGs + native async** — Prefect's decorator-first ergonomics + dynamic DAGs (tasks can be added at runtime) + native async + concurrent task runner + Dask + Ray task runner vs Temporal's SDK-first Workflow + Activity + Worker class API where Workflows are subclasses of temporalio.workflow.Workflow and Activities are async def functions invoked through the worker.
4. **Pydantic AI + Marvin AI + Prefect AI agents + Prefect Pipelines + AI Gateway** — Prefect's AI-agent substrate (Pydantic AI integration + Marvin AI integration + Prefect AI) is distinct from Temporal's AI-Activities (released May 2026) + Temporal Agents + Workflow Agent posture.
5. **First-party Evidence ladder** — `prefect.io/company` + `api.github.com/repos/PrefectHQ/prefect` (23,474 stars verified live 2026-07-25 via api.github.com) + `api.github.com/orgs/PrefectHQ` (is_verified=true + Washington DC HQ + 41 public_repos + 924 followers) + Wikipedia (Prefect_(software)) + Crunchbase — real company + real product + real founder Jeremiah Lowin CEO verified.

**What's missing from your current public surface (per the rubric above):**

- Cross-tenant-no-bleed invariant document (per-work-pool cross-tenant isolation + per-cloud-tenant credential scoping + per-Cloud-Worker-Process tenant boundary).
- Per-AI-agent-per-invocation audit-provenance (per-pydantic-ai-agent-run + per-marvin-agent-run + per-prefect-ai-agent-run + per-model-name + per-model-version + per-prompt-hash + per-response-hash + per-decision-citation-anchor + per-decision-confidence + per-human-override-acknowledgment + 14-day change-notification SLA on AI sub-processor swaps).
- EU AI Act Aug 2 2026 readiness per-Article (Art. 6 + Art. 9 + Art. 10 + Art. 13 + Art. 14 + Art. 15 + Art. 26 + Art. 27 + Art. 50 + Annex III high-risk list) + cross-border data-residency posture for EU + UK + CH + BR + AU + SG + JP + CA + IN + ZA.
- ISO/IEC 42001:2023 AIMS clause-by-clause evidence map (Prefect Cloud AI agent surface — clause 6.1.2 + 7.2 + 8.4 + 9.3 + 10.1) + per-clause citation pinning.
- Per-day-of-2026 incident-manifest export lane (per-flow + per-task + per-worker + per-tenant + per-work-pool + per-cloud-tenant + per-day reproducible machine-readable artifact — same artifact reproducible across Cloud + Server + per-flow-runtime + per-pipeline-runtime + per-ai-agent-runtime).

**Cohort cohort-floor offers:**

- **$500/48h fixed-scope evidence-gap map** (1 deliverable: 1 PDF + 1 machine-readable artifact per-vendor above)
- **$497/mo quarterly refresh** (quarterly artifact + per-Article coverage)
- **$2,000 cohort benchmark** (5-vendor package: Temporal 1210 + Prefect 1211 + Sibling #3 + Sibling #4 + Closer #5)

Reply with which surface you'd like scoped first.

— Atlas
mailto:hello@prefect.io
Cohort: ai_agent_workflow_orchestration (NEW VERTICAL #60, 1/5 → 2/5)
