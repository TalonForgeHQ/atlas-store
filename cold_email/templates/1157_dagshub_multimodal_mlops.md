# 1157 — DagsHub — ai_mlops_feature_store SIBLING #4/5

**First-party recipient route:** `FORM:https://dagshub.com/contact` (not submitted)  
**Company:** DagsHub — dagshub.com  
**Founder:** Dean Pleban, Co-Founder & CEO (verified first-party at `dagshub.com/blog/author/dean/`)  
**Cohort:** `ai_mlops_feature_store`; W&B 1154 → ClearML 1155 → Comet + Opik 1156 → DagsHub 1157

## Subject options

1. **DagsHub — one replay from multimodal data to deployed model**
2. **A 22-column lineage receipt for DagsHub AI teams**
3. **Can every DagsHub deployment resolve to its exact golden dataset?**

## Message

Hi Dean,

DagsHub brings multimodal data curation and annotation, MLflow-compatible experiment tracking, and model management into one platform. The diligence question for AI teams is whether a deployed model can be replayed to the exact source-data version, annotation workspace, experiment, artifact, model version, approval, and deployment without crossing repository or tenant boundaries.

Atlas can deliver a fixed-scope **$500 / 48-hour** evidence-gap map for that lineage, or maintain it for **$497/month** with quarterly refreshes. The proposed receipt below is grounded in DagsHub's first-party platform, pricing, About, and founder-author pages.

## Five non-overlap wedges

1. **Multimodal-first data plane:** petabyte-scale vision, audio, and LLM data curation precedes experiment and model evidence.
2. **Golden-dataset lineage:** annotation and auto-labeling evidence joins directly to source-data versions and downstream models.
3. **Open-stack compatibility:** MLflow experiment tracking, Label Studio compatibility, and open-source formats reduce proprietary evidence gaps.
4. **Repository-native collaboration:** public/private repositories, notebook versioning, interactive pipelines, and CI/CD/CT form a code-to-model trail.
5. **Deployment-boundary choice:** Team cloud, customer storage, Enterprise VPC, air-gapped on-premise, and OpenShift create explicit residency boundaries.

## Proposed audit receipt

`tenant_id + organization_id + repository_id + source_storage_id + dataset_id + dataset_version_id + annotation_workspace_id + annotation_task_id + label_schema_version_id + annotator_id + experiment_id + mlflow_run_id + pipeline_id + notebook_commit_id + artifact_id + model_id + model_version_id + approval_id + deployment_id + cluster_id + cross_tenant_no_bleed_invariant + replay_hash`

First-party verification used `https://dagshub.com/`, `/pricing`, `/about`, and `/blog/author/dean/`. Public pricing checked 2026-07-24 lists Individual at $0, Team at $119/user/month, and Enterprise with VPC/air-gapped installation. The contact route was not submitted.

Best,  
Atlas @ Talon Forge

**Status:** template-only; no email or form submitted. `$0 sent / $0 received`.
