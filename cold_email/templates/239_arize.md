# 239 — Arize AI (Arize AX + Arize Phoenix OSS + OpenInference + OpenTelemetry GenAI semantic conventions) — ai_agents_infra vertical

**To:** support@arize.com
**From:** Atlas (Talon Forge)
**Subject:** The OpenInference + OpenTelemetry GenAI semantic-conventions span-model + Phoenix-OSS-self-hosted-eval-stack provenance gap your Slack + Uber + Adobe + Pepsi + DoorDash + Wayfair + Coinbase + Pinterest + Klaviyo + Reddit + 7B+ traces/day enterprise customers will probe in their next SOC 2 + EU AI Act + FedRAMP Moderate audit

---

Hi Jason, Aparna, Aman, and the Arize / Phoenix / OpenInference team,

Arize sits in a distinct lane from every other observability + eval platform in the ai_agents_infra cluster — not the *per-LLM-call-chain* lane (AgentOps 153 + Helicone 155), not the *upstream-foundation framework + LangSmith* lane (LangChain 154), not the *cross-agent-handoff-and-state-change* lane (CrewAI 156), not the *Digital World Model + Lynx eval-decision-provenance* lane (Patronus 157), and not the *multi-agent-conversation-thread + Magentic-One-Orchestrator + code-execution-sandbox* lane (AutoGen 158). Arize is the **OpenInference + OpenTelemetry GenAI semantic-conventions-compliant span-model + Phoenix-OSS-self-hosted-eval-stack** lane. When DoorDash deploys Phoenix OSS self-hosted to evaluate an LLM-driven customer-support agent, the audit question isn't "what did each agent's LLM call do?" — it's "what was the full OpenTelemetry-GenAI-semantic-conventions-compliant span-model provenance: which spans were recorded per the OpenInference attribute schema, what was the per-span inference-trace + per-prompt + per-completion token + cost + latency, what was the per-tool-call + per-retrieval hit + per-agent-handoff + per-eval-decision, what was the per-payload-evals-decision that flowed into the production-monitoring-alert, and what downstream state changed?" Five gaps that map to that surface:

1. **End-to-end Arize AX + Phoenix OpenInference OpenTelemetry GenAI semantic-conventions span-model provenance join-table reconstructible per openinference_trace_id + otel_span_id.** Per **SOC 2 CC7.2 + EU AI Act Art. 12 + ISO 42001 §9.4** — 14-column reconstruction from a single `openinference_trace_id` + `otel_span_id` linking `otel_trace_id` + `otel_span_id` + `otel_parent_span_id` + `openinference_span_kind` + `gen_ai.request.model` + `gen_ai.request.max_tokens` + `gen_ai.request.temperature` + `gen_ai.usage.input_tokens` + `gen_ai.usage.output_tokens` + `gen_ai.usage.cost_usd` + `llm.prompt_template_hash` + `llm.completion_hash` + `tool_call_hash` + `retrieval_hit_hash` + `downstream_state_change_hash` for 7yr WORM + a quarterly reconstruction drill on the Slack + Uber + Adobe + Pepsi + DoorDash + Wayfair + Coinbase + Pinterest + Klaviyo + Reddit + 1000s-of-regulated-enterprise-AI-agent-teams stack. The auditor will want this bundled *per-openinference-trace*, not per-LLM-call — and Arize is the only platform that ships the OpenInference + OpenTelemetry GenAI semantic-conventions-compliant span-model surface as a first-class concept tied to the upstream-canonical-OSS-source (Arize is a major OpenTelemetry GenAI semantic-conventions contributor).

2. **Phoenix OSS self-hosted-eval-stack + OpenInference + OpenTelemetry GenAI semantic-conventions training-corpus license-provenance + copyright-attribution + copyleft-contamination evidence per EU AI Act Art. 53(d) GPAI training-data transparency + EU AI Act Art. 53(1)(b) copyright-summary + Apache-2.0-OSS-package-license-inheritance.** This is the unique Arize lane — the Phoenix OSS + OpenInference + OpenTelemetry GenAI semantic-conventions stack ships under Apache-2.0, and any Phoenix-self-hosted customer who inherits the upstream-OSS-anchor license chain carries a copyleft-contamination risk that does not exist in proprietary observability platforms. The audit surface is a per-source license-detection + per-source attribution-chain + per-source copyleft-flag + license-collision-flag + training-corpus-evidence join-table for the Phoenix OSS + OpenInference + OpenTelemetry GenAI semantic-conventions + per-evals-judge-model training corpus. The question isn't whether the OSS is well-licensed — it's whether the *customer's self-hosted deployment* is free of cross-license-contamination, and whether the per-eval-judge-model training corpus has the EU AI Act Art. 53(d) training-data transparency summary that the regulator will demand.

3. **Prompt-injection + retrieval-poisoning + tool-call-poisoning + agent-handoff-poisoning + per-eval-judge-poisoning detection evidence per OWASP LLM Top 10 LLM01 + ISO 42001 §6.1.4 + NIST AI RMF MEASURE.** This is the *unique Arize audit lane* — the OpenInference span-model is the natural instrumentation point for prompt-injection + retrieval-poisoning + tool-call-poisoning + agent-handoff-poisoning + per-eval-judge-poisoning detection across the entire downstream AI-agent deployment stack. The audit surface is a 9-column per-payload detection-log: `inbound_payload_hash` + `phoenix_detection_classification` + `openinference_span_flag` + `blocked_event_flag` + `downstream_state_change_flag` + `incident_response_escalation_id` + `per_tenant_quarantine_flag` + `regulator_notification_flag` + `regulatory_retain_until`. A Phoenix-flagged span that flowed into a production-monitoring-alert creates a chain-of-custody-evidence problem when the regulator reconstructs the OpenTelemetry trace — and Arize is the only platform where the span-model ties the eval-decision to the downstream-production-alert in one audit trail.

4. **Cross-tenant Arize AX enterprise-observability isolation-evidence for the multi-tenant SaaS serving 7B+ traces/day on the Slack + Uber + Adobe + Pepsi + DoorDash + Wayfair + Coinbase + Pinterest + Klaviyo + Reddit + 1000s-of-regulated-enterprise-AI-agent-teams stack.** Per **SOC 2 CC6.1 + GDPR Art. 28 + HIPAA + FedRAMP Moderate + EU AI Act Art. 10** — per-tenant isolation-test-result + per-tenant CMK/BYOK optionality + per-openinference-trace-no-leakage-evidence + per-tenant Phoenix-self-hosted-eval-residue-cleanup-procedure + completion-of-deletion timestamp + per-customer-no-leakage-evidence. The auditor will specifically probe OpenTelemetry-genai-semantic-conventions-compliant span-model isolation across tenants because the span-model is the natural cross-tenant-data-leakage surface if isolation is not per-tenant-per-span — and Arize's 7B+ traces/day volume means the per-tenant-span-isolation-evidence is the *primary* audit packet, not a secondary one.

5. **EU AI Act Annex III §4 high-risk classification for any Arize customer using OpenInference + Phoenix + Arize AX + per-eval-decision-provenance to greenlight an agent that materially influences a high-risk decision lane** (credit + employment + healthcare + education + law-enforcement + access-to-essential-services + biometric-identification + emotion-recognition + critical-infrastructure) per **EU AI Act Art. 6 + 14 + 27 + 43 + 72** (Aug 2026 GPAI enforcement + Annex III §4 conformity-assessment deadline). The Arize-supplied OpenTelemetry-GenAI-semantic-conventions-compliant-span-model-evidence package flows down to every Arize-using customer that used Phoenix + Arize AX output to greenlight a production agent — and a finding here propagates to *every* Slack + Uber + Adobe + DoorDash + Wayfair + Coinbase + Pinterest + Klaviyo + Reddit customer in the regulated-enterprise AI-agent stack.

---

**What I do:** 48-hour OpenInference + OpenTelemetry GenAI semantic-conventions + Phoenix-OSS-self-hosted-eval-stack audit. $500 flat. You get a written gap-analysis against SOC 2 CC7.2 + EU AI Act Art. 12 + 14 + Art. 53(d) + ISO 42001 §9.4 + OWASP LLM Top 10 LLM01 + ISO 42001 §6.1.4 + GDPR Art. 25 + FedRAMP Moderate + ISO 27001 + ISO 27701, with the actual 14-column OpenTelemetry-GenAI-semantic-conventions-compliant-span-model join-table schema + the Phoenix-OSS-self-hosted-eval-stack license-provenance schema + the per-payload detection-log schema + the per-tenant isolation-evidence package you can hand to the auditor — and the upstream-canonical-OSS-source language that flows down to every Phoenix-self-hosted + Arize-AX-using customer in your ecosystem (which is *every* Slack + Uber + Adobe + Pepsi + DoorDash + Wayfair + Coinbase + Pinterest + Klaviyo + Reddit + 1000s-of-regulated-enterprise-AI-agent-teams deployment, given Arize's 7B+ traces/day + OpenTelemetry GenAI semantic-conventions leadership + Foundation Capital + TCV + Battery + $61M raised).

**The ask:** 30-min call this week with you or your head of compliance/eng to walk through which of these 5 gaps your Slack + Uber + Adobe + Pepsi + DoorDash + Wayfair + Coinbase + Pinterest + Klaviyo + Reddit enterprise customers are already asking about — and whether the Arize / Phoenix / OpenInference team wants to be the canonical upstream answer for the next 1000+ OpenTelemetry-GenAI-eval-and-observability audit-cycles.

— Atlas
Talon Forge | atlas@talonforge.io