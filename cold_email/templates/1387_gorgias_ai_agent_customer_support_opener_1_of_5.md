# Email template — Lead 1387 — Gorgias (NEW VERTICAL #88 ai_agent_customer_support OPENER #1/5)

**To:** FORM:https://www.gorgias.com/demo (canonical demo form, NOT submitted per PITFALL #28)
**Subject:** Gorgias evidence-gap map — 48h, $500 (per-ticket + per-macro + per-automation audit-trail sub-$497 benchmark)

**Body:**

Hi Romain,

Talon Forge here — we run a three-tier evidence-gap audit for AI-first SaaS that maps the audit trail your EU AI Act + ISO 42001 buyers will ask for in their next procurement review.

Gorgias is the opener of our NEW VERTICAL #88 ai_agent_customer_support cohort — five-vendor benchmark on the ecommerce-helpdesk substrate. We're sequencing Gorgias, Ada, Forethought, Decagon, and a closer anchored to the agentic-handoff substrate.

For Gorgias specifically, the 22-column evidence wedge we ship in 48h:

```
tenant_id + gorgias_workspace_id + gorgias_user_id + gorgias_brand_id +
shopify_shop_id + bigcommerce_store_id + magento_store_id + woocommerce_store_id +
ticket_id + ticket_message_id + intent_classification_id + macro_id + macro_application_id +
rule_id + automation_id + flow_id + voice_call_id + sms_message_id + livechat_session_id +
campaign_id + audit_export_id + cross_tenant_no_bleed_invariant + replay_hash
```

What you get:

1. **$500/48h fixed-scope Gorgias evidence-gap map** — per-tenant + per-workspace + per-ticket + per-macro + per-rule + per-automation + per-flow + per-voice-call + per-sms + per-livechat + cross-tenant RBAC scope + EU AI Act Art. 13 logging + ISO/IEC 42001 AIMS clause 8.4 evidence.
2. **$497/mo quarterly refresh** — Gorgias version updates + new integration coverage + EU AI Act Art. 26 updates.
3. **$2,000 five-vendor ai_agent_customer_support COHORT BENCHMARK** at vertical close (Gorgias + 4 siblings + CLOSER #5) — cross-vendor intent-classification + macro-library + ecommerce-integration + AI-agent-handoff + voice/sms/livechat-channel comparison + EU AI Act readiness score per-vendor.
4. **$2,485 MRR ceiling** per YanXbt pattern (5 clients × $497/mo).
5. **$10,000 CLOSER-only cohort sponsorship tier** — locked until the cohort closes.

Booking a 20-minute working session this week? Reply with two windows and a calendar link and we'll send a video-confirmation the same day.

Romain — I can also send the 22-column CSV scaffolded against Gorgias's actual ticket schema if you want to pressure-test the audit-trail surface before procurement calls.

— Talon Forge (Atlas, autonomous)
talonforge.com · $500/48h · $497/mo · $2,000 cohort benchmark
