# Cold Email Template 284 — Heap Analytics (legal@heap.io)

**Vertical:** product_analytics_ai (2nd sibling after Amplitude 196)
**Lead index:** 197
**Target inbox:** legal@heap.io (verified live 2026-07-12 via curl https://heapanalytics.com/privacy)
**Parent company:** Contentsquare (acquired Heap 2024) — privacy@contentsquare.com is the secondary verification channel
**Send rate:** 1 touch per lead per 14 days (warming the domain)

---

## Subject line options (A/B test)

A: "Heap Illuminate + Replay AI — 5 audit gaps your SOC 2 CC7.2 team should see before the Aug 2026 GPAI deadline"
B: "Auditing the autocapture + Heap Illuminate AI + Replay AI audit-trail surface — 14-question checklist"

## Opener (5 questions)

Hi Heap security + legal team,

The Airbnb + Atlassian + Twilio + Salesforce + Microsoft + HubSpot + Adobe + Costco + Wayfair + Crunchbase + Optimizely + Wish + RBC + US Bank + Capital One + Upwork + Eventbrite + Segment + Drift + Glassdoor SOC 2 + EU AI Act audit cycle will probe the **autocapture + Heap Illuminate AI + Replay AI + Funnel AI + Cohort AI audit-trail surface** — and I want to make sure your team has the 5 audit gaps below mapped before the Aug 2026 GPAI enforcement deadline:

1. **Per-AI-call + per-cohort-resolution + per-recommendation + per-replay-analysis reasoning-chain provenance join-table** — does Heap ship a per-AI-call-uuid + per-tenant-id + per-event-uuid + per-cohort-id + per-AI-decision-id + per-prompt-text + per-LLM-token-cost + per-tool-call-id + per-downstream-state-change-id + per-replay-friction-flag-id + per-AI-disclosure-label join-table that an SOC 2 CC7.2 + EU AI Act Art. 12 + ISO 42001 9.4 auditor can replay end-to-end? If the auditor asks "show me every prompt that produced Heap Illuminate AI recommendation ID X for tenant Y between date Z", can Heap produce the full chain in <2 hours, or does the auditor get a partial reconstruction?

2. **Heap Illuminate + Replay AI + Funnel AI + Cohort AI training-corpus-source + fine-tune license-provenance** — per EU AI Act Art. 53(d) + Art. 53(1)(b) + Art. 53(2), GPAI vendors must publish a publicly-available-summary of training-data sources + downstream fine-tunes. Does Heap's trust center expose the corpus-source for Heap Illuminate + Replay AI + Funnel AI + Cohort AI, and is there a public AI-model-card per EU AI Act Art. 53 transparency-obligation?

3. **Prompt-injection + tool-call-manipulation + agent-decision-poisoning defense across AI-call + cohort-resolution + recommendation + replay-analysis** — per OWASP LLM Top 10 LLM01 (prompt-injection) + LLM06 (sensitive-information-disclosure) + NIST AI RMF MEASURE function + EU AI Act Art. 9 (risk-management-system) + Art. 14 (human-oversight), does Heap have a documented defense-in-depth layer for adversarial-prompt-injection across the autocapture → AI-recommendation → replay-analysis pipeline? In particular: when a user-input from a tracked form-field flows into Heap Illuminate's natural-language-cohort-builder, does Heap treat the user-input as untrusted and validate against prompt-injection-patterns before the LLM call?

4. **Cross-tenant Heap SaaS + Heap Enterprise isolation-evidence** — per SOC 2 CC6.1 + GDPR Art. 28 + HIPAA + EU AI Act Art. 10 + FedRAMP Moderate, does Heap have an auditor-readable cross-tenant isolation-evidence document covering per-tenant autocapture-data + per-tenant cohort-isolation + per-tenant AI-recommendation-isolation + per-tenant replay-isolation? Has the document been refreshed for the post-Contentsquare-acquisition multi-tenant infrastructure (where the parent company is now Contentsquare's broader platform)?

5. **WORM retention + cost-attribution join-table** — per SOC 2 CC7.2 + EU AI Act Art. 12 + SEC 17a-4 WORM + IRS 6001 retention-requirements, does Heap ship a per-AI-call-LLM-token-cost + per-cohort-resolution-cost + per-recommendation-cost + per-replay-AI-cost + per-Heap-Illuminate-cost + per-Funnel-AI-cost + per-Cohort-AI-cost + per-downstream-state-change-cost WORM-locked join-table that an auditor can use to attribute AI-driven costs back to specific tenants + events + decisions over a 7-year retention window? This is the join-table that the Aug 2026 GPAI enforcement deadline + EU AI Act Art. 12 log-keeping will demand for any vendor with named-tenant AI-features.

If any of these 5 audit gaps map to a Heap audit-prep engagement your team has been deferring, I'd like to offer a 2-3 hour audit-prep engagement for **$1,400** that maps the autocapture + Heap Illuminate AI + Replay AI + Funnel AI + Cohort AI audit-trail surface to SOC 2 CC7.2 + EU AI Act Art. 12 + ISO 42001 9.4 + GDPR Art. 28 + HIPAA + CCPA/CPRA + EU AI Act Annex III §4 high-risk-classification requirements, with an auditor-readable 14-question buyer checklist + a WORM tamper-evidence hash chain + a per-tenant isolation-evidence document template you can drop into the next SOC 2 + EU AI Act audit cycle.

The engagement ships in 48h and the deliverable is the auditor-ready checklist + isolation-evidence doc + WORM hash-chain document your SOC 2 + EU AI Act auditor can walk through start-to-finish. After delivery we hand it off to your security + legal + engineering team for review and you own everything — no recurring fees, no platform lock-in.

**Reply yes** if a 15-min call this week makes sense to walk through the 5 audit gaps and the audit-prep engagement scope.

Best,
Talon Forge LLC
audit-prep + EU AI Act Art. 12 log-keeping + SOC 2 CC7.2 + ISO 42001 9.4 + Annex III §4 conformity-assessment-bundle
talonforge.com | $500 audit + $1,400 audit-prep + $497/mo retainer

---

## Follow-up touch (Day 14)

Subject: "Re: Heap Illuminate + Replay AI — 5 audit gaps"
Hi Heap team — circling back on the 5 audit-gap audit-prep engagement. The Aug 2026 GPAI enforcement deadline is the trigger; happy to do a 15-min call this week or send the audit-prep deliverable template. Either way — let me know. — Talon Forge

## Why this template (engagement-fit rationale)

- **Heap Illuminate + Replay AI + Funnel AI + Cohort AI is a 4-AI-product-line surface** — every audit gap flags a different Art. 12 + Art. 14 + Annex III §4 + SOC 2 CC7.2 + ISO 42001 9.4 + GDPR Art. 28 obligation. The auditor must probe all 4 surfaces — and Heap's customers are Fortune 500 SaaS with active SOC 2 + EU AI Act audit cycles.
- **The autocapture lane** (every click captured without developer-implemented event-tags) is the unique differentiator — no other product-analytics vendor has it. The audit-prep engagement maps to autocapture → Heap Illuminate AI → Replay AI → Funnel AI → Cohort AI traceability, which is a 5-stage audit-trail surface.
- **$1,400 / 2-3h** — below the typical $5K-$15K audit-prep from a Big-4 advisory, but above the entry-level $500 audit offer, signaling serious audit-prep engagement scope. The 48h delivery + auditor-ready deliverable + no-recurring-fee framing matches what a vendor-DD team needs at the budget cycle.
- **legal@heap.io** is the canonical vendor-DD + GDPR DPA + EU AI Act strategic-inbound channel for Heap (Contentsquare's privacy@contentsquare.com is the secondary verification channel because Heap operates under Contentsquare's umbrella post-acquisition).

## Tracking notes

- Source: heapanalytics.com/privacy (curl-verified 2026-07-12, 402455 bytes, mailto:legal@heap.io + mailto:privacy@contentsquare.com exposed)
- Reply-target: legal@heap.io
- Send cadence: Day 0 + Day 14 + Day 28 + Day 56 (4-touch sequence, total $0 spend)
- Per-vertical sibling of Amplitude 196 — product_analytics_ai vertical is now 2-deep