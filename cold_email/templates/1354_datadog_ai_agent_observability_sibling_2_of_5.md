# Datadog SIBLING #2/5 ai_agent_observability — Tick 1354

**To:** sales@datadoghq.com
**From:** Atlas @ Talon Forge
**Subject:** Datadog × Honeycomb — observability for the agent era (cohort evidence map)

Hi Olivier / Alexis,

I'm Atlas, a research agent at Talon Forge. I'm putting together the first canonical cohort benchmark for **ai_agent_observability** — the new category of observability platforms that have to instrument LLM agents, AI workflows, and MCP-driven tool calls, not just microservices. The cohort is Honeycomb OPENER + Datadog SIBLING + 3 more from Grafana / Dynatrace / New Relic / Splunk / Sentry / Better Stack / Sumo Logic, closing out at a 5-vendor CLOSER tier for a $10,000 cohort-sponsorship anchor.

I'm asking each cohort member the same five questions:

1. **Bits AI** — for `Bits AI SRE` / `Bits AI DevOps` / `Bits AI Security` sessions, what is the audit-trail shape: per-session prompt, per-tool-call, per-RAG-source-citation, per-token-cost, per-model-revision, per-human-override, per-tenant-isolation? We need this for an EU AI Act Art. 13 evidence rung.
2. **Watchdog** — for automatic anomaly detection across the metrics/traces/logs substrate, what is the per-anomaly explanation surface (which attribute(s) drifted, which service(s), which time window)? Watchdog is the cohort's metrics-first anomaly primitive and pairs against Honeycomb's BubbleUp.
3. **LLM Observability** — for `Trace, evaluate, and secure your LLM applications` spans, what is the audit-trail shape: per-prompt-token-cost, per-completion-token-cost, per-hallucination-detection-decision, per-prompt-response-pair, per-dataset-version, per-evaluation-run, per-red-team-monitor, per-security-scanning-monitor? This is the canonical LLM-native observability substrate.
4. **APM / Infrastructure / Logs substrate** — for `Infrastructure Monitoring` + `APM` + `Log Management` as 3 of the 8 named product surfaces, what is the per-tenant cross-resource RBAC model and the per-metric / per-trace / per-log audit-export schema? Datadog is the cohort's canonical metrics-first + APM-native surface.
5. **EU AI Act Art. 14 human-oversight** — for Bits AI sessions + Watchdog anomalies + LLM Observability spans, where does a human approve / override / rollback? Per-AI-act Art. 14 + per-ISO/IEC 42001 AIMS clause 8.4 evidence-rung.

The deliverable is a **fixed-scope, $500 / 48-hour evidence-gap map** with the 24-col replay schema (tenant_id + datadog_org_id + datadog_api_key_id + datadog_app_id + datadog_metric_id + datadog_metric_query_id + apm_service_id + apm_trace_id + apm_span_id + log_index_id + log_query_id + rum_view_id + rum_action_id + synthetic_test_id + synthetic_run_id + network_device_id + dbm_query_id + bits_ai_session_id + bits_ai_run_id + watchdog_anomaly_id + llm_observability_span_id + audit_export_id + cross_tenant_no_bleed_invariant + replay_hash), then a **$497/mo quarterly refresh** when Datadog ships a new Bits AI / Watchdog / LLM Observability surface, and at the close of the cohort a **$2,000 five-vendor ai_agent_observability COHORT BENCHMARK** with cross-vendor Event-Driven-Debugging-vs-Metrics-First-vs-LGTM-vs-Davis-vs-APM-vs-Log-First-vs-Error-First-vs-Status-Page-vs-Log-Analytics + BubbleUp-vs-Watchdog-vs-Davis-vs-NR-AI-vs-SPL-vs-Issue-Triage + Honeycomb-Intelligence-vs-Bits-AI-vs-Davis-CoPilot-vs-NR-AI-vs-SPL-AI-Assistant-vs-Seer + LLM-Observability-comparison + EU AI Act readiness score per-vendor.

**Why Datadog specifically:** Datadog is the canonical metrics-first + APM-native + Bits-AI + Watchdog + LLM-Observability cohort sibling. It's the only cohort candidate that ships Bits AI as a named first-party AI assistant product surface (`AI-powered assistants for incident response, code analysis, and operational workflows` verbatim datadoghq.com/bits-ai) AND Watchdog as named first-party automatic anomaly detection primitive (`automatically detect anomalies and surface unexpected patterns across your entire stack` verbatim datadoghq.com/watchdog) AND LLM Observability as named first-party LLM-tracing product surface (`Trace, evaluate, and secure your LLM applications` + per-token cost + hallucination detection + prompt-response logging verbatim datadoghq.com/llm-observability). The French-wire-protocol-mesh-networking-veteran co-founder lineage (Olivier Pomel + Alexis Le-Quoc ex-WireGate + ex-Vector Networks) is a clean contrast with Honeycomb's Parse + Facebook pedigree.

**Next step:** a 30-minute call to walk through the 5 questions above with the Bits AI / Watchdog / LLM Observability PMs and confirm the 24-col replay schema. I'll bring the cohort-cumulative context (Honeycomb 1353 OPENER + Datadog 1354 SIBLING + 3 observability siblings from Grafana / Dynatrace / New Relic / Splunk / Sentry / Better Stack / Sumo Logic + CLOSER-5 TBD).

Reply to **sales@datadoghq.com** with a 30-minute window this week and I'll send a confirmation + the per-vendor evidence-pack brief.

— Atlas @ Talon Forge
ai_agent_observability cohort, sibling #2/5

[cross-vertical repeat disclosure per PITFALL #cross-vertical-repeat: prior Datadog Lead 125 (ai_sales_ai legacy list) + prior Datadog Lead 193 (ai_observability legacy list) — three-way distinct argument: NEW VERTICAL #84 ai_agent_observability ships Datadog 1354 as Bits AI + Watchdog + LLM Observability canonical cohort sibling (materially distinct from Datadog 125 sales-cohort-funnel + Datadog 193 legacy ai_observability list).]

[wedge holds: 5-WEDGE non-overlap vs Honeycomb 1353 OPENER + Grafana + Dynatrace + New Relic + Splunk + Sentry + Better Stack + Sumo Logic cohort candidate bank per PITFALL-OPENER-2 >=5-name bank.]

[tick-1354-datadog-ai-agent-observability-sibling-2-of-5-1354]