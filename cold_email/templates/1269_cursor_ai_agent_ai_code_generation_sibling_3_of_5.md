# 1269 — Cursor (Anysphere) — SIBLING #3/5 ai_agent_ai_code_generation

**To:** hi@cursor.com (first-party verified 2026-07-25 on cursor.com/about)
**Subject A:** Cursor + 5-WEDGE cohort question — ai_agent_ai_code_generation sibling 3/5
**Subject B:** Anysphere Composer Tab + Background Agents — 22-field per-Cursor-session replay receipt short audit
**Subject C:** Cursor privacy mode + Composer Tab — 5 evidence questions + $500/48h

Hi Aman + Michael,

Atlas here — closing the cohort benchmark for **NEW VERTICAL #68 ai_agent_ai_code_generation** (OpenHands 1267 OPENER → Cognition Devin 1268 SIBLING #2/5 → Cursor 1269 SIBLING #3/5). I built a 22-field per-Cursor-session + per-Composer-Tab + per-Background-Agent + per-Tab-completion + per-Agent-Mode + per-Bugbot replay receipt for SOC 2 + GDPR + EU AI Act Art. 13 logging + Art. 14 human-oversight readiness and want to sanity-check the wedge before it goes public.

**5 evidence questions** (60-second reads, no prep needed):

1. **Composer Tab + Background Agents hybrid execution** — first-party 2026-06-26 Background Agents launch means long-running coding tasks execute in Cursor's cloud while the editor stays local. Does the per-session replay receipt join `composer_tab_id + background_agent_id + editor_session_id + file_edit_id + file_diff_hash + model_id + token_input_count + cost_usd` so a Fortune 100 GitHub Admin can attribute which AI-generated commits came from in-IDE Composer Tab edits vs cloud-orchestrated Background Agents vs Tab autocomplete?

2. **Privacy Mode zero-retention + no-training guarantee** — first-party cursor.com/privacy says Cursor does not use customer code for model training by default, and Privacy Mode enforces this at the per-session level. Does the per-session replay receipt pin `privacy_mode_enabled` (zero-retention + no-training boolean) as a queryable primitive so enterprise customers can prove the Privacy Mode guarantee per-AI-commit without manual log exports?

3. **Cursor Enterprise SCIM + audit log + custom data retention** — first-party cursor.com/enterprise says SOC 2 Type II + SCIM + SSO + audit log + custom data retention. Does the per-session replay receipt join `cursor_workspace_id + anysphere_org_id + audit_export_id + file_edit_id` so an enterprise compliance officer can replay any AI-generated commit across the SCIM-provisioned user boundary?

4. **Anysphere Series D $29.3B + cap table** — first-party 2026-05-29 Series D reporting: $2.3B raised at $29.3B valuation led by Accel + Coatue + NVIDIA + Google + Andreessen Horowitz + Thrive + DST Global + Spark. Does the per-session replay receipt pin `model_provider` (Claude / GPT-5 / Gemini / Cursor-native sub-processor) so a Fortune 100 procurement officer can attribute sub-processor exposure per-AI-commit?

5. **Composer Tab multi-file edit primitive vs Cognition Devin's single-product substrate** — first-party cursor.com/composer says Composer Tab ships as an in-IDE multi-file edit primitive with diff visualization + accept/reject per-file. Does the per-session replay receipt join `file_edit_id + file_diff_hash + human_override_id` so an EU AI Act Art. 14 human-oversight auditor can prove per-file human-override decisions were captured for every AI-suggested edit?

**Offer ladder:**
- **$500 / 48h** — fixed-scope per-Cursor-session + per-Composer-Tab + per-Background-Agent 22-field evidence-gap map (delivered as a 22-field CSV + 5-page audit memo).
- **$497 / mo** — quarterly refresh retainer (Composer schema version-pinning + Background Agents release notes + Privacy Mode roadmap + new Cursor-native model updates).
- **$2,000** — five-vendor ai_agent_ai_code_generation cohort benchmark at close (OpenHands 1267 OPENER + Cognition Devin 1268 SIBLING #2 + Cursor 1269 SIBLING #3 + SIBLING #4 + CLOSER #5).

**What I do NOT need:** a phone call, a demo, a security review. Just an email reply telling me which of the 5 questions the public docs already answer (and a link), and which need a 22-field Cursor replay primitive to be queryable.

— Atlas @ Talon Forge
[tick-1269-cursor-ai-agent-ai-code-generation-sibling-3-of-5]
