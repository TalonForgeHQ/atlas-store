# 1101 — Vendor Management / Procurement Follow-Up — Closed ai_agent_operations Cohort

**Purpose:** Stage a vendor-management / procurement-persona second-touch for the closed `ai_agent_operations` cohort (Resolve AI 1094 → incident.io 1095 → Rootly 1096 → Causely 1097 → FireHydrant 1098). This is a buyer-persona variant, not a duplicate lead.
**Frame:** vendor risk re-evaluation when an AI agent ops platform changes hands (vendor-of-record, sub-processor, data-residency, contract SLA, evidence export portability).
**Status:** TEMPLATE ONLY — no email or form submission is claimed.

## Why this frame now

Vendor-management and procurement teams own the contract surfaces that decide whether an AI ops tool keeps its evidence trail through a re-org. Whether the trigger is the Freshworks → FireHydrant handoff, a Cause-of-Causely investment, a Rootly enterprise re-tier, or a Resolve AI self-hosted → managed migration, the procurement question is the same: can the audit trail, sub-processor list, and contractual SLA move with the product?

## Subject options

1. Re-evaluating an AI ops vendor after the announcement
2. One vendor change, one row of evidence your auditors can read
3. A 48-hour vendor-risk evidence map after an AI ops handoff

## Body

Hi {{first_name}},

When an AI ops vendor's ownership, sub-processor chain, or hosting boundary moves, the audit trail you bought usually does not move with it. The vendor-management question is concrete: for one production incident, can you reproduce the vendor of record, the sub-processor list, the data-residency pin, the contractual SLA, and the final audit export under the new ownership?

I map one vendor change into a portable receipt joining:

`vendor_of_record + sub_processor_id + sub_processor_change_id + data_residency_region + hosting_mode + contract_sla_id + incident_id + service_catalog_id + service_owner_id + runbook_id + runbook_step_execution_id + ai_insight_id + ai_enriched_incident_id + workflow_or_model_version + human_override_id + cross_tenant_no_bleed_audit_trail + retention_window + deletion_propagation_id + audit_export_id`

The fixed scope is **$500 / 48 hours**. I return a red/amber/green gap map across the five vendors, the five highest-priority missing joins for vendor-risk sign-off, and a blank replay schema for one real incident. Ongoing vendor-change coverage can be covered by a **$497/month quarterly refresh**. The complete five-vendor `ai_agent_operations` benchmark is **$2,000**.

## Five replay questions

1. Which vendor of record, sub-processor chain, and `sub_processor_change_id` correspond to the moment the AI ops platform changed hands?
2. Which `data_residency_region` and `hosting_mode` (cloud / dedicated / self-hosted / hybrid / air-gapped) apply to the new boundary, and which customer data stays inside it?
3. Which `contract_sla_id`, `incident_id`, and `runbook_step_execution_id` produced the customer-impact record, and which new vendor milestone recorded it?
4. Which AI Insights / AI Enriched Incidents output, workflow/model version, and `human_override_id` explain the remediation across the boundary?
5. Can the retention window, deletion-propagation row, `cross_tenant_no_bleed_audit_trail`, and final `audit_export_id` be reproduced across the new boundary?

Worth sending the one-page blank sample for {{company}}?

Best,
Atlas