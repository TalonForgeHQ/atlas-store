# Letta SIBLING #3/5 — stateful-agent memory evidence-gap map

**Vendor:** Letta (letta.com) | @Letta_AI
**Cohort:** ai_agent_conversation_memory (SIBLING #3/5 after Mem0 + Zep)
**Route:** FORM:https://www.letta.com/contact (verified, not submitted)
**Founders:** Charles Packer + Sarah Wooders (Co-Founder & CTO), verified first-party

## Subject variants

1. **Charles — one receipt for every self-editing Letta memory block**
2. **Letta memory replay: source turn → block edit → next agent action**
3. **Five evidence gaps across Letta core, archival, and recall memory**

## Email

Charles — five quick questions before I send the full map:

1. Can an enterprise export one immutable receipt joining tenant, Letta agent, conversation, source message, memory block, block version, self-edit operation, and downstream model/tool call?
2. When an agent rewrites core memory, can the buyer reconstruct the previous block, the evidence used, the new block, the context-window version, and the next action influenced by that edit?
3. Can archival and recall retrievals prove user/tenant isolation, source lineage, retention, deletion, and which retrieved token span reached the model?
4. For sleep-time agents and git-backed context repositories, does every asynchronous memory improvement preserve run ID, commit ID, reviewer/override, rollback commit, and audit export?
5. Can a GDPR erasure request replay the deletion cascade across core blocks, archival memory, recall history, context repositories, embeddings, and downstream caches?

Letta's differentiated surface is the stateful agent harness: self-editing memory blocks plus core, archival, and recall memory organized like an operating-system hierarchy. Procurement needs the complete memory-edit-to-action chain, not another trace dashboard.

The fixed-scope deliverable defines a 22-column Letta memory-action receipt and a PASS / PARTIAL / GAP table, sample replay row, retention/responsibility matrix, deletion cascade, and prioritized remediation list.

**$500 / 48h evidence-gap map** or **$497/month quarterly refresh**. The completed five-vendor benchmark will be **$2,000**.

Reply “send” and I will forward the five-gap map.

— Atlas @ Talon Forge
https://talonforgehq.github.io/atlas-store
