# Zep Temporal-Knowledge-to-Agent-Action Follow-up

**Template family:** `ai_agent_conversation_memory` — follow-up for verified Lead 1038
**Status:** TEMPLATE ONLY — no email or form submission is claimed
**Offer:** $500 / 48-hour evidence-gap map, $497/month refresh, or $2,000 five-vendor benchmark

## Subject options

1. `Zep fact edge → agent action: one replay row`
2. `Can Zep reproduce which temporal fact changed the answer?`
3. `A GraphRAG deletion-cascade test for agent memory`

## Open

Daniel — following up with one concrete way to pressure-test Zep's temporal knowledge graph and agent-memory evidence.

I mapped one replay row from source message and extracted fact through graph edge, validity interval, retrieval, prompt context, model/tool action, human override, rollback, and deletion export. It turns “the agent used memory” into evidence a buyer can inspect.

## Five-question replay

1. Which source message created or superseded each retrieved fact and graph edge?
2. Can the buyer export the fact, validity interval, source, graph version, retrieval score, and prompt span together?
3. Which downstream response or tool action consumed that retrieved memory?
4. Can a reviewer replay the same historical query, reverse an incorrect fact, and prove the rollback reached derived summaries and caches?
5. Does user deletion cascade across messages, summaries, entities, edges, embeddings, and downstream stores?

## CTA

If useful, reply **send** and I will return the sample row plus a PASS / PARTIAL / GAP map. The fixed-scope deliverable is **$500 / 48 hours**; quarterly refresh is **$497/month**; the completed five-vendor comparison is **$2,000**.

— Atlas @ Talon Forge  
https://talonforgehq.github.io/atlas-store
