# Lead 1104 — ilert (OPENER #1/5 ai_agent_operations_eu_sovereign)

**Subject lines:**
1. ilert AI SRE + DORA evidence map — $500/48h, EU-sovereign
2. ilert AI handoff receipt → audit export for DORA-aligned buyers
3. ilert vs Resolve AI vs Rootly vs Causely vs FireHydrant — $2,000 EU-sovereign benchmark

Hi {{first_name}},

ilert's cohort-unique wedge is the EU-sovereign + AI-SRE + DORA-aligned + human-in-the-loop handoff receipt: the replay can show which `ilert_ai_agent_id` ran an autonomous investigation, which `ilert_ai_investigation_id` recorded the chain of evidence, which `ilert_ai_handoff_id` + `ilert_ai_handoff_reason` documented the human approval, which `ilert_status_page_incident_id` + `ilert_status_page_component_id` was published, and which `dora_compliance_package_id` + `audit_export_id` is portable to a regulated EU financial-sector buyer.

A fixed-scope ilert EU-sovereign AI-SRE evidence-gap map is **$500 / 48 hours** — one normalized production replay with the 20-column receipt joining tenant, ilert organization, alert source, alert, incident, on-call schedule, escalation policy, ilert AI agent, AI investigation, AI handoff, human override, ChatOps channel, status page incident, status page component, status page subscriber, postmortem, postmortem action items, DORA compliance package, and audit export.

For teams comparing the broader ai_agent_operations_eu_sovereign landscape, Atlas also offers a **$497/month quarterly refresh** or a **$2,000 five-vendor benchmark** covering ilert + four sibling vendors that match the EU-sovereign + ISO 27001 + DORA + AI-SRE + human-in-the-loop wedge.

Commercial route: `FORM:https://www.ilert.com/get-demo` — verified first-party demo page (JS-rendered form, not submitted) — or `mailto:sales@ilert.com` for pentest report and ISO 27001 certificate requests per the /security page. The normalized replay ends at `audit_export_id` for a portable evidence handoff into a DORA Article 28 ICT-third-party register.

— Atlas @ Talon Forge

## Five replay questions

1. Which `ilert_ai_agent_id` performed the autonomous investigation, and which `ilert_ai_investigation_id` recorded the chain of evidence (sources queried, hypotheses tested, alerts correlated)?
2. Which `ilert_ai_handoff_id` + `ilert_ai_handoff_reason` documents the human approval before any high-risk action, and which `ilert_human_override_id` was logged?
3. Which `ilert_alert_source` + `ilert_alert_id` triggered, and which `ilert_on_call_schedule_id` + `ilert_escalation_policy_id` fired across Twilio + Vonage + MessageBird?
4. Which `ilert_status_page_incident_id` + `ilert_status_page_component_id` + `ilert_status_page_subscriber_id` was published, and which `ilert_chatops_channel_id` carried the orchestration?
5. Which `dora_compliance_package_id` + `audit_export_id` reproduces the chain with no cross-tenant bleed and no non-EU data routing?

## Evidence and compliance posture

- First-party title/description: "ilert: AI-First Incident Management Platform. With Privacy in Mind"; "AI-first platform for alerting, on-call management, and status pages."
- First-party imprint: ilert GmbH, Bayenstraße 65, 50678 Köln, Germany; VAT ID DE277768937; Amtsgericht Köln HRB 83715; Managing Directors Birol Yildiz + Christian Fröhlingsdorf.
- First-party leadership: LinkedIn first-party post 2026-07-22 bylines "Our CEO and Co-founder, Birol Yildiz" (DOP 360: What Is an AI SRE?).
- First-party security: ISO 27001 certified verbatim; GDPR compliant; external Data Protection Officer (dsb@secjur.com); DORA Compliance Package + standard DORA Addendum published; AWS Frankfurt primary + Dublin DR; RTO ≤ 60 min / RPO ≤ 15 min.
- First-party customers: Bertelsmann, IKEA, REWE digital.
- First-party products: Alerting, On-Call Management, Call Routing, Status Pages, ChatOps, ilert AI, ilert Terraform provider.

**Verification:** mailto:sales@ilert.com + FORM:https://www.ilert.com/get-demo — both verified first-party channels; SMTP/form gated; `$0 sent / $0 received`. OPENER #1/5, canonical slug `opener-1-of-5`, vertical #38 ai_agent_operations_eu_sovereign.
