# 813_everlaw.md — Everlaw (ai_legal_hold_ediscovery sibling #4/5)

> Stage: 5-question audit letter + 3 subject A/B/C + 3 engagement options.
> Cohort: ai_legal_hold_ediscovery (Relativity 810 OPENER + Exterro 811 #2 + Veritas 812 #3).
> Route: FORM:https://www.everlaw.com/contact-sales/ (verified live 2026-07-21).

## Subject Lines (A/B/C)

A. **Everlaw Storybuilder AI audit — 5 questions before AmLaw procurement signs**
B. **Per-document coding provenance for Everlaw AI-Assisted Review — 5-question gap map**
C. **Rule 502(d) + ABA Model Rule 1.6 + EU AI Act Art. 14(4) for Everlaw Assistant — 5 questions**

## 5-Question Audit Letter Opener

1. **What is the per-document continuous-active-learning (TAR 2.0) relevance-score provenance for Everlaw AI-Assisted Review** — does the per-document decision record persist the seed-coding set, the per-issue-coding tag reconciliation, the per-cluster Conceptual Analytics hash, the per-custodian family member dedup event, the per-foreign-language translation-isolation event, and the per-reviewer reassignment audit-log entries?
2. **What is the Storybuilder AI per-case-narrative-assembled provenance** — for each auto-generated witness-by-witness or exhibit-by-exhibit exhibit-list, does the audit ledger persist the per-source-document bates range, the per-deposition transcript page-line citation, the per-coding-decision timestamp, the per-Rule-26(b)(1) relevance-tag, the per-Rule-26(b)(5)(B) privilege log entry, and the per-confidentiality-tier redaction event so the AmLaw 100 firm can reproduce the narrative for a Rule 26(f) conference or an appellate record?
3. **What is the Everlaw Assistant LLM per-prompt provenance cascade** — for each per-matter generation, does the platform publish the per-prompt hash, the per-citation block (per-source-document bates), the per-matter-wall authorization snapshot, the per-cross-matter no-bleed invariant, the per-sub-processor invocation (OpenAI / Anthropic / Google / Mistral / internal), the per-retention window, and the per-region routing decision?
4. **What is the Everlaw Federal (GovCloud + FedRAMP-Moderate + StateRAMP + ITAR + EAR + DFARS 7012 + CMMC L2) SKU-vs-Commercial-SKU boundary** — for state-AG or federal agency customers (DOJ + SEC + FTC + EPA), which Everlaw AI Suite surfaces are scoped into the GovCloud FedRAMP-Moderate boundary vs. the commercial SaaS boundary, and which sub-processor cascade (OpenAI GovCloud / Anthropic Claude for Government / Google Vertex AI Government / Cohere for Government / Azure OpenAI for Government) is the designated FedRAMP-authorized route?
5. **What is the per-custodian family member dedup + per-foreign-language detection + per-privilege-log Rule 502(d) clawback-order-protocol + per-Order-Governing-Protective-Order confidentiality-tier enforcement** for cross-border matters where U.S. discovery reaches into EU + UK + Switzerland + Brazil + Australia + Singapore + Japan + Canada + India + China data-subject populations — how does the platform reconcile FRCP 34(b) U.S. production-format with GDPR Art. 6 + Art. 9 + Art. 22 + UK GDPR + Brazil LGPD + APPI + PIPEDA + Australia Privacy Act + Singapore PDPA + China PIPL lawful-basis requirements without producing privileged or personally-identifying content beyond the Rule 502(d) order scope?

## Body (~440 words)

Atlas audits Everlaw for the **AmLaw 100 law-firm buyer question** that no other ai_legal_hold_ediscovery cohort member in our pipeline answers: **per-document coding-decision provenance under Rule 26(f) conference, Rule 34(b) production-format, Rule 502(d) clawback-order, ABA Model Rule 1.6 confidentiality-tier, EU AI Act Art. 14(4) human-oversight, and Rule 26(b)(5)(B) privilege-log requirements — at the per-document and per-prompt granularity that a U.S. district court or a state-AG subpoena actually demands.**

**Why Everlaw is sibling #4/5 and not a duplicate of Relativity 810 / Exterro 811 / Veritas 812.** The cohort wedge is non-overlapping:

- **Relativity 810** (OPENER) is the **enterprise-corporate in-house legal-counsel eDiscovery + aiR + MCP-server + Calder7 security** lane. Per-custodian preservation + aiR per-decision audit provenance + FedRAMP-Moderate + StateRAMP + TX-RAMP inheritance path. Anchors the enterprise corporate counsel buyer question.
- **Exterro 811** is the **enterprise in-house legal + privacy + breach-notification + ARMOUR forensics + FTK** lane. Smart Breach Review + Subpoena Manager + per-state-attorney-general breach-notification response + per-image SHA-256 chain-of-custody + per-state-deadline timer. Anchors the in-house legal + privacy-operations buyer question.
- **Veritas 812** is the **enterprise data-protection + Enterprise Vault + NetBackup + Data Insight** lane. Email-archive + IM-archive + Slack-archive + Teams-archive + Microsoft 365 + Google Workspace + per-matter retention-vs-erasure reconciliation + Object Lock WORM + InfoScale storage-virtualization. Anchors the enterprise data-protection + archival-evidence-capture buyer question.
- **Everlaw 813** (this lead) is the **AmLaw 100 law-firm plaintiff-defense ediscovery + Rule 26 + Rule 34 + Rule 502(d) + ABA Model Rule + EU AI Act Art. 14(4) + FedRAMP-Moderate GovCloud self-hosted** lane. Everlaw AI Suite (Storybuilder AI + AI-Assisted Review TAR 2.0 + Conceptual Analytics + Predictive Coding + Everlaw Assistant LLM) + Everlaw Federal GovCloud self-hosted-air-gapped for DOJ + SEC + FTC + state-AGs. Anchors the AmLaw 100 law-firm + federal-agency plaintiff-defense investigation buyer question that no other cohort sibling owns.

**The 5-question audit letter is mapped to a 19-column provenance cascade:** per-document coding decision → per-Issue-coding-tag → per-Cluster-Conceptual-Analytics-hash → per-Custodian-family-dedup → per-Foreign-language-translation-isolation → per-Reviewer-reassignment → per-Storybuilder-AI-source-bates-range → per-Deposition-page-line-citation → per-Rule-26(b)(1)-relevance-tag → per-Rule-26(b)(5)(B)-privilege-log → per-Rule-502(d)-clawback-protocol → per-OGPO-confidentiality-tier → per-Everlaw-Assistant-per-prompt-hash → per-citation-block → per-matter-wall-authorization → per-sub-processor-invocation → per-retention-window → per-region-routing → per-FedRAMP-Moderate-GovCloud-sku-scope-decision. Each column maps to FRCP 26(f) + 34(b) + 26(b)(5)(B) + 502(d) + ABA Model Rule 1.1 + 1.6 + 5.5 + 5.3 + UK SRA + EU Member State Bar + EU AI Act Art. 14(4) + Art. 50 + Art. 53(1)(b) + FedRAMP-Moderate + StateRAMP + ITAR + EAR + DFARS 7012 + CMMC L2 + SOC 2 Type II + ISO 27001 + ISO 27017 + ISO 27018 + HIPAA + GDPR + UK GDPR + CCPA + LGPD + APPI + PIPEDA + Australia Privacy Act + Singapore PDPA + China PIPL.

The 14-20 column provenance cascade is the deliverable Atlas hands a buyer AmLaw 100 GC or a U.S. state-AG chief-deputy who asks: *what does the per-document, per-prompt, per-custodian, per-jurisdiction decision record actually contain, and can we reproduce it for a Rule 26(f) conference, a state-AG subpoena, a DPA audit, a US-EU cross-border SCC review, or an EU AI Act Art. 14(4) human-oversight review?*

## 3 Engagement Options

**Option A — $500 / 48h Evidence-Gap Map:** A fixed-scope, 48-hour deliverable that maps the 5-question audit letter answers against your current Everlaw contract, your current Rule 26(f) conference-stance, your current ABA Model Rule 1.6 confidentiality-tier posture, and your current cross-border DPA posture. Output: a single PDF + a single CSV with the 19-column provenance cascade and the 27-row compliance-map gap rows.

**Option B — $497 / month Quarterly Refresh:** A subscription that re-runs the 5-question audit letter every 90 days against the latest Everlaw AI Suite release, the latest EU AI Act + Rule 502(d) + ABA Model Rule guidance, and the latest per-state-AG breach-notification-state-deadline changes. Output: a quarterly PDF + CSV delta report.

**Option C — $497 / month × 5-client YanXbt Projection:** The 5-client retainer pattern: one Atlas profile per AmLaw 100 firm (or per state-AG) on your platform, $497/month each, with each profile carrying the 19-column provenance cascade + the 27-row compliance-map + the per-quarterly Rule 502(d) clawback-order-protocol reconciliation. 5 clients × $497/month = $2,485/month MRR per Atlas operator, matching the verified YanXbt IBuzovskyi x.com/IBuzovskyi pattern.

## PS

Everlaw's first-party commercial route is FORM:https://www.everlaw.com/contact-sales/ (verified live 2026-07-21, server-rendered, HTTP 200). No general-business inbox is published on everlaw.com/contact or everlaw.com/company/team. Atlas does not guess inboxes, does not submit forms, and does not claim delivery. SMTP and authenticated community routes remain gated; $0 sent / $0 received this tick.
