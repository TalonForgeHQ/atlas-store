# Lead 1210 — Temporal — OPENER #1/5 ai_agent_workflow_orchestration

**Tick:** 2026-07-25 ~00:55 UTC
**Vendor:** Temporal (temporal.io)
**Cohort role:** OPENER #1/5 of NEW VERTICAL #60 ai_agent_workflow_orchestration
**Commercial route:** FORM:https://temporal.io/contact
**Status:** NOT submitted (SMTP/form gated)

---

## 3 Subject lines (in priority order)

1. `Temporal evidence-gap map — durable execution substrate + signal/query/update primitives for AI-agent workloads`
2. `Workflow-as-code audit for Temporal + 4 future ai_agent_workflow_orchestration siblings — $500/48h`
3. `Cohort benchmark: Temporal + Prefect + Inngest + Restate + DBOS — workflow orchestration evidence-replay`

## 3-line body

Hi Samar Abbas + Maxim Fateev — I run a $0→$1K B2B SaaS evidence-replay practice at Talon Forge and I'm opening a NEW VERTICAL #60 ai_agent_workflow_orchestration cohort with Temporal as OPENER #1/5, anchored by 21,824-star Apache-2.0 temporalio/temporal (engine) + MIT SDKs + first-party SOC 2 Type II via trust.temporal.io + Nexus cross-namespace federation (GA 2026) + signal/query/update triadic workflow-control primitives (cohort-unique agent-side door for human-approval mid-run + state-lookup + state-mutate-with-validation).

I'd like to send a $500/48h fixed-scope Temporal evidence-gap map covering (1) per-workflow-type durability comparison, (2) retry-semantics audit for activity + workflow + signal + query + update primitives, (3) cross-language SDK coverage matrix (Go + Python + TypeScript + Java + .NET + PHP + Ruby), (4) Nexus federation analysis, (5) replay-hash invariant.

If useful I can also quote $497/mo quarterly refresh + $2,000 five-vendor cohort benchmark at cohort close. Form submitted via https://temporal.io/contact — happy to walk through the rubric on a 20-min call.

— Atlas (Talon Forge LLC)

## 5-question audit-replay rubric

1. Which Temporal workflow primitives are first-class API surface versus wrappers over the SDK? (signal + query + update + Nexus as named primitives)
2. Can the durable execution substrate handle long-running workflows (>30 days) with deterministic replay after a process crash?
3. How does Temporal's Nexus federation primitive enable cross-namespace + cross-cluster + cross-cloud workflow composition?
4. What compliance evidence is first-party at trust.temporal.io (SOC 2 Type II + HIPAA BAA + GDPR + ISO 27001)?
5. Does Temporal ship multi-language SDK parity for workflow + activity + signal + query + update primitives across Go + Python + TypeScript + Java + .NET + PHP + Ruby?

## Cohort positioning

ai_agent_workflow_orchestration NEW VERTICAL #60 OPENER #1/5. Cohort opens with Temporal as the canonical OSS durable execution substrate. 4 OPEN slots remaining for SIBLINGS #2-5/5 (Prefect + Inngest + Restate + DBOS candidates).

## Offer ladder

- $500/48h fixed-scope Temporal evidence-gap map (per-workflow-type durability comparison + retry-semantics audit + cross-language SDK coverage matrix + Nexus federation analysis + replay-hash invariant)
- $497/mo quarterly refresh (Temporal version updates + new SDK coverage + EU AI Act Art. 10 alignment updates)
- $2,000 five-vendor ai_agent_workflow_orchestration cohort benchmark at close (Temporal + Prefect + Inngest + Restate + DBOS)
- $2,485 MRR ceiling per YanXbt pattern (5 clients × $497/mo)

## 3-bump follow-up cadence

- Bump 1 (+3 days): Re-send body only, reference temporalio/temporal 21,824 stars + first-party trust.temporal.io SOC 2 Type II evidence
- Bump 2 (+8 days): Reference NEW VERTICAL #60 cohort progress (now at 2/5 after SIBLING #2 ships) + replay-hash invariant detail
- Bump 3 (+14 days): Final bump with cohort benchmark discount + 20-min call CTA

## Route-safety rules

- Form route only — never auto-submit
- No @mention of Temporal staff in cold form
- $0 sent / $0 received claim must remain accurate
- All first-party evidence verbatim from temporal.io + temporalio/temporal README + trust.temporal.io
