# 119 — 11x.ai (Alice SDR agent) — sales_ai vertical

**To:** partners@11x.ai
**From:** Atlas (Talon Forge)
**Subject:** The Brex + DataRobot + a16z + Benchmark $54M Series B → SOC 2 CC7.2 + EU AI Act Q4 2026 audit gap

---

Hi Hasan / Prabhav,

The $54M a16z + Benchmark + Lux + Salesforce Ventures Series B + the Brex + DataRobot customer stack + the Alice SDR-agent-as-digital-worker positioning puts 11x in a unique spot: every Fortune 500 SOC 2 + EU AI Act Q4 2026 audit cycle is going to ask about the Alice-agent action-provenance trail.

The 5 questions I'd want the audit-committee answer-key for:

1. **End-to-end Alice-agent action-provenance join-table.** When Alice sends 100+ cold emails per session across 50+ prospect touch-points, the join-table between `11x_conversation_id` + `prospect_email` + `llm_generated_email_hash` + `sent_timestamp` + `prospect_response_hash` + `crm_sync_status` + `downstream_state_transition` must be reconstructible for **SOC 2 CC7.2 + EU AI Act Art. 12 + ISO 42001 §9.4** for 7yr WORM + a quarterly reconstruction drill. What's the current export shape?

2. **Prompt-injection-via-prospect-reply vector.** When a malicious prospect replies with attacker-controlled text that Alice ingests into the next reasoning step — OWASP LLM Top 10 **LLM01** + ISO 42001 §6.1.4 — what's the per-payload detection + per-blocked-event audit-trail? This is the new "agent-as-attack-surface" lane every 2026 RFP will probe.

3. **CAN-SPAM + CASL + GDPR Art. 6(1)(f) + FTC Section 5 compliance-evidence** when Alice sends on behalf of the customer. The per-email join-table between `alice_session_id` + `prospect_consent_status` + `sender_identity_physical_address` + `unsub_link_injection_flag` + `sending_domain_spf_dkim_dmarc_pass` must be exportable for FTC-rule-compliant 10yr retention.

4. **AI-personalized-line provenance** for the personalized-cold-email enforcement line (per-line join-table between `alice_session_id` + `prospect_enrichment_signal_id` + `llm_prompt` + `llm_response` + `intent_classifier_decision` + `human_override` + `enrichment_signal_source_license` + `personalization_opt_out_flag` for CAN-SPAM + CASL + GDPR Art. 22).

5. **Downstream-CRM sync-conflict + dedup join-table** when Alice writes back to Salesforce/HubSpot/Pipedrive (per-write join-table between `alice_session_id` + `crm_system_id` + `crm_record_id` + `conflict_resolution_decision` + `audit_trail_of_which_field_won` for ISO 42001 §9.4 + SOC 2 CC6.1). Plus **EU AI Act Annex III §4** high-risk classification for any materially-influences-prospecting-decision lane (Art. 6 + 14 + 43) — and the Brex fintech customer stack triggers GLBA + Reg E + SOC 2 CC6.1 on top.

---

**What I do:** 48-hour AI-agent audit. $500 flat. You get a written gap-analysis against SOC 2 CC7.2 + EU AI Act Art. 12 + ISO 42001 §9.4 + CAN-SPAM + CASL + GDPR + FTC Section 5, with the actual join-table schemas you can hand to the auditor.

**The ask:** 30-min call this week with you or your head of compliance/eng to walk through which of these 5 gaps your customers are already asking about.

— Atlas
Talon Forge | atlas@talonforge.io