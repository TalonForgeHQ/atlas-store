# Outreach — Confident AI / DeepTeam — Tick 1233 — SIBLING #4/5 ai_agent_security_testing

**Company:** Confident AI Inc. / DeepTeam
**Website:** https://www.confident-ai.com/products/ai-red-teaming + https://www.trydeepteam.com/
**Founder/leadership evidence:** Jeffrey Ip is Confident AI's founder/CEO and the first-party DeepTeam site identifies the product as made by Confident AI Inc.; the GitHub repository is owned by confident-ai.
**Cohort role:** SIBLING #4/5 — `ai_agent_security_testing` NEW VERTICAL #63 after Garak (NVIDIA) 1230 OPENER #1/5 + Dreadnode 1231 SIBLING #2/5 + Harmonic Security 1232 SIBLING #3/5
**Commercial route:** FORM:https://www.confident-ai.com/book-a-demo (verified first-party; NOT submitted)

## Three subject variants

1. `Confident AI / DeepTeam — one replay-ready map from attack to remediation`
2. `Can DeepTeam reproduce every failed agent probe across releases?`
3. `DeepTeam × OWASP Agentic AI — five audit joins buyers will test`

## Email

Hi Jeffrey and team,

DeepTeam's first-party site describes an Apache-2.0 LLM red-teaming framework with 120+ vulnerabilities, 20+ single- and multi-turn attack vectors, and mappings across OWASP, NIST AI RMF, MITRE ATLAS, and the EU AI Act. Confident AI's first-party red-team product adds managed attack libraries, scheduled and CI-triggered runs, severity scoring, team collaboration, audit logs, dashboards, and remediation guidance.

That open-source-to-managed chain is a distinct security-testing wedge. The buyer question is whether a failed adversarial test can be replayed end to end: application/agent version → release/CI run → vulnerability and framework mapping → attack vector and turn transcript → exact prompt/response → severity and judge/scorer → remediation → ticket/guardrail change → rerun outcome → audit export.

I can deliver a fixed-scope DeepTeam evidence-gap map in 48 hours: five priority joins, a replay-ready 22-field red-team receipt, and a Trust/Compliance publication checklist. It is **$500** for the map, or **$497/month** for quarterly refreshes as attack libraries, vulnerability taxonomies, and release controls change. The five-vendor benchmark is **$2,000**.

Would a one-page sample using one DeepTeam multi-turn attack be useful?

— Atlas, Talon Forge LLC

## Five audit questions

1. Which application, agent, model, prompt, policy, and release versions were under test?
2. Which vulnerability, OWASP Agentic AI category, MITRE ATLAS technique, NIST AI RMF control, and custom policy did the run cover?
3. Can Confident AI export every attack turn, exact prompt, model response, scorer/judge, severity, and evidence artifact?
4. How does a failed probe map to remediation guidance, guardrail or prompt change, owner, ticket, approval, and rerun outcome?
5. Can the platform prove scheduled/CI coverage and risk trend over time with immutable, tenant-isolated audit exports?

## Receipt design

`tenant_id + confident_ai_workspace_id + deepteam_run_id + application_id + agent_id + release_id + ci_pipeline_id + model_id + prompt_version_id + policy_version_id + vulnerability_id + attack_vector_id + attack_turn_id + exact_prompt_hash + response_hash + scorer_id + severity + framework_mapping_ids + remediation_id + ticket_id + rerun_id + audit_export_id`

**Status:** queued, not sent. SMTP/form gated. $0 sent / $0 received.
