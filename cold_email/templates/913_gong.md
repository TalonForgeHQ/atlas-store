**Subject:** Gong + the per-call consent ledger your EU buyer's GRC team will ask about in Q3 2026

Hi Amit — and please pass to whoever owns Gong AI / Gong Astro (conversational-deal-coaching LLM-backed) + Gong Data (customer-managed zone-isolation export),

The 5 audit gaps that map to Gong in a way they don't map to Clari or Chorus (because of the AI + EU data-residency overlap) — the questions your Fortune 500 buyer's GRC lead + EU AI Act Annex III conformity assessor + SEC Reg-FD compliance officer will all ask in the same diligence cycle:

1. **Per-call consent-ledger entry required for EU AI Act Article 6(2) Annex III high-risk applicability** — Gong Reality Capture (SIP-recorded telephony + email-integration + meeting-bot transcription) is on the audit line because the Gong-AI / Gong-Astro LLM layer is materially influencing employment/promotion/contract-renewal decisions about identifiable EU natural persons (per the 2025 EU AI Act Annex III §4 reclassification). Gaps to evidence: (a) per-call `consent_ledger_entry_id`, (b) per-integration `integration_id` (Gong-Salesforce + Gong-Outreach + Gong-Salesloft + Gong-Apollo + Gong-LinkedIn-Sales-Navigator), (c) per-tenant `pii_detected_indicator`, (d) per-call `retention_class`, (e) per-EU AI Act Article 6(2) `explicit_consent_flag` — auto-pause when the EU/CA/UK/WA/IL consent ledger entry is missing. Audit artifact: per-call consent-ledger join-table + per-integration gap-detection log + 30-day sub-processor zero-retention SLA across OpenAI + Anthropic + Google Vertex + Microsoft Azure OpenAI.

2. **Gong Forecast accuracy under scenario-shock — per-quarter SOX 404 + ASC 606 audit-trail** — predictive-forecast accuracy disclosure obligations (SF/AZ/CO) + Revenue Recognition ASC 606 deliverable-contract liability SOX 404 audit-trail + ASU 2023-07 fair-value-of-Goodwill-disclosure + ASC 805-50 customer-list asset-recovery per-quarter impairment-test mean the per-quarter per-scenario forecast-accuracy artifact must be reproducible for the last 24 quarters. Gaps to evidence: (a) per-quarter `forecast_accuracy_artifact_id`, (b) per-scenario `model_hash`, (c) per-quarter `accuracy_report_hash`, (d) per-quarter retention-vs-rebuild reconciliation under ASU 2023-07. Audit artifact: per-quarter forecast-accuracy join-table + 24-quarter reconstruction drill.

3. **Gong AI / Gong Astro per-call model-version + prompt-hash + user-override chain** — Reg BI SEC 17 CFR 248.10-15 + NAIC Suitability in Best Interest + CFPB Reg Z + NY DFS 23 NYCRR 224 + CO Rev Stat §6-1-1003 disparate-impact + NYC LL 144 AEDT all require that the per-deal-coaching-session trace include (a) `model_name`, (b) `model_version`, (c) `prompt_hash`, (d) `response_hash`, (e) `citation_anchor`, (f) `confidence_score`, (g) `user_override_event`, (h) cross-tenant-no-bleed invariant. Gaps to evidence: per-call model-name + model-version pinning + per-customer GK-gated fine-tuning training-data isolation + per-llm-sub-processor DPA flow-down with 14-day change-notification SLA. Audit artifact: per-deal-coaching session evidence row + per-LLM-sub-processor change-notification log.

4. **Gong Deal Board pipeline <-> CRM-opportunity <-> SOX-404-quarter-evidence join-table** — FRCP 34(b) production-discovery + ASC 606-10-32 contract-modification-impact SOX 404 audit-trail + Material Cybersecurity Incident 8-K Reg-FD (24-hour disclosure) + ASU 2023-07 fair-value-of-Goodwill-disclosure per-quarter impairment-test mean Gong Deal Board must produce, on demand, an evidence-join-table across (a) per-pipeline-deal `pipeline_deal_id`, (b) per-deal-record `record_hash`, (c) per-deal-coaching `coaching_hash`, (d) per-deal-history `history_hash`, (e) per-deal-relationship-chain `relationship_chain_hash`, (f) per-call `table_join_hash`. Audit artifact: the sound-card-style "Gong Reality-Platform" join-table covering pipeline-deal ↔ call-recording ↔ email-recording ↔ calendar-meeting ↔ Gong-AI-coaching ↔ Gong-AI-forecast ↔ Gong-AI-engage ↔ CRM-opportunity ↔ finance-system-revenue-line ↔ SOX-404-quarter-evidence.

5. **Gong Data per-customer Region-Pin + per-customer Zone-Isolation Flag for cross-border matters** — EU + UK + CH + BR + AU + SG + JP + CA + IN + ZA cross-jurisdiction export posture + GDPR Art. 17 erasure reconciliation with per-customer zone-isolation lock + cross-jurisdiction Matter-Acknowledgment envelope across Gong Data Lakehouse + BigQuery + Snowflake + Databricks + AWS S3 + GCP GCS + Azure ADLS Gen2 sub-processor stack. Gaps to evidence: (a) per-customer `region_pin`, (b) per-customer `delta_export_isolation_flag`, (c) per-customer `zone_isolation_lock`, (d) per-jurisdiction `cross_border_data_residency_report`, (e) per-tenant zone-isolation replay window. Audit artifact: per-customer Gong-Data zone-isolation report + per-jurisdiction cross-border-data-residency report.

I'm running a 48-hour, fixed-price **$500 audit** that delivers all five artifacts as a working reference implementation + the per-call consent-ledger pipeline + the per-quarter forecast-accuracy replay + the Gong Deal Board Table-Join-Action + the Gong Data cross-jurisdiction export posture — for Gong-AI + Gong-Astro + Gong-Forecast + Gong-Engage + Gong-Calls + Gong-Emails + Gong-Meetings + Gong-Data. References: chunks 101 (Arize), 108 (Spellbook), 112 (Cisco AI Defense), and the cross-mapping chunk for ai_revenue_intelligence_for_saas live at talonforgehq.github.io/atlas-store.

Worth a 30-min call this week?

— Atlas
