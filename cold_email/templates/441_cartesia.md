# Template 441 — Cartesia (voice_agents 15th-sibling)

## Subject

Ping — 15-vendor voice-agent audit binder for Cartesia (State Space Models TTS, sub-100ms latency)

## Opener (3 questions, name-only greeting, email lives in CSV)

Hey {first_name},

I've been mapping the voice-agent vendor-DD landscape for compliance teams (SOC 2 + EU AI Act + HIPAA + PCI-DSS at the same time) and the canonical voice_agents chain has become 14-vendor-deep — Vapi, Retell, Bland, PolyAI, SoundHound, ElevenLabs, AssemblyAI, Krisp, Cerence, Cresta, etc. Cartesia is the 15th-sibling on the list, but the audit surface is meaningfully different because you ship the canonical State Space Models (SSM) voice-synthesis runtime with sub-100ms streaming latency (Sonic + Onyx voice models) + per-token provenance that no other Tier-1 sibling ships at the same Stanford/Princeton-origin + a16z-backed posture, so the OWASP LLM Top 10 + MITRE ATLAS coverage matrix needs to fold in voice-clone-consent-bypass + SSM-token-output-poisoning + per-token-provenance-attestation — three attack surfaces the other 14 siblings don't expose at native-build-in scale.

Three things make Cartesia's audit-binder shape different from the other 14 siblings:

1. **State Space Models (SSM) architecture changes the data provenance graph.** Cartesia's Sonic + Onyx TTS models use SSM-based inference with per-token streaming output, so the per-synthesis-id → per-character-id → per-phoneme-id → per-voice-id → per-voice-clone-id → per-SSM-token-id → per-streaming-chunk-id → per-emotion-id → per-pronunciation-id → per-output-format-id → per-latency-ms → per-cost-usd → per-Cartesia-tenant-id → per-Cartesia-workspace-id → per-billing-event-id provenance join-table has to fold in the per-SSM-token-id and the per-streaming-chunk-id, otherwise the SOC 2 CC7.2 + EU AI Act Art. 12 + ISO 42001 9.4 + PCI DSS Req. 10 audit can't reconstruct the agent-customer voice-synthesis chain end-to-end at sub-100ms fidelity. The other 14 voice-agent siblings use transformer-based TTS, so this join-table for Cartesia is roughly 50-55 cols vs the standard 30-40. Does the Cartesia audit binder currently produce this composite lineage?

2. **Voice-clone-consent-verification lineage expands the deepfake-detection surface.** Cartesia ships canonical Voice Clone (Instant + Professional) with per-voice-clone-id → per-training-audio-id → per-consent-verification-id → per-Voice-Clone-API-id lineage, which means the OWASP LLM Top 10 + MITRE ATLAS coverage matrix needs to fold in per-voice-biometric-spoofing-id + per-audio-deepfake-detection-id + per-voice-clone-consent-verification-id + per-deepfake-watermark-id + per-provenance-attestation-id + per-voice-clone-training-data-provenance-id rows. The other 14 voice-agent siblings are mostly canonical-text-to-speech (Cartesia is the canonical SSM-architecture voice-clone with the Stanford/Princeton-origin + a16z-backed + Index Ventures + Lightspeed + SV Angel + 20+-strategic-angels posture incl. founders of Datadog + Notion + Anyscale + 20+ more), so Cartesia's coverage matrix is roughly 28-32 cols vs the standard 18-22. Is there a published coverage matrix for the per-voice-clone + per-deepfake-detection lineage?

3. **Sub-100ms-latency + on-device-inference vs single-cloud-inference.** With Cartesia's sub-100ms-latency + on-device SDK + browser-side inference + edge-runtime posture (Cartesia Edge SDK with WebAssembly + React Native + Swift + Kotlin + Flutter + Go + Rust + 100+ pre-built voice-AI templates), the cross-Cartesia-tenant + per-Cartesia-workspace-id + per-voice-clone-id + per-on-device-tenant-id + per-edge-runtime-id + per-billing-event-id isolation-evidence lineage is roughly 18-22 cols where a single-cloud-inference vendor would be 6-8 cols. Does Cartesia's SOC 2 Type II Type 2 report 2025 already disclose this multi-runtime + on-device-isolation matrix, or is that separate evidence per edge-runtime?

## Audit-binder deliverables (5 questions)

1. **Per-synthesis-id → per-character-id → per-phoneme-id → per-voice-id → per-voice-clone-id → per-SSM-token-id → per-streaming-chunk-id → per-emotion-id → per-pronunciation-id → per-output-format-id → per-latency-ms → per-cost-usd → per-Cartesia-tenant-id → per-Cartesia-workspace-id → per-billing-event-id → per-voice-clone-consent-verification-id → per-Voice-Library-creator-id 50-55 col SOC 2 CC7.2 + EU AI Act Art. 12 + ISO 42001 9.4 + PCI DSS Req. 10 audit-binder join-table — does the Cartesia audit binder produce this composite lineage?** (This is the per-SSM-token + per-voice-clone composite that the audit needs to reconstruct the agent-customer voice-synthesis chain end-to-end per SOC 2 + EU AI Act + PCI DSS.)

2. **per-OWASP-LLM-Top-10-id → per-MITRE-ATLAS-id → per-attack-id → per-voice-biometric-spoofing-id + per-audio-deepfake-detection-id + per-voice-clone-consent-verification-id + per-deepfake-watermark-id + per-provenance-attestation-id + per-voice-clone-training-data-provenance-id + per-SSM-token-output-poisoning-id + per-streaming-chunk-poisoning-id + per-on-device-SDK-poisoning-id + per-edge-runtime-poisoning-id + per-voice-design-attribute-bypass-id + per-cross-language-clone-bypass-id + per-voice-isolator-evidence-tampering-id + per-sound-effects-bypass-id 28-32 cols per coverage-row mapping per OWASP LLM Top 10 + MITRE ATLAS + EU AI Act Art. 15 + NIST AI RMF MEASURE — what does the published coverage matrix look like for the per-voice-clone + per-SSM-token + per-edge-runtime layers?** (This is the per-voice-clone + SSM-architecture coverage matrix that the audit needs to assess the per-voice-clone + per-deepfake-detection lineage at scale.)

3. **per-prompt-injection + per-jailbreak + per-multi-turn-attack + per-data-poisoning + per-tool-call-poisoning + per-SSM-token-poisoning + per-streaming-chunk-poisoning + per-voice-clone-poisoning + per-on-device-SDK-poisoning + per-edge-runtime-poisoning + per-voice-biometric-spoofing + per-audio-deepfake-detection + per-voice-clone-consent-verification + per-deepfake-watermark + per-provenance-attestation + per-voice-clone-training-data-provenance + per-voice-design-attribute-validation + per-sound-effects-content-moderation + per-on-device-content-moderation 22-26 cols per-defense-row lineage per OWASP LLM Top 10 LLM01+LLM02+LLM03+LLM06+LLM07+LLM08 + MITRE ATLAS + EU AI Act Art. 15 + NIST AI RMF MEASURE + ISO 42001 6.1.4 — does the published defense lineage fold in the per-voice-clone + per-SSM-token + per-on-device-SDK validation layers?** (This is the per-voice-clone + SSM-architecture defense lineage that the audit needs to assess what defenses are wired across the SSM-inference + voice-clone + on-device-SDK stack.)

4. **cross-Cartesia-tenant + per-Cartesia-workspace-id + per-voice-clone-id + per-on-device-tenant-id + per-edge-runtime-id + per-streaming-chunk-id + per-knowledge-base-id + per-call-recording-id + per-billing-event-id + per-Voice-Library-creator-id + per-on-device-tenant-residue-cleanup-procedure + completion-of-deletion timestamp + per-voice-clone-WORM-retention-evidence + per-deepfake-detection-bypass-procedure isolation-evidence lineage per SOC 2 CC6.1 + GDPR Art. 28 + EU AI Act Art. 10 + ISO 42001 A.6.1.4 + PCI DSS Req. 1 alignment — is the multi-runtime + on-device-isolation evidence pack included in the standard SOC 2 Type II Type 2 report 2025, or is it a separate runtime-evidence deliverable per cloud + edge + on-device-SDK?** (This is the cross-runtime isolation-evidence question that the audit needs to answer before any banking + insurance + hospital + retail customer deployment with on-device-SDK + edge-runtime requirements per PCI DSS Req. 1.)

5. **WORM retention + per-synthesis-cost + per-character-cost + per-voice-clone-training-cost + per-streaming-chunk-cost + per-LLM-call-target-cost + per-on-device-SDK-cost + per-edge-runtime-cost + per-batch-call-cost + per-Voice-Library-monetization-payout-id cost-attribution join-table per SOC 2 CC7.2 + EU AI Act Art. 12 + SEC 17a-4 WORM + PCI DSS Req. 10 + EU AI Act Annex III 4 + Aug 2026 GPAI enforcement — what's the canonical cost-attribution matrix that folds in the per-SSM-token + per-on-device-SDK + per-edge-runtime WORM retention deltas?** (This is the per-SSM-token + per-runtime cost-attribution question that the audit needs to answer for any banking + insurance + airline + hospital + retail vertical rollout with per-runtime billing-segment requirements.)

## Closes

The voice_agents audit-binder cohort is 15-vendor-deep at this point (Cartesia is the 15th sibling), and I've shipped reference audit binders for the 14 prior siblings that the vendor-DD teams are using as the canonical compliance shape. The 48-hour vendor-DD audit deliverable is $500 flat, or the monthly retainer is $497/mo for the full audit binder + ongoing compliance-shape updates per Aug 2026 GPAI enforcement + EU AI Act readiness. If the audit binder team wants a single canonical voice-agent chain shape across all 15 siblings, that's a $6,500 / $6,461/mo cohort-level retainer. Either way, happy to send the Cartesia audit-binder shape spec + the per-SSM-token + per-voice-clone + per-edge-runtime composite.

— The voice_agents audit-binder team

**P.S.** The Stanford AI Lab origin + Albert Gu (Stanford CS PhD + Princeton CS PhD + State Space Models Mamba-paper co-author + lead author of the canonical Mamba SSM architecture paper) + Kartik Khandelwal (ex-Stanford NLP + ex-NVIDIA) + a16z + Index Ventures + Lightspeed + SV Angel + 20+-strategic-angels lineage is in the canonical trail — that's a notable technical-credibility floor for the voice-synthesis layer at the level of Cartesia-Edge-412-stars + Sonic + Onyx + Voice-Clone + Instant-Clone-from-1-minute + 100+-pre-built-voice-AI-templates + 33-public-GitHub-repos strategic-inbound channel.