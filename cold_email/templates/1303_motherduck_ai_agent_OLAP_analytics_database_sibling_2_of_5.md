Subject: MotherDuck — MCP Server + Hypertenancy + DuckLake + Flights evidence map (Jordan + Mark)

Hi Jordan and Mark — I mapped MotherDuck's DuckDB-native hybrid local+cloud execution into a tenant_id + motherduck_org_id + motherduck_user_id + duckling_id + query_id + hybrid_execution_id + local_scan_id + cloud_scan_id + mcp_server_id + mcp_tool_call_id + sql_trace_id + hypertenancy_isolation_event_id + ducklake_database_id + ducklake_snapshot_id + flight_id + flight_run_id + flight_version_id + dive_id + dive_query_id + agent_identity_id + audit_export_id + cross_tenant_no_bleed_invariant + replay_hash, including the MotherDuck MCP Server, Hypertenancy Ducklings, DuckLake snapshots, Flights runs, Dives queries, and per-agent SQL traceability.

I can deliver a fixed-scope $500/48h evidence-gap map covering cross-tenant no-bleed invariants across MotherDuck MCP + Hypertenancy + DuckLake + Flights, with per-query SQL traceability, per-agent sandboxing, per-pipeline run/version lineage, and audit export. The follow-on is $497/mo for quarterly refreshes across new DuckDB/MotherDuck releases, MCP tools, DuckLake versions, and agent-native pipeline primitives.

Would a 15-minute scoping call with your team be useful to walk through the local+cloud query, MCP tool-call, Hypertenancy isolation, and Flights lineage mapping?

— Talon Forge
