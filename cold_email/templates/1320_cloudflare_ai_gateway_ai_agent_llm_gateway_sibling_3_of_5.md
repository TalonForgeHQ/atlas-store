---
template_id: tick-1320-cloudflare-ai-agent-llm-gateway-sibling-3-of-5
vendor: Cloudflare AI Gateway
cohort: ai_agent_llm_gateway
cohort_role: sibling-3-of-5
target: FORM:https://www.cloudflare.com/cloudflare-ai-gateway (canonical first-party product page; pattern guesses mailto:support@cloudflare.com + mailto:sales@cloudflare.com retained separately as unverified per PITFALL #28)
subject_line: Cloudflare AI Gateway + 5-vendor ai_agent_llm_gateway cohort SIBLING #3/5 (Cloudflare Workers edge runtime + 50+ provider routing + zero-egress billing model)
preheader: First-party verified developers.cloudflare.com/ai-gateway 2026-07-26 — edge-native AI Gateway with caching, rate limiting, retries, fallback, BYOK, logs, analytics — on the global Cloudflare network
---

Hi Cloudflare AI Gateway team,

First-party verified 2026-07-26 (developers.cloudflare.com/ai-gateway): Cloudflare AI Gateway is the canonical edge-native AI Gateway — "Get observability, caching, rate limiting, and retries for your AI requests" — and the unique SIBLING #3/5 of the ai_agent_llm_gateway cohort I'm building, sitting alongside Portkey 1318 OPENER and LiteLLM 1319 SIBLING #2/5. Cloudflare's substrate is: 50+ provider integrations (OpenAI + Anthropic + Google Vertex + Azure OpenAI + Workers AI + Mistral + Cohere + Groq + Perplexity + Hugging Face + Replicate + Together + Fireworks + DeepInfra + xAI + DeepSeek + AWS Bedrock + many more), Cloudflare Workers edge runtime, unified cache layer, per-model rate limiting, automatic retries, fallback across providers, BYOK (Bring Your Own Key) credential isolation, request logs, analytics, and the zero-egress billing model — all on top of the global Cloudflare network.

Why Cloudflare AI Gateway is SIBLING #3/5 (not OPENER, not CLOSER): Portkey holds the canonical SaaS-control-plane OPENER position (provider credential vault + virtual keys + 25+ providers + guardrails); LiteLLM holds the canonical OSS-first + drop-in OpenAI replacement + 100+ provider Python SDK position; Cloudflare AI Gateway holds the canonical edge-native + Cloudflare Workers runtime + zero-egress billing position. Three non-overlapping wedges — Portkey's Vault, LiteLLM's OSS SDK, Cloudflare's edge network — together form the cohort's foundation.

5-WEDGE non-overlap vs Portkey 1318 + LiteLLM 1319 + broader ai-agent infrastructure:

1. ONLY cohort sibling that ships AI Gateway as a Cloudflare Workers edge runtime primitive — the request path runs on Cloudflare's global network of 300+ data centers in 100+ countries, not on a SaaS control plane (Portkey) or a self-hosted Python SDK (LiteLLM). Edge-native execution is the cohort's unique substrate.
2. ONLY cohort sibling with the zero-egress billing model — Cloudflare charges no egress fees on cached responses or analytics, materially different cost economics than the per-token SaaS pricing of Portkey + the self-host-cost profile of LiteLLM.
3. ONLY cohort sibling with the Workers AI provider (Cloudflare's own first-party model serving primitive: Llama 3.1 70B + Llama 3.1 8B + Llama 3.2 11B Vision + Llama 3.3 70B + Mistral 7B + Qwen 1.5 + embedding models + automatic Whisper transcription) joined to the gateway substrate in a single product surface.
4. ONLY cohort sibling shipping the canonical unified cache + rate-limit + retry + fallback + BYOK + logs + analytics seven-primitive envelope across 50+ providers, with automatic caching of identical prompts at the edge (latency reductions on cache-hit measured at the network layer).
5. ONLY cohort sibling with Cloudflare's first-party network-level compliance posture (SOC 2 Type II + ISO 27001 + ISO 27701 + GDPR + HIPAA + PCI DSS Level 1 + EU AI Act readiness + per-request audit logging at the Workers runtime) — distinct from Portkey's per-tenant SaaS posture and LiteLLM's MIT-license self-host posture.

I'm running a five-vendor benchmark report (Portkey 1318 OPENER + LiteLLM 1319 SIBLING #2 + Cloudflare AI Gateway 1320 SIBLING #3 + SIBLING #4 + CLOSER #5 to be selected) that would publish a per-vendor evidence-gap map covering virtual-key isolation + per-provider credential-vault + per-prompt-version + per-completion + per-cache-hit + per-fallback-chain + per-cost-cents + per-latency-ms + per-status + cached-flag + guardrail-decision + edge-runtime-request-id + per-audit-export + cross-tenant no-bleed + EU AI Act Art. 13 logging + Art. 14 human-oversight.

22-col evidence wedge for Cloudflare AI Gateway joins tenant_id + cloudflare_account_id + cloudflare_zone_id + gateway_id + provider + model_id + model_version_hash + prompt_hash + completion_hash + input_tokens + output_tokens + cost_cents_usd + latency_ms + edge_pop_code + cache_hit_flag + fallback_chain_id + rate_limit_remaining + guardrail_decision + byok_credential_id + audit_export_id + cross_tenant_no_bleed_invariant + replay_hash.

Offer ladder:
- $500 / 48 hours — fixed-scope Cloudflare AI Gateway per-virtual-key + per-provider + per-model + per-prompt-version + per-completion + per-cache + per-fallback + per-guardrail + per-audit-trail + per-edge-POP evidence-gap map
- $497 / month — quarterly refresh: Cloudflare AI Gateway version updates + new provider additions + Workers AI model additions + EU AI Act Art. 26 updates + BYOK credential-isolation audit
- $2,000 — five-vendor ai_agent_llm_gateway COHORT BENCHMARK at close (Portkey 1318 + LiteLLM 1319 + Cloudflare AI Gateway 1320 + SIBLING #4 + CLOSER #5)
- $2,485 MRR ceiling per YanXbt pattern (5 clients x $497/mo)
- $10,000 CLOSER-only cohort sponsorship tier unlocked at vertical #79 closure

I am running the SIBLING #3/5 position with $0 sent / $0 received, mailto/form gated. If the cohort-benchmark shape is interesting for Cloudflare AI Gateway as the canonical edge-native AI-Gateway SIBLING, the right next step would be a 20-minute walk-through of the proposed benchmark rubric before any commercial ask — I'll wait for you to set that up if useful.

Best,
Atlas
TalonForge — Atlas Store
https://talonforgehq.github.io/atlas-store