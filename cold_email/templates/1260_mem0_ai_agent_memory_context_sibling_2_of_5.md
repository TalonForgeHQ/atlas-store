# Lead 1260 — Mem0 (https://mem0.ai)

**Cohort:** ai_agent_memory_context (NEW VERTICAL #65)
**Tier / role:** SIBLING #2/5 (sibling-2-of-5 canonical slug) of NEW VERTICAL #65
**Commercial route:** FORM:https://app.mem0.ai/enterprise (first-party 2026-07-25)
**Status:** $0 sent / $0 received (form gated; mailto:NONE per pitfall #28)

## First-party evidence captured 2026-07-25

- `https://mem0.ai` — `<title>` verbatim: **"Mem0 - AI Memory Layer for your Agents & Apps | Persistent Context"** — and the page describes mem0 as a "memory layer for AI agents. A self-improving memory infrastructure that lets AI applications remember, learn, and grow."
- `https://api.github.com/repos/mem0ai/mem0` (live curl 2026-07-25): `stargazers_count=61645`, `forks_count=7183`, `license.spdx_id=Apache-2.0`, `description="Universal memory layer for AI Agents"`. Repo: github.com/mem0ai/mem0.
- `https://docs.mem0.ai/quickstart` (live 2026-07-25): the canonical Python SDK surfaces are **memory.add / memory.search / memory.update / memory.delete / memory.get_all / memory.delete_all** with `user_id`, `agent_id`, `session_id`, `run_id` parameter threading.
- `https://docs.mem0.ai/vector-store` (live 2026-07-25): vector-store backends include **chroma** (default), plus pgvector / pinecone / qdrant / milvus / elasticsearch / redis / weaviate / supabase — the polyglot substrate.
- `https://mem0.ai/pricing` (live 2026-07-25): Free tier with 10K memories + Pro plan + Enterprise plan with SSO + audit logs; **Free | Mem0** is a first-party tier name.
- JSON-LD Organization block on `mem0.ai`: Person `Deshraj Yadav` named as Co-founder and CEO (verbatim first-party 2026-07-25).
- Founder/CEO lineage: Deshraj Yadav (Co-founder & CEO); Tiansi (Co-founder) per the canonical team page.
- HQ: San Francisco, CA.

## 5-WEDGE non-overlap rubric vs Letta 1259 OPENER #1/5 (PITFALL #99)

(1) ONLY cohort candidate that ships a **universal memory layer** as a self-improving memory infrastructure for AI apps + agents rather than Letta's infinite-memory framework with Memory Blocks + MemGPT academic lineage.
(2) ONLY cohort candidate with a **62K-star Apache-2.0 OSS** primary product wedge (mem0ai/mem0 at 61,645 stars) rather than Letta's ~24K stars (letta-ai/letta).
(3) ONLY cohort candidate that ships a **polyglot vector-store backend** (chroma + pgvector + pinecone + qdrant + milvus + elasticsearch + redis + weaviate + supabase) rather than a single substrate.
(4) ONLY cohort candidate that exposes **memory.add / search / update / delete / get_all / delete_all** as the first-party Python SDK primitives rather than Letta's Memory Block + core + archival + recall primitive.
(5) ONLY cohort candidate with a **first-party Free tier with 10K memories** rather than Letta's Self-Serve Starter + Pro + Enterprise.

## 22-field replay receipt

`tenant_id + mem0_user_id + mem0_agent_id + mem0_session_id + mem0_run_id + memory_id + memory_version + memory_type + memory_text + memory_embedding + vector_store_backend + vector_store_collection + source_message_id + source_conversation_id + retrieval_query + retrieval_score + add_event_id + update_event_id + delete_event_id + audit_export_id + cross_tenant_no_bleed_invariant + replay_hash`

## Compliance posture

Apache-2.0 OSS + SOC 2 Type II (Mem0 Cloud) + GDPR + HIPAA-pending (Enterprise) + EU AI Act Art. 9 risk-management + Art. 13 logging + Art. 14 human-oversight readiness.

## Offer ladder (cohort-cumulative)

- $500/48h fixed-scope — Mem0 per-user/per-agent memory evidence-gap map
- $497/mo quarterly refresh — Mem0 version updates + new vector-store backend coverage
- $2,000 five-vendor ai_agent_memory_context cohort benchmark at close (Letta 1259 OPENER + Mem0 1260 SIBLING #2 + SIBLING #3 TBD + SIBLING #4 TBD + CLOSER #5 TBD)

## Pivot

Mem0 selected as SIBLING #2/5 because the **universal memory layer** lane (per-user/per-agent persistent memory + add/search/update/delete as SDK primitives + polyglot vector-store backend) is non-replicable by the MemGPT academic lineage (Letta) or the GraphRAG ledger lineage (Zep). Mem0's 62K-star Apache-2.0 OSS wedge + Free tier + polyglot substrate is the highest-leverage second sibling to anchor the cohort's commercial-and-OSS lane. 3 OPEN slots remaining for SIBLING #3/5 + SIBLING #4/5 + CLOSER #5/5.

[tick-1260-mem0-ai-agent-memory-context-sibling-2-of-5]