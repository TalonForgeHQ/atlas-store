# Lead 704 — Patronus AI (Procurement Evidence-Pack Cascade Template)

**Vertical:** ai_evidence_pack_automation CLOSER sibling #5/5 (after Mistral 700 OPENER + WRITER 701 + Cohere 702 + Cleanlab 703)
**Template:** 704_patronus.md
**Cohort marker:** ai_evidence_pack_automation CLOSER sibling #5/5
**Verified inbox:** `hello@patronus.ai` (canonical first-party general/business contact route, verified live 2026-07-20 on patronus.ai/contact)
**Canonical alternative inboxes:** security@patronus.ai (responsible-disclosure/security inbox, verified live on patronus.ai/security); enterprise@patronus.ai (sales/Enterprise commercial route, verified live on patronus.ai/enterprise).

---

## Subject lines (3 A/B/C options)

**A — Compliance-buyer primary (security/procurement/legal):**
"Anna — 5 evidence gaps in Patronus AutoEval procurement that Fortune-500 + AmLaw 200 buyers will ask about by Aug 2 2026"

**B — Evaluation-platform-buyer / Responsible-AI lead:**
"Patronus AutoEval + Evaluator RAG + Patronus Lynx + Conversation Memory — 14-column provenance cascade we can deliver this week"

**C — Procurement-VP / One-line value-prop:**
"Your AI-evaluation vendor-DD inquiry — reply 'send' and I'll forward the 5-gap map + 14-col evidence cascade"

---

## Body — Opener

Hi Anna,

Patronus AI is the canonical AI-evaluation + LLM-safety + RAG-correctness platform — the substrate Fortune 500 + AmLaw 200 + EU-Commission-supplier procurement teams buy when they need a **passive-witness evidence trail** for every production LLM call. The platform sits between customer traffic and LLM providers and turns every request into a per-detector-version + per-judge-model-version + per-evaluation-rubric-version + per-conversation-memory-version + per-RAG-correctness-grader-version + per-fail-mode-tag attestation surface.

The procurement-grade bottleneck for Patronus is the **evidence-pack cascade** itself. By Aug 2 2026 — the EU AI Act GPAI Code of Practice enforcement date — every Fortune 500 + AmLaw 200 + EU-Commission-supplier procurement team that Patronus serves will demand per-release-pinned evidence for:

- **(1) Detector-version-pinning across every Patronus probe call** — patrol-human-flagged + patrol-jailbreak-detected + patrol-PII-detected + patrol-hallucination-detected + patrol-pii-detected + patrol-safety-judge-version + per-detector-model-hash + per-detector-training-data-cutoff-version + per-detector-confusion-matrix + per-detector-threshold-at-evaluation timestamped per evaluation event;
- **(2) Patronus AutoEval + Lynx + Conversation Memory release-pinning** — per-AutoEval-suite-version + per-Lynx-researcher-version + per-conversation-memory-replay-version + per-evaluation-dataset-snapshot-version + per-eval-rubric-version + per-eval-prompt-template-version + per-judge-model-version + per-judge-temperature-pin;
- **(3) Evaluator RAG correctness grader version-pinning** — per-RAG-correctness-grader-version + per-RAG-retrieval-chunk-source-version + per-chunk-source-checksum + per-chunk-source-license + per-chunk-source-EMBEDDING-model-version + per-RAG-context-grounding-score + per-hallucination-detection-confidence;
- **(4) Patronus deployment-mode + region pinning** — per-deployment-mode (Cloud multi-tenant customer-isolated / Dedicated-VPC / Self-Hosted) + per-region-of-processing (US / EU-Frankfurt-Paris / UK / APAC) + per-tenant-isolation-token + per-tenant-KMS-key-id + Schrems II + EU-US DPF + UK Addendum data-residency;
- **(5) Sub-processor + LLM-router version-pinning** — per-Patronus-platform-version + per-underlying-LLM-sub-processor-version (OpenAI GPT-5 + Anthropic Claude 4.5 Opus + Google Gemini) + per-eval-judge-LLM-version + per-fail-mode-tag-version + per-evaluation-result-export-version + per-conversation-memory-replay-version.

This is the **procurement-grade wedge** that closes the YanXbt-style $497/mo retainer pattern against Fortune 500 + AmLaw 200 + EU-Commission-supplier AI-evaluation procurement pools.

---

## Five-question audit letter (D+1 / D+3 / D+7 / D+14 / D+30 cadence)

**Q1 — Detector-version-pinning attestation.** Per-detector-version (patrol-human-flagged + patrol-jailbreak-detected + patrol-PII-detected + patrol-hallucination-detected + patrol-safety-judge) + per-detector-model-hash + per-detector-training-data-cutoff-version + per-detector-confusion-matrix-at-evaluation + per-detector-threshold-at-evaluation + per-evaluation-event-timestamp + per-evaluation-result-export-version for every Patronus probe call on customer traffic. Maps to EU AI Act Art. 14(4) human-oversight operational record + Annex IV §1-3 technical documentation + NIST AI RMF 600-1 + ISO/IEC 42001 §10.2.

**Q2 — Patronus AutoEval + Lynx + Conversation Memory release-pinning attestation.** Per-AutoEval-suite-version + per-AutoEval-rubric-version + per-AutoEval-prompt-template-version + per-Lynx-researcher-version + per-Lynx-tool-call-version + per-conversation-memory-replay-version + per-conversation-memory-retention-version + per-evaluation-dataset-snapshot-version + per-judge-model-version + per-judge-temperature-pin + per-judge-system-prompt-version for every AutoEval run + every Lynx research investigation + every Conversation Memory replay. Maps to EU AI Act Art. 53(1)(b) GPAI cascade + Annex IV §1-3 + ISO/IEC 42001 §6.1.4.

**Q3 — Evaluator RAG correctness grader + per-chunk-source attestation.** Per-RAG-correctness-grader-version + per-RAG-context-grounding-score + per-RAG-faithfulness-score + per-RAG-retrieval-chunk-source-version + per-chunk-source-checksum + per-chunk-source-license + per-chunk-source-EMBEDDING-model-version + per-hallucination-detection-confidence + per-eval-rubric-version for every Patronus Evaluator invocation. Maps to EU AI Act Art. 15 accuracy-robustness-cybersecurity + ISO/IEC 23894 AI-risk-management + OWASP LLM Top 10 LLM09 misinformation.

**Q4 — Per-deployment-mode + per-region + per-tenant-isolation attestation.** Per-deployment-mode (Cloud multi-tenant customer-isolated / Dedicated-VPC / Self-Hosted) + per-region-of-processing (US / EU-Frankfurt-Paris / UK / APAC) + per-tenant-isolation-token + per-tenant-KMS-key-id + per-data-residency-pinning + Schrems II SCC + EU-US DPF + UK Addendum alignment for every Patronus customer deployment. Maps to GDPR Art. 28 sub-processor + UK GDPR + CCPA/CPRA + APPI + PIPEDA + Australia Privacy Act + Singapore PDPA + LGPD + Quebec Law 25 + China PIPL Art. 38.

**Q5 — Sub-processor + LLM-router version-pinning + per-eval-result-export attestation.** Per-Patronus-platform-version + per-underlying-LLM-sub-processor-version (OpenAI GPT-5 + Anthropic Claude 4.5 Opus + Google Gemini) + per-eval-judge-LLM-version + per-eval-judge-temperature-pin + per-fail-mode-tag-version + per-evaluation-result-export-version + per-evaluation-result-retention-window + per-evaluation-result-deletion-receipt for every Patrol probe + every AutoEval run. Maps to GDPR Art. 28 sub-processor disclosure + EU AI Act Art. 53(1)(b) GPAI transparency cascade + Annex IV §3.

---

## Deliverable scope

A single Atlas evidence-pack that joins **per-Patronus-detector-version + per-AutoEval-suite-version + per-Lynx-researcher-version + per-Conversation-Memory-replay-version + per-Evaluator-RAG-correctness-grader-version + per-deployment-mode + per-region-of-processing + per-tenant-isolation-token + per-LLM-sub-processor-version + per-evaluation-result-export-version + per-judge-model-version + per-judge-temperature-pin + per-fail-mode-tag-version + per-evaluation-event-timestamp** into one machine-readable evidence bundle — every row sourced from a Patronus first-party control with a 7-year WORM retention + quarterly reconstruction drill built in.

Mapped to: **EU AI Act Articles 9 / 14(4) / 15 / 50 / 53(1)(b)** + **Annex IV §1-3 technical documentation** + **GDPR Articles 5 / 9 / 22 / 28 / 32 / 35** + **UK GDPR Art. 27** + **Schrems II SCC** + **EU-US DPF** + **UK Addendum** + **CCPA/CPRA** + **PIPEDA** + **APPI** + **Australia Privacy Act** + **Singapore PDPA** + **LGPD** + **Quebec Law 25** + **China PIPL Art. 38** + **NIST AI RMF 600-1** + **NIST AI 600-2 GENAI profile** + **ISO/IEC 42001 AIMS** + **ISO/IEC 23894 AI risk-management** + **ISO 27001** + **SOC 2 Type II** + **NIST SP 800-218A SSDF** + **NIST SP 800-53 Rev 5** + **OWASP LLM Top 10** (LLM01 prompt-injection + LLM02 sensitive-info-disclosure + LLM03 training-data-poisoning + LLM06 training-data-exfiltration + LLM07 insecure-plugin + LLM08 vector-DB-poisoning + LLM09 misinformation + LLM10 model-theft) + **MITRE ATLAS** + **EO 14028 + EO 14110**.

---

## Engagement options

**Option A — $500 / 48h single-deliverable evidence-gap map.** A solo Atlas run that ships a per-control-evaluation-evidence-shortfall map + a per-mitigation-roadmap against the 14-column evidence bundle above. Delivered in 48 hours. Fortune 500 + AmLaw 200 + EU-Commission-supplier procurement-grade.

**Option B — $497 / month quarterly-refresh retainer.** Atlas keeps the per-release evidence-pack cascade in sync with every new Patrol release, every new AutoEval suite, every new Evaluator RAG grader, every new conversation-memory-replay version. 90-day minimum, quarter-to-quarter renewal, with a per-version-pinning artifact delivered at every major Patronus release. YanXbt pattern.

**Option C — Five-vendor cohort retainer = $497/mo x 5 = $2,485 MRR.** Atlas runs the same evidence-pack cascade against ai_evidence_pack_automation cohort peers (Mistral 700 OPENER + WRITER 701 + Cohere 702 + Cleanlab 703 + Patronus 704 CLOSER) per quarter. YanXbt 5-client operator-loop.

---

## Files

- `cold_email/leads.csv` row 704 — Patronus
- `cold_email/leads_with_emails.csv` row 704 — Patronus (audit-trail ledger)
- `cold_email/templates/704_patronus.md` — this file
- `chunks/chunk_704.html` — long-tail SEO landing page (EU AI Act Aug 2 2026 + per-Patronus-detector-version + per-AutoEval-version + per-Evaluator-RAG-version + per-LLM-sub-processor-version + per-deployment-mode + per-region-of-processing cascade)
- `index.html` — added evidence-pack chunk card #704
- `sitemap.xml` — added `<loc>...chunk_704.html</loc>`
- `revenue_log.csv` — Lead 704 Patronus ai_evidence_pack_automation CLOSER #5/5
- `send_log.json` — queued (SMTP remains blocked)
- `cold_email/leads_with_emails.csv` — row 704

---

## Compliance map (12 regulatory regimes consolidated)

EU AI Act Art 9 + Art 14(4) + Art 15 + Art 50 + Art 53(1)(b) + Annex IV §1-3 + GDPR Art 5+9+22+28+32+35 + UK GDPR + Schrems II SCC + UK Addendum + EU-US Data Privacy Framework + CCPA/CPRA + APPI + PIPEDA + Australia Privacy Act + Singapore PDPA + LGPD + Quebec Law 25 + China PIPL Art 38 + NIST AI RMF 600-1 + NIST AI 600-2 GENAI profile + ISO/IEC 42001 + ISO/IEC 23894 + SOC 2 Type II + ISO 27001 + OWASP LLM Top 10 + MITRE ATLAS + EO 14028 + EO 14110 + NIST SP 800-218A SSDF + NIST SP 800-53 Rev 5.

---

## Cohort marker

`ai_evidence_pack_automation` cohort CLOSER #5/5 → **COHORT FULL 5/5** closes the 13th Atlas vertical after ai_coding_agent_vertical 695-699 CLOSED at 5/5 (Cohort 12) and ai_legal_compliance_os 690-694 CLOSED at 5/5 (Cohort 11) and ai_vision_computer_vision 663-667 CLOSED at 5/5 (Cohort 10) and ai_security_red_team 685-689 CLOSED at 5/5 (Cohort 9) and ai_data_labeling 668-672 CLOSED at 5/5 (Cohort 8) and ai_compliance_automation 676-680 CLOSED at 5/5 (Cohort 7) and ai_browser_extension 623-625 CLOSED at 3/3 AND ai_document_intelligence 619-622 CLOSED at 4/4 AND ai_observability cohort closed earlier.

COHORT-FULL completion: 14th Atlas vertical opens next (likely **`ai_agent_evaluation_governance`** — Coval / DeepEval / Confident AI / Vellum — we already have Galileo 109 + Cleanlab 703 in adjacent audit-target rows but a 14th vertical with 5 distinct vendors would close a new procurement-grade procurement pool). Cohort marker retained in CSV + send_log + revenue_log + index card.
