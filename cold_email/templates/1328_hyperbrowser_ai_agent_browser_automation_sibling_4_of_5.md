# Cold Email Template — Lead 1328 Hyperbrowser

**Vertical:** #80 `ai_agent_browser_automation` (SIBLING #4/5)  
**Target:** Shri Sukhani, Founder & CEO; Akshay Shekhawat, Founder  
**Verified route:** info@hyperbrowser.ai (official Python/Node SDK docs)  
**Angle:** HyperAgent MCP-client calls + persistent profiles create a concrete audit-evidence opportunity.

## Subject variants
1. HyperAgent MCP calls + profiles: the missing audit join
2. A 48h evidence map for Hyperbrowser's persistent profiles
3. Browser actions → MCP tools → profile state: can buyers replay it?

## Body
Hi Shri,

Hyperbrowser's strongest buyer wedge is the combination of HyperAgent and reusable profiles: official docs show an AI-native Playwright framework that can consume external MCP tools, while profiles preserve cookies, local storage, session storage, and cache across browser sessions.

That creates a diligence question regulated buyers will ask: can one export join each HyperAgent action to the external MCP server/tool call and the exact profile version it read or changed—without crossing tenant boundaries?

I can deliver a **$500 fixed-scope evidence-gap map in 48 hours** covering profile attach/persist events, HyperAgent runs, `page.ai()` and extraction calls, MCP server/tool identity, managed Claude Computer Use tasks, proxy/CAPTCHA events, retention, tenant isolation, and an audit-export schema. The deliverable is a short buyer-facing report plus a CSV evidence map your sales and security teams can reuse.

If a recurring version fits better, it is **$497/month** for quarterly refreshes as HyperAgent, managed agents, and MCP integrations change.

Worth sending the one-page scope?

— Atlas @ Talon Forge
[tick-1328-hyperbrowser-sibling-4-of-5]

## Operator notes
- Do not call Hyperbrowser a native browser MCP server; its docs describe HyperAgent as an MCP client.
- Do not use the unverified `hello@` inbox or 404 `/contact` route.
- Do not claim SOC 2, ISO, GDPR, or other certifications without a readable first-party statement.
