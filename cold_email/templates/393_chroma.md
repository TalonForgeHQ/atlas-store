"393","Chroma","@trychroma","privacy@trychroma.com","vector_database","1","393_chroma.md","Lead 393 — Chroma (Jeff Huber, Co-Founder & CEO). Tier-1 vector_database 3rd-sibling after Pinecone 391 (VERTEX) + Weaviate 392. Chroma is the canonical open-source AI-native vector database serving 28.8K+ GitHub stars + 2.3K+ forks + 1M+ monthly Python SDK downloads + Chroma Cloud (managed serverless vector index in private beta) + ChromaDB (open-source self-hosted embedded vector store) + Chroma Research (research arm building the next-generation vector-search infrastructure for LLM applications). privacy@trychroma.com verified live 2026-07-17 by curl scrape of https://www.trychroma.com/privacy (HTTP 200) exposing mailto:privacy@trychroma.com as the canonical vendor-DD + GDPR DPA + CCPA/CPRA + EU AI Act strategic-inbound channel. Founder evidence: Jeff Huber (Co-Founder & CEO since 2022) formerly led search-infrastructure at Google where he was VP of Google Ads + VP of Google Apps + led the Google Maps + Google Earth ground-truth teams + Stanford CS background; co-founded Chroma October 2022 with the explicit mission to build 'the search infrastructure for AI' (per chroma-core/chroma GitHub repo description) after recognizing the production vector-database gap when serving LLM applications at scale; HQ San Francisco CA 94114 (per trychroma.com/privacy footer). Backed $28M+ Series A from Astasia Myers (Quiet Capital) + others; key GitHub contributors include rescrv (lead maintainer) + HammadB + codetheweb + jeffchuber (Jeff Huber himself) + Sicheng-Pan + sanketkedia + itaismith + tanujnay112 + levand + jairad26 per api.github.com/repos/chroma-core/chroma/contributors. Customers: 1M+ monthly Python SDK downloads + 28.8K+ GitHub stars + production deployments across OpenAI + Anthropic + LangChain + LlamaIndex + Hugging Face ecosystems + thousands of RAG-native startups + the canonical embed-and-retrieve surface for LLM applications. SOC 2 Type II in progress + GDPR DPA + CCPA/CPRA + EU AI Act readiness + HIPAA roadmap. Distinct because Chroma is the ONLY Tier-1 vendor in the vector_database cohort that ships BOTH the canonical open-source-first embedded-vector-store surface (Python-native pip install chromadb + WASM + Node.js + Rust bindings + per-collection + per-embedding-function + per-metadata-filter + per-persistent-disk-storage + per-in-memory-storage + per-ephemeral-client) AND the canonical AI-native vector-search API surface (chroma.Client + chroma.Collection + .add() + .query() + .get() + .update() + .upsert() + .delete() + .peek() + per-namespace + per-embedding-model + per-distance-metric: cosine/L2/IP) AND the canonical multimodal-embeddings surface (OpenAI CLIP + Sentence-Transformers + Cohere + OpenAI text-embedding-3 + Hugging Face + Instructor + per-embedding-function lambda + per-multimodal-image-text-audio embedding support) AND the canonical RAG-native surface (LangChain + LlamaIndex + Haystack integrations + per-RAG-retrieval pipeline + per-source-document chunking + per-metadata-grounded generation) AND the canonical Chroma Cloud managed-serverless surface (in private beta per trychroma.com, multi-region + per-tenant-isolation + per-encrypted-at-rest + per-bring-your-own-key + per-SOC-2-Type-II) — the unified AI-native + open-source-first + Python-idiomatic surface no other vector_database vendor combines. 5 audit gaps: (1) end-to-end per-collection-id -> per-document-id -> per-embedding-id -> per-vector-id -> per-metadata-id -> per-query-id -> per-embedding-function-id -> per-distance-metric-id -> per-namespace-id -> per-tenant-id -> per-region-id -> per-Chroma-Cloud-workspace-id -> per-billing-event-id provenance join-table per SOC 2 CC7.2 + EU AI Act Art. 12 + ISO 42001 9.4 + ISO 27001 A.12.4 (60+ cols), (2) prompt-injection + per-document-metadata-poisoning + per-embedding-poisoning + per-vector-poisoning + per-query-payload-poisoning + per-embedding-function-output-poisoning + per-multimodal-CLIP-image-poisoning + per-RAG-retrieval-poisoning + per-LangChain-LlamaIndex-integration-payload-poisoning defense per OWASP LLM Top 10 LLM01+LLM03+LLM04+LLM06+LLM08 + MITRE ATLAS AML.T0051 + EU AI Act Art. 15 + NIST AI RMF MEASURE with 12-column per-RAG-retrieval-id join-table, (3) cross-region vector-data-residency for EU + India + Brazil + UAE + Australia + Canada + UK + Singapore + Japan + Philippines customer cohort per Schrems II + GDPR Art. 28 + EU AI Act Art. 10 + India DPDP Act 2023 + Brazil LGPD Art. 33 + UAE PDPL + Canadian PIPEDA + Singapore PDPA + Philippines DPA + Japan APPI — Chroma Cloud currently US-only at launch; per-collection region flag must be auditable on the EU + APAC expansion roadmap, (4) HIPAA-eligible Chroma Cloud for healthcare-vector-RAG + clinical-retrieval-pipelines with BAA-ready configuration per HIPAA 164.312(a)(2)(iv) + 164.312(b) + 164.312(e)(1) + 164.514(b) + per-document-id PHI-flag + per-collection PHI-segregation (Chroma HIPAA on roadmap per trychroma.com/security), (5) cross-tenant ChromaDB self-hosted + Chroma Cloud managed + per-tenant-id + per-workspace-id + per-collection-id + per-API-key-id + per-document-id + per-query-id + per-embedding-function-id isolation-evidence per SOC 2 CC6.1 + GDPR Art. 28 + EU AI Act Art. 10 + FedRAMP Moderate readiness. privacy@trychroma.com is the verified SOC 2 + GDPR DPA + CCPA/CPRA + EU AI Act + vendor-DD strategic-inbound channel for ChromaDB self-hosted + Chroma Cloud managed + Chroma Research + Multimodal Embeddings + RAG Integrations + LangChain + LlamaIndex + Haystack + vector_database audit-target inquiries."
