---
template_id: tick-1318-portkey-ai-agent-llm-gateway-opener-1-of-5
vendor: Portkey
cohort: ai_agent_llm_gateway
cohort_role: opener-1-of-5
target: mailto:hi@portkey.ai (canonical first-party from portkey.ai/contact 2026-07-26)
subject_line: Portkey + 5-vendor ai_agent_llm_gateway cohort OPENER (OPENER #1/5, YC W22 founding + 250+ model route catalog)
preheader: First-party verified portkey.ai 2026-07-26 — canonical AI Gateway for LLM applications, 25+ providers, virtual keys + BYOC + semantic cache + guardrails substrate
---

Hi Portkey team,

First-party verified 2026-07-26 (portkey.ai): Portkey is the canonical AI Gateway for LLM applications — "Routing, fallbacks, caching, observability, and provider credentials - all in one control plane" — and the unique OPENER #1/5 of the ai_agent_llm_gateway cohort I'm building. Portkey's substrate is: 25+ provider integrations + 250+ model route catalog + Virtual Keys + Provider Credentials Vault + Guardrails + Fallbacks + Routing + Semantic Caching + Prompt Templates + LLM Observability + BYOC + per-tenant rate limits.

Why Portkey is the cohort OPENER (not a cohort sibling): the ai_agent_llm_gateway vertical is the canonical LLM-call control plane between agent apps and 250+ upstream model providers — a lane that observability-first (Arize + LangSmith + Maxim), tracing-first (Helicone), and eval-first (Confident AI) cohorts do not occupy. Portkey's BYOC + Virtual Keys + Provider Credential Vault is the cohort-canonical audit-trail primitive.

I'm running a five-vendor benchmark report (Portkey 1318 OPENER + 4 AI-Gateway siblings to be selected) that would publish a per-vendor evidence-gap map covering virtual-key isolation + per-provider credential-vault + per-prompt-version + per-completion + per-cache-hit + per-fallback-chain + per-cost-cents + per-latency-ms + per-status + cached-flag + guardrail-decision + per-audit-export + cross-tenant no-bleed + EU AI Act Art. 13 logging + Art. 14 human-oversight.

The 22-col evidence wedge for Portkey joins tenant_id + portkey_workspace_id + portkey_project_id + virtual_key_id + provider + model_id + model_version_hash + prompt_hash + completion_hash + input_tokens + output_tokens + cost_cents_usd + latency_ms + status_code + fallback_chain_id + semantic_cache_hit_flag + rate_limit_remaining + guardrail_decision + audit_export_id + cross_tenant_no_bleed_invariant + replay_hash.

Offer ladder:
- $500 / 48 hours — fixed-scope Portkey per-virtual-key + per-provider + per-model + per-prompt-version + per-completion + per-cache + per-fallback + per-guardrail + per-audit-trail evidence-gap map
- $497 / month — quarterly refresh: Portkey version updates + new provider additions + EU AI Act Art. 26 updates + BYOC credential-isolation audit
- $2,000 — five-vendor ai_agent_llm_gateway COHORT BENCHMARK at close (Portkey 1318 OPENER + 4 AI-Gateway siblings to be selected)
- $2,485 MRR ceiling per YanXbt pattern (5 clients x $497/mo)
- $10,000 CLOSER-only cohort sponsorship tier unlocked at vertical #79 closure

I am running the OPENER position with $0 sent / $0 received, mailto/form gated. If the cohort-benchmark shape is interesting for Portkey as the canonical AI-Gateway OPENER, the right next step would be a 20-minute walk-through of the proposed benchmark rubric before any commercial ask — I'll wait for you to set that up if useful.

Best,
Atlas
TalonForge — Atlas Store
https://talonforgehq.github.io/atlas-store
