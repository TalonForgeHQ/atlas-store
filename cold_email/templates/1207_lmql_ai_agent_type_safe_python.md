# 1207_lmql_ai_agent_type_safe_python.md

**Cohort:** ai_agent_type_safe_python — SIBLING #3/5
**Vendor:** LMQL (lmql.ai)
**Tick:** 2026-07-25-fast-exec-lmql-ai-agent-type-safe-python-sibling-3-1207
**Position in cohort:** SIBLING #3/5 of NEW VERTICAL #59 (after Pydantic 1201 OPENER + Instructor 1202 SIBLING #2)

---

## Subject line options (3, A/B test)

1. **Type-safe LLM scripting — the 7-row matrix that survives decoder-time constraints** *(deck-builder hook)*
2. **ETH Zürich ships a type-safe LLM DSL — your Pydantic + Instructor stack is 2 of 5, not 5 of 5** *(competitive anchor)*
3. **Pydantic + Instructor + LMQL — the only 5-vendor type-safe Python cohort worth benchmarking in 2026** *(cohort anchor)*

---

## Body (3-line personalized + signature)

> Hi {first_name} —
>
> I benchmarked LMQL against Pydantic + Instructor for type-safe LLM outputs in agent flows. The 7-row matrix — validation timing, provider coverage, error recovery, constraint expressiveness, compile target, determinism, research lineage — is the only one that survives a real agent. The deck maps your stack to a 2-library or 5-library decision and ships with a per-vendor 22-column audit receipt.
>
> $500 / 48h audit, or $497/mo retainer, or a $2,000 fixed 5-vendor cohort benchmark. Want me to send the 7-row matrix + the audit receipt template?
>
> — Atlas @ Talon Forge

---

## Deliverable (5-question audit-replay rubric)

1. **Validation timing** — are your constraints enforced pre-decode, parse-time, or post-call? If all three are post-call, you are paying for wasted tokens on every ValidationError.
2. **Provider coverage** — does your stack cover all of OpenAI / Anthropic / Azure OpenAI / Vertex / llama.cpp / vLLM / SGLang with a single constraint surface?
3. **Error recovery** — what is your max_retries on ValidationError? Is the retry deterministic? Does the retry preserve the model-side seed?
4. **Constraint expressiveness** — regex / JSON-schema / CFG grammar / DSL / token-level? If you have only JSON-schema, you are missing the long-context + regulated subset.
5. **Determinism + research lineage** — is the same seed + same prompt + same model guaranteed to yield the same output? Is the research lineage named first-party on the about page?

---

## Cohort positioning (NEW VERTICAL #59 ai_agent_type_safe_python)

- **Pydantic 1201** OPENER #1/5 — first-party Python schema validation
- **Instructor 1202** SIBLING #2/5 — first-party patched response + retry on ValidationError
- **LMQL 1207** SIBLING #3/5 — first-party declarative LLM scripting DSL + decoder-time constraints
- **Outlines** SIBLING #4/5 — first-party regex / CFG grammar / determinism
- **Guidance** CLOSER #5/5 — first-party token-level control flow

---

## Offer

- $500 / 48h — single-vendor audit
- $497 / mo — retainer (YanXbt pattern, 1 of 5 client slots)
- $2,000 fixed — full 5-vendor cohort benchmark + 5-vendor 22-column audit receipt
- 30-min handoff close (calendar booking)

---

## Follow-up bumps (cadence)

- **Day 3:** short bump with the 7-row matrix PDF + audit receipt template
- **Day 7:** cohort positioning note (where Pydantic / Instructor / LMQL / Outlines / Guidance fit in 2026)
- **Day 14:** opt-out line

---

## Route-safety

- **mailto:contact@lmql.ai** verified first-party 2026-07-25, NOT submitted
- **FORM:** none verified
- **SMTP/form gated** — do NOT auto-send; require human approval before any send
- **Compliance posture (LMQL first-party 2026-07-25):** LMQL is research-grade open-source software under Apache 2.0; commercial governance posture inherited from ETH Zürich
- **Cohort-unique 5-WEDGE:** (1) first-party named declarative LLM scripting DSL, (2) decoder-time constraint enforcement, (3) named Compile transpiler, (4) ETH Zürich research lineage, (5) named first-party Playground + Compile as product surface
- **Cross-tenant no-bleed invariant:** per-tenant workspace_id + per-query compile_revision_id + per-decoder-mask id must be replayable from a single workspace_id
- **$0 sent / $0 received this tick**

---

Atlas @ Talon Forge — fast-exec cron tick `2026-07-25-fast-exec-lmql-ai-agent-type-safe-python-sibling-3-1207` — LMQL SIBLING #3/5 ai_agent_type_safe_python NEW VERTICAL #59 advanced 3/5.
