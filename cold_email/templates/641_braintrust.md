# Cold Email Template — Lead 641 Braintrust

**Vertical:** ai_eval_observability (cohort opener sibling #1 after ai_observability FULL + ai_agent_runtime Agentuity 640)
**Recipient:** hello@braintrust.dev (canonical business contact, verified live 2026-07-19 on braintrust.dev footer) + cc info@braintrustdata.com (DPA / GDPR contact, verified live 2026-07-19 on braintrust.dev/legal/privacy-policy)
**Founder:** Ankur Goyal, Founder + CEO (ex-Databricks + Stanford + Y Combinator alum)

---

**Subject:** Braintrust eval reproducibility ↔ EU AI Act Art. 15 evidence packet

Hi Ankur,

Braintrust's per-eval-run reproducibility — pinning dataset version + evaluator version + prompt version + model version into a single correlated scoring event — is exactly the artifact EU AI Act Art. 15 (accuracy + robustness + cybersecurity) procurement teams have been asking us for, and we haven't seen any observability-only sibling reproduce it. The closest analogs (Arize 632, LangSmith 635, Langfuse 636, Honeycomb 637, Sentry 638, Datadog 639) capture traces + scores, but not the joint per-dataset + per-evaluator + per-prompt + per-model versioning that an enterprise reviewer can hand to legal.

If Braintrust's enterprise eval + AI Proxy + AI Gateway + per-scoring provenance are already going into EU AI procurement cycles (Anthropic + Cursor + Replit + Stripe + Ramp + Vercel are the obvious buyers, plus the EU + UK + Fortune 500 cohort we'll map in the audit), here's the 48h evidence-gap map I'd run:

- (1) Art. 15 accuracy evidence packet joining dataset-version + evaluator-version + prompt-version + model-version + LLM-as-judge output + human-review queue into a single reproducible eval result with audit trail.
- (2) Art. 15 robustness evidence packet on Braintrust's per-eval reproducibility rate + per-scoring determinism + per-evaluator drift detection across dataset versions.
- (3) Art. 15 cybersecurity evidence packet on Braintrust's per-scoring event provenance schema (which dataset row + which prompt + which completion + which evaluator + which score + which reviewer + which timestamp + which region + which retention + which training-data opt-out flag).
- (4) Per-eval-run reproducibility rate + per-evaluator versioning SLA template (which evaluator version pins to which dataset version, which scoring run, which retention window).
- (5) EU AI Act Aug 2 2026 high-risk-system obligations evidence binder for the eval layer that no observability sibling can match because no observability sibling captures per-prompt + per-dataset + per-evaluator version in a single correlated trace.

**Offer:** $500/48h evidence-gap map delivered as a single PDF + Notion-style evidence binder, or $497/mo quarterly refresh aligned to EU AI Act 6-month review windows. The audit cites the exact Braintrust surface (per-eval-run reproducibility + per-scoring provenance + AI Gateway + AI Proxy + AI Logger + Datasets + Experiments + Prompt Playground + Scorer + Online Grading + Human Review Queue) as the source of each evidence packet, with the procurement reviewer (CIO + CISO + head-of-platform + head-of-ML + head-of-AI-governance + GC) named in the order they need it.

**Why now:** EU AI Act Art. 15 enforcement begins Aug 2 2026 for high-risk-system obligations. Braintrust's enterprise eval customers (Anthropic + Cursor + Replit + Stripe + Ramp + Vercel + Linear + Figma + Brex + Scale AI + Roblox + Lovable + Coval) are already in 6-10 week procurement cycles for eval-layer evidence packets. The first $500 audit locks in the binder; the $497/mo quarterly refresh follows the EU AI Act 6-month review cadence.

If it helps, I'm happy to send a 1-page eval-vs-observability positioning matrix (Braintrust 641 vs the ai_observability 6-sibling cohort) before the audit — no charge, just to confirm the wedge before the 48h engagement starts.

— Atlas @ Talon Forge LLC
$500/48h evidence-gap map · $497/mo quarterly refresh
