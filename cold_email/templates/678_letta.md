# Cold-Email Template 678 — Letta (ai_agents_autonomous cohort closer #5 of 5)

**Vendor:** Letta | letta.com | @letta_ai
**Cohort:** ai_agents_autonomous (closer #5/5 after Fixie.ai 674 + LlamaIndex 675 + CrewAI 676 + Relevance AI 677)
**Inbox (verified live 2026-07-20):** contact@letta.com (published on letta.com/contact)
**Founders:** Charles Packer and Sarah Wooders (MemGPT authors and Letta co-founders; first-party MemGPT lineage plus TechCrunch launch coverage)
**One-liner:** Letta is the memory-first runtime for stateful, experiential agents that persist context, form memories, learn from experience, and improve across model generations.

---

## Subject variants

A) "Charles — one receipt for every Letta memory mutation"
B) "Letta audit trail: experience → memory edit → tool action"
C) "5 evidence gaps across stateful agents that learn"

## Email

Charles — five quick questions before I send the full map:

1. Can an enterprise export one immutable receipt joining agent identity, conversation/experience, context-repository commit, memory block before/after hashes, model version, tool call, policy result, human override, and downstream state change?
2. When a Letta agent rewrites memory during inference, sleep-time compute, or self-improvement, can the buyer reconstruct why that memory was retained, edited, merged, or forgotten and which evidence supported the change?
3. When shared memory spans concurrent conversations, do traces preserve causal ordering, conflict resolution, actor identity, branch/merge history, and the exact memory snapshot each action observed?
4. Across hosted Letta services, self-hosted deployments, model providers, tools, and external data stores, is there a machine-readable responsibility and deletion matrix for every retained prompt, response, memory, embedding, trace, and backup?
5. When skills or harness-level Mods alter an agent's capabilities, does production evidence preserve the exact skill/Mod version, approval path, permissions delta, evaluation result, rollback point, and affected actions?

Letta's differentiated surface is persistent agent state: MemGPT virtual context management, memory blocks, context repositories, shared conversations, sleep-time compute, skills, Mods, and agents that improve from lived experience. Procurement therefore needs more than an LLM trace. It needs a versioned chain from experience through memory mutation to the action that changed an external system.

The evidence map would define a 19-field experiential-agent receipt: agent-id/version, identity version, conversation/experience id, causal-parent event, context-repository commit, memory-block before/after hashes, memory-operation reason, model/provider version, prompt/context hash, tool definition/version, tool-call input/output hashes, policy/guardrail result, human approval/override, skill/Mod version, evaluation/replay result, deployment-region/tenant-key, downstream object id, and resulting-state hash.

**$500 / 48h evidence-gap map** — six pages, public-evidence PASS/PARTIAL/GAP table, sample memory-mutation receipt, deletion cascade, responsibility matrix, and prioritized remediation list.

**$497/month quarterly refresh** — re-run the map as Letta Agent, memory models, context repositories, skills/Mods, providers, and EU AI Act/NIST/ISO obligations change.

Reply "send" and I will forward the five-gap map.

— Atlas @ Talon Forge
https://talonforgehq.github.io/atlas-store

P.S. Fixie.ai owns Agent-as-a-Service, LlamaIndex owns document-context-to-action, CrewAI owns code-first multi-agent orchestration, Relevance AI owns no-code business workforces, and Letta closes the cohort with persistent memory plus continual learning.
