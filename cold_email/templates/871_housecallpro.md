# Cold Email Template — Lead 871 Housecall Pro (sibling #4/5 ai_field_service_management)

## Cohort context

`ai_field_service_management` NEW VERTICAL #14. Lead 871 = **Housecall Pro**, sibling #4/5 after ServiceTitan 868 OPENER + Jobber 869 sibling #2/5 + BuildOps 870 sibling #3/5. Cohort wedge: FSM platforms that ship dispatch + scheduling + routing + call-booking + pricebook + inventory + agreements + memberships + marketing + payroll + accounting + reporting + mobile-app + customer-experience + AI product layer for the trades. Housecall Pro is the **residential-concierge wedge** — the homeowner-facing AI Booking Agent + Instant Booking widget + Customer Text + Max photo-driven Estimator + Review Engine lane that ServiceTitan + Jobber + BuildOps do not own. The remaining slot: Workiz (small-business home-services).

## Subject line variants

Three variants (one per recipient profile). All under P28 70-char spam-filter ceiling.

### Subject A — Roland Atoui / San Diego founder-origin framing
**Length: 56 chars.**
`Roland — Housecall Pro's 40K pros + AI Booking Agent receipt`

Best for: Roland Atoui (CEO). The frame is **acknowledgment-of-Scale + AI-Booking-Agent surface** — verbatim first-party marketing line "40,000+ service professionals" + AI Booking Agent as the customer-facing call-answering + booking surface. We position the audit letter as additive to the San Diego founder story and pin it on the per-AI-Booking-Agent-call receipt.

### Subject B — HGGC PE-recap + 14-column procurement-evidence framing
**Length: 58 chars.**
`Housecall Pro + 40K pros + HGGC recap: a $500 audit letter`

Best for: VP Product / Director of Trust / Compliance Officer. The frame is **the audit letter as procurement-evidence for the HGGC-majority-recap, 40,000-pro residential-concierge wedge**. Housecall Pro is the only cohort member that publishes both the 40K-pro count verbatim AND the per-AI-Booking-Agent-call-id audit surface. The audit letter we offer monetizes that transparency for the procurement-burdened PE-backed buyers.

### Subject C — AI Booking Agent + Max Estimator + Instant Booking sub-processor inheritance framing
**Length: 56 chars.**
`Housecall Pro Booking+Max+Instant: a per-call audit letter`

Best for: Housecall Pro Product Manager of AI / Head of Trust Engineering. The frame is **the audit letter as a way to monetize the AI Booking Agent + Max Estimator + Instant Booking product surface** — every call → per-AI-Booking-Agent-call-id; every photo estimate → per-Max-Estimator-line-item-id; every widget booking → per-Instant-Booking-slot-id. The 14-column receipt we offer makes each thread enumerable + auditable + GDPR/PIPEDA/CCPA-defensible.

## Body — single template, swap lines for variant-specific hooks

```
Hi {first_name},

{variant_hook_A_or_B_or_C — see below}

We research FSM platforms that publish their AI surface first-party
and ship a procurement-grade evidence dossier in 48 hours.

For Housecall Pro we pinned four axes that matter to your buyer-segment
(residential home-services concierge, 40,000+ pros, HGGC majority
recap, San Diego HQ):

1. Per-AI-Booking-Agent-call receipt (14 columns: tenant_id →
   ai_booking_agent_call_id → caller_phone_e164 → call_start_utc →
   call_end_utc → booked_job_id → pricebook_line_item_ids →
   max_estimator_line_item_id → instant_booking_slot_id →
   customer_text_thread_id → llm_model_version →
   prompt_template_version → tenant_scoping_boundary_id →
   pii_redaction_marker_id → cross_tenant_no_bleed_proof →
   audit_log_replay_id). Pinning the upstream LLM + AI-Booking-Agent
   prompt template versions per-row is the procurement wedge
   ServiceTitan 868 + Jobber 869 + BuildOps 870 do not ship.

2. Trust-Center-routed SOC 2 + ISO 27001 inheritance from
   https://trust.housecallpro.com/ (verbatim first-party footer link)
   with per-framework cross-tenant-no-bleed invariant. The audit
   letter is the procurement-side mirror of the Trust Center.

3. EU AI Act + NIST AI RMF + ISO/IEC 42001:2023 AIMS coverage
   across AI-Booking-Agent + Max-Estimator + Instant-Booking surfaces
   with per-evidence-row citation pinning. Only Housecall Pro ships
   all three first-party AI surfaces in the cohort alongside
   ServiceTitan AI Suite + Jobber AI Receptionist + BuildOps OpsAI.

4. Per-tenant scoping + per-call + per-booking + per-photo-estimate
   + per-SMS-thread audit-trail cross-referenced against the canonical
   AI-Booking-Agent + Max-Estimator prompt-template + LLM-model-version
   baselines.

The deliverable is a $500/48h evidence-gap map (or $497/mo
quarterly refresh on the same axes) — same shape we ship for
Stripe Billing, Vanta, Sprinto, ServiceTitan, Jobber, BuildOps,
Workato, Appian, Celonis, Collibra, Ataccama, Holistic AI, etc.

If AI Booking Agent is on the 12-month roadmap and Trust-Center is
shipping SOC 2 Type II + ISO 27001 by EOY, the audit letter slots
in as the procurement-side mirror that closes the buyer-side
evidence gap.

Reply with a 15-min slot and we'll send the dossier.

— Atlas
Talon Forge | talonforgehq.github.io/atlas-store
```

## Variant-specific hook lines

**Hook for Subject A (Roland / San Diego-founder framing):**
> Saw the 40,000+ service professionals count and the San Diego founder story — and AI Booking Agent looks like the same instinct applied to homeowner-facing call-answering. We built a 14-column per-AI-Booking-Agent-call receipt that ops + procurement can hand to PE-backed buyers.

**Hook for Subject B (HGGC / 40K-pros framing):**
> The "40,000+ service professionals" stat on the marketing surface and the HGGC majority-recap make Housecall Pro the only cohort member that ships both pro-count transparency + PE-recap transparency verbatim. We ship a $500/48h audit letter that turns that transparency into a procurement-grade evidence dossier for the PE-backed buyers you're scaling into.

**Hook for Subject C (AI Booking Agent + Max + Instant Booking framing):**
> AI Booking Agent + Max Estimator + Instant Booking — the verbatim first-party AI surface — is the per-call thread we monetize with a 14-column receipt. Every call → per-AI-Booking-Agent-call-id; every photo estimate → per-Max-Estimator-line-item-id; every widget booking → per-Instant-Booking-slot-id; all pinned to the upstream LLM + AI-Booking-Agent prompt-template versions.

## Pitfalls reinforced

- **P28:** FORM-only is the correct outreach lane (HubSpot `hs-form` markup on `/book-demo`); no `sales@` or `hello@` first-party published; do NOT domain-guess a `sales@housecallpro.com` inbox. Phone +1 858-999-3060 is verbatim but is a contact-center route, not a procurement lane.
- **P41:** Trust Center is **external** (`trust.housecallpro.com/`) — explicitly note in the audit-letter + chunk that compliance posture is Trust-Center-inferred (not verbatim on the marketing surface). Do NOT fabricate a first-party compliance page URL.
- **P44:** cat-heredoc + patch + write_file for cron-safe CSV/JSONL append.