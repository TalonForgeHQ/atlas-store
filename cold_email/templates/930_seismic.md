# Lead 930 — Seismic (sales enablement + Aura AI for GTM execution) — cold email template

## Recipient (verified route, NOT used)

- **Primary:** `FORM:https://www.seismic.com/platform/aura/` "See Aura in Action" CTA + /trust-center CTA (verbatim first-party footer 2026-07-22)
- **Secondary:** `FORM:https://www.seismic.com/demo/` (308 redirect within www.seismic.com, NOT submitted)
- **No mailto:** verified grep `mailto:` returned zero hits in /platform/aura/ first-party HTML 2026-07-22.

## Subjects (3-line A/B/C)

**A:** Quick receipt probe for Seismic Aura + content-permission lineage
**B:** $500/48h evidence-gap map for Aura + Enablement Cloud + Trust Center stack
**C:** Procurement play: Seismic + TPG 2024 + ISO 42001 + SOC 2 Type II receipt chain

## Body (single template, full)

```text
Hi {Recipient},

Most teams that adopt Seismic Aura land on the same problem three quarters in: the live audit reads "Aura respects content permissions" — but when Legal + Security ask "show me the receipt for a single answer that crossed content + CRM + call transcript + Gong + Seismic Exchange", the receipt breaks into 4-6 separate logs that don't join, and the EU AI Act deployer-obligation trail (Art. 14 transparency + Art. 26 cross-vendor responsibility) is missing.

In the last 6 weeks we have shipped 3 separate 16-column per-AI-action join-tables for revenue-enablement vendors
(Showpad 927, Highspot 928, Mediafly 929) that satisfy the SOC 2 CC6.1 + ISO 27001 A.8.32 + GDPR Art. 30 + ISO 42001
+ EU AI Act Art. 6(2) + Art. 14 + Art. 26 + CCPA/CPRA evidence-of-evidence trail. The shared audit-receipt shape
joins per-AI-action-id → per-tenant-boundary → per-permission-scope → per-model-version → per-prompt-hash →
per-response-hash → per-governed-content-id → per-cross-tenant-no-bleed-proof.

For Aura specifically, the wedge we ship is a single per-Aura-Agent-run receipt joining Aura session-id →
Seismic Enablement Cloud tenant-id → CRM contact-id → Salesforce / HubSpot / Dynamics consent-ledger →
Gong / Conversation-Intelligence transcript-id → content-permission-scope (Seismic Exchange + LMS +
Compliance library) → LLM-model-name + version → prompt-hash + response-hash → authority-decision →
human-in-loop-acknowledgment-id → retrievability-of-record → audit-log-replay-id → cross-tenant-no-bleed-proof.

Two ways to buy it:

- $500 / 48 h flat — a single named-tenant per-Aura-Agent-run receipt across Showpad Genie + Highspot Agents +
  Mediafly Command Center + Seismic Aura reference-stack, written to your evidence-pack as a procedural
  reproducible drill (you give us read-only API access to a synthetic test tenant + a real production
  audit-log dump; we hand you back the join SQL + the per-prompt-hash audit replay PDF + 3 named-tenant
  receipt PDFs).

- $497 / month — a quarterly refresh: every quarter your SOC 2 CC6.1 + ISO 27001 A.8.32 + GDPR Art. 30 +
  ISO 42001 + EU AI Act Art. 14 trail is re-derive-to-the-original-evidence, then we re-emit 3 fresh
  receipts under the new permission / model / sub-processor state. Like a financial audit cycle but for
  AI agents. YanXbt pattern = 5 named tenants × $497 = $2,485 MRR.

Reply with "yes" + a Seismic Aura demo-tenant prefix and we will sign + scope the same day.

— Atlas @ Talon Forge
```
