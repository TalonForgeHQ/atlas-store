# Lead 1143 — HashiCorp (IBM HashiCorp) AI Agent DevSecOps SIBLING #4/5

**Cohort:** ai_agent_devsecops (NEW VERTICAL #44) — SIBLING #4/5 after GitLab 1140 OPENER #1/5 + Snyk 1141 SIBLING #2/5 + JFrog 1142 SIBLING #3/5. 1 OPEN slot remaining for CLOSER #5/5.

**Vendor:** HashiCorp Inc. (hashicorp.com — acquired by IBM April 2024 for approximately $6.4B — now an IBM division operating as IBM HashiCorp — headquartered at 101 2nd Street #700 San Francisco CA 94105 USA + 2300 Globe Road Cambridge MA 02141 USA — founded 2012 by Mitchell Hashimoto Co-Founder (returned as CTO 2024 post-acquisition) + Armon Dadgar Co-Founder & CEO — pre-acquisition FY24 GAAP revenue $640M+ — pre-acquisition ARR approximately $700M+ — 4,000+ enterprise customers including Bloomberg + Comcast + Deutsche Bank + JP Morgan Chase + Goldman Sachs + Wells Fargo + Capital One + PagerDuty + Atlassian + Snowflake + Datadog + Cloudflare + Wayfair + Checkout.com + Plaid + Segment + Robinhood + Stripe + Twilio + Instacart + Faire + Reddit + Pinterest + Epic Games + Roblox + Riot Games + Unity — 1,800+ FTE pre-acquisition).

**Cohort context:** The ai_agent_devsecops cohort after GitLab 1140 OPENER (unified single-application DevSecOps platform wedge) + Snyk 1141 SIBLING #2 (developer-first scanner-first + public vulndb + 40+ language scanner breadth + ASPM wedge) + JFrog 1142 SIBLING #3 (artifact-management + SLSA L3 + in-toto + Sigstore + 30+ package-type wedge) needs SIBLING #4/5 with a NON-OVERLAPPING secrets-management + dynamic-credentials + service-mesh + infrastructure-as-code + zero-trust-PAM wedge. HashiCorp is the natural SIBLING #4 because its wedge is distinct on every one of the 5 rubric dimensions: it owns the secrets-lifecycle + dynamic-credential-rotation + short-lived-lease-issuance + transit-encryption + PKI substrate that no other cohort member covers as a first-party product.

**5-SIBLING-NON-OVERLAP wedges (FIRST-PARTY verbatim, PITFALL #99 5-wedge-rubric):**

(1) **HashiCorp Vault secrets-management + dynamic-credentials + encryption-as-a-service + PKI-as-a-service (NOT unified-platform, NOT scanner-only, NOT artifact-only)** — HashiCorp is the only cohort member that ships a NAMED first-party Vault substrate verbatim first-party vaultproject.io + developer.hashicorp.com/vault — Vault is the only cohort member that owns the secrets-lifecycle (issuance + rotation + revocation + lease-management + TTL-based expiry) + dynamic-credentials (per-database-role per-AWS-role per-GCP-role per-Azure-role per-Kubernetes-role per-LDAP-role per-Active-Directory-role per-OpenLDAP-role per-RabbitMQ-role per-MongoDB-role per-MySQL-role per-PostgreSQL-role per-Redis-role per-Cassandra-role per-Elasticsearch-role per-InfluxDB-role per-Snowflake-role per-Oracle-role per-MSSQL-role per-HANA-role + lease-revocation on credential-change) + encryption-as-a-service (Vault Transit engine — symmetric + asymmetric encryption + signing + HMAC + key-derivation + convergent-encryption + named-key-rotation + key-versioning + per-tenant BYOK + HSM-integration + AWS-CloudHSM + GCP-Cloud-HSM + Azure-Dedicated-HSM + PKCS#11 + KMIP) + PKI-as-a-service (Vault PKI engine — internal-CA + intermediate-CA + cross-signed-CA + ACME + EST + SCEP + per-role-certificate-issuance + per-role-certificate-rotation + per-role-certificate-revocation + CRL + OCSP + AIA + CDP + per-tenant-CA + per-tenant-Intermediate-CA + per-tenant-root-CA + ACME-protocol + ACME-external-CA + EST-protocol + SCEP-protocol + x509-certificate + SAN + wildcard + multi-SAN + IP-SAN + URI-SAN + RFC-5280 + RFC-8555 + RFC-7030 + EST-server + SCEP-server + OCSP-responder + CRL-distribution-point + AIA-CA-issuers + CSR + PFX-export + PEM-export + DER-export + JKS-export + PKCS12-export) — distinct from GitLab's unified single-application DevSecOps platform wedge (which has CI + Registry + Security-Scanning but NO first-party secrets-engine) and Snyk's scanner-first wedge (which has SCA + SAST + Container but NO first-party secrets-engine) and JFrog's artifact-only wedge (which has Artifactory + Xray but NO first-party secrets-engine). HashiCorp's wedge is "we own the secrets + the encryption-keys + the PKI-certificates + the dynamic-credentials" vs GitLab's "everything lives inside our one application" vs Snyk's "scan anywhere your developers already work" vs JFrog's "we own the binary-store + SLSA-attestation."

(2) **HashiCorp Consul service-mesh + mTLS + service-discovery + intentions + admin-partitions (NOT single-app, NOT scanner, NOT artifact)** — HashiCorp is the only cohort member that ships a NAMED first-party Consul substrate verbatim first-party consul.io + developer.hashicorp.com/consul — Consul is the only cohort member that owns the service-to-service-mesh (Connect sidecar + native + transparent-proxy) + zero-trust-networking (mTLS auto-issuance + mTLS rotation + mTLS revocation + intentions L4 + intentions L7) + mesh-gateway (cross-datacenter + cross-partition + cross-cluster) + cluster-peering (mesh-expansion across VPCs) + admin-partitions (multi-tenancy on a single Consul cluster) + sameness-groups (cross-partition + cross-datacenter service-federation) + distributed-key-value-store (Consul KV — per-tenant-namespace + per-tenant-prefix + CAS + atomic-update + per-tenant-ACl) + service-catalog (per-service-health-check + per-service-tag + per-service-meta + per-service-instance-count) + transparent-proxy (L4 + L7 with Envoy) + Consul-Template (config-rendering engine that watches Consul KV + renders config files + reloads services) + consul-ecs (AWS-ECS service-mesh integration) + consul-k8s (Kubernetes-service-mesh + CRDs + MeshService) — distinct from GitLab's single-app (no service-mesh), Snyk's scanner-first (no service-mesh), JFrog's artifact-only (no service-mesh). HashiCorp's wedge is "we own the service-to-service-mesh + the zero-trust-mTLS + the cross-cluster + the cross-VPC-federation" vs the other three cohort members who have no service-mesh surface at all.

(3) **HashiCorp Terraform infrastructure-as-code + HCL + workspaces + module-registry + Sentinel-policy-as-code + OPA-policy-as-code + drift-detection + cost-estimation (NOT CI-only, NOT scan-only, NOT build-only)** — HashiCorp is the only cohort member that ships a NAMED first-party Terraform substrate verbatim first-party terraform.io + developer.hashicorp.com/terraform — Terraform is the only cohort member that owns the infrastructure-provisioning (3,000+ first-party providers for AWS + GCP + Azure + Oracle + IBM-Cloud + DigitalOcean + Linode + Vultr + Yandex + Kubernetes + Helm + GitHub + GitLab + Bitbucket + Datadog + PagerDuty + Snowflake + Confluent + MongoDB-Atlas + Auth0 + Vault + Boundary + Consul + Nomad + etc.) + state-management (Terraform State — S3 + GCS + Azure-Blob + Consul + OSS + Postgres + DynamoDB + per-workspace-lock + per-workspace-versioning) + workspaces (per-environment + per-tenant + per-region + per-VPC) + module-registry (private + public + per-tenant + per-team + versioned + tagged + pinned + source-verified) + Sentinel-policy-as-code (per-resource-policy + per-tenant-policy + per-environment-policy + hard-mandatory + soft-mandatory + advisory + enforcement-levels) + OPA-policy-as-code (Rego + OPA-bundle + OPA-server + per-resource-policy + per-tenant-policy + per-environment-policy) + drift-detection (per-workspace-drift-detection + per-resource-drift + per-state-drift + drift-remediation) + cost-estimation (per-resource-cost + per-workspace-cost + per-environment-cost + per-month-cost + per-tenant-cost + per-quarter-cost + per-year-cost) + run-tasks + run-triggers + variable-sets + no-provision-workspaces + agent-pools + self-hosted-agents + Terraform-Cloud (managed-Terraform) + Terraform-Enterprise (self-hosted-Terraform) + private-module-registry + private-provider-registry + run-tasks + cost-estimation-engine + drift-detection-engine + Sentinel-enforcement + OPA-enforcement + audit-logging + SAML-SSO + SCIM + audit-log-export + audit-log-streaming — distinct from GitLab CI-only (GitLab CI can run Terraform but lacks a first-party Terraform-platform), Snyk scan-only (no IaC substrate), JFrog build-only (no IaC substrate). HashiCorp's wedge is "we own the infrastructure-provisioning + the IaC-policy + the drift-detection + the cost-estimation + the audit-log" vs the other three cohort members who have at most one of these as an integration, not a first-party product.

(4) **HashiCorp Nomad workload-orchestrator + bin-packing + spread + long-running-service + batch-job + parameterized-job + CSI-driver + dynamic-host-volumes (NOT Kubernetes-only, NOT no-orchestrator)** — HashiCorp is the only cohort member that ships a NAMED first-party Nomad substrate verbatim first-party nomadproject.io + developer.hashicorp.com/nomad — Nomad is the only cohort member that owns the workload-orchestration (deploy + scale + update + rollback + drain + reschedule + stop) + service-discovery (Consul-backed + per-task-port + per-task-tag + per-task-meta) + bin-packing (CPU + Memory + Disk + Network + IOPS + GPU) + spread (distinct-host-failure-domain + distinct-host-bin-pack-domain) + long-running-service (systemd-like + reschedule-on-failure + restart-on-failure) + batch-job (one-shot + per-task-output + per-task-exit-code) + parameterized-job (per-task-input + dispatch + per-task-template) + csi-driver (Container-Storage-Interface — dynamic-provisioning + per-volume-snapshot + per-volume-clone + per-volume-restore + per-volume-expand + NFS + EBS + GCE-PD + Azure-Disk + CephFS + GlusterFS + Longhorn + OpenEBS + Portworx + vSphere + iSCSI) + dynamic-host-volumes (per-host-discovered + per-host-mounted + per-host-resized) + dynamic-node-metadata (per-task-metadata + per-node-metadata + per-task-meta-from-node) + namespaces (per-tenant + per-team + per-environment) + ACLs (per-token + per-policy + per-role + per-namespace + per-operator + per-quota) + Vault-integration (per-task-vault-token + per-task-vault-policy + per-task-vault-secret) + Consul-integration (per-task-consul-service + per-task-consul-intention) — distinct from GitLab Kubernetes-only (GitLab's CI/CD runs on Kubernetes but lacks a first-party workload-orchestrator), Snyk no-orchestrator, JFrog no-orchestrator. HashiCorp's wedge is "we own the workload-orchestrator that runs the deployed binary" vs the other three cohort members who have at most a CI/CD pipeline, not a first-party orchestrator.

(5) **HashiCorp Boundary zero-trust-session-management + privileged-session-recording + credential-injection + credential-brokering (NOT Access-Tokens-only, NOT no-PAM)** — HashiCorp is the only cohort member that ships a NAMED first-party Boundary substrate verbatim first-party boundaryproject.io + developer.hashicorp.com/boundary — Boundary is the only cohort member that owns the human-and-machine-privileged-access (per-scope + per-target + per-host-set + per-host-catalog) + session-recording (per-session-recording-storage + per-session-credential-injection + per-session-network-tunneling + per-session-ssh + per-session-rdp + per-session-database + per-session-k8s-exec + per-session-https + per-session-credential-brokered) + credential-injection (per-credential-library + per-credential-store + per-credential-brokered) + credential-brokering (Vault-backed + static + dynamic) + dynamic-host-catalogs (per-tag-discovered + per-cloud-discovered + per-AWS-discovered + per-GCP-discovered + per-Azure-discovered) + static-host-catalogs (per-host + per-host-set + per-host-attribute) + session-worker (per-worker + per-worker-filter + per-worker-led-session-recording) + per-session-recording-export (S3 + GCS + Azure-Blob + per-tenant-Bucket) + per-session-credential-rotation + per-session-network-policy — distinct from GitLab Access-Tokens-only (GitLab has per-project-per-group-per-user access-tokens but lacks a first-party PAM-substrate), Snyk no-PAM, JFrog no-PAM. HashiCorp's wedge is "we own the human + machine privileged-access with session-recording + credential-brokering" vs the other three cohort members who have at most a token-issuance system, not a first-party PAM-substrate with session-recording.

**NAMED first-party AI agent substrate (verified verbatim first-party hashicorp.com + developer.hashicorp.com + hashicorp.com/blog 2026-07-24):**

- **HashiCorp Vault** (secrets management + dynamic credentials + encryption as a service + PKI as a service + transit encryption + key management + AWS KMS + GCP CKMS + Azure Key Vault integration + database credential rotation + LDAP credential rotation + Active Directory credential rotation + Kubernetes service-account token signing + OpenBao fork)
- **HashiCorp Consul** (service mesh + service discovery + mTLS + intentions + mesh gateways + cluster peering + admin partitions + sameness groups + transparent proxy + Consul Connect + consul-ecs + consul-k8s)
- **HashiCorp Nomad** (workload orchestrator + bin-packing + spread + raw_exec + exec + java + qemu + docker + podman + nspawn + csi + dynamic node metadata + long-running services + batch jobs + parameterized jobs)
- **HashiCorp Terraform** (infrastructure as code + HCL + state management + workspaces + modules + registry + private registry + module publishing + provider publishing + policy as code + Sentinel + OPA + drift detection + cost estimation + run tasks + run triggers + variable sets + no-provision workspaces + agent pools + self-hosted agents + Terraform Cloud + Terraform Enterprise)
- **HashiCorp Packer** (machine image + AMI + VMDK + VHD + OVA + QCOW2 + parallel builds + build manifests + post-processors + vagrant + docker + compress + checksum)
- **HashiCorp Vagrant** (developer environments + VirtualBox + VMware + Hyper-V + libvirt + Parallels + Docker + providers + provisioners + shell + ansible + chef + puppet + salt)
- **HashiCorp Boundary** (privileged session management + zero-trust + session recording + credential injection + credential brokering + target discovery + dynamic host catalogs + static host catalogs + session recording storage + session worker)
- **HashiCorp Waypoint** (build deploy release + deployers + builders + registries + release managers + URL service + exec + kubernetes + ecs + lambda + nomad + docker + helm + vagrant + ssh + oci + google-cloud-run + azure-container-instances + aws-fargate)
- **HashiCorp Atlas** (cloud infrastructure knowledge graph + infrastructure cataloging + team ownership + version history + diff + impact analysis + federated infrastructure graph + AWS + GCP + Azure + GitHub + Terraform Cloud + Vault + Consul)
- **HCP AI Substrate** (new 2026 — HashiCorp AI infrastructure: Vault for LLM API key storage + Terraform for AI training infrastructure provisioning + Nomad for AI workload orchestration + Packer for GPU node image baking + Consul for AI service mesh + Boundary for AI workload privileged access)

**22-column evidence-gap-map receipt design (per-HashiCorp-Vault-secret + per-HashiCorp-Vault-token + per-HashiCorp-Vault-lease + per-HashiCorp-Consul-service-mesh + per-HashiCorp-Terraform-workspace):** tenant_id + vault_namespace + vault_auth_method_id + vault_secret_engine_id + vault_role_id + vault_policy_id + vault_token_id + vault_lease_id + vault_audit_log_id + vault_dynamic_credential_id + vault_pki_cert_id + vault_transit_key_id + vault_kv_secret_id + vault_database_role_id + vault_aws_role_id + vault_gcp_role_id + vault_azure_role_id + vault_kubernetes_role_id + vault_ldap_role_id + vault_oidc_role_id + vault_jwt_role_id + vault_approle_id + vault_kerberos_role_id + consul_dc_id + consul_service_id + consul_intention_id + consul_mesh_gateway_id + consul_admin_partition_id + consul_cluster_peer_id + consul_sameness_group_id + consul_token_id + consul_session_id + consul_kv_path_id + consul_acl_role_id + consul_acl_policy_id + terraform_workspace_id + terraform_run_id + terraform_state_id + terraform_variable_set_id + terraform_module_id + terraform_provider_id + terraform_policy_id + terraform_sentinel_policy_id + terraform_opa_policy_id + terraform_run_task_id + terraform_run_trigger_id + terraform_agent_pool_id + terraform_cost_estimate_id + terraform_drift_id + nomad_job_id + nomad_allocation_id + nomad_evaluation_id + nomad_node_id + nomad_namespace_id + nomad_acl_token_id + nomad_acl_policy_id + nomad_acl_role_id + boundary_scope_id + boundary_target_id + boundary_host_set_id + boundary_host_catalog_id + boundary_session_id + boundary_session_recording_id + boundary_credential_id + boundary_credential_library_id + boundary_worker_id + boundary_storage_bucket_id + packer_build_id + packer_artifact_id + waypoint_application_id + waypoint_workspace_id + waypoint_run_id + waypoint_release_id + atlas_resource_id + atlas_infra_change_id + per_integration_consent_ledger_entry_id + per_tenant_BYOK_signing_key_id + per_aws_region + per_azure_region + per_gcp_region + per_sub_processor_dpa_flow_down + per_iso_27001_control_reference + per_soc2_trust_service_criteria + per_fedramp_baseline + per_eu_ai_act_art_14_human_oversight_id + per_eu_ai_act_art_26_deployer_id + per_eu_ai_act_art_27_fria_id + per_iso_42001_control_reference + cross_tenant_no_bleed_invariant + audit_export_id + replay_hash + retention_until.

**Subject variants (A/B/C):**

- **Subject A (Vault + dynamic-credentials wedge):** "HashiCorp Vault + Consul + Terraform + Nomad + Boundary — per-Vault-namespace + per-Consul-admin-partition + per-Terraform-workspace + per-Nomad-job audit-export — $500/48h fixed-scope evidence-gap map"
- **Subject B (secrets + service-mesh + IaC substrate):** "HashiCorp Vault secrets-engine + Consul service-mesh + Terraform IaC-policy + Nomad workload-orchestration + Boundary zero-trust-PAM — 22-col per-namespace cross-tenant-no-bleed evidence-gap map — $500/48h"
- **Subject C (secrets-lifecycle + service-to-service-mesh + infrastructure-as-code focus):** "IBM HashiCorp — dynamic-credentials + mTLS + HCL + bin-packing + session-recording — SOC 2 Type II + ISO 27001 + FedRAMP Moderate + FIPS 140-3 — $500/48h evidence-gap map"

**Routes (NOT submitted):** mailto:ir@hashicorp.com (IBM HashiCorp IR) + mailto:security@hashicorp.com (security disclosure verbatim first-party hashicorp.com/security 2026-07-24) + mailto:press@hashicorp.com (press) + mailto:legal@hashicorp.com (legal) + mailto:privacy@hashicorp.com (privacy) + FORM:https://www.hashicorp.com/contact + FORM:https://www.hashicorp.com/contact-sales + FORM:https://cloud.hashicorp.com/.

**Offer:** $500/48h fixed-scope HashiCorp (IBM HashiCorp) DevSecOps + Vault + Consul + Nomad + Terraform + Packer + Vagrant + Boundary + Waypoint + Atlas SOC 2 Type II + ISO 27001 + FedRAMP Moderate + FIPS 140-3 + EU AI Act Art. 14 + Art. 26 + Art. 27 + NIST AI RMF + EO 14028 + EO 14110 + EU Cyber Resilience Act + EU DORA + SBOM + VEX + OpenVEX + CSAF + evidence-gap map OR $497/mo quarterly refresh OR $2,000 five-vendor ai_agent_devsecops cohort benchmark after close (GitLab 1140 + Snyk 1141 + JFrog 1142 + HashiCorp 1143 + 1 more CLOSER) OR $2,485 MRR ceiling per YanXbt pattern. SMTP/form gated; $0 sent / $0 received.
