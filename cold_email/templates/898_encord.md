# Template 898 — Encord — ai_computer_vision_infrastructure sibling #3/5

## Variant A — Encord Active labelling workflow + per-frame annotation audit-trail

**Subject (52 chars):** Encord Active — per-frame annotation audit lane?

**Body:**

Hi Eric + Boris,

Encord Active's model-assisted + per-frame annotation validation + per-annotator inter-annotator agreement (IAA) lane is the cohort-unique wedge — landing.ai LandingLens + Roboflow + V7 ship label-quality features but none ship a per-annotator-IAA disagreement matrix reproducible across training-set versions. For B2B SaaS in the 200-2000 employee range with production CV models built on human-in-the-loop annotation pipelines, the dominant failure mode isn't model accuracy — it's label-drift between annotator cohorts, where per-annotator-IAA disagreement matrix + per-frame-annotation provenance matters more than +1 mAP.

Atlas runs a $500/48h CV-infrastructure ROI audit + $497/mo retainer focused on: per-annotator-IAA disagreement matrix reproducible across dataset-versions, per-frame-annotation-provenance audit-replay, per-labeler-credential scoping.

Worth 20 minutes? — Atlas, Talon Forge LLC

## Variant B — Encord DICOM + medical-imaging segmentation lane

**Subject (51 chars):** Encord DICOM segmentation — HIPAA audit lane?

**Body:**

Hi Encord team,

Encord's first-party DICOM + NIfTI medical-imaging segmentation workflow with per-DICOM-tag preservation across Train → Inference → Act, plus per-patient PHI redaction + per-tenant HIPAA-eligible-service boundary, is cohort-unique — Roboflow + landing.ai + V7 don't ship a comparable per-DICOM-tag preservation invariant under HIPAA. For clinical-AI shops building per-patient segmentation models where per-DICOM-tag preservation is FDA SaMD II-submission evidence, the wedge is the per-DICOM-tag audit-replay trail.

Atlas runs a $500/48h HIPAA + per-DICOM-tag ROI audit + $497/mo retainer focused on: per-DICOM-tag preservation audit-replay, per-patient PHI redaction lane, per-tenant HIPAA-eligible-service boundary.

15-min call? — Atlas, Talon Forge LLC

## Variant C — Encord Agents + per-segmentation-mask provenance

**Subject (54 chars):** Encord Agents — per-mask provenance audit lane?

**Body:**

Hi Eric,

Encord's Encord Agents workflow with per-segmentation-mask provenance + per-mask model-name + per-mask model-version + per-mask annotator-acknowledgment + per-mask downstream-claim cross-tenant-no-bleed is the most production-grade per-mask audit lane in the cohort. V7 Darwin + landing.ai LandingLens + Roboflow ship segmentation-provenance features but don't ship per-mask model-name + per-mask model-version hash reproducible for downstream FDA SaMD II submission.

Atlas runs a $500/48h Encord Agents ROI audit + $497/mo retainer focused on: per-mask model-version hash audit-replay, per-mask annotator-acknowledgment, per-mask FDA SaMD II-evidence-pinning.

20-min call? — Atlas, Talon Forge LLC

---

## Variant Z — Internal send note

**Channel:** first-party form at https://encord.com/contact/ (sanctioned commercial channel — no guessed general-business inbox).

**Sequence:** Variant A on day-0 → Variant B on day-3 → Variant C on day-7. Stop on reply.

**Compliance posture referenced:** SOC 2 Type II + ISO/IEC 27001:2022 + HIPAA + GDPR + EU AI Act Aug 2 2026.

**Pricing in copy:** $500/48h fixed-scope ROI audit (deliverable: Encord ROI gap-map + per-annotator-IAA disagreement matrix) OR $497/mo quarterly retainer (deliverable: per-quarter Encord ROI refresh + per-DICOM-tag preservation audit-replay + per-mask provenance lane).

**Sender:** Atlas, Talon Forge LLC — autonomous AI revenue ops agent.
