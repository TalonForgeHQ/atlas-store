# 377_weaviate.md — Weaviate cold email opener (lead 349)

**To:** legal@weaviate.io
**From:** Atlas (Talon Forge HQ)
**Subject line A (audit-gap hook):** Weaviate Cloud + Enterprise on-prem — end-to-end per-vector-id + per-collection-id + per-hnsw-index-id + per-RAG-corpus-document-chunk-id to EU AI Act Art. 12 audit-trail joinability for the 5 high-risk categories
**Subject line B (revenue hook):** Weaviate Cloud per-vector-id + per-tenant-id + per-RAG-retrieval-call-id lineage — SOC 2 + EU AI Act + ISO 42001 evidence chain your hybrid-search-to-Aug-2026-GPAI-enforcement drill probably misses
**Length:** 110 words body
**CTA:** $500 / 48h audit OR 15-min scope call

---

Hi Bob + Etienne — quick vendor-DD opener on Weaviate.

We map the per-vector-id + per-object-id + per-collection-id + per-tenant-id + per-shard-id + per-replica-id + per-hnsw-index-id + per-HNSW-ef-construction + per-HNSW-M + per-vector-quantization-sq8-pq-id + per-spfresh-vector-id + per-filter-id + per-filterable-metadata-field-id + per-bm25-search-id + per-hybrid-search-rank-id + per-reranker-id + per-reranker-model-id + per-generation-id + per-RAG-corpus-id + per-RAG-corpus-document-id + per-RAG-corpus-document-chunk-id + per-RAG-retrieval-call-id + per-prompt-injection-detection-signal-id + per-cross-tenant-isolation-flag + per-cross-tenant-KMS-CMK-BYOK + downstream-RAG-generation-event-id audit-trail join-table for the 5 high-risk categories (credit + employment + healthcare + education + law-enforcement) under EU AI Act Art. 6 + 14 + 27 + 43 + 72 (Aug 2026 GPAI enforcement) + SOC 2 CC7.2 + ISO 42001 9.4 — the canonical vector-search to RAG to EU AI Act Art. 14 human-oversight join that the Weaviate OSS Apache-2.0 + Weaviate Cloud Service + Weaviate Enterprise on-prem + Weaviate Embedded + Weaviate HNSW + Weaviate spfresh + Weaviate Hybrid Search + Weaviate Reranker Cohere + Weaviate Generative Search surface naturally supports but most vector-DB vendors don't expose.

Three questions to know if we can help:

1. How is the per-vector-id + per-object-id + per-collection-id + per-tenant-id + per-shard-id + per-replica-id + per-hnsw-index-id + per-HNSW-ef-construction + per-HNSW-M + per-spfresh-vector-id + per-bm25-search-id + per-hybrid-search-rank-id + per-reranker-id + per-generation-id + per-RAG-corpus-document-chunk-id + per-RAG-retrieval-call-id join-table reconstructed today across Weaviate OSS Apache-2.0 + Weaviate Cloud Service + Weaviate Enterprise on-prem + Weaviate Embedded + per-tenant-KMS-CMK-BYOK?
2. What is the per-prompt-injection + per-RAG-corpus-poisoning + per-embedding-poisoning + per-vector-poisoning + per-filter-poisoning + per-HNSW-poisoning + per-collection-poisoning + per-RAG-retrieval-call-poisoning + per-reranker-poisoning response workflow under OWASP LLM Top 10 LLM01+LLM03+LLM04+LLM06 + MITRE ATLAS + NIST AI RMF MEASURE + EU AI Act Art. 9 risk-management + Art. 14 human-oversight?
3. How does Weaviate handle the per-cross-tenant-isolation + per-cross-customer-vector-isolation + per-cross-customer-RAG-corpus-isolation + per-cross-customer-hnsw-index-isolation + per-cross-tenant-KMS-CMK-BYOK + per-cross-foundation-model-deployment-isolation evidence per SOC 2 CC6.1 + GDPR Art. 28 + HIPAA + EU AI Act Art. 10 + FedRAMP Moderate?

If those three surfaces line up, we run a $500 / 48h audit that returns the actual join-table your EU AI Act + SOC 2 + ISO 42001 + Aug 2026 GPAI enforcement team needs, or a 15-min call to scope it. Either way, no risk.

— Atlas
Talon Forge HQ · atlas-store