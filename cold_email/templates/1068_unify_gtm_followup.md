# Lead 1068 — Unify (unifygtm.com — SIBLING #4/5 ai_agent_llm_gateway)

**Cohort position:** SIBLING #4/5 of NEW VERTICAL #36 ai_agent_llm_gateway
after Portkey 1065 OPENER #1/5 + Helicone 1066 SIBLING #2/5 + LiteLLM
1067 SIBLING #3/5.

**Vendor:**
- Company: Unify (unifygtm.com — AI observability + AI gateway for B2B
  GTM (go-to-market) revenue teams — model-agnostic LLM routing +
  per-rep prompt/cost/quality ledger + sales-call transcript mining +
  CRM-synced opportunity lineage)
- Substrate: AI Gateway + GTM-revenue stack (call recording → transcript →
  LLM extraction → CRM field update → rep coaching + manager dashboard)
- Distinct from prior siblings:
  - vs Portkey (provider-credential vault, BYOC, 200+ providers, general
    agent runtime)
  - vs Helicone (observability-first, OpenAI-compatible proxy, cost +
    latency monitoring for any LLM call)
  - vs LiteLLM (open-source Python SDK + proxy + multi-provider router
    in dev tools)
  - Unify = only sibling that fuses AI-Gateway routing with GTM-revenue
    telemetry: per-rep + per-campaign + per-account lineage + CRM-synced
    opportunity IDs.

## Why now

You have an AI-Gateway in production routing Anthropic + OpenAI + Google
calls. You probably already route on cost / latency / fallback. What you
probably do NOT have is the per-rep + per-account + per-deal lineage
showing which AI-routed call landed a $X expansion / churned a logo /
triggered an exec escalation. Unify turns the AI gateway into a GTM
revenue ledger — every routed prompt + completion is tied back to a CRM
opportunity, a campaign, a rep, and a $ outcome.

## Offer

$500/48h fixed-scope **Unify AI-Gateway + GTM-lineage evidence-gap map**
covering:

1. Per-rep + per-campaign + per-account prompt/completion lineage joined
   to CRM opportunity IDs
2. AI-routing cost + latency + fallback replay with per-campaign
   cost-per-meeting + cost-per-pipeline-dollar
3. Per-call transcript → LLM extraction → CRM field update audit log
4. Manager-dashboard + rep-coaching lineage with EU AI Act Art. 14
   human-oversight evidence
5. Tenant-isolation + per-rep rate-limit + cost-reconciliation +
   audit_export_id on demand

…or $497/mo quarterly refresh, or $2,000 four-/five-vendor cohort
benchmark.

## Form route

https://www.unifygtm.com/contact — verified first-party pattern, NOT
submitted; SMTP/form gated; $0 sent / $0 received.

## Subject lines (3 procurement-friendly)

1. "Unify AI-Gateway audit-trail evidence-gap map — 22-column
   per-rep + per-call + per-CRM-opportunity lineage"
2. "Reconstruct per-rep AI-spend + AI-quality at $500/48h"
3. "Unify per-account $X → LLM-routing-cause → CRM-lineage for SOC 2 + EU
   AI Act Art. 14"

## Follow-up touchpoints

- T+0 (this template)
- T+3d: short nudge — "did the AI-Gateway lineage brief land?"
- T+10d: short close — "closing the file unless you want the 22-column
  per-rep lineage walk-through"

## What you do NOT submit

- No email sent
- No form submission
- No payment claim
- No fabricated revenue

(Pitfall #28: mailto:NONE on rendered surface; form-only path.)
