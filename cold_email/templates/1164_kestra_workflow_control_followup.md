# Kestra — workflow-control evidence follow-up

**Vertical:** ai_workflow_orchestration
**Cohort role:** OPENER #1/5
**First-party route:** https://kestra.io/get-started
**Offer:** $500 / 48h fixed-scope audit · $497/mo refresh · $2,000 five-vendor benchmark

## Subject options

1. Kestra execution evidence gap
2. A 48-hour control map for Kestra
3. Can one receipt explain a Kestra run?

## Email

Hi Emmanuel —

Kestra’s declarative workflows, event triggers, 1,200+ plugins, and Cloud/Enterprise deployment options give teams a broad orchestration control plane. Enterprise buyers still need one exportable receipt that explains each run across flow revision, trigger, task, plugin, namespace, worker, retry, secret reference, operator action, and final output.

I can map that evidence gap in 48 hours for $500, or maintain it for $497/month. The result is a buyer-ready control matrix—not a platform replacement—and can anchor a five-vendor workflow-orchestration benchmark.

Worth sending the five-question scope?

— Atlas

## Five-question scope

1. Which immutable IDs join a flow revision, execution, trigger, task run, retry, and output?
2. How are plugin versions, worker versions, and deployment boundaries preserved for replay?
3. Which operator, service account, or AI copilot action changed execution state?
4. Can Cloud and self-hosted Enterprise evidence be exported in one buyer-readable schema?
5. What proves namespace and tenant isolation across secrets, logs, files, and task outputs?

## Evidence receipt proposed

`tenant_id` + `kestra_namespace_id` + `flow_id` + `flow_revision` + `execution_id` + `trigger_id` + `task_run_id` + `task_type` + `plugin_id` + `plugin_version` + `worker_id` + `deployment_version` + `retry_attempt` + `state_transition_id` + `secret_reference_id` + `input_artifact_id` + `output_artifact_id` + `operator_identity` + `ai_copilot_action_id` + `audit_log_id` + `export_checksum` + `cross_tenant_no_bleed_invariant`

## Evidence and guardrails

- First-party `kestra.io/about-us` identifies Emmanuel Darras as CEO and Ludovic Dehon as CTO.
- First-party `kestra.io` describes declarative orchestration and 1,200+ plugins.
- First-party `kestra.io/pricing` lists Open Source, Cloud, and Enterprise Edition.
- First-party commercial route: `https://kestra.io/get-started`.
- Draft only; no form submitted and no email sent.
- $0 sent / $0 received; SMTP remains gated.
