# Cold Email Template — Lead 879 Veridas (ai_voice_biometric_authentication sibling #2/5)

**Route:** HubSpot Meetings booking — `https://meetings-eu1.hubspot.com/edu-gozalo?embed=true` (default — Edu Gozalo is the first of four named-bookable sales-rep URLs on the rendered /en/contact/ surface).
**Backup routes (same cohort, same widget):** `https://meetings-eu1.hubspot.com/joe-scheschareg?embed=true` (Joe), `https://meetings-eu1.hubspot.com/maider-ullate?embed=true` (Maider), `https://meetings-eu1.hubspot.com/pablo-herreras?embed=true` (Pablo).
**Contact name slot:** Eduardo Azanza (CEO and co-founder) — verbatim first-party /en/about-us/ 2025-12-09.
**No first-party inbox published per pitfall #28** — Cloudflare bot-wall on direct curl + no mailto: link on Wayback /en/contact/ capture 2026-01-16.

## Subject Variant A (47 chars)
`Eduardo — Veridas needs a 17-col NIST/BIPA voiceprint receipt`

## Subject Variant B (54 chars)
`ISO 27001 + SOC 2 Type II + NIST Face+Voice: a Veridas audit map`

## Subject Variant C (49 chars)
`25 countries + BBVA + Zambia: a Veridas identity audit map`

## Body (variant A — use as default)

Eduardo — Atlas @ Talon Forge here.

I noticed Veridas ships voice + face + document identity verification on a single platform — with verbatim NIST Face + Voice distinctions, ISO 27001 + ISO 9001 + SOC 2 Type II + SOC 3, and a BBVA Mexican-pensioners proof-of-life deployment that puts you in the BIPA / GDPR Art. 9 / EU AI Act "low-or-non-existent-risk non-remote biometric" lane.

The 17-column per-voice-biometric-auth receipt buyers ask for in procurement DD usually looks like:

```
{tenant_id, voiceprint_id, voiceprint_enrollment_audio_hash,
 voiceprint_enrollment_consent_record_id, voiceprint_model_version_id,
 voiceprint_liveness_model_version_id, face_template_id,
 face_template_consent_record_id, document_template_id,
 document_template_mrz_id, hubspot_meeting_booking_id,
 hubspot_sales_rep_id, nist_face_evaluation_score,
 nist_voice_evaluation_score, deepfake_detection_model_version_id,
 deepfake_detection_score, cross_tenant_no_bleed_proof}
```

I'd like to map that against your 8 cited compliance frameworks (ISO 27001, ISO 9001, SOC 2 Type II, SOC 3, GDPR, CCPA, BIPA, EU AI Act) in a $500 / 48h fixed-scope evidence-gap map — no retainer, no auto-renew, just one exportable receipt you can hand to your next BBVA-grade banking or Zambia-grade government procurement DD.

If the per-call evidence is the bottleneck, the second-tier $497 / month quarterly refresh keeps the receipt current as Voice / Face / Document lanes ship. The YanXbt-pattern retainer ($497 / month x 5 clients) only matters if you decide to productize this internally later.

I tried to find a contact inbox on your surface but Cloudflare's email-obfuscation hides it — so I'm requesting the HubSpot Meetings slot on Edu Gozalo's calendar (the first bookable rep on /en/contact/) instead. If you'd rather point me at a different rep (Joe / Maider / Pablo are all on the same widget), I can re-route.

— Atlas
atlas@talonforgehq.com

(Submitted via Veridas HubSpot Meetings booking, not email — no inbox is published on your rendered surface, which I'm respecting as the canonical contact surface.)
