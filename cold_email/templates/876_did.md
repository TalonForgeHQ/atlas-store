# Lead 876 — D-ID (SIBLING #4/5 of ai_video_agent_platform)

**Subject line variants (A/B/C):**

- A — "Gil — D-ID Agents needs a per-tenant render receipt"
- B — "D-ID Studio + PowerPoint plugin: a 16-column audit map"
- C — "D-ID + MyHeritage pedigree + Warner Bros: a $500 audit"

---

Gil —

D-ID's first-party surfaces cover three procurement lanes that most AI avatar competitors do not own at once: a web STUDIO for batch enterprise videos, an Agents lane for conversational AI with a face, and a PowerPoint plug-in that puts the avatar inside the deck the operator is already presenting. That is a stronger enterprise story than "generate a video," but it creates a harder procurement question: can a buyer reconstruct which face seed, consent record, agent version, voice clone, language dialect, and audit-log line belong to one render without crossing tenant boundaries?

I would package that answer as a 16-column per-render evidence receipt:

1. tenant_id
2. render_job_id
3. agent_id
4. agent_version_id
5. face_seed_image_id
6. face_seed_consent_record_id
7. voice_clone_id
8. voice_clone_consent_record_id
9. script_id
10. language_code
11. dialect_code
12. llm_model_version_id
13. prompt_template_version_id
14. tenant_scoping_boundary_id
15. cross_tenant_no_bleed_proof
16. audit_log_replay_id

That ledger maps directly to the claims already published across D-ID's own surfaces: D-ID Studio, D-ID Agents, Video Translate, PowerPoint plug-in, Live Portrait, Creative Reality Studio, named MyHeritage + Warner Bros + Storytel customers, and a Trust Center listing SOC 2 Type II + GDPR + EU AI Act readiness. It also separates D-ID cleanly from HeyGen, Synthesia, and Tavus: HeyGen owns batched avatar video with a DPO inbox, Synthesia owns enterprise avatar video with sales@/dpo@ inboxes and C2PA + ISO 42001, Tavus owns stateful real-time conversational video with Phoenix + Raven + Sparrow — and D-ID owns the desktop-plugin + consumer-app + Agents-with-face lanes none of those three ship.

**Offer this week:** $500 fixed scope, delivered in 48 hours — a buyer-ready evidence-gap map for one representative D-ID Studio + Agents + PowerPoint-plug-in deployment, including face-seed + voice-clone consent lineage, agent version pinning, LLM + prompt template version pinning, language + dialect pinning, and cross-tenant no-bleed proof. If useful, I can maintain the evidence map for $497/mo as the API, agent versions, and trust posture change.

— Atlas @ Talon Forge
