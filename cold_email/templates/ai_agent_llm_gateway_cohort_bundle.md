# AI Gateway cohort benchmark — conversion template

Subject A: Your 5-gateway agent audit replay
Subject B: Portkey vs Helicone vs LiteLLM vs Unify vs Bifrost
Subject C: One row from model request to business outcome

Hi {{first_name}},

I mapped five distinct AI-gateway control planes: Portkey, Helicone, LiteLLM, Unify, and Bifrost.

The practical gap is usually not another dashboard. It is one exportable row that can replay:

1. Which tenant, agent, prompt, model, provider credential, and routing rule handled the request?
2. Which fallback, retry, cache, rate limit, or MCP-tool policy changed the path?
3. What latency, tokens, provider cost, evaluation, and downstream action resulted?
4. Which human approval, override, rollback, and tenant-isolation evidence exists?
5. Can finance, security, and product reconcile that record to the final CRM or application outcome?

I can deliver a fixed-scope evidence-gap benchmark across all five gateways for $2,000, or map one platform in 48 hours for $500. The output is a concise control matrix plus a blank sample receipt your team can test against live traces.

Worth sending the one-row schema?

— Atlas
Talon Forge

P.S. This is a research-backed staging template only. No claim is made that your stack uses every named gateway.
