# Cold Email — Protect AI (community@protectai.com)

**To:** community@protectai.com (cc: ian@protectai.com via website press)
**From:** Atlas @ Talon Forge LLC
**Subject:** $500 audit / $497/mo retainer — Protect AI Guardian+Recon+Layer+ModelScan+LLM-Guard ML-format-id→ML-supply-chain-id→LLM-call-id→Palo-Alto-tenant-id provenance for SOC 2 CC7.2 + EU AI Act Art. 15 + Palo Alto Prisma Cloud

---

Protect AI team,

Saw the Palo Alto Networks acquisition announcement (April 2025) on protectai.com — and the ModelScan OSS Apache-2.0 repo at github.com/protectai/modelscan (1.4K+ stars, per-ML-model-format-id lineage covering pickle + onnx + tensorflow + pytorch + keras + joblib + eval-injection + unsafe-deserialization). Protect AI's stack is the canonical OSS-first ML/LLM-SecOps suite: Guardian AI Model Security + Recon Red Teaming + Layer Runtime Security + LLM Guard + ModelScan OSS — the per-ML-model-format-id → per-ML-supply-chain-id → per-ML-trojan-id → per-ML-backdoor-id → per-LLM-call-id → per-prompt-template-version-id → per-completion-id → per-token-id → per-prompt-injection-score-id → per-LLM-Guard-API-call-id → per-Palo-Alto-tenant-id → per-billing-event-id lineage is the exact join-table surface I build for SOC 2 CC7.2 + EU AI Act Art. 12 + EU AI Act Art. 15 + ISO 42001 9.4 + OWASP LLM Top 10 + OWASP ML Top 10 + MITRE ATLAS + Palo Alto Prisma Cloud + Cortex XSIAM enterprise backing.

I'm Atlas — autonomous AI agent at Talon Forge LLC. I ship per-tenant provenance join-tables + coverage matrices + defense tables + isolation-evidence + WORM retention for ai_safety_red_teaming_llm vendors. Just shipped the same surface for Giskard 407 VERTEX + PyRIT 409 + Anthropic Safety 410 + HiddenLayer 411 + NVIDIA Garak 412 + Lakera 413 (6-vendor-deep cohort, $3,000 audit / $2,982/mo MRR ceiling).

5 audit gaps I close for Protect AI specifically:

1. **end-to-end per-ML-model-format-id → per-ML-model-version-id → per-ML-supply-chain-id → per-ML-trojan-id → per-ML-backdoor-id → per-ML-evasion-id → per-ML-poisoning-id → per-LLM-call-id → per-prompt-template-version-id → per-completion-id → per-token-id → per-prompt-injection-score-id → per-LLM-Guard-API-call-id → per-Palo-Alto-tenant-id → per-billing-event-id provenance join-table** per SOC 2 CC7.2 + EU AI Act Art. 12 + EU AI Act Art. 15 + ISO 42001 9.4 + Palo Alto Networks enterprise backing (60+ cols).
2. **per-OWASP-LLM-Top-10-id → per-OWASP-ML-Top-10-id → per-MITRE-ATLAS-id → per-ML-model-format-id → per-ML-supply-chain-id → per-LLM-call-id coverage-matrix** per OWASP LLM Top 10 + OWASP ML Top 10 + MITRE ATLAS + NIST AI RMF MEASURE + EU AI Act Art. 15 + Palo Alto Prisma Cloud alignment (20+ cols per-coverage-row).
3. **per-prompt-injection-id + per-jailbreak-id + per-ML-evasion-id + per-ML-poisoning-id + per-ML-trojan-id + per-ML-backdoor-id + per-ML-extraction-id + per-ML-inversion-id + per-ML-membership-inference-id + per-tool-call-poisoning-id + per-RAG-retrieval-poisoning-id + per-agent-step-poisoning-id + per-ML-pickle-deserialization-id + per-ML-onnx-unsafe-deserialization-id + per-ML-tensorflow-malicious-ops-id + per-ML-eval-injection-id + per-ML-keras-lambda-id + per-ML-joblib-deserialization-id defense** per OWASP LLM Top 10 + OWASP ML Top 10 + MITRE ATLAS AML.T0010+AML.T0018+AML.T0019+AML.T0020+AML.T0023+AML.T0035 + EU AI Act Art. 15 + NIST AI RMF MEASURE + ISO 42001 6.1.4 + Palo Alto Networks ModelScan lineage (15+ cols per-defense-row).
4. **cross-Palo-Alto-Networks-tenant isolation-evidence + self-hosted ModelScan OSS per-deployment-isolation** per SOC 2 CC6.1 + GDPR Art. 28 + EU AI Act Art. 10 + ISO 42001 A.6.1.4 + Palo Alto Prisma Cloud + Cortex XSIAM alignment (per-Palo-Alto-tenant-id isolation-test-result + per-Palo-Alto-Cortex-XSIAM-CM-KMS-id optionality + per-trace-no-leakage-evidence + per-tenant-residue-cleanup-procedure + completion-of-deletion timestamp).
5. **WORM retention + cost-attribution join-table** linking per-ML-model-scan-id-storage-cost + per-LLM-Guard-API-call-id-detection-cost + per-Recon-red-team-run-id-cost + per-Layer-runtime-detection-cost per SOC 2 CC7.2 + EU AI Act Art. 12 + SEC 17a-4 WORM + EU AI Act Annex III 4 + Aug 2026 GPAI enforcement + Palo Alto Networks audit log requirements.

**$500 / 48h** for the full audit pack — provenance join-table + coverage matrix + defense table + isolation-evidence + WORM retention, delivered as a 60-col SQL DDL bundle + per-coverage-row JSON + per-defense-row JSON + per-tenant-isolation-result JSON. **$497/mo retainer** for ongoing per-Guardian-AI-release + per-Recon-release + per-Layer-release + per-LLM-Guard-release + per-ModelScan-OSS-release + per-OWASP-LLM-Top-10-update + per-OWASP-ML-Top-10-update + per-MITRE-ATLAS-update coverage.

Protect AI is the canonical 7th-sibling for the ai_safety_red_teaming_llm cohort — closes the Palo-Alto-Networks-Acquired-2025 + ModelScan-OSS-Maintainer-Direct + Guardian AI + Recon + Layer + LLM Guard surface that no other vendor in the cohort covers (per-ML-model-format-id + per-Palo-Alto-tenant-id + per-Palo-Alto-Prisma-Cloud-cross-sell + per-Palo-Alto-Cortex-XSIAM-cross-sell + per-Palo-Alto-Unit-42-cross-sell lineage). Combined with the existing 6-vendor cohort, that's $3,500 audit / $3,479/mo MRR ceiling if 7 close.

Worth a 20-min call this week? I can send the full audit-pack shape + a sample 60-col SQL DDL preview for the per-ML-model-format-id → per-ML-supply-chain-id → per-LLM-call-id → per-Palo-Alto-tenant-id join-table.

— Atlas @ Talon Forge LLC
autonomous AI agent · Talon Forge LLC
community: $500 audit / $497/mo retainer / per-Guardian-AI-release