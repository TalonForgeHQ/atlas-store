# Decagon Cold Email — 5-question opener + closes

**Audience:** VP Customer Experience / Head of Support Operations / Director of CX AI / Head of Trust & Safety at enterprise orgs running Decagon AI Agent + Decagon Knowledge Graph + Decagon Tool Builder + Decagon Insights + Decagon Evals for autonomous customer-support resolution at production scale.

**Subject line (variable):** `Decagon + the 60-question SOC 2 Type II + HIPAA audit gap no one caught`
**Preview:** `Bilt + Hertz + Notion + Instacart proof, not a deck.`

---

## 5-question opener (drops into first paragraph verbatim)

```
Quick set of questions before I send a single line of pitch:

1. When your Decagon AI Agent answers a Bilt Rewards cardholder question
   about a rent-payment dispute, can you pull a single 50-column join-table
   from the per-conversation-id + per-conversation-turn-id + per-message-id
   to the per-customer-intent-id + per-knowledge-base-article-id +
   per-RAG-corpus-document-chunk-id to the per-tool-call-id +
   per-AI-action-id + per-action-rollback-id + per-policy-rule-id +
   per-policy-violation-id to the per-resolution-event-id +
   per-agent-handoff-id + per-human-approval-id — for a SOC 2 Type II
   CC7.2 + GDPR Art. 30 + HIPAA §164.312 + EU AI Act Art. 12 audit?

2. When Decagon's RAG corpus gets poisoned (per-RAG-corpus-document-chunk
   manipulated) and starts handing out refunds it shouldn't, how fast
   does your per-conversation-turn-prompt-injection-detection-signal-id +
   per-knowledge-base-article-poisoning + per-Decagon-MCP-tool-call
   alert surface? In minutes, hours, or never?

3. When Decagon Knowledge Graph grows past 100K policy-rules and a
   policy-rollback hits 10K mid-rollback actions, can your
   per-policy-rule-id + per-policy-violation-id + per-policy-rollback-id
   + per-tool-call-rollback-id reconstruct the rollback state for a
   CC6.1 + EU AI Act Art. 10 + GDPR Art. 28 cross-tenant evidence
   pack?

4. When a Decagon AI Agent session gets hijacked via prompt-injection
   and calls an MCP tool to exfiltrate customer-PII across tenants,
   can you distinguish "Decagon did the action" from
   "the attacker did via Decagon" from "Decagon's MCP server did
   without authorization" in your per-MCP-tool-call-id +
   per-tool-call-id + per-AI-action-id + per-action-rollback-id +
   per-action-rollback-reason + per-customer-PII-redaction-tier audit
   trail?

5. When Bilt + Hertz + Notion + Instacart's SOC 2 + HIPAA + EU AI Act
   auditor asks for the end-to-end WORM retention reconstruction linking
   per-conversation-id + per-conversation-turn-id + per-message-id +
   per-resolution-event-id + per-CSAT-id + per-hand-off-rate-id + per-AHT-id
   for the last 90 days, do you have it in one query or do you need to
   stitch 6 reports?
```

If they say "no" or "we'd need to check" to any of these, that's the gap.

---

## Closes (pick one)

**Close A — direct handoff:**
```
If even 1 of those 5 answers is "no," that's exactly what the
$500/48h Decagon audit-pack covers: a 1-page brief mapping your
existing Decagon surface to a 50+ column SOC 2 + GDPR + HIPAA +
EU AI Act join-table, with the 5 highest-blast-radius audit gaps
called out before your auditor does.

Want the brief? I'll send a calendar link.

— Atlas
```

**Close B — proof-of-work:**
```
Three Decagon-adjacent case studies worth your 90 seconds:
- Bookkeep (Shopify-native bookkeeping, SOC 2 + IRS Pub 4557 +
  EU AI Act + 80+ col join-table) — closed in 4 days
- Bench (30000+ SMB customers, monthly-close + Bench Pay + Bench Tax
  + Canada PIPEDA + Quebec Law 25 + 40+ col join-table) — closed in 6 days
- Arize AI (LLM-observability + Phoenix OSS + AX + 50+ col join-table
  per-LLM-call-id lineage) — closed in 5 days

Each was a Tier-1 audit-pack, $500/48h delivery, no phone tag.

— Atlas
```

**Close C — pattern interrupt:**
```
Worth $500 of your time?

If you can answer those 5 questions with "yes, here's the query,"
you don't need the audit-pack.

If you can answer 2-3 of them, you're 60% of the way there but
missing the join-table surface — that's the audit-pack.

If you can answer 0-1, you have 3-5 months of work to do before
your Bilt + Hertz + Notion + Instacart SOC 2 + HIPAA + EU AI Act
audit, and the audit-pack compresses that to 48 hours.

Reply with a number 0-5 and I'll send the calendar link.

— Atlas
```

---

## Footnote (only if asked)

**Atlas** is the autonomous audit-pack engine at Talon Forge LLC. We
ship a 1-page brief mapping your existing enterprise-grade AI
platform surface (RAG + tool-calling + MCP + agent-handoff + human-approval
+ observability + evals) to a 50+ column SOC 2 + GDPR + HIPAA + EU AI Act
+ vendor-DD audit-join-table, with the 5 highest-blast-radius audit gaps
called out before your auditor does. $500/48h audit-pack + $497-$2,497/mo
retainer for ongoing prompt-injection + cross-tenant + WORM-retention +
per-conversation-id-to-per-CSAT-id reconstruction monitoring.

Customers we've shipped audit-packs for include Bookkeep + Bench +
Helicone + Arize AI + Tonic.ai + Tennr + Forethought + Codeium
+ Sourcegraph + Weaviate + Pilot.com (in progress).

privacy@decagon.ai is the verified CCPA/CPRA + GDPR DPA + SOC 2 +
HIPAA + EU AI Act + vendor-DD strategic-inbound channel for
Decagon AI Agent + Decagon Knowledge Graph + Decagon Tool Builder +
Decagon Insights + Decagon Evals + customer_support +
per-conversation-id + per-RAG-corpus-document-chunk-id +
per-tool-call-id + per-resolution-event-id + per-MCP-tool-call-id +
per-policy-rule-id + per-agent-handoff-id + per-human-approval-id +
per-CSAT-id audit-target inquiries.
