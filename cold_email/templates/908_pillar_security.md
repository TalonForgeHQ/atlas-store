# Lead 908 — Pillar Security (Cold Email Template)

**Recipient:** Dor Sarig, Co-founder & CEO  
**Co-founder:** Ziv Karliner, Co-founder & CTO  
**Commercial route:** `sales@pillar.security` + `https://www.pillar.security/get-a-demo` (verbatim first-party, verified 2026-07-22)  
**Vertical:** `ai_agent_security_red_teaming` — SIBLING #3/5 (after Mindgard 906 + HiddenLayer 907)

## Subject variants

**A:** `Dor — Pillar's discovery-to-runtime audit replay`  
**B:** `Pillar — one receipt across AI discovery, testing and protection`  
**C:** `A 48-hour evidence map for Pillar's AI security lifecycle`

## Recommended body

Hi Dor,

Pillar's first-party platform story connects business context to three layers buyers usually review separately: shadow-AI discovery, build-time security testing, and runtime protection. The customer-VPC deployment and RBAC posture make the evidence boundary especially concrete.

The procurement gap is often the join between those layers. Can a reviewer replay one risky AI asset from discovery, through the test that reproduced the weakness, to the runtime control that blocked it — while preserving the business owner, model and data context that made it high priority?

I build that evidence map. For Pillar, I would scope a 16-column receipt around:

`tenant_id + ai_asset_id + business_owner_id + model_id + data_source_id + discovery_snapshot_id + risk_finding_id + red_team_run_id + attack_scenario_id + remediation_id + retest_run_id + runtime_alert_id + protection_action_id + policy_version_id + tenant_boundary_id + audit_replay_id`

Five buyer-facing questions:

1. Can every AI asset and shadow-AI finding be pinned to the discovery snapshot and business owner that exposed it?
2. Can each red-team scenario retain the model, prompt, tool, data source and policy versions used at test time?
3. Can remediation and retest evidence prove closure before deployment?
4. Can runtime alerts and protection actions link back to the exact pre-deployment finding that informed them?
5. Can the full receipt be exported from a customer VPC while proving RBAC and cross-tenant isolation?

Fixed scope is **$500/48h** for the evidence-gap map and one-page audit-replay example, or **$497/mo** for a quarterly refresh.

Worth sending a one-page preview to `sales@pillar.security`?

— Atlas  
Talon Forge LLC

---

**First-party verification (2026-07-22):**

- `https://www.pillar.security/about` — “Dor Sarig Co-founder & CEO” and “Ziv Karliner Co-founder & CTO”; mission: “complete visibility and control to empower enterprises to build and run secure AI systems.”
- `https://www.pillar.security/platform` — “One Platform to Secure your AI Stack” connecting “discovery, testing, and protection.”
- `https://www.pillar.security/` — “Deployed in your Cloud”; “SOC 2 Type II audit”; RBAC; personalized demo form.
- `https://www.pillar.security/get-a-demo` — canonical first-party commercial form; `/about` also publishes `sales@pillar.security`.

**Compliance note:** artifact only. No form was submitted; no email was sent; SMTP remains gated; $0 sent / $0 received.
