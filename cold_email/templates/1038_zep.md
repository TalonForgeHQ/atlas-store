# Zep SIBLING #2/5 — ai_agent_conversation_memory GraphRAG + auto-summarization evidence-gap map

**Vendor:** Zep (getzep.com) | @getzep
**Cohort:** ai_agent_conversation_memory (sibling #2/5 after Mem0 1037 OPENER #1/5)
**Form (verified live 2026-07-23):** https://www.getzep.com/contact
**Founders:** Daniel Chalef (CEO) + Alex Reibman (CTO) — YC W22 batch

## Subject variants

A) "Daniel — Zep GraphRAG + fact extraction audit trail in 5 fields"
B) "Zep memory receipt: graph node → fact table → LLM recall()"
C) "5 evidence gaps across Zep Buffer + GraphRAG + structured facts"

## Email

Daniel — five quick questions before I send the full map:

1. Can an enterprise export one immutable receipt joining tenant, Zep user, session, thread, GraphRAG node id, GraphRAG edge id, fact table id, auto-summary id, and downstream LLM call?
2. When Zep auto-summarizes a long conversation, can the buyer reconstruct the source message window, summary buffer state, token buffer overflow, fact extraction event, and the resulting model context?
3. When a fact is extracted, updated, or deleted in Zep's structured fact table, is every change, cross-tenant boundary check, retention window, and downstream tool call tied to a least-privilege audit trail?
4. Across Zep's hosted control plane, GraphRAG, vector search, four buffer types (Message Window + Summary + Token + Perpetual), and customer data stores, is there a machine-readable responsibility, retention, residency, and deletion matrix?
5. When Zep's memory layer fires a fact-validated recall during a regulated workflow (HIPAA, GDPR, EU AI Act), does the trace preserve the policy check, fact-evidence chain, confirmation boundary, downstream object id, and rollback or remediation path?

Zep's differentiated surface is GraphRAG + auto-summarization + structured fact extraction. Procurement therefore needs more than application logs. It needs a verifiable chain from conversation turn through graph construction, fact extraction, and buffer selection to the downstream model call.

The evidence map would define a 22-field Zep-memory-action receipt: tenant id, Zep user id, Zep session id, Zep thread id, GraphRAG node id, GraphRAG edge id, fact table id, auto-summary id, summary buffer id, message window id, token buffer id, perpetual buffer id, vector search query id, vector search result id, memory PII filter id, retention window, cross-user no-bleed audit trail, LLM call id, tool call id, agent decision id, audit export id, and Zep integration id.

**$500 / 48h evidence-gap map** — six pages, public-evidence PASS/PARTIAL/GAP table, sample Zep memory-action receipt, retention/responsibility matrix, deletion cascade, and prioritized remediation list.

**$497/month quarterly refresh** — re-run the map as Zep ships new buffer types, GraphRAG primitives, and EU AI Act / GDPR / SOC 2 obligations.

Reply "send" and I will forward the five-gap map.

— Atlas @ Talon Forge
https://talonforgehq.github.io/atlas-store

P.S. Mem0 opened the ai_agent_conversation_memory cohort with the memory-CRUD-first substrate. Zep lands the GraphRAG + structured-fact sibling wedge, with cohort-unique buffer selection and fact-evidence audit surface.
