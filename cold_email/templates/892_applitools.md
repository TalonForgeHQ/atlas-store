# 892 — Applitools — Cold Email Template (3-subject variant)

## Subject line A — "deterministic AI" angle
**Subject:** Auditing your Visual AI: SOC 2 + ISO 27001 evidence gap map for Eyes

Hi Anand —

Applitools positions Visual AI as "deterministic by design" (verbatim from your homepage), which is exactly the framing enterprise procurement teams need to defend during an AI-governance audit. But the SOC 2 Type II report + ISO 27001 certificate on the homepage doesn't tell a buyer *how* Eyes handles per-tenant visual-comparison audit-trail replay, per-baseline-version drift-detection, and per-AI-suggestion acceptance-rejection logging at audit time.

I run a 48-hour fixed-scope audit that pins your current evidence surface to a per-test-case 16-column receipt (tenant_id + test_run_id + test_case_id + baseline_snapshot_hash + actual_snapshot_hash + pixel_diff_bounding_box + visual_ai_suggestion_id + suggestion_confidence_score + acceptance_rejection_status + assertion_failure_root_cause_code + cross_browser_render_artifact_id + cross_device_viewport_signature + accessibility_axe_run_id + self_healing_decision_id + tenant_scoping_boundary_id + cross_tenant_no_bleed_proof) cross-tenant-no-bleed. The deliverable is a one-page evidence-gap map you can hand to a Fortune-500 CISO during a SOC 2 audit window or an EU buyer during an AI Act readiness check.

Engagement: **$500 flat, 48h delivery** (single fixed-scope deliverable, no recurring fee) OR **$497/mo quarterly refresh** (rolling evidence-gap map refreshed every 90 days). Neither touches your stack — it's a paper deliverable mapping your current public evidence surface to the AICPA SOC 2 Trust Service Criteria + ISO 27001 Annex A controls + EU AI Act Art. 26 obligations + NIST AI RMF GOVERN/MAP/MEASURE/MANAGE functions.

I don't have a guessed inbox for Applitools — your public surface hides it under Cloudflare email-obfuscation. **If you'd like the gap map, the simplest path is the demo-request form on your site (which I'm not submitting without your say-so).** Happy to walk through the 16-column schema on a 20-minute call first if that's useful.

— Atlas @ Talon Forge

## Subject line B — Thoma Bravo + board-control angle
**Subject:** A 48h evidence-gap map for the Applitools board-control governance lane

Hi Anand —

Thoma Bravo's five-board-seat footprint (David Weiss + Tom Clark + Jay Larson + Sam Yules + Carl Press — all operating partners per your /about/ page) means every material-customer event on Eyes + Ultrafast Test Cloud + Self-Healing already flows through a PE-controlled governance lane. The question an enterprise buyer will ask next is: *what does the audit-trail look like when a board member wants to replay per-tenant per-test-case evidence during a SOC 2 Type II audit window?*

I ship a fixed-scope, paper-only 48h deliverable that pins your current public evidence surface (verbatim first-party SOC 2 Type II + ISO 27001 + SAML SSO + on-prem deployment + dedicated-cloud-on-AWS-Azure-GCP + SaaS-multi-tenant triad) to a per-test-case 16-column receipt with cross-tenant-no-bleed invariant. **$500 flat, 48h delivery** OR **$497/mo quarterly refresh**.

Your public surface hides the inbox under Cloudflare email-obfuscation, so the sanctioned commercial route is `https://applitools.com/contact/demo-request-next/`. I'm not submitting it without your say-so. If a 20-minute call to walk through the 16-column schema is useful first, I'm happy to.

— Atlas @ Talon Forge

## Subject line C — Fortune-500 named-roster angle
**Subject:** Lloyds + NASDAQ + Abbvie + Intuit + Aetna + RBS: Eyes audit-evidence map

Hi Anand —

Your public logo wall (Lloyds Bank + NASDAQ + Abbvie + Intuit + Aetna + RBS + Eli Lilly + Medtronic + Bayer) plus customer-stories panel (Eversana + EVERFI + Frontdoor + Medalia + KPN) puts Applitools in the Fortune-500 visual-AI lane. The next buyer question is: *can you replay, on demand, per-tenant per-test-case evidence for any of those customers during a SOC 2 audit window?*

I run a 48h fixed-scope paper-only audit that produces a per-test-case 16-column receipt (tenant_id + test_run_id + test_case_id + baseline_snapshot_hash + actual_snapshot_hash + pixel_diff_bounding_box + visual_ai_suggestion_id + suggestion_confidence_score + acceptance_rejection_status + assertion_failure_root_cause_code + cross_browser_render_artifact_id + cross_device_viewport_signature + accessibility_axe_run_id + self_healing_decision_id + tenant_scoping_boundary_id + cross_tenant_no_bleed_proof) cross-tenant-no-bleed, pinned to AICPA SOC 2 Trust Service Criteria + ISO 27001 Annex A + EU AI Act Art. 26 + NIST AI RMF.

**$500 flat, 48h delivery** OR **$497/mo quarterly refresh**. No stack access required — paper deliverable only. I'm not submitting your demo-request form (`https://applitools.com/contact/demo-request-next/`) without your say-so; your inbox is hidden under Cloudflare email-obfuscation per pitfall #28.

— Atlas @ Talon Forge
