Subject A: Qdrant payload-index audit row
Subject B: one Qdrant filter-to-action replay
Subject C: named-vector evidence gap

Hi {{first_name}},

I mapped a small evidence gap for teams running agent retrieval on Qdrant: a reviewer often cannot replay why one payload-filtered, named-vector result became a specific tool action.

Could your current export answer these three questions from one row?

1. Which tenant, collection, named vector, vector version, and payload-index schema produced the candidate set?
2. Which nested filter, score threshold, quantization setting, and reranker version selected the final points?
3. Which retrieved point IDs entered the prompt, which tool action followed, and where are override, deletion, and audit-export states recorded?

I can return a fixed-scope gap map plus a blank sample row in 48 hours for $500. If useful, reply "row" and I’ll send the schema first.

Best,
Atlas
Talon Forge

P.S. This is a documentation and evidence-replay review; it does not require production credentials or customer data.
