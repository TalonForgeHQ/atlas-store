# Cold Email Template 325 — Tenstorrent (RISC-V AI Chip + Rack-Scale AI Server Cohort)

**Target:** privacy@tenstorrent.com (Tenstorrent — RISC-V AI chip + rack-scale AI server + DevCloud inference cohort)
**Index:** 235
**Vertical:** ai_infra
**Tier:** 1 (highest priority — wafer-scale-engine-Cerebras cohort + chip-vendor-RISC-V-Tenstorrent cohort + rack-scale-AI-server-Tenstorrent Galaxy + RISC-V-cross-vendor-ecosystem-inheritance + Jim Keller legendary CPU architect as CTO)
**Source:** Tier-1 ai_infra audit-target — verified live 2026-07-15 via curl on https://tenstorrent.com/privacy-policy HTTP 200 160454 bytes exposing mailto:privacy@tenstorrent.com

---

## Subject Line Options (pick one)

1. `AI Act Art. 53 + RISC-V — auditing the Tensix-NOC + Galaxy-rack-scheduler trace for Wormhole + Blackhole`
2. `FedRAMP Moderate + ITAR for Tenstorrent Galaxy + on-prem-air-gapped + defense-natl-lab`
3. `WORM + 7yr reconstruction drill for per-Tensix-core-router-decision + per-Buda-graph-compile`
4. `Jim Keller → Tenstorrent CTO — RISC-V cross-vendor inheritance + per-tenant-KMS-key-id`
5. `per-RISC-V-instruction-trace + Wormhole-NOC + Blackhole-NOC + downstream-agent-decision join-table`

## Email Body (3-line personalized opener → audit-pitch → CTA)

```
Hi Tenstorrent Privacy/Legal/Security team,

Saw Tenstorrent ship Grayskull → Wormhole → Blackhole + TT-Metalium + TT-Buda + TT-Forge + the open-standard RISC-V cross-vendor-ecosystem under Jim Keller's CTO leadership (ex-Apple A4/A5 + AMD K8/Zen + Tesla Autopilot HW3 + Intel x86 — the only chip-vendor with a legendary CPU-architect + the open-standard-RISC-V-inheritance in the same building). The Tensix-NOC + Galaxy-rack-scheduler + DevCloud-per-tenant-isolation surface is a unique audit-trail opportunity — distinct from NVIDIA H100 + AWS Inferentia2 + Trainium2 + TPU + Groq LPU + SambaNova RDU + Cerebras WSE because Tenstorrent is the ONLY chip-vendor that ships the full stack from the open-standard RISC-V instruction-set + the open-source TT-Metalium + TT-Buda + TT-Forge compiler down to the rack-scale-AI-server Galaxy with 32 Wormhole processors per rack.

Atlas runs EU AI Act Art. 12 + Art. 14 + Art. 53(d) + Art. 53(1)(b) + Art. 53(2) + SOC 2 CC7.2 + ISO 42001 9.4 + FedRAMP Moderate AU + ITAR + CMMC Level 2/3 audits for the canonical-distributed-AI-compute + chip-vendor-full-stack sub-vertical. Specifically for Tenstorrent I'd lead with: (1) end-to-end 16-column per-Tensix-core-router-decision + per-RISC-V-instruction-trace + per-Buda-graph-compile + per-Metalium-kernel-launch + per-Wormhole-NOC-router-decision + per-Blackhole-NOC-router-decision + per-Galaxy-rack-scheduler-route + per-DevCloud-tenant + per-LoudBox-tenant + per-QuietBox-tenant + downstream-agent-action provenance join-table for 7yr WORM + quarterly reconstruction drill, (2) 12-column TT-Metalium + TT-Buda + TT-Forge + per-customer-fine-tune + per-on-prem-Tenstorrent-training-corpus + per-government-classified-Tenstorrent-training-corpus license-provenance join-table per EU AI Act Art. 53(d) GPAI training-data transparency + Art. 53(1)(b) copyright-summary + Art. 53(2) publicly-available-summary, (3) prompt-injection + TT-Metalium-kernel-poisoning + TT-Buda-graph-poisoning + RISC-V-instruction-trace-poisoning + Wormhole-NOC-router-bypass + Blackhole-NOC-router-bypass + Galaxy-rack-scheduler-bypass + DevCloud-tenant-bypass + downstream-agent-decision-poisoning defense per OWASP LLM Top 10 LLM01+LLM03+LLM04+LLM06+LLM08 + NIST AI RMF MEASURE + EU AI Act Art. 9 risk-management + Art. 14 human-oversight, (4) 11-column cross-tenant Tenstorrent DevCloud + TT-LoudBox + TT-QuietBox + Galaxy + on-prem + air-gapped + government + defense + national-lab isolation-evidence per SOC 2 CC6.1 + GDPR Art. 28 + HIPAA + EU AI Act Art. 10 + FedRAMP Moderate + ITAR + CMMC Level 2/3 with the Tenstorrent-specific per-Tensix-KMS-key-id + CMK/BYOK + per-government-classified-isolation-flag, (5) 13-column per-Tensix-router-decision + per-RISC-V-instruction-trace + per-Buda-graph-compile + per-Metalium-kernel-launch + per-Wormhole-NOC-router-decision + per-Blackhole-NOC-router-decision + per-Galaxy-rack-scheduler-route + per-inference-call + per-downstream-decision + per-human-oversight-approval + per-cost-attribution + per-WORM-retention + per-quarterly-reconstruction-drill join-table with the Tenstorrent-specific GDPR Art. 17 deletion-propagation drill confirming that right-to-erasure propagates from the per-tenant DevCloud deployment + the per-tenant TT-LoudBox + the per-tenant TT-QuietBox + the per-tenant Tenstorrent Galaxy rack-scheduler deployment + the per-tenant on-prem air-gapped deployment + the per-tenant government-classified deployment + the per-tenant defense deployment + the per-tenant national-lab deployment.

Not pitching generic SOC 2 — pitching the specific Tenstorrent audit-trail surface: the RISC-V-cross-vendor-ecosystem-inheritance + the per-Tensix-core-router-decision + the per-Buda-graph-compile + the per-Metalium-kernel-launch + the per-Galaxy-rack-scheduler-route + the per-tenant-KMS-key-id + the per-government-classified-isolation-flag + the per-on-prem-air-gapped-deployment-isolation-evidence join-table that only a chip-vendor with full-stack-from-RISC-V-AI-chip-to-rack-scale-AI-server has because only Tenstorrent + Cerebras + SambaNova + Groq + Graphcore + Lightmatter + Hailo operate the full chip-to-deployment stack and only Tenstorrent does it on the open-standard-RISC-V cross-vendor-ecosystem with Jim Keller's legendary CPU-architect CTO-pedigree.

48h delivery, $497/mo retainer OR $1,500 one-time audit (10-column TT-Metalium-kernel-poisoning + per-Wormhole-NOC-router-bypass + per-Galaxy-rack-scheduler-bypass defense map). Reply with "Atlas RISC-V" and I'll send the canonical-distributed-AI-compute sub-vertical audit-scope + the Tenstorrent-specific 16-column join-table template + the EU AI Act Art. 53(d) GPAI training-data transparency + Art. 53(1)(b) copyright-summary + Art. 53(2) publicly-available-summary evidence-chain request list.

— Atlas (autonomous AI auditor for the chip-vendor-full-stack + canonical-distributed-AI-compute + RISC-V-cross-vendor-ecosystem cohort)
privacy@trychroma.com (the canonical channel for the 9-vendor-cohort, but open to working the Tenstorrent-specific audit-scope)
```

## Personalization Variables (insert for Tenstorrent cohort)

- `{{company_short}}` = Tenstorrent
- `{{inbox_pattern}}` = privacy@tenstorrent.com (canonical GDPR DPA + SOC 2 + EU AI Act + vendor-DD channel)
- `{{product_surface}}` = RISC-V AI chip (Grayskull + Wormhole + Blackhole + Tensix) + rack-scale AI server (Galaxy + TT-LoudBox + TT-QuietBox) + DevCloud inference + TT-Metalium + TT-Buda + TT-Forge
- `{{chip_architect_pedigree}}` = Jim Keller CTO (ex-Apple A4/A5 silicon lead + AMD K8/Zen architect + Tesla Autopilot HW3 chip architect + Intel x86 lead) — the ONLY legendary CPU architect in the chip-vendor-cohort
- `{{open_standard_inheritance}}` = RISC-V open-standard cross-vendor ecosystem (vs NVIDIA CUDA-only + Cerebras WSE-only + Groq LPU-only + SambaNova RDU-only)
- `{{cohort_narrative}}` = 10-vendor inference-stack-taxonomy now including the chip-vendor-cohort with the wafer-scale-engine-Cerebras + the RISC-V-AI-chip-Tenstorrent + the LPU-Groq + the RDU-SambaNova + the IPU-Graphcore + the GPU-cloud-Lambda + the inference-API-modular-mesh-Together + the marketplace-mesh-HuggingFace + the cog-OSS-packaging-Replicate + the deterministic-LPU-Groq
- `{{distinct_claim}}` = ONLY AI-infrastructure vendor operating the full chip-to-rack-scale-server-to-inference-cloud stack on the open-standard-RISC-V cross-vendor ecosystem

## Follow-up Sequence (Day 3, Day 7, Day 14)

- **Day 3:** "Quick bump — adding the Tenstorrent-specific 16-column per-Tensix-core-router-decision join-table to the audit-scope doc. Want me to send it before the next EU AI Act Aug 2026 GPAI enforcement deadline?"
- **Day 7:** "Wormhole-NOC + Blackhole-NOC + Galaxy-rack-scheduler isolation-evidence per-tenant question — most chip-vendor audit-trail templates default to NVIDIA H100 GPU memory isolation. Does Tenstorrent's open-standard-RISC-V isolation surface need its own per-Tensix-core-isolation template, or does the RISC-V cross-vendor inheritance mean you can reuse the Anyscale + Cerebras + SambaNova isolation templates?"
- **Day 14:** "Final follow-up — closing the loop on the Tenstorrent audit-trail conversation. If timing's not right, no worries. Will keep you on the canonical-distributed-AI-compute + RISC-V-cross-vendor-ecosystem cohort update list."

## Compliance Hooks (for Atlas-tier-1 follow-ups)

- **EU AI Act Art. 12** + **Art. 14** + **Art. 53(d)** + **Art. 53(1)(b)** + **Art. 53(2)** + **Aug 2026 GPAI enforcement**
- **SOC 2 CC7.2** (system operations monitoring) + **CC6.1** (logical access controls)
- **ISO 42001 9.4** (AI system lifecycle) + **6.1.4** (AI risk management)
- **FedRAMP Moderate AU** (audit and accountability)
- **OWASP LLM Top 10 LLM01** (prompt injection) + **LLM03** (training data poisoning) + **LLM04** (model DoS) + **LLM06** (sensitive information disclosure) + **LLM08** (excessive agency)
- **NIST AI RMF MEASURE** function
- **GDPR Art. 28** (processor obligations) + **Art. 17** (right to erasure / deletion-propagation drill)
- **HIPAA** (if Tenstorrent DevCloud hosts healthcare workloads)
- **ITAR** (if Tenstorrent Galaxy hosts defense workloads) + **CMMC Level 2/3** (defense industrial base)
- **SEC 17a-4 WORM** + **IRS 6001** (for downstream regulated-financial-services audit-targets)

## Cohort Position (verified 2026-07-15)

9th ai_infra vertical sibling in the canonical-distributed-AI-compute + chip-vendor-full-stack cohort:
1. Pinecone 227 (retrieval-vector-database)
2. Groq 228 (deterministic-LPU Language Processing Unit)
3. Modal 208 (serverless-runtime)
4. Together AI 229 (multi-vendor-inference-mesh)
5. Modular 230 (compiler-defined-full-stack Mojo + MAX)
6. Hugging Face 231 (marketplace-mesh)
7. Lambda 232 (dedicated-GPU-cloud-rental + supercluster + AI-factory)
8. Replicate 233 (cog-OSS-packaging-tool-per-prediction-mesh)
9. Cerebras 234 (wafer-scale-engine-full-stack-on-prem-cluster) — IMMEDIATE PREDECESSOR
10. **Tenstorrent 235 (RISC-V-AI-chip + rack-scale-AI-server + Tensix-core + open-standard-RISC-V cross-vendor ecosystem with Jim Keller CTO)** — THIS TICK