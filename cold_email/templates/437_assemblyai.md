# Template 437 — AssemblyAI

**To:** contact@assemblyai.com
**Cc:** legal@AssemblyAI.com
**Subject A:** AssemblyAI audit pack — Universal-1/2 + LeMUR provenance join-table SOC 2 + EU AI Act intake
**Subject B:** Atlas AI Audit — AssemblyAI vendor DD for Universal-1/Streaming + LeMUR + Audio Intelligence
**Subject C:** Atlas audit-pack intake for AssemblyAI Universal-1 + LeMUR + Audio Intelligence + EU AI Act

Hi AssemblyAI,

I'm reaching out because AssemblyAI's Universal-1 + Universal-2 + Universal-Streaming + Slam-1 + nano stack ships a 60+ column per-call-id → per-LeMUR-call-id provenance join-table that we'd like to audit for one specific shape: per-LeMUR-prompt-template-extraction-id → per-LeMUR-prompt-leak-id → per-prompt-injection-id → per-jailbreak-id → per-multi-turn-attack-id → per-voice-cloning-id → per-voice-biometric-spoofing-id → per-audio-deepfake-detection-id defense coverage per OWASP LLM Top 10 + MITRE ATLAS + EU AI Act Art. 15.

Five questions to scope it:

1. Does AssemblyAI publish a per-call-id → per-audio-chunk-id → per-utterance-id → per-transcript-id → per-token-id → per-word-id → per-confidence-score-id → per-language-detect-id → per-diarization-id → per-speaker-id → per-topic-detect-id → per-sentiment-id → per-intent-id → per-entity-id → per-pii-redaction-id → per-custom-keyword-id → per-keyword-boost-id → per-model-id → per-Universal-1-model-id → per-Universal-2-model-id → per-Universal-Streaming-model-id → per-Slam-1-model-id → per-nano-model-id → per-model-version-id → per-deployment-id → per-LeMUR-call-id → per-LLM-call-target-id → per-AssemblyAI-tenant-id → per-AssemblyAI-org-id → per-billing-event-id SOC 2 CC7.2 + EU AI Act Art. 12 + ISO 42001 9.4 provenance join-table?
2. Is the per-LeMUR-prompt-poisoning-id + per-RAG-retrieval-poisoning-id + per-knowledge-base-document-poisoning-id + per-LeMUR-prompt-template-extraction-id + per-system-prompt-extraction-id LeMUR-specific attack surface covered in the OWASP LLM Top 10 + MITRE ATLAS coverage matrix?
3. Does AssemblyAI publish a per-PII-redaction-bypass-defense-id + per-content-moderation-bypass-defense-id + per-topic-detection-poisoning-defense-id + per-entity-detection-poisoning-defense-id + per-custom-keyword-poisoning-defense-id + per-LeMUR-prompt-no-residue-evidence defense layer for LeMUR + Audio Intelligence + PII-redaction?
4. Is the per-AssemblyAI-tenant-id + per-AssemblyAI-org-id + per-API-key-id + per-model-id + per-transcript-id + per-LeMUR-call-id + per-billing-event-id cross-tenant isolation evidence auditable per SOC 2 CC6.1 + GDPR Art. 28 + EU AI Act Art. 10?
5. Does AssemblyAI publish a per-Audio-Intelligence-cost + per-PII-redaction-cost + per-topic-detection-cost + per-entity-detection-cost + per-sentiment-cost + per-auto-chapters-cost + per-Universal-1-second-cost + per-Universal-2-second-cost + per-Universal-Streaming-second-cost + per-Slam-1-second-cost + per-nano-second-cost + per-LeMUR-call-cost + per-LLM-call-target-cost + per-transcription-cost + per-batch-call-cost + per-API-call-monthly-cost WORM retention + cost-attribution join-table per SOC 2 CC7.2 + EU AI Act Art. 12 + SEC 17a-4 WORM + PCI DSS Req. 10 + EU AI Act Annex III 4 + Aug 2026 GPAI enforcement?

Reply with the audit-pack PDF or the SOC 2 + EU AI Act vendor-DD form and we'll close the loop in a week.

— Atlas
Atlas AI Audit, Talon Forge LLC
https://atlas-audit.example.com