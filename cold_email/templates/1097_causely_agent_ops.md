# Lead 1097 — Causely (SIBLING #4/5 ai_agent_operations)

**Subject lines:**
1. Causely causal model → remediation evidence map — $500/48h
2. Root cause + blast radius + prevention replay for Causely
3. Causely vs Resolve AI vs incident.io vs Rootly — $2,000 benchmark

Hi {{first_name}},

Causely’s cohort-unique wedge is the live causal model: the replay can show which `topology_snapshot_id` + `causal_model_version` connected a symptom and anomaly to a `root_cause_id`, which `blast_radius_assessment_id` bounded impact, and which `remediation_action_id` + `prevention_policy_id` closed the loop.

A fixed-scope Causely evidence-gap map is **$500 / 48 hours** — one normalized production replay with the 20-column receipt joining tenant, workspace, incident, topology, causal model, symptoms, anomalies, service, causal edges, root-cause confidence, blast radius, remediation, prevention, human override, deployment mode, token cost, latency, and audit export.

For teams comparing the whole ai_agent_operations landscape, Atlas also offers a **$497/month quarterly refresh** or a **$2,000 five-vendor benchmark** covering Resolve AI, incident.io, Rootly, Causely, and the closer.

Commercial route: FORM:https://www.causely.ai/demo — verified first-party demo page with work-email field; not submitted. The normalized replay ends at `audit_export_id` for a portable evidence handoff.

— Atlas @ Talon Forge

## Five replay questions

1. Which `topology_snapshot_id` and `causal_model_version` were active when the symptom appeared?
2. Which `causal_graph_edge_set_hash` supports the `root_cause_id` and confidence score?
3. Which `blast_radius_assessment_id` identified affected services before action?
4. Which remediation execution occurred, and did a `human_override_id` approve or stop it?
5. Which `prevention_policy_id` prevents recurrence, and can the audit export reproduce the chain with no cross-tenant bleed?

## Evidence and compliance posture

- First-party title/description: “Causal Intelligence for AI Agents”; live causal model, root cause, blast radius, incident resolution without human intervention.
- First-party security: no sensitive data or PII stored or processed; encrypted in transit and at rest; SOC 2 compliance.
- Deployment evidence: hybrid mediation layer, on-premise, and air-gapped options.
- Pricing evidence: free 30-day trial; Professional Edition $2,000/month; Enterprise custom.
- Leadership evidence: Shmuel Kliger founder; Christine Miller, Endre Sara, and Enlin Xu co-founders; Yotam Yemini CEO.

**Verification:** mailto:NONE; SMTP/form gated; `$0 sent / $0 received`. SIBLING #4/5, canonical slug `sibling-4-of-5`.
