# Lead 757 — Replicate (cog-inference catalog · cycle-2 sibling #2/5)

**Vertical:** `ai_agent_sandbox_infrastructure` cycle-2 (after cycle-1 FULL 5/5: E2B 751 + Daytona 752 + Modal 753 + Railway 754 + Northflank 755; cycle-2 OPENER #1/5: Fireworks 756)
**Cohort role:** sibling #2/5 — "who owns the agent's open-model catalog inference loop?"
**Tier:** 1 (first-party verified, two named Co-Founders + 100K+ public models + cog containers + ~$60M+ raise)
**Commercial route:** `hello@replicate.com` (first-party sales/business inquiry route on replicate.com/about + replicate.com/contact; `support@replicate.com` is restricted to customer support and is excluded)
**Offer:** $500 fixed-scope 48-hour evidence-gap map, OR $497/month quarterly refresh

---

## Subject variants (pick one)

- `Re: Replicate cog-inference + per-model version + per-cog isolation audit for Cursor/Notion-class agent builders`
- `Quick Replicate cog-inference evidence-gap map — 5 questions, 48 hours`
- `Ben Firshman / Andreas Jansson — Replicate open-model catalog audit, $500 fixed, 48h`
- `100K+ cog models on Replicate — per-model version + per-cog isolation + EU AI Act cascade`

---

## First-touch note (≤120 words)

> Subject: Quick Replicate cog-inference evidence-gap map — 5 questions, 48 hours
>
> Hi Replicate team,
>
> I run a 5-vendor sandbox-infrastructure benchmark (E2B, Daytona, Modal, Railway, Northflank) for production AI agent teams. Cycle-2 opens with inference — the agent's hot loop, not the warm sandbox — and Replicate is sibling #2/5 after the Fireworks OPENER: 100K+ open-source models inside cog containers on AWS + GCP + CoreWeave, Ben Firshman (CEO, ex-Docker) + Andreas Jansson (CTO, ex-Stripe ML), ~$60M+ raised, 50K+ paying developers, Fortune 500 private deployments.
>
> Five questions, $500 fixed, 48 hours:
> 1. per-model version pinning across 100K+ models (SDXL-1.0 vs 1.1, FLUX [schnell]/[dev]/[pro])
> 2. per-cog container isolation proof (memory + network + filesystem between concurrent inferences)
> 3. GPU-pool sub-processor DPA cascade (Replicate + AWS + GCP + CoreWeave + per-region residency)
> 4. per-inference deletion-cascade SLA (input + output + log + cache purge receipt)
> 5. EU AI Act Art. 50 + Art. 14 + Art. 53(1)(b) cascade across the whole 100K+ catalog
>
> Each one becomes a buyer-facing join the Cursor or Notion team can hand to procurement, security, or compliance. $500 fixed, 48 hours, no subscription.
>
> — Atlas @ Talon Forge