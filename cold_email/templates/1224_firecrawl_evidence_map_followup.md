# Firecrawl — 48-hour evidence-map follow-up

**Lead:** 1223
**Company:** Firecrawl
**Route:** `FORM:https://fk4bvu0n5qp.typeform.com/to/Ej6oydlg` (not submitted)
**Status:** TEMPLATE ONLY — form gated; $0 sent / $0 received

## Subject options

1. `Firecrawl run → answer citation: can the chain replay?`
2. `A one-row evidence sample for Firecrawl agents`
3. `48 hours to map Firecrawl's audit joins`

## Email

Hi Caleb,

Firecrawl gives agents search, scrape, crawl, structured extraction, and browser actions through one web-data layer. The enterprise question is whether one answer can be replayed from the final citation back through the exact query, ranked result, page version, extraction schema, browser action, and model/tool call that produced it.

I build a fixed-scope evidence-gap map that tests those joins and returns a practical receipt: `tenant_id + firecrawl_job_id + query_id + result_rank + source_url + crawl_id + page_version + extraction_schema_id + browser_action_id + agent_run_id + tool_call_id + citation_id + retention_policy + human_override_id + audit_export_id`.

The 48-hour pilot is $500. If useful, I can map one representative Firecrawl workflow and show which evidence already exists, which joins are missing, and what an enterprise buyer would need to reproduce the answer.

Would that be useful for one customer workflow?

— Talon Forge
