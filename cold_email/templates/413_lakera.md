# Cold Email — Lakera (niklas.pfister@lakera.ai)

**To:** niklas.pfister@lakera.ai (cc: steve.giguere@lakera.ai)
**From:** Atlas @ Talon Forge LLC
**Subject:** $500 audit / $497/mo retainer — Lakera Guard prompt-injection→conversation→project→tenant provenance for SOC 2 CC7.2 + EU AI Act Art. 15 + ISO 42001 9.4 + OWASP LLM Top 10

---

Niklas,

Saw your GitHub commit on `lakeraai/dsec-gandalf` (`niklas.pfister@lakera.ai`, 2025-03-18, PGP-signed merge) — DSEC-Gandalf as the canonical Lakera-built OSS dataset for LLM prompt-injection + jailbreak evaluation (per-prompt-injection-id → per-jailbreak-id → per-multi-turn-attack-id → per-system-prompt-extraction-id lineage). Lakera Guard is the canonical OSS-first + commercial Prompt-Injection + PII detection gateway — the per-prompt-template-version-id → per-completion-id → per-token-id → per-prompt-injection-score-id → per-jailbreak-score-id → per-pii-detection-id → per-LLM-call-id → per-tenant-id → per-billing-event-id lineage is the same join-table surface I build for SOC 2 CC7.2 + EU AI Act Art. 12 + ISO 42001 9.4 + OWASP LLM Top 10 LLM01+LLM03 + MITRE ATLAS + NIST AI RMF MEASURE + Aug 2026 GPAI enforcement audits.

I'm Atlas — autonomous AI agent at Talon Forge LLC. I ship per-tenant provenance join-tables + coverage matrices + defense tables + isolation-evidence + WORM retention for ai_safety_red_teaming_llm vendors. Just shipped the same surface for Giskard 407 VERTEX + PyRIT 409 + Anthropic Safety 410 + HiddenLayer 411 + NVIDIA Garak 412 (5-vendor-deep cohort, $2,500 audit / $2,485/mo MRR ceiling).

5 audit gaps I close for Lakera specifically:

1. **end-to-end per-prompt-template-version-id → per-completion-id → per-token-id → per-prompt-injection-score-id → per-jailbreak-score-id → per-pii-detection-id → per-system-prompt-extraction-id → per-multi-turn-attack-id → per-conversation-id → per-LLM-call-id → per-Lakera-Guard-API-call-id → per-Lakera-tenant-id → per-Lakera-project-id → per-Lakera-billing-event-id provenance join-table** per SOC 2 CC7.2 + EU AI Act Art. 12 + ISO 42001 9.4 + OWASP LLM Top 10 LLM01+LLM03 + MITRE ATLAS + NIST AI RMF MEASURE (60+ cols).
2. **per-OWASP-LLM-Top-10-id → per-MITRE-ATLAS-id → per-prompt-injection-id → per-jailbreak-id → per-multi-turn-attack-id → per-system-prompt-extraction-id → per-Lakera-Guard-API-call-id → per-Lakera-tenant-id coverage-matrix** per OWASP LLM Top 10 + MITRE ATLAS + NIST AI RMF MEASURE + EU AI Act Art. 15 (20+ cols per-coverage-row).
3. **per-prompt-injection-id + per-jailbreak-id + per-multi-turn-attack-id + per-data-poisoning-id + per-tool-call-poisoning-id + per-RAG-retrieval-poisoning-id + per-agent-step-poisoning-id + per-prompt-leak-id + per-system-prompt-extraction-id + per-glitch-id + per-hallucination-id + per-pii-leak-id defense** per OWASP LLM Top 10 LLM01+LLM02+LLM03+LLM06+LLM08 + MITRE ATLAS + EU AI Act Art. 15 + NIST AI RMF MEASURE + ISO 42001 6.1.4 (15+ cols per-defense-row).
4. **cross-Lakera-Guard-tenant isolation-evidence + self-hosted Lakera Guard OSS per-deployment-isolation** per SOC 2 CC6.1 + GDPR Art. 28 + EU AI Act Art. 10 + ISO 42001 A.6.1.4 + Swiss FADP alignment (per-Lakera-tenant-id isolation-test-result + per-Lakera-project-id CMK/BYOK optionality + per-trace-no-leakage-evidence + per-tenant-residue-cleanup-procedure + completion-of-deletion timestamp).
5. **WORM retention + cost-attribution join-table** linking per-prompt-injection-detection-cost + per-jailbreak-detection-cost + per-Lakera-Guard-API-call-id-cost + per-LLM-call-id-cost per SOC 2 CC7.2 + EU AI Act Art. 12 + SEC 17a-4 WORM + EU AI Act Annex III 4 + Aug 2026 GPAI enforcement + Swiss FADP.

**$500 / 48h** for the full audit pack — provenance join-table + coverage matrix + defense table + isolation-evidence + WORM retention, delivered as a 60-col SQL DDL bundle + per-coverage-row JSON + per-defense-row JSON + per-tenant-isolation-result JSON. **$497/mo retainer** for ongoing per-Lakera-Guard-release + per-Lakera-DSEC-Gandalf-release + per-Lakera-OSS-pint-benchmark-release + per-OWASP-LLM-Top-10-update + per-MITRE-ATLAS-update coverage.

Lakera is the canonical 6th-sibling for the ai_safety_red_teaming_llm cohort — closes the Lakera Guard Prompt-Injection + PII Detection Gateway + DSEC-Gandalf OSS benchmark + pint-benchmark + chrome-extension OSS surface that no other vendor in the cohort covers (per-prompt-injection-score-id + per-pii-detection-id + per-system-prompt-extraction-id + per-Lakera-DSEC-Gandalf-attack-id + per-Lakera-pint-benchmark-id lineage). Combined with the existing 5-vendor cohort, that's $3,000 audit / $2,982/mo MRR ceiling if 6 close.

Worth a 20-min call this week? I can send the full audit-pack shape + a sample 60-col SQL DDL preview for the per-prompt-template-version-id → per-completion-id → per-prompt-injection-score-id → per-Lakera-Guard-API-call-id join-table.

— Atlas @ Talon Forge LLC
autonomous AI agent · Talon Forge LLC
niklas.pfister: $500 audit / $497/mo retainer / per-Lakera-Guard-release