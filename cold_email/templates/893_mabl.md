# 893 — Mabl — Cold Email Template (3-subject variant)

## Subject line A — "Mabl Trainer ML-model per-tenant governance" angle
**Subject:** Auditing Mabl Trainer: per-tenant ML-model artifact-versioning + auto-heal decision trail

Hi Izzy —

Mabl's positioning ("AI-native test automation that makes it easy to build, run, and maintain reliable tests") lands well with enterprise teams, but the SOC 2 Type II report on your trust page doesn't tell a buyer *how* Mabl Trainer handles per-tenant ML-model retraining, per-baseline-version drift-detection, and per-AI-suggestion acceptance-rejection logging when a Fortune-500 CISO opens the audit window.

I run a 48-hour fixed-scope audit that pins your current public evidence surface to a per-test-case 16-column receipt (tenant_id + test_run_id + test_case_id + baseline_snapshot_hash + actual_snapshot_hash + pixel_diff_bounding_box + visual_ai_suggestion_id + suggestion_confidence_score + acceptance_rejection_status + assertion_failure_root_cause_code + cross_browser_render_artifact_id + cross_device_viewport_signature + accessibility_axe_run_id + self_healing_decision_id + tenant_scoping_boundary_id + cross_tenant_no_bleed_proof) cross-tenant-no-bleed. The deliverable is a one-page evidence-gap map you can hand to a Fortune-500 CISO during a SOC 2 Type II audit window or an EU buyer during an AI Act readiness check.

Engagement: **$500 flat, 48h delivery** (single fixed-scope deliverable, no recurring fee) OR **$497/mo quarterly refresh** (rolling evidence-gap map refreshed every 90 days). Neither touches your stack — it's a paper deliverable mapping your current public evidence surface to the AICPA SOC 2 Trust Service Criteria + ISO 27001 Annex A controls + EU AI Act Art. 26 obligations + NIST AI RMF GOVERN/MAP/MEASURE/MANAGE functions.

I don't have a guessed inbox for Mabl — your public surface hides it. **If you'd like the gap map, the simplest path is the /contact-sales form on your site (which I'm not submitting without your say-so).** Happy to walk through the 16-column schema on a 20-minute call first if that's useful.

— Atlas @ Talon Forge

## Subject line B — GitHub + Jira integration governance angle
**Subject:** A 48h evidence-gap map for the Mabl GitHub App + Mabl Link Jira lane

Hi Izzy —

The Mabl GitHub App + Mabl Link for Jira pairing is the integration most enterprise buyers will scrutinize first — because it's the surface where per-PR-build test-result-status flows back into a developer's commit-decision. The question an enterprise buyer will ask next is: *what does the audit-trail look like when a security team wants to replay per-PR-build per-test-run per-auto-heal-decision during an AI-governance review?*

I ship a fixed-scope, paper-only 48h deliverable that pins your current public evidence surface to a per-test-case 16-column receipt with cross-tenant-no-bleed invariant. **$500 flat, 48h delivery** OR **$497/mo quarterly refresh**. Neither touches your stack — it's a paper deliverable mapping your current public evidence surface to the AICPA SOC 2 Trust Service Criteria + ISO 27001 Annex A controls + EU AI Act Art. 26 obligations + NIST AI RMF GOVERN/MAP/MEASURE/MANAGE functions. Happy to walk through the per-PR-build per-test-run audit-trail schema on a 20-minute call.

— Atlas @ Talon Forge

## Subject line C — Visual Coverage + Visual Reuse angle
**Subject:** Visual Coverage + Visual Reuse: the audit-trail surface buyers will ask about next

Hi Izzy —

Visual Coverage + Visual Reuse is the most differentiated wedge on the Mabl homepage — but it's also the surface that raises the most procurement questions, because a buyer reading "reuse trained ML models across tests" wants to know: *which ML model, trained on whose data, with what per-tenant retraining cadence, and what happens to my baseline snapshots when the model updates?*

I ship a fixed-scope, paper-only 48h deliverable that pins your current public evidence surface to a per-element-pixel-diff-bounding-box per-reusable-element-tag-id cross-page aggregation with per-baseline-snapshot SHA-256 + per-actual-snapshot SHA-256 + per-visual-coverage-percentage-per-page + per-AI-suggestion acceptance-rejection logging + per-auto-heal-decision audit-trail. **$500 flat, 48h delivery** OR **$497/mo quarterly refresh**. Neither touches your stack — it's a paper deliverable mapping your current public evidence surface to the AICPA SOC 2 Trust Service Criteria + ISO 27001 Annex A controls + EU AI Act Art. 26 obligations + NIST AI RMF GOVERN/MAP/MEASURE/MANAGE functions. Happy to walk through the per-reusable-element cross-page audit-trail schema on a 20-minute call.

— Atlas @ Talon Forge

## TO: izzy@mabl.com (CF: ce@mabl.com, hello@mabl.com) — FORM PREFERRED: https://www.mabl.com/contact-sales
## VERTICAL: ai_visual_test_automation (NEW VERTICAL #24, sibling #2/5 after Applitools 892 OPENER #1/5)
## TIER: 1 — Real company (mabl.com), real co-founder + CEO (Izzy Azeri, ex-HP Software/Mercury Interactive/Accenture, JSON-LD verified verbatim first-party /about 2026-07-22), real founder (Dan Belcher, ex-HP Software/Mercury Interactive/Accenture). SOC 2 Type II + ISO 27001:2022 on trust page.
## OFFER: $500/48h fixed-scope evidence-gap map OR $497/mo quarterly refresh
