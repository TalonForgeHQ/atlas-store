# 1268 — Cognition (Devin) — SIBLING #2/5 ai_agent_ai_code_generation

**To:** hello@cognition.ai (first-party verified 2026-07-25 on cognition.ai)
**Subject A:** Devin + 5-WEDGE cohort question — ai_agent_ai_code_generation sibling 2/5
**Subject B:** Cognition + SCOTT — 22-field per-Devin-session replay receipt short audit
**Subject C:** Devin session evidence-gap map — 5 evidence questions + $500/48h

Hi Scott,

Atlas here — closing the cohort benchmark for **NEW VERTICAL #68 ai_agent_ai_code_generation** (OpenHands 1267 OPENER → Cognition 1268 SIBLING #2/5). I built a 22-field per-Devin-session + per-conversation + per-reasoning-step + per-AI-extended-engineer-hour replay receipt for SOC 2 + GDPR + EU AI Act Art. 13 logging + Art. 14 human-oversight readiness and want to sanity-check the wedge before it goes public.

**5 evidence questions** (60-second reads, no prep needed):

1. **Cortex Code-Intelligence substrate** — does the per-Devin-session replay receipt join `tenant_id + devin_workspace_id + cognition_org_id + conversation_id + session_id + reasoning_step_id + thought_id + action_id + tool_call_id + tool_call_inputs + tool_call_outputs + swe_model_version + token_input_count + token_output_count + cost_usd + repository_id + commit_sha + pr_id + pr_review_id + human_override_id + ai_extended_engineer_hours + cross_tenant_no_bleed_invariant + replay_hash + audit_export_id` as the canonical 22-field Cortex evidence primitive, or do you ship a different Cortex schema across SWE-1.7 + Devin Fusion + Devin 2.2?

2. **In-house frontier-model SWE-1.7 cost-performance Pareto curve** — first-party 2026-07-08 SWE-1.7 launch post says frontier intelligence at fraction of cost. Does the per-session replay receipt expose `swe_model_version + cost_usd + token_input_count + token_output_count` so a Fortune 100 GitHub Admin can attribute which AI-generated commits used SWE-1.7 vs sidekick vs Claude sub-processor vs GPT-5 sub-processor?

3. **AI Productivity Guarantee ($10M upside)** — first-party `cognition.ai/blog/ai-guarantee` says if Devin delivers less engineering value than paid for, Cognition funds usage up to $10M. Does the per-session replay receipt expose `ai_extended_engineer_hours` as a queryable metric so enterprise customers can self-attest the Guarantee without manual log exports?

4. **Cognition for Government + FedRAMP-trajectory + GovCloud** — first-party `cognition.ai/government` 2026-02-25 launch post. Does the per-session replay receipt pin `residency_id + region_id + govcloud_availability + fedramp_baseline` so Federal procurement can replay any AI-generated commit across the agency-tenancy boundary?

5. **Stack acquisition history (Windsurf + The Interaction Company of California / Poke)** — first-party `cognition.ai/careers` Welcoming The Interaction Company 2026-07-23. Does the per-session replay receipt span the consumer Poke agent + enterprise Devin agent + Windsurf IDE so a single `tenant_id` query can replay agent decisions across the consumer/enterprise seam?

**Offer ladder:**
- **$500 / 48h** — fixed-scope per-Devin-session + per-Cortex-substrate 22-field evidence-gap map (delivered as a 22-field CSV + 5-page audit memo).
- **$497 / mo** — quarterly refresh retainer (Cortex schema version-pinning + SWE-1.7 release notes + Devin Fusion rerouting + Federal/RSS replay).
- **$2,000** — five-vendor ai_agent_ai_code_generation cohort benchmark at close (OpenHands 1267 OPENER + Cognition 1268 SIBLING #2 + SIBLING #3 + SIBLING #4 + CLOSER #5).

**What I do NOT need:** a phone call, a demo, a security review. Just an email reply telling me which of the 5 questions the public docs already answer (and a link), and which need a 22-field Cortex replay primitive to be queryable.

— Atlas @ Talon Forge
[tick-1268-cognition-devin-ai-agent-ai-code-generation-sibling-2-of-5]
