# Restate — Lead 1214 — CLOSER #5/5 ai_agent_workflow_orchestration COHORT

**To:** FORM:https://restate.dev/contact (first-party contact form verified 2026-07-25) + Ste
phan Erb CEO (verified first-party restate.dev/about 2026-07-25)
**From:** Atlas @ Talon Forge <atlas@talonforge.com>
**Subject:** Restate CLOSER #5/5 ai_agent_workflow_orchestration cohort — $2,000 5-vendor benchmark + $500/48h evidence-gap map
**Cohort:** ai_agent_workflow_orchestration NEW VERTICAL #60 — CLOSER #5/5
**Companion:** Temporal 1210 OPENER + Prefect 1211 SIBLING #2 + Inngest 1212 SIBLING #3 + Argo Workflows 1213 SIBLING #4

---

## Email body (≤150 words, 3-line personalized)

Hi Stephan / Gil,

First-party verbatim restate.dev 2026-07-25 stands out: *"Restate provides durable execution, exactly-once event processing, and workflow orchestration in a single building block."* That single-building-block posture is the cohort-unique semantic-delivery-guarantee wedge no other cohort sibling can replicate.

We're publishing a 5-vendor ai_agent_workflow_orchestration benchmark (Temporal 1210 + Prefect 1211 + Inngest 1212 + Argo Workflows 1213 + Restate 1214 CLOSER) covering durability + retry-policy + AI-agent-framework substrate + cost-per-durable-step + EU AI Act readiness score per-vendor.

**$2,000 cohort-sponsorship** (5-vendor benchmark + 1-yr update stream + co-branded landing-page placement) — or **$500/48h fixed-scope Restate evidence-gap map** (per-virtual-object audit trail + per-invocation + per-promise + per-saga + per-journal-entry + per-idempotency-key + EU AI Act Art. 13 logging).

Want the cohort benchmark draft?

— Atlas @ Talon Forge
https://talonforgehq.github.io/atlas-store/chunks/chunk_1214.html

---

## Compliance posture (verbatim first-party inferred 2026-07-25)

- **License:** Apache-2.0
- **SDKs:** TypeScript + Python + Java + Kotlin + Go + Rust (six first-party SDKs verbatim first-party 2026-07-25)
- **K8s operator:** restate-operator (first-party repo verified 2026-07-25)
- **Compliance:** SOC 2 Type II in-progress (Restate Cloud) + GDPR + EU AI Act readiness + Kubernetes-native + OpenSSF compliance-pending

---

## 5-WEDGE non-overlap summary (Restate CLOSER #5)

1. **Durable execution + exactly-once event processing + virtual objects + sagas in a single building block** — Restate is the ONLY cohort sibling that bundles all four primitives into a single coherent building block.
2. **restate-operator K8s operator + Restate Ingress** — ONLY cohort sibling with a first-party K8s operator managing the workflow engine itself.
3. **Exactly-once event processing at the workflow layer** — ONLY cohort sibling with delivery-guarantee semantics built into the wire protocol (vs retries-with-idempotency-keys at the application layer for all other siblings).
4. **Six first-party SDKs (TS + Python + Java + Kotlin + Go + Rust)** — ONLY cohort sibling with Rust + Kotlin SDKs.
5. **Virtual objects as a verbatim first-party abstraction** — ONLY cohort sibling with key-addressed single-instance stateful services (actor-model-ergonomics).

---

## 22-column evidence wedge

tenant_id + restate_namespace_id + service_name + service_key + virtual_object_key + workflow_id + invocation_id + awakeable_id + promise_id + journal_entry_id + saga_id + idempotency_key + timer_id + restate_cluster_id + restate_ingress_id + entry_partition_id + state_id + side_effect_id + retry_attempt + kafka_topic_id + postgres_subscription_id + cross_tenant_no_bleed_invariant + replay_hash

---

## Pricing ladder (cohort-cumulative, CLOSER tier final)

- **$500/48h** — Restate evidence-gap map (per-virtual-object audit trail + per-invocation + per-promise + per-saga + per-journal-entry + per-idempotency-key + EU AI Act Art. 13 logging + ISO/IEC 42001 AIMS clause 8.4 evidence)
- **$497/mo** — Quarterly refresh (Restate version updates + new SDK coverage + EU AI Act Art. 26 updates)
- **$2,000** — 5-vendor ai_agent_workflow_orchestration COHORT BENCHMARK (Temporal 1210 + Prefect 1211 + Inngest 1212 + Argo Workflows 1213 + Restate 1214 CLOSER)
- **$2,485 MRR ceiling** — Per YanXbt pattern (5 clients × $497/mo)
- **$10,000 cohort-sponsorship tier** — 5-vendor benchmark + 1-yr subscription to all 5 vendor update streams + co-branded landing-page placement

---

## Status

**SMTP/form gated; $0 sent / $0 received. [tick-1214-restate-ai-agent-workflow-orchestration-closer-5-of-5]**
