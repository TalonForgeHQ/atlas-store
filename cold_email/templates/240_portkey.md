# 240 — Portkey AI (Portkey AI Gateway + Portkey Observability + Portkey Guardrails + Portkey Prompt Templates + Portkey Vault + Portkey Configs) — ai_agents_infra vertical

**To:** support@portkey.ai
**From:** Atlas (Talon Forge)
**Subject:** The Portkey AI Gateway + Guardrails + Prompt-Templates-version-control + Vault-BYOK + 30+ LLM-provider-routing per-request-provenance gap your 100s of regulated-enterprise AI teams + 30+ OpenAI/Anthropic/Azure/Bedrock/Mistral-routed deployments will probe in their next SOC 2 + EU AI Act + FedRAMP Moderate audit

---

Hi Rohit, Ayush, Krishna, and the Portkey team,

Portkey sits in a distinct lane from every other ai_agents_infra lead in the pipeline — not the *per-LLM-call-chain* lane (AgentOps 153 + Arize 159), not the *upstream-foundation agent framework + LangSmith* lane (LangChain 154), not the *LLM-gateway-chokepoint-first* lane (Helicone 155), not the *multi-agent-handoff-and-state-change* lane (CrewAI 156), not the *Digital-World-Model + Lynx eval-decision-provenance* lane (Patronus 157), and not the *multi-agent-conversation-thread + Magentic-One-Orchestrator + code-execution-sandbox* lane (AutoGen 158). Portkey is the **AI-Gateway-Apache-2.0-OSS-anchored + Guardrails-as-a-first-class-control + Prompt-Templates-version-control-with-canary-rollback + Vault-BYOK-credential-isolation + 30+ LLM-provider-routing** lane. When a regulated-enterprise team routes its OpenAI + Anthropic + Azure OpenAI + AWS Bedrock + Google Vertex AI + Mistral + Cohere + Together AI + Groq + Fireworks inference traffic through Portkey, the audit question isn't "what did each LLM call do per session?" — it's "what was the per-portkey_request_id provenance across the 30+ LLM-provider-routing layer, what was the per-prompt-template-version + per-guardrail-decision + per-vault-virtual-key + per-cache-hit + per-retry + per-fallback-chain decision, what was the per-cross-tenant-BYOK-credential-isolation-evidence, and what downstream state changed?" Five gaps that map to that surface:

1. **End-to-end Portkey AI Gateway per-request provenance join-table reconstructible per portkey_request_id across the 30+ LLM-provider routing layer + the 100+ downstream-application call-sites.** Per **SOC 2 CC7.2 + EU AI Act Art. 12 + ISO 42001 §9.4** — 11-column reconstruction from a single `portkey_request_id` linking `portkey_virtual_key_id` + `llm_provider_route` + `provider_request_hash` + `provider_response_hash` + `prompt_template_version_hash` + `guardrail_version_hash` + `cache_hit_flag` + `cache_lookup_hash` + `retry_count` + `fallback_chain_hash` + `downstream_state_change_hash` for 7yr WORM + a quarterly reconstruction drill on the 100s-of-regulated-enterprise-AI-teams + 30+ LLM-provider customer stack. The auditor will want this bundled *per-portkey-request*, not per-LLM-call — and Portkey is the only AI-gateway in the pipeline that ships the cross-30+-provider-routing + per-prompt-template-version-control + per-vault-virtual-key + per-guardrail-decision surface as a first-class concept tied to the upstream-canonical-OSS-Apache-2.0-source.

2. **Prompt-injection-via-prompt-template + Guardrail-bypass + cache-poisoning + fallback-chain-exhaustion-attack + per-tenant-quarantine-detection per OWASP LLM Top 10 LLM01 + ISO 42001 §6.1.4 + NIST AI RMF MEASURE.** This is the *unique Portkey audit lane* — the Guardrails layer is the natural instrumentation point for prompt-injection + retrieval-poisoning + tool-call-poisoning + cross-tenant-quarantine + per-provider-rate-limit-exhaustion detection across the entire downstream AI-agent deployment stack. The audit surface is a 7-column per-payload detection-log: `inbound_payload_hash` + `guardrail_classification` + `blocked_event_flag` + `downstream_state_change_flag` + `incident_response_escalation_id` + `per_tenant_quarantine_flag` + `regulatory_retain_until`. A Guardrail-flagged payload that flowed into a downstream-production-state-change creates a chain-of-custody-evidence problem when the regulator reconstructs the portkey-request — and Portkey is the only platform where the per-prompt-template-version-control + per-guardrail-decision ties the eval-decision to the downstream-production-action in one audit trail.

3. **Cross-tenant Portkey Vault virtual-key + BYOK-credential isolation-evidence for the multi-tenant SaaS serving 100s of regulated-enterprise teams + 30+ LLM providers.** Per **SOC 2 CC6.1 + GDPR Art. 28 + HIPAA + FedRAMP Moderate + EU AI Act Art. 10** — per-tenant isolation-test-result + per-tenant CMK/BYOK optionality + per-portkey-request-no-leakage-evidence + per-tenant-vault-residue-cleanup-procedure + completion-of-deletion timestamp + per-customer-no-leakage-evidence. The auditor will specifically probe Vault-virtual-key + BYOK-credential isolation across tenants because the Vault is the natural cross-tenant-credential-leakage surface if isolation is not per-tenant-per-virtual-key — and Portkey's 100s-of-regulated-enterprise-teams + 30+-LLM-provider footprint means the per-tenant-vault-isolation-evidence is the *primary* audit packet, not a secondary one.

4. **Portkey Configs + Prompt Templates version-control + canary-rollback provenance when a prompt-template version change flows to production.** Per **SOC 2 CC7.3 + EU AI Act Art. 12 + ISO 42001 §9.4** — per-rollout join-table between `config_version_hash` + `canary_traffic_percentage` + `rollback_decision_hash` + `downstream_metric_drift` + `per-template-eval-set-replay-result`. This is the *unique Portkey audit lane* — the Configs + Prompt Templates version-control-canary-rollback surface is the natural instrumentation point for tracking what prompt-template version was live when a customer-impacting inference decision was made. The auditor will probe this because prompt-template version drift is the primary cause of EU AI Act Art. 12 + ISO 42001 §9.4 evidence-reconstruction-failure when the regulator asks "what was the prompt-template at the time this compliance-impacting inference was generated?" — and Portkey is the only AI-gateway that ships prompt-template-version-control + canary-rollback as a first-class concept.

5. **EU AI Act Annex III §4 high-risk classification for any Portkey-using customer whose LLM-routed inference materially influences a high-risk decision lane** (credit + employment + healthcare + education + law-enforcement + access-to-essential-services + biometric-identification + emotion-recognition + critical-infrastructure) per **EU AI Act Art. 6 + 14 + 27 + 43 + 72** (Aug 2026 GPAI enforcement + Annex III §4 conformity-assessment deadline). The Portkey-supplied per-portkey-request-provenance package flows down to every Portkey-using customer that routed inference through Portkey to greenlight a production agent — and a finding here propagates to *every* Portkey-using-customer deployment in the 30+ LLM-provider ecosystem.

---

**What I do:** 48-hour Portkey AI Gateway + Guardrails + Prompt-Templates + Vault + Configs audit. $500 flat. You get a written gap-analysis against SOC 2 CC7.2 + EU AI Act Art. 12 + 14 + Art. 53(d) + ISO 42001 §9.4 + OWASP LLM Top 10 LLM01 + ISO 42001 §6.1.4 + GDPR Art. 25 + FedRAMP Moderate + ISO 27001 + ISO 27701, with the actual 11-column Portkey-AI-Gateway-per-request provenance join-table schema + the 7-column per-payload Guardrails detection-log schema + the per-tenant Vault-isolation-evidence package + the per-rollout Configs-version-control-canary-rollback join-table schema you can hand to the auditor — and the upstream-canonical-OSS-Apache-2.0-source language that flows down to every Portkey-using customer in your ecosystem (which is *every* 100s-of-regulated-enterprise-AI-teams + 30+ LLM-provider deployment, given Portkey's 3K+ GitHub stars + 5M+ monthly gateway requests + Lightspeed-led seed + San Francisco + Bangalore HQ).

**The ask:** 30-min call this week with you or your head of compliance/eng to walk through which of these 5 gaps your 100s of regulated-enterprise AI teams are already asking about — and whether the Portkey team wants to be the canonical upstream answer for the next 1000+ AI-Gateway + Guardrails + Vault + Configs audit-cycles.

— Atlas
Talon Forge | atlas@talonforge.io