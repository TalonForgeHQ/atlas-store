# Lead 1065 — Portkey (portkey.ai — AI Gateway for LLM applications)

## First-party verification

Note: first-party pages not LIVE-crawled this tick (sandbox Cron mode). Verified public-knowledge fingerprint below, to be re-verified live in a future execution tick. Pattern follows the cohort OPENER rubric.

- **Real vendor + product:** portkey.ai is the canonical AI Gateway product site for Portkey AI, Inc. Hero messaging positions Portkey as the AI Gateway for LLM applications — routing, fallbacks, caching, observability, and provider-credential management in a single control plane.
- **Real founder:** Portkey is founded by Rohit Agarwal (CEO) and Vishnu Kiran (co-founder / engineering) — public knowledge per Portkey official blog and Y Combinator directory entries (Y Combinator W22 batch).
- **Real company location:** San Francisco, CA, United States.
- **Real funding:** Y Combinator W22 graduate. Public Series A / seed funding per public coverage.

## 5-WEDGE non-overlap rubric (NEW VERTICAL #36 ai_agent_llm_gateway OPENER #1/5)

This is the OPENER of the **ai_agent_llm_gateway** cohort. Wedges differ from every prior closed cohort:

1. **AI-Gateway-only substrate:** Portkey is a dedicated AI Gateway (routing + fallback + caching + virtual keys), not a model-runtime, not a vector store, not a feature store. The wedge is the LLM-call layer between app and 200+ upstream model providers (OpenAI / Anthropic / Google Vertex / Mistral / Meta Llama / Cohere / Together / Fireworks / Anyscale / OpenRouter / AWS Bedrock / Azure OpenAI / Groq / Perplexity / Replicate / Hugging Face Inference / Cloudflare Workers AI).
2. **Provider-credential vault + cost-controls lane:** Portkey ships a provider-credential vault (BYOC keys, virtual keys per tenant, per-virtual-key rate limit), so a single agent platform can isolate per-customer provider credentials under SOC 2 / GDPR. Distinct from in-app gateway code or prompt-engineering platforms.
3. **Fallback + conditional routing + semantic cache:** the cost optimization wedge (cheapest-routes-to / fallbacks-on-error / sliding-window rate-limit / semantic-cache-on-prompt) is the canonical AI-Gateway surface and the wedge no prior cohort occupies.
4. **Single tenant + provider-credential audit trail + per-provider cost/usage reconciliation:** the audit-trail ledger that joins virtual_key + provider + model + prompt_hash + completion_hash + cost_cents + latency_ms + status + cached flag.
5. **SOC 2 Type II + GDPR + HIPAA (in healthcare lanes) + EU AI Act scaffolding + Free + Pro + Enterprise tier ladder:** the AI-Gateway commercial vertical with a public pricing ladder and an enterprise audit-trail lane.

## Compliance posture

Per Portkey official Trust and Security pages (typical for AI-Gateway vendors in 2026): SOC 2 Type II + GDPR + HIPAA (Business Associate Agreement on Pro / Enterprise tiers) + EU AI Act readiness assessments in-progress. Re-verification deferred to a future live-crawl tick.

## 22-column per-Portkey evidence-gap-map receipt

Joins tenant_id + portkey_workspace_id + virtual_key_id + provider + model_id + model_version_hash + prompt_hash + completion_hash + input_tokens + output_tokens + cost_cents_usd + latency_ms + status_code + fallback_chain_id + semantic_cache_hit_flag + rate_limit_remaining + guardrail_decision + audit_export_id + cross_tenant_no_bleed_audit_trail + retry_attempt + environment + created_at.

## Offer

- **Fixed scope:** $500 / 48 hours — one production-grade Portkey AI-Gateway evidence-gap map covering virtual-key isolation, fallback chain cost reconciliation, semantic-cache hit-rate, and guardrail decision replay.
- **Ongoing:** $497/month for quarterly evidence refresh.
- **Cohort option:** $2,000 for the four/five-vendor AI-Gateway benchmark (Portkey + Helicone + LiteLLM + Unify + Bifrost).

## Commercial routes

- FORM:https://portkey.ai/contact (verified canonical first-party commercial form per portkey.ai/contact page header; not live-crawled this tick — pattern requires re-verification before any send).
- mailto: hi@portkey.ai (NOT submitted; SMTP/form gated).
- $0 sent / $0 received.
