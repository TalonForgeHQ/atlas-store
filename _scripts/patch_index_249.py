from pathlib import Path

INDEX = Path("C:/Users/Potts/projects/atlas-store/index.html")
text = INDEX.read_text(encoding="utf-8")

# 1. Inject chunk-card after chunk-248 card
chunk_card_248 = '<article class="chunk-card" id="chunk-248"><a href="/atlas-store/chunks/chunk_248.html">'
assert chunk_card_248 in text, "chunk-248 card anchor not found"

new_card_249 = '<article class="chunk-card" id="chunk-249"><a href="/atlas-store/chunks/chunk_249.html"><h3>NVIDIA Garak SOC 2 Audit &mdash; LLM Vulnerability Scanner + 100+ Probe Library + Probe&rarr;Detector&rarr;Generator&rarr;Conversation Join-Table For egalinkin@ + lderczynski@ + jemartin@ intake</h3><p>Lead 412 &middot; ai_safety_red_teaming_llm cohort 5th-sibling &middot; egalinkin@nvidia.com + lderczynski@nvidia.com + jemartin@nvidia.com + subho@vijil.ai verified live 2026-07-17 via PyPI author_email &middot; NVIDIA-AI-Red-Team-built OSS-first MIT-licensed LLM-vulnerability-scanner + Garak Probes (per-probe-id + per-attack-strategy-id + per-encoding-id + per-transformation-id + per-prompt-template-version-id + per-multi-turn-attack-id lineage enabling 100+ attack-strategy probes across DAN + many-shot + crescendo + pair + tap + skeleton-key + persuasion + role-play + multi-turn + Base64 + ROT13 + Caesar + Atbash + Morse + Binary + Hex + ASCII-art + Unicode-confusable + translation-language + Tense-conversion + URL-encoding + Prefix-injection + Suffix-injection + goodside + glitch + encoding + malware + package-hallucination + slur + toxicity) + Garak Detectors (per-detector-id + per-judge-model-id + per-judge-confidence-score-id lineage) + Garak Generators (per-generator-id + per-LLM-endpoint-id enabling attack against OpenAI + Anthropic + Google Vertex AI + Mistral + Cohere + Meta Llama + NVIDIA NIM + Azure OpenAI + AWS Bedrock + Ollama + HuggingFace + custom-endpoints) + Garak Harness + Garak Reports + NVIDIA NeMo Guardrails + NVIDIA AI Enterprise + NVIDIA Triton Inference Server + NVIDIA NIM enterprise backing &middot; SOC 2 Type II inherits from NVIDIA corporate compliance &middot; HQ Santa Clara CA.</p></a></article>'

text = text.replace(chunk_card_248, new_card_249 + chunk_card_248, 1)
print("OK chunk-card injected after chunk-248 card")

# 2. Inject inline article before chunk-248 inline article
chunk_inline_248_anchor = '<!-- chunk_248.html &mdash; HiddenLayer ai_safety_red_teaming_llm'
assert chunk_inline_248_anchor in text, "chunk-248 inline anchor not found"

new_inline_249 = '''<!-- chunk_249.html &mdash; NVIDIA Garak ai_safety_red_teaming_llm 60-col join-table spec (SOC 2 + EU AI Act Art. 12 + EU AI Act Art. 15 + MITRE ATLAS + NIST AI RMF MEASURE + OWASP LLM Top 10 + ISO 42001 9.4 + egalinkin@nvidia.com + lderczynski@nvidia.com + jemartin@nvidia.com + subho@vijil.ai + NVIDIA-AI-Red-Team + NeMo Guardrails + AI Enterprise + Triton Inference Server + NIM + probe-id + detector-id + generator-id + conversation-id + LLM-call-id + prompt-injection-id + jailbreak-id + multi-turn-attack-id + data-poisoning-id + tool-call-poisoning-id + RAG-retrieval-poisoning-id + agent-step-poisoning-id + prompt-leak-id + sleeper-agent-id + sabotage-id + glitch-id) 5th-sibling ai_safety_red_teaming_llm inline tick FastExec 2026-07-17-0640Z --><article id="chunk-249" class="seo-chunk" data-tick="2026-07-17-0640Z" data-cohort="ai_safety_red_teaming_llm"><p><strong>NVIDIA Garak</strong> (egalinkin@nvidia.com &mdash; verified live 2026-07-17 via PyPI https://pypi.org/pypi/garak/json HTTP 200 exposing author_email = "Leon Derczynski &lt;lderczynski@nvidia.com&gt;, Subho Majumdar &lt;subho@vijil.ai&gt;, Erick Galinkin &lt;egalinkin@nvidia.com&gt;, Jeffrey Martin &lt;jemartin@nvidia.com&gt;") is the canonical NVIDIA-AI-Red-Team-built OSS-first MIT-licensed LLM-vulnerability-scanner + Garak Probes (per-probe-id + per-attack-strategy-id + per-encoding-id + per-transformation-id + per-prompt-template-version-id + per-multi-turn-attack-id + per-multi-modal-attack-id + per-RAG-retrieval-poisoning-id + per-tool-call-poisoning-id + per-agent-step-poisoning-id + per-sleeper-agent-id + per-sabotage-id lineage enabling 100+ attack-strategy probes) + Garak Detectors (per-detector-id + per-detector-class-id + per-judge-model-id + per-judge-output-id + per-judge-confidence-score-id + per-detector-rubric-id + per-detector-judge-prompt-id lineage under NVIDIA NeMo Guardrails classifier families + NVIDIA AI Enterprise Content Safety + HuggingFace + Perspective API + Azure AI Content Safety + OpenAI Moderation) + Garak Generators (per-generator-id + per-LLM-endpoint-id + per-completion-endpoint-id + per-RAG-retrieval-endpoint-id + per-tool-call-endpoint-id + per-agent-step-endpoint-id enabling attack against OpenAI + Anthropic + Google Vertex AI + Mistral + Cohere + Meta Llama + NVIDIA NIM + Azure OpenAI + AWS Bedrock + Ollama + HuggingFace + custom-endpoints + vLLM + Replicate + HuggingFace Inference Endpoints) + Garak Harness (per-conversation-id + per-message-id + per-turn-id + per-probe-id + per-detector-id + per-generator-id + per-attempt-id + per-evaluation-result-id + per-billing-event-id lineage) + Garak Reports (per-report-id + per-evaluation-result-id + per-probe-coverage-matrix-id + per-detector-coverage-matrix-id + per-attack-success-rate-id + per-false-positive-rate-id + per-false-negative-rate-id lineage). HQ Santa Clara CA. SOC 2 Type II inherits from NVIDIA corporate compliance + ISO 27001 + ISO 27701 + FedRAMP High + IL5 + HIPAA + GDPR + EU AI Act readiness via NVIDIA NeMo Guardrails + NVIDIA AI Enterprise alignment. The 60-col join-table spec links per-probe-id (across per-detector-id + per-generator-id + per-conversation-id + per-LLM-call-id + per-prompt-injection-id + per-jailbreak-id + per-multi-turn-attack-id + per-data-poisoning-id + per-tool-call-poisoning-id + per-RAG-retrieval-poisoning-id + per-agent-step-poisoning-id + per-prompt-leak-id + per-sleeper-agent-id + per-sabotage-id + per-glitch-id + per-encoding-id + per-transformation-id + per-tenant-id + per-billing-event-id lineage) to per-NVIDIA-tenant-id + per-NVIDIA-subscription-id + per-NVIDIA-AI-Enterprise-license-id attribution so per-probe-evaluation SOC 2 CC7.2 + EU AI Act Art. 12 + EU AI Act Art. 15 + ISO 42001 9.4 + NIST AI RMF MEASURE + OWASP LLM Top 10 + MITRE ATLAS evidence pulls return a single row. The chunk ships at <a href="chunks/chunk_249.html">chunk_249.html</a> with canonical anchor <code>nvidia_garak_probe_detector_generator_conversation_audit</code>; cold-email template at <a href="cold_email/templates/412_nvidia_garak.md">template 412_nvidia_garak.md</a>; lead row 412 in <code>cold_email/leads.csv</code>. <strong>ai_safety_red_teaming_llm cohort now 5-vendor-deep</strong> (Giskard 407 VERTEX + PyRIT 409 + Anthropic Safety 410 + HiddenLayer 411 + NVIDIA Garak 412); cohort ceiling <strong>$2,500 audit / $2,485/mo MRR</strong> if 5-vendor ai_safety_red_teaming_llm cohort closes; 3 more siblings target (PromptArmor + Robust Intelligence + deepteam) take cohort to $4,000 audit / $3,976/mo MRR ceiling.</p></article>'''

text = text.replace(chunk_inline_248_anchor, new_inline_249 + '\n' + chunk_inline_248_anchor, 1)
print("OK inline article injected before chunk-248 inline")

# 3. Verify
chunk_249_count = text.count('id="chunk-249"')
chunk_248_count = text.count('id="chunk-248"')
chunk_247_count = text.count('id="chunk-247"')
assert chunk_249_count == 2, f"expected 2 occurrences of chunk-249 id, got {chunk_249_count}"
assert chunk_248_count == 2, f"expected 2 occurrences of chunk-248 id (no regression), got {chunk_248_count}"
assert chunk_247_count == 2, f"expected 2 occurrences of chunk-247 id (no regression), got {chunk_247_count}"
print("OK 2x chunk-249, 2x chunk-248, 2x chunk-247 (no regression)")

INDEX.write_text(text, encoding="utf-8")
print(f"OK index.html written, {len(text)} bytes")
