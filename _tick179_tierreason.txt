Canonical open-source data-orchestration + data-asset + data-pipeline + data-observability + AI-workflow + ML-pipeline + feature-engineering + lineage + testing + partitioning + sensor + schedule + asset-materialization platform for AI/ML + analytics + ETL + ELT + reverse-ETL workloads across cloud + hybrid + on-prem + multi-tenant deployments. Dagster combines Dagster + Dagster Cloud + Dagster+ (the multi-tenant managed control plane for OSS Dagster) + Dagster Pipes (the open-source integration layer that lets orchestrators invoke external compute + models + notebooks + scripts and bring the results back as Dagster assets) + Dagster's Software-Defined Assets (the asset-first abstraction that binds a Python function + its dependencies + its partition + its freshness policy + its auto-materialization policy to a single versioned, observable data product) + Dagster's asset graph (the lineaged DAG of every asset, its upstream dependencies, its downstream consumers, its run history, its retry/replay records, its freshness status, and its partitioning keys) + Dagster's run launcher + run worker + run coordinator + run retries (the underlying execution machinery that schedules, launches, coordinates, retries, and observes every asset materialization) + Dagster's sensors + schedules + automation conditions (the event-driven + cron-driven + partition-driven triggers that drive materialization on new data, on a fixed cadence, on freshness SLA breach, on asset-check failure, or on demand) + Dagster's asset checks (the per-asset data-quality + schema + freshness + null-rate + completeness + custom-policy checks that run after every materialization and surface violations in the asset graph) + Dagster's partitions + backfills + dynamic partitions (the time-window + categorical + ID-based slicing that powers incremental + historical + parallel + rematerialization of large asset sets) + Dagster's I/O managers (the pluggable read/write/serialization/storage layer that moves DataFrames + models + files + blob payloads between the asset's Python function and the underlying storage system like S3, GCS, Azure Blob, Snowflake, BigQuery, Databricks, Postgres, DuckDB, or filesystem) + Dagster's resources (the pluggable client/connection layer for external services like Snowflake, BigQuery, Databricks, dbt, Great Expectations, Soda, Monte Carlo, Slack, PagerDuty, AWS, GCP, Azure, Kubernetes, Spark, Ray, Dask) + Dagster's type system + metadata + structured-event tagging + structured-event breadcrumbs (the typed-output + structured-event surface that lets asset functions emit schema info + row counts + null counts + distinct counts + sample values + custom tags + custom breadcrumbs that show up in the Dagster UI + asset graph + run timeline + observability dashboard) + Dagster's logging + event logs + structured JSON logs (the observability surface that records every step + asset + retry + sensor + schedule + check + alert + metadata + tag + user-action + system-action as a structured event with timestamp + actor + run_id + step_key + asset_key + tags + metadata + parent_run_id + root_run_id + event_type for downstream audit-trail reconstruction) + Dagster's ops + jobs + graphs + job-runs (the legacy graph-DAG abstraction that pairs with Software-Defined Assets for fine-grained step-level orchestration) + Dagster's instance configuration + run launcher + run storage + event log storage + schedule storage + sensor storage + compute log storage (the persistent state backend that stores every run + event + schedule tick + sensor tick + check result + compute log to Postgres + MySQL + SQLite + cloud-managed-DB with configurable retention + WORM storage + per-tenant retention policy + per-tenant-region pinning + per-tenant-encryption-at-rest + per-tenant-KMS-CMK-BYOK + per-tenant-data-residency-region) + Dagster's multi-tenancy + role-based-access-control (RBAC) + permissions + teams + per-role permissions per asset + per job + per sensor + per schedule (the tenant-isolation + authorization surface that enforces per-user + per-team + per-role + per-asset-key + per-job-name + per-sensor + per-schedule read/write/launch/admin permissions across the Dagster Cloud multi-tenant control plane and the OSS Dagster+ on-prem deployment) + Dagster's web UI + asset catalog + run timeline + sensor/schedule view + asset-graph view + lineage view + per-asset-detail view + per-asset-check-result-history view (the operator-facing surface where data engineers + analytics engineers + ML engineers + platform engineers + data product owners + governance teams see the live state of every asset + run + check + alert + sensor + schedule). privacy@elementl.com verified live 2026-07-16 on the official https://dagster.io/privacy page (HTTP 200 exposing privacy@elementl.com as the data-protection contact for Elementl, the parent company of Dagster Labs). Nick Schrock verified live 2026-07-16 on the official https://dagster.io/about page as Founder, CTO of Dagster Labs (with bio "Prior to that, he was a Principal Engineer and Director at Facebook between 2009-17, where he founded the Product Infrastructure team and co-created GraphQL"); Pete Hunt verified as CEO (with bio "previously led teams at Twitter, co-founded Smyte, and was a member of the early React team at Facebook"). Tier-1 ai_agents_infra target for per-asset-key + per-asset-materialization + per-asset-check-result + per-run-id + per-step-key + per-sensor-tick + per-schedule-tick + per-backfill-id + per-partition-key + per-I/O-manager-version + per-resource-config-version + per-type-system-version + per-compute-log + per-event-log + per-KMS-CMK-BYOK-key-id + per-tenant-data-residency-region + per-WORM-retention-flag + per-cross-border-transfer-sccs-version + per-Opt-Out-Of-AI-Training-flag provenance per SOC 2 CC7.2 + GDPR Art. 5(2) accountability + Art. 17 deletion-propagation + Art. 28 processor-controller + Art. 30 records-of-processing + EU AI Act Art. 12 risk-management + Art. 14 human-oversight + Art. 17 deletion-propagation, prompt-injection + Dagster-asset-poisoning + Dagster-asset-check-evasion + Dagster-run-replay-poisoning + Dagster-sensor-poisoning + Dagster-Pipes-bridge-poisoning + Dagster-I/O-manager-poisoning + Dagster-resource-config-poisoning defense per OWASP LLM Top 10 LLM01+LLM03+LLM04+LLM06+LLM08 + NIST AI RMF MEASURE + ISO 42001 6.1.4, and per-tenant + per-asset-graph + per-run-history + per-I/O-manager + per-resource + per-asset-check + per-sensor-tick + per-schedule-tick isolation-evidence per SOC 2 CC6.1 + GDPR Art. 28 + HIPAA + EU AI Act Art. 10. privacy@elementl.com is the verified privacy + GDPR + SOC 2 + ISO 27001 + vendor-DD strategic-inbound channel for AI-workflow + data-orchestration + ML-pipeline + asset-graph + lineage + asset-check + Dagster-Pipes + data-observability + data-quality + multi-tenant-data-platform audit-target inquiries at Dagster.