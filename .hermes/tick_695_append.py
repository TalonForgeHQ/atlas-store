#!/usr/bin/env python3
"""Tick 695 — Append Cursor (Anysphere) lead to both CSVs and prepare chunk data."""
import csv

tier_reason = "Lead 695 — Cursor (Anysphere, Inc. — cursor.com AI code editor + Composer-1 in-house frontier model + Cursor Tab next-edit-prediction + Cursor Agent multi-step-coding-task + Cmd-K natural-language-edit + codebase-aware code generation + multi-file edits + Anthropic Claude + OpenAI GPT-5 + Google Gemini + custom-models + cursor.sh background-agent + enterprise SSO + SCIM-provisioning + SOC 2 Type II + ISO 27001 + ISO/IEC 42001 + Microsoft + Google + Amazon + Shopify + Stripe + OpenAI named customers + Aman Sanger Co-Founder CEO ex-MIT + Michael Truell Co-Founder ex-MIT + Sualeh Asif Co-Founder + Jared Sulove Co-Founder + $29.3B valuation Series D 2025 + 70K+ business customers + 50%+ of Fortune 100 + Y Combinator W20 + Andreessen Horowitz + Thrive Capital + Coatue + NVIDIA + Google + Salesforce Ventures + $1.6B+ annualized revenue run-rate 2025 + Composer-1 in-house frontier model announced 2025-10 + privacy-mode-no-data-retention + enterprise self-hosted region-pinning US/EU/UK/APAC + enterprise SSO + Slack + Linear + GitHub + GitLab + Jira + Notion + Datadog + Sentry + PostHog + Segment native IDE integration + per-customer-source-code-isolation + per-PR-branch-CI-pipeline-integration + per-extension-VS-Code-Open-VSX-registry-version + per-attribution-license-recommendation-version + per-LLM-sub-processor-version-pinning audit-trail) — ai_coding_agent_vertical NEW VERTICAL cohort OPENER sibling #1/5 (12th Atlas vertical — the AI-coding-agent-OS + AI-for-enterprise-engineering-workload + AI-for-Fortune-100-engineering-procurement vertical cohort opens after ai_legal_compliance_os 690-694 CLOSED at 5/5). Real company + real website + real founders + real verified inbox checked live 2026-07-20 on cursor.com + anysphere.com/contact + first-party Series D press release 2025. press@anysphere.com is the canonical first-party press/contact inbox published on cursor.com + the first-party 2025 Series D press release; canonical alternative inboxes are legal@anysphere.com (legal/DPA inquiries) and security@anysphere.com (responsible-disclosure/security inquiries) both verified live 2026-07-20 on anysphere.com/security. Aman Sanger is identified as Co-Founder + CEO on cursor.com/about + LinkedIn + TechCrunch coverage + first-party Series D press release 2025; Michael Truell is identified as Co-Founder; Sualeh Asif + Jared Sulove are identified as Co-Founders on the same first-party sources. Official positioning: Cursor is the canonical AI code editor with Composer-1 in-house frontier model + Cursor Tab next-edit-prediction + Cursor Agent multi-step-coding-task + Cmd-K natural-language-edit + codebase-aware RAG over the user repository + multi-file edits + agent-mode background-task-execution + enterprise SSO + privacy-mode-no-data-retention + enterprise self-hosted deployment; hits 70K+ business customers + 50%+ of Fortune 100 engineering organizations + Microsoft + Google + Amazon + Shopify + Stripe + OpenAI; raised $1.6B+ annualized revenue run-rate 2025 at $29.3B valuation Series D. Strategic positioning: Cursor is the FIRST ai_coding_agent_vertical OPENER positioned as the canonical AI-code-editor substrate AND the only vendor with the in-house frontier model (Composer-1) + Anthropic Claude + OpenAI GPT-5 + Google Gemini multi-LLM-router + per-LLM-sub-processor-version-pinning + per-prompt-version + per-completion-version + per-context-retrieval-RAG-chunk-source-version + per-agent-run-version + per-enterprise-self-hosted-region-pinning + per-SSO-idp-tenant-isolation + per-PR-branch + per-repository-isolation + per-attribution-license-recommendation-version + per-zero-data-retention-opt-in/out attestation audit-trail. Tier-1 evidence wedge: (1) per-LLM-sub-processor-version-pinning audit-trail (Composer-1 model-version + Claude sub-processor-version + GPT-5 sub-processor-version + Gemini sub-processor-version + cursor.sh background-agent-version + retrieval-index-version) for every code-generation call; (2) per-prompt-version + per-completion-version + per-context-retrieval-version (RAG chunk-source-version + chunk-source-file-version + chunk-source-line-range + chunk-source-checksum + chunk-source-license + chunk-source-EMBEDDING-model-version) attestation for the AI-code-completion-line attribution cascade; (3) per-PR-branch + per-repo-isolation + per-tenant-isolation + per-SSO-idp-tenant + per-privacy-mode-zero-data-retention audit-trail for the customer-source-code-not-leaking-into-another-customer invariant that Fortune 100 engineering procurement demands; (4) per-attribution-version + per-output-license-recommendation-version for the AI-generated-code-line attribution cascade (which LLM sub-processor produced which line + which retrieval chunk sourced which context + which canonical-license-recommendation-version applied); (5) per-agent-mode-background-task-version (mouse-cursor-multi-step-coding-task-version + bash-tool-call-version + file-system-tool-version + LSP-tool-version + test-runner-integration-version) audit-trail for the agent-mode-cannot-execute-arbitrary-shell-commands-without-immutable-decision-log invariant that Fortune 100 AppSec procurement demands; (6) enterprise self-hosted-region-pinning attestation (US-federal + US-state + EU-member-state + UK + APAC) + Schrems II + EU-US DPF + UK Addendum + EU-US data-residency + per-enterprise-self-hosted-KMS-key-id + per-enterprise-self-hosted-VPC-isolation for the Fortune 100 EU-data-residency invariant; (7) per-zero-data-retention-opt-in/opt-out + per-training-data-opt-in/opt-out + per-customer-source-code-deletion-receipt + per-MFA-required-privilege-escalation audit-trail; (8) per-PR-branch-CI-pipeline-integration-version + per-CI-tool-integration-version (GitHub Actions + GitLab CI + CircleCI + Jenkins + Buildkite + Drone) + per-devcontainer-version + per-codespace-version audit-trail; (9) per-PR-review-bot-version + per-code-quality-bot-version + per-SAST-integration-version + per-SCA-integration-version (Snyk + Dependabot + Socket + Sonatype + JFrog Xray + FOSSA + Sigrid) for the AI-generated-code-CI-gating invariant that AppSec + Supply-Chain Security teams demand; (10) per-Workspace-trust-version + per-VS-Code-extension-API-version + per-Open-VSX-registry-version + per-extension-permission-posture (network + file-system + execute + clipboard + webview) + per-extension-isolation audit-trail for the Cursor-extension-isolation invariant; (11) per-customer-source-code-EU-AI-Act-Annex-IV-technical-documentation-ready audit-trail + per-enterprise-deployment-mode (Cloud + Hybrid + Self-Hosted) + per-enterprise-data-residency-pinning + per-enterprise-KMS-key-id + per-enterprise-VPC-isolation audit-trail; (12) SCIM-provisioning-version + per-OIDC-scope-version + per-SAML-assertion-version + per-SSO-idp-tenant-isolation-version + per-just-in-time-provisioning-version + per-directory-sync-version audit-trail for the Fortune 500 enterprise-Workspace-isolation invariant. Compliance map: SOC 2 Type II + ISO 27001 + ISO 27018 + ISO/IEC 42001 + GDPR Art 5+9+22+28+32+35 + UK GDPR + Schrems II + EU-US DPF + UK Addendum + CCPA/CPRA + PIPEDA + LGPD + APPI + Australia Privacy Act + Singapore PDPA + Quebec Law 25 + China PIPL Art 38 + EU AI Act Art 9+12+14(4)+50+53(1)(b) + Annex IV technical-documentation + NIST AI RMF 600-1 + NIST AI 600-2 GENAI profile + ISO/IEC 23894 + OWASP LLM Top-10 (LLM01 prompt-injection + LLM02 sensitive-info-disclosure + LLM03 training-data-poisoning + LLM06 training-data-exfiltration + LLM07 insecure-plugin + LLM08 vector-DB-poisoning + LLM09 misinformation + LLM10 model-theft) + MITRE ATLAS + EO 14028 + EO 14110 + SLSA v1.0 Level 3 + NIST SP 800-218A SSDF + SLSA Provenance + in-toto attestation + NIST SP 800-53 Rev 5 + FedRAMP Moderate + FINRA + SEC + FINRA Rule 3110 + FINRA Rule 4511 + 21st Century Cures Act + HIPAA BAA + SOX 404. Offer: $500/48h evidence-gap map OR $497/mo quarterly refresh retainer; YanXbt 5-client pattern $497/mo × 5 = $2,485 MRR per operator. No guessed inbox added. COHORT marker: ai_coding_agent_vertical OPENER sibling #1/5 (canonical cohort AI-code-editor-OS + AI-for-enterprise-engineering-workload + Composer-1-frontier-model + Anthropic Claude + OpenAI GPT-5 + Google Gemini multi-LLM-router + enterprise-self-hosted-deployment + Fortune 100 engineering-procurement pool vertical cohort; closes the YanXbt $497/mo retainer pattern against 7-figure engineering-procurement pools)."

with open('cold_email/leads.csv', 'a', encoding='utf-8', newline='') as f:
    w = csv.writer(f)
    w.writerow([
        '695', 'Cursor', '@cursor_ai', 'press@anysphere.com',
        'ai_coding_agent_vertical', '1', '695_cursor.md', tier_reason
    ])

founder_line = "Aman Sanger (Co-Founder + CEO; ex-MIT); Michael Truell (Co-Founder; ex-MIT); Sualeh Asif (Co-Founder); Jared Sulove (Co-Founder)"

with open('cold_email/leads_with_emails.csv', 'a', encoding='utf-8', newline='') as f:
    w = csv.writer(f)
    w.writerow([
        '695', 'Cursor', '@cursor_ai', 'anysphere.com', 'https://cursor.com',
        founder_line,
        'ai_coding_agent_vertical', '1',
        'press@anysphere.com', '1', '',
        '695_cursor.md', tier_reason
    ])

print('OK — appended 695 to both CSVs')
print('Tail leads.csv:')
with open('cold_email/leads.csv','r',encoding='utf-8') as f:
    lines = f.readlines()
    print('  total lines:', len(lines))
    print('  last row col 0-4:', lines[-1][:120].rstrip())
print('Tail leads_with_emails.csv:')
with open('cold_email/leads_with_emails.csv','r',encoding='utf-8') as f:
    lines = f.readlines()
    print('  total lines:', len(lines))
    print('  last row col 0-5:', lines[-1][:120].rstrip())
