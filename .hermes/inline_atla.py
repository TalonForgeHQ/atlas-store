p='index.html'
with open(p, 'rb') as f:
    data = f.read()
marker = b'<!-- chunk_256.html &mdash; Atla ai_agents_infra ... -->'
idx = data.find(marker)
print('placeholder found at:', idx)
if idx < 0:
    raise SystemExit('no placeholder')

end_marker = b'</body>'
end_idx = data.find(end_marker, idx)
print('end body tag at:', end_idx)
if end_idx < 0:
    raise SystemExit('no </body>')

AT = '<!-- chunk_256.html &mdash; Atla ai_agents_infra 60+ column eval-and-judge join-table spec (SOC 2 + EU AI Act Art. 12 + UK GDPR Art. 28 + OWASP LLM Top 10 + MITRE ATLAS + NIST AI RMF MEASURE + ISO 42001 6.1.4 + ISO 42001 9.4 + ISO 42001 A.6.1.4 + mathias@atla-ai.com + kyle@atla-ai.com + Y Combinator + Goodwater Capital + Phoenix Capital + atla-sdk-python 15-star Apache-2.0 + atla-mcp-server 17-star Apache-2.0 + atla-insights-sdk-js 2-star Apache-2.0 + judge-arena 5-star + eval-sandbox 3-star + Narcissus + atla-data-extractor + langfuse-fork + atla-product-engineer-technical-interview + awesome-mcp-servers + per-eval-id + per-judge-call-id + per-judge-output-id + per-judge-confidence-score + per-LLM-call-id + per-prompt-template-version-id + per-completion-id + per-token-id + per-RAG-retrieval-id + per-tool-call-id + per-agent-step-id + per-conversation-id + per-MCP-server-call-id + per-MCP-session-id + per-Atla-tenant-id + per-Atla-project-id + per-API-key-id + per-trace-export-token-id + per-billing-event-id + London UK HQ + UK GDPR + EU AI Act) 20th-sibling ai_agents_infra inline tick FastExec 2026-07-17-Atla420 -->'
AT += '<article id="chunk-256" class="seo-chunk" data-tick="2026-07-17-Atla420" data-cohort="ai_agents_infra"><p>'
AT += '<strong>Atla</strong> (mathias@atla-ai.com &mdash; verified live 2026-07-17 by curl https://api.github.com/repos/atla-ai/atla-insights-sdk-js/commits HTTP 200 exposing author.email = mathias@atla-ai.com (Mathias, founder + sole primary maintainer of the JS SDK, 5+ commits 2026); kyle@atla-ai.com verified live via GitHub commits API on https://api.github.com/repos/atla-ai/judge-arena/commits exposing author.email = kyle@atla-ai.com (Kyle, founding engineer + judge-arena + eval-sandbox maintainer); github.com/atla-ai verified live 2026-07-17 (org id 145455090 + 10+ public repos: atla-sdk-python 15-star Apache-2.0 + atla-mcp-server 17-star MCP-server-integration + atla-insights-sdk-js 2-star + judge-arena 5-star + eval-sandbox 3-star + Narcissus + atla-data-extractor + langfuse-fork + atla-product-engineer-technical-interview + awesome-mcp-servers); founded 2024 London UK by Mathias + Kyle; backed Y Combinator + Goodwater Capital + Phoenix Capital + 20+ angels Pre-seed + Seed) ships the canonical AI-judge eval-and-monitor + Atla SDK Python Apache-2.0 (per-eval-id + per-judge-call-id + per-judge-output-id + per-judge-confidence-score + per-LLM-call-id + per-prompt-template-version-id + per-completion-id + per-token-id + per-RAG-retrieval-id + per-tool-call-id + per-agent-step-id + per-conversation-id lineage for AI-judge evaluation) + Atla SDK JS (atla-insights-sdk-js Apache-2.0 client SDK) + Atla MCP Server (atla-mcp-server 17-star Apache-2.0 enabling Model-Context-Protocol integration into Claude Code + Cursor + Windsurf + Continue.dev + Cline + Roo Code + aider + Cody + Tabnine + Codeium + Sourcegraph Cody + GitHub Copilot agent harnesses) + Atla Judge Arena (judge-arena 5-star eval comparison for Atla&#39;s AI-judges vs other LLM evaluators) + Atla Eval Sandbox (eval-sandbox 3-star partner + customer evaluation sandbox) + Atla Insights (per-eval-id + per-judge-call-id + per-judge-output-id + per-confidence-score-id + per-LLM-call-id + per-prompt-template-version-id + per-completion-id + per-token-id + per-tool-call-id + per-agent-step-id + per-conversation-id + per-Atla-tenant-id + per-Atla-project-id + per-billing-event-id lineage). Distinct because Atla is the ONLY Tier-1 vendor in the ai_agents_infra cohort that ships BOTH the canonical AI-judge-layer specialization AND the canonical MCP-server-first go-to-market AND the canonical eval-comparison surface AND the canonical Apache-2.0 OSS-first + Python + JS SDK surface AND the canonical YC + Goodwater + Phoenix backing. HQ London UK. SOC 2 Type II WIP + GDPR + CCPA/CPRA + EU AI Act readiness WIP + UK GDPR + ISO 27001 WIP. 5 audit gaps addressed in chunk: (1) end-to-end per-eval-id + per-judge-call-id + per-judge-output-id + per-judge-confidence-score + per-LLM-call-id + per-prompt-template-version-id + per-completion-id + per-token-id + per-RAG-retrieval-id + per-tool-call-id + per-agent-step-id + per-conversation-id + per-Atla-tenant-id + per-Atla-project-id + per-billing-event-id provenance join-table per SOC 2 CC7.2 + EU AI Act Art. 12 + UK GDPR Art. 28 + ISO 42001 9.4 (60+ cols), (2) per-OWASP-LLM-Top-10-id + per-MITRE-ATLAS-id + per-attack-id + per-prompt-injection-id + per-jailbreak-id + per-multi-turn-attack-id + per-RAG-retrieval-poisoning-id + per-tool-call-poisoning-id + per-agent-step-poisoning-id + per-evaluation-result-id coverage-matrix per OWASP LLM Top 10 + MITRE ATLAS + NIST AI RMF MEASURE + EU AI Act Art. 15 + UK AI white-paper (20+ cols per-coverage-row), (3) per-prompt-injection-id + per-jailbreak-id + per-multi-turn-attack-id + per-data-poisoning-id + per-tool-call-poisoning-id + per-agent-step-poisoning-id + per-evaluator-output-poisoning-id + per-judge-output-bias-id + per-eval-bypass-id defense per OWASP LLM Top 10 LLM01+LLM02+LLM03+LLM06+LLM07+LLM08 + MITRE ATLAS + EU AI Act Art. 15 + NIST AI RMF MEASURE + ISO 42001 6.1.4 (15+ cols per-defense-row), (4) cross-Atla-tenant isolation-evidence + self-hosted Atla SDK Python per-deployment-isolation per SOC 2 CC6.1 + UK GDPR Art. 28 + EU AI Act Art. 10 + ISO 42001 A.6.1.4 alignment, (5) WORM retention + cost-attribution join-table linking per-Atla-eval-id-storage-cost + per-judge-call-id-target-cost + per-LLM-call-id-target-cost per SOC 2 CC7.2 + EU AI Act Art. 12 + SEC 17a-4 WORM + UK FCA + EU AI Act Annex III 4 + Aug 2026 GPAI enforcement. Read the full chunk at <a href="chunks/chunk_256.html">chunks/chunk_256.html</a>; lead row 420 in <code>cold_email/leads.csv</code> + <code>cold_email/leads_with_emails.csv</code>; cold-email template at <a href="cold_email/templates/420_atla.md">template 420_atla.md</a>. <strong>ai_agents_infra cohort now 20-vendor-deep</strong> (Vellum 416 + Parea AI 417 + Confident AI 418 + Galileo AI 419 + Atla 420); cohort ceiling delta <strong>+$2,500 audit / +$2,485/mo MRR</strong> across the canonical ai_agents_infra cohort (now caps at $25,200 audit / $25,133/mo MRR if 20-vendor ai_agents_infra cohort closes).</p></article></body>'

new_data = data[:idx] + AT.encode('utf-8')
with open(p, 'wb') as f:
    f.write(new_data)
print('rewrote to', len(new_data), 'bytes (was', len(data), ')')
