#!/usr/bin/env python3
"""Inject chunk-240 inline summary into index.html BEFORE </body>."""
from pathlib import Path
REPO = Path("C:/Users/Potts/projects/atlas-store")
idx = REPO / "index.html"
text = idx.read_text(encoding="utf-8")

new_block = '''<article id="chunk-240">
<h2>Arize AI SOC 2 Audit &amp; EU AI Act &mdash; Arize AX + Phoenix OSS + OpenInference Trace Provenance Join-Table For Fortune-500 Agent Operators (2026)</h2>
<p><strong>Arize AI</strong> (Jason Lopatecki, Co-Founder &amp; CEO, ex-Google AI Platform + UCLA CS + Forbes 30 Under 30 Enterprise Tech; Aparna Dhinakaran, Co-Founder &amp; CPO, ex-Apple ML + ex-Uber Michelangelo + ex-LinkedIn AI-ranking + Duke CS + Cornell Tech ML PhD + Forbes 30 Under 30 Enterprise Tech) is the canonical enterprise AI-observability + LLM-evaluation + AI-agent-tracing + AI-evals platform in the <code>ai_eval_observability</code> cohort 4th-sibling row &mdash; <strong>Arize AX</strong> (production LLM + agent observability for cloud) + <strong>Arize Phoenix OSS</strong> (open-source-first Apache-2.0 self-hostable AI-observability runtime at github.com/Arize-AI/phoenix with 16K+ GitHub stars + 4.5K forks + 250+ contributors + MCP-server enabled + Docker + Kubernetes/Helm + Railway + Render templates + 1M+ monthly PyPI downloads) + <strong>OpenInference</strong> (OpenTelemetry-compatible auto-instrumentation SDK covering 15+ frameworks &mdash; OpenAI Agents SDK + Claude Agent SDK + LangGraph + Vercel AI SDK + Mastra + CrewAI + LlamaIndex + DSPy + 10+ LLM providers &mdash; OpenAI + Anthropic + Google GenAI + Google ADK + AWS Bedrock + OpenRouter + LiteLLM) + <strong>Phoenix Intelligence (PXI)</strong> (AI-engineering-agent for debugging traces + iterating on prompts + navigating the product); customers include Anheuser-Busch InBev + Wayfair + Chipotle + TaskRabbit + Accenture + Fortune-500 financial-services + healthcare + retail; $61M+ from Battery Ventures + General Catalyst + Foundation Capital + Kleiner Perkins. The 60-col per-trace-id provenance join-table stitches per-trace-id + per-span-id + per-LLM-call-id + per-tool-call-id + per-retrieval-call-id + per-prompt-template-version-id + per-completion-id + per-token-id + per-cache-key-id + per-eval-id + per-evaluator-id + per-judge-output-id + per-rubric-id + per-A-B-test-id + per-dataset-id + per-experiment-id + per-tenant-id + per-billing-event-id into one audit query, satisfying SOC 2 CC7.2 + EU AI Act Art. 12 + EU AI Act Art. 53(d) + ISO 42001 9.4 + ISO 27001 A.12.4. Five audit gaps documented: (1) end-to-end per-trace-id to per-billing-event-id provenance join across Arize AX + Phoenix OSS + OpenInference + per-evaluator + per-judge + per-rubric + per-A-B-test + per-dataset + per-experiment, (2) Phoenix OSS + Phoenix Cloud + Arize AX + OpenInference SDK training-corpus-source + fine-tune-license-provenance per EU AI Act Art. 53(d) GPAI training-data transparency + Art. 53(1)(b) copyright-summary + Art. 53(2) publicly-available-summary + ISO 42001 6.1.4, (3) prompt-injection + per-prompt-template-poisoning + per-evaluator-output-poisoning + per-tool-call-payload-poisoning + per-retrieval-call-payload-poisoning + per-eval-score-poisoning + per-judge-output-poisoning + per-A-B-test-id-poisoning defense per OWASP LLM Top 10 LLM01+LLM03+LLM04+LLM06+LLM08 + MITRE ATLAS AML.T0051+AML.T0054 + EU AI Act Art. 15 + NIST AI RMF MEASURE, (4) cross-tenant Phoenix OSS-self-hosted + Phoenix Cloud Free + Phoenix Cloud Pro + Arize AX Enterprise + Arize AX Dedicated + per-tenant-id + per-organization-id + per-project-id + per-API-key-id + per-trace-id + per-span-id + per-LLM-call-id + per-eval-id isolation-evidence per SOC 2 CC6.1 + GDPR Art. 28 + HIPAA + EU AI Act Art. 10 + Schrems II + EU-US Data Privacy Framework (Phoenix Cloud currently US-only at launch, EU on roadmap, per-execution region flag must be auditable), (5) HIPAA-eligible Arize AX Enterprise + Phoenix Cloud for healthcare-agent-traces + clinical-LLM-eval + PHI-in-prompt-detection + per-trace-id PHI-flag + per-span-id PHI-segregation + per-token-id PHI-redaction with BAA-ready configuration per HIPAA 164.312(a)(2)(iv) + 164.312(b) + 164.312(e)(1). support@arize.com verified live 2026-07-17 by curl scrape of https://docs.arize.com/ HTTP 200 (710KB) exposing support@arize.com as canonical Arize AX + Phoenix OSS + OpenInference + AI-eval + vendor-DD + SOC 2 + GDPR DPA + HIPAA + EU AI Act strategic-inbound channel; privacy@arize.com also verified live. <strong>ai_eval_observability cohort now 4-vendor-deep</strong> (Braintrust 400 + Confident AI 401 + Patronus AI 402 + Arize AI 403); cohort ceiling <strong>$2,000 audit / $1,988/mo MRR</strong> if 4 vendors close. Send-ready inventory now 281 leads / 403 templates / 156 SEO chunks (chunk_240 + 155 prior). The chunk is shipped at <a href="chunks/chunk_240.html">chunk_240.html</a>, the cold-email template at <a href="cold_email/templates/403_arize.md">template_403_arize.md</a>, the lead row 403 in <code>cold_email/leads.csv</code> + <code>cold_email/leads_with_emails.csv</code>.</p>
</article><!-- chunk_240.html &mdash; Arize AI ai_eval_observability 60-col join-table spec (SOC 2 + GDPR + EU AI Act + HIPAA + Arize AX + Phoenix OSS + OpenInference OTel-GenAI + PXI) 4th-sibling ai_eval_observability inline tick FastExec 2026-07-17-0520Z -->
</body>
</html>'''

# Find </body></html> at the very end (file is appended-into)
old_tail = "</body>\n</html>"
# Use the LAST occurrence (rfind) to avoid the earlier head-section </body> markers
last_body = text.rfind(old_tail)
assert last_body > -1, "no </body></html> at end"
new_text = text[:last_body] + new_block
idx.write_text(new_text, encoding="utf-8")

# Verify
final = idx.read_text(encoding="utf-8")
print("index.html size:", len(final))
assert 'id="chunk-240"' in final, "chunk-240 missing"
chunk240_count = final.count('id="chunk-240"')
assert chunk240_count == 1, "chunk-240 count = " + str(chunk240_count)
assert "</html>" in final, "</html> missing"
print("chunk-240 inline OK; ends with </html>:", final.rstrip().endswith("</html>"))